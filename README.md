# Trip Wallet

海外旅行で使った外貨を、その日の為替レートで自分の通貨に換算して記録する PWA。データは端末内(IndexedDB)にだけ保存する。

## できること

- 旅行(Trip)ごとに通貨・期間・予算・人数を設定
- 支出の記録: 金額・個別/共有・カテゴリ・支払い方法・メモ・レシート写真
- レートは Frankfurter(ECB)から自動取得。取れないときは直近キャッシュ、支出ごとの手動上書きも可能
- 記録時のレートを支出に焼き付けるので、あとから合計金額が変わらない
- 合計 / 個別 / 共有(人数割り)の集計、カテゴリ別の帯グラフと日別推移の棒グラフ(個別 / 共有 / 自己負担で切り替え)、予算の残額表示
- 換算先の通貨は旅行ごとに選べる(既定値は設定タブ / 初回は端末の言語から推定)
- 表示言語は日本語・English・한국어・中文の 4 つ。初回は端末の言語に合わせ、設定タブで変えられる
- オフラインで入力・閲覧できる
- JSON でのバックアップ書き出しと取り込み

## 開発

```bash
npm install
npm run dev -- --host   # 同一 LAN のスマホから実機確認する
npm run test
npm run build
npm run preview
```

アイコンを変えたら `public/icon.svg` を編集して `npm run icons` を実行し、生成された PNG も commit する。

## 配布

`main` に push すると GitHub Actions が GitHub Pages へデプロイする。リポジトリの Settings → Pages で Source を「GitHub Actions」にしておくこと。

公開先が `https://<user>.github.io/trip-wallet/` 以外になる場合は `vite.config.ts` の `BASE` を変更する。

## 対応言語

日本語 / English / 한국어 / 中文 の 4 つ。文言は `src/i18n/<lang>.ts` の 1 ファイルに閉じてあるので後から差し替えやすい。

韓国語(`ko.ts`)と中国語(`zh.ts`)はネイティブのレビューを受けていない。おかしな表現を見つけたら該当ファイルの 1 行を直すだけで済む。

## 対応通貨

Frankfurter が使う ECB 参照レートの対象 30 通貨: AUD / BRL / CAD / CHF / CNY / CZK / DKK / EUR / GBP / HKD / HUF / IDR / ILS / INR / ISK / JPY / KRW / MXN / MYR / NOK / NZD / PHP / PLN / RON / SEK / SGD / THB / TRY / USD / ZAR。

台湾ドル(TWD)・ベトナムドン(VND)は ECB が公表しておらず自動取得できないため入れていない。ブルガリアレフ(BGN)はユーロ導入で公表が終わるため入れていない。

現地通貨と換算先が同じ旅行では換算せず、レート行も出さない。

## 手動確認チェックリスト

リリース前に実機で確認する。チェック済みは 2026-08-11 の実機確認分。

- [x] iPhone / Android のブラウザで開き、ホーム画面に追加してアプリとして起動できる
- [x] 旅行を作成し、テンキーで金額を入れて保存できる(3 タップで完了する)
- [x] レート行に「1元 = 23.47円」が出て、タップで手動上書きできる
- [x] レシート写真を撮って保存し、アプリを再起動しても一覧にサムネイルが出る
- [x] 機内モードにして支出を保存でき、直近レートを使っている旨の表示が出る
- [x] 手動レートを入れたあと機内モードで新しい支出を開くと、引き継いだレートが「前回入力した手動レート」と警告色で出る
- [x] 機内モードを解除すると、直近レートの表示がその日のレートに切り替わる(手動レートを使っている間は「自動に戻す」を押すまで切り替わらない)
- [x] キャッシュが空の状態(初回からオフライン)でレート手動入力を求められ、入力すれば保存できる
- [x] 集計タブで個別/共有/人数割りが正しく出る
- [x] 予算を設定した旅行で残額の帯が減り、超過すると赤くなる
- [x] 設定からバックアップを書き出し、別のブラウザで取り込むとデータが復元される
- [x] 旅行の終了日を過ぎた状態で起動するとエクスポート促しバナーが出て、閉じると再表示されない(記憶するのは直近に閉じた 1 旅行のみ)
- [x] 旅行を新規作成すると通貨が AUD → USD の ISO 順に並び、初期選択が米ドルになっている
- [x] 旅行名の入力例が「NY 2026-09」になっている
- [x] ホーム/集計のヘッダーをタップするとドラムが出て、回して「決定」すると旅行が切り替わる
- [x] ドラムを回さずに ✕ で閉じると旅行は切り替わらない
- [x] 旅行が 1 件のときはヘッダーがタップできない
- [x] 個別予算と共有予算を別々に設定でき、ホームにバーが 2 本出てそれぞれの残額が正しい
- [x] 片方だけ予算を設定するとバーは 1 本だけ出る
- [x] v1.0.2 で書き出したバックアップを取り込むと、旧予算が個別予算に入る
- [x] v1.0.2 で入れたアプリを更新すると、既存の旅行の予算が個別予算に移っている
- [x] 集計タブを開くと「自己負担」が選ばれていて、カテゴリ別が帯 + 内訳、日別が縦棒になっている
- [x] 「個別」「共有」を押すと 2 つの図が同時に切り替わり、見出しの右の金額も変わる
- [x] 切り替えても合計カードの数字は変わらない
- [x] カテゴリ別の内訳の金額を足すと、見出しの右の合計と一致する
- [x] 支出のない日が 0 円の細い線で残り、日付が飛んでいない
- [x] 平均線と「1日あたり平均」「最高」の数字が図と合っている
- [x] 2 週間以上の旅行で日付ラベルが間引かれ、横スクロールが出ない
- [x] 共有支出が 1 件もない旅行で「共有」を押すと案内が出る
- [x] アプリを開き直すと「自己負担」に戻っている
- [ ] 端末の言語が英語の状態で初回起動すると、画面が英語で出る
- [ ] 設定 → 表示 → 言語で 4 言語を切り替えると、全画面(ホーム・集計・設定・支出シート)の文言が即座に変わる
- [ ] アプリを再起動しても選んだ言語が残っている
- [ ] 曜日名が言語に合わせて変わる(日別推移の軸とホームの日付見出し)
- [ ] カテゴリ・支払い方法・個別/共有の表示が言語に合わせて変わる
- [ ] 設定 → 表示 → 換算先通貨を変えると、新しく作る旅行の初期値がその通貨になる
- [ ] 旅行の編集で換算先通貨を変えられ、支出を 1 件でも入れると通貨・換算先の両方が変更できなくなる
- [ ] 換算先をユーロにした旅行で、金額が「€1,234.56」の形で出る(記号が前、小数 2 桁)
- [ ] 換算先をスウェーデンクローナにすると記号が数字の後ろに出る
- [ ] 通貨の選択肢に国旗と、表示言語に合わせた通貨名が出る
- [ ] 現地通貨と換算先を同じにした旅行では、支出シートにレート行と換算プレビューが出ない
- [ ] 韓国ウォン → 日本円のように小数の無い通貨どうしでも、レートが 0.1 未満にならず桁が落ちない
- [ ] 米ドル → 韓国ウォンのようにレートが大きい組み合わせでも金額が合っている
- [ ] v1.0.4 で書き出したバックアップを取り込むと、換算先が円の旅行として復元される
- [ ] v1.0.4 で入れたアプリを更新すると、既存の旅行が換算先=円のまま開ける
