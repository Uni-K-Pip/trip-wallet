# v1.0.3 Implementation Plan

> **履歴資料:** この計画は完了済み実装の記録であり、現在の開発方法論や開発ワークフローを拘束しない。

**Goal:** 通貨の並び順を ISO 4217 順にし、旅行名の入力例を差し替え、ホーム/集計タブからドラム式に旅行を切り替えられるようにし、予算を個別と共有で別々に設定できるようにする。

**Architecture:** 既存の React + Dexie 構成をそのまま使う。ホイールは依存を増やさず CSS `scroll-snap` で自作し、スクロール位置と選択インデックスの変換だけを純関数に切り出して jsdom でテストできるようにする。予算の 2 分割は `Trip` のデータモデル変更を伴うので、変換規則を `migrateTripBudget` という純関数に集約し、Dexie の version 2 upgrade とバックアップ取り込みの両方から呼ぶ。

**Tech Stack:** React 19 / TypeScript / Vite 8 / Dexie 4 / Vitest + @testing-library/react + fake-indexeddb

## Global Constraints

- 依存ライブラリを追加しない。`package.json` の dependencies は `dexie` / `dexie-react-hooks` / `react` / `react-dom` のまま
- 対応通貨を増やさない。ECB 非対象の TWD / VND / IDR は追加してはいけない(`src/domain/currency.ts` 冒頭のコメントの通り)
- 検証コマンドは `npm run test`(vitest run)と `npm run build`(`tsc --noEmit && vite build`)。タスクの完了前に両方通す
- `tsconfig.json` は `strict` / `noUnusedLocals` / `noUnusedParameters` が有効。未使用の変数・引数を残すとビルドが落ちる
- UI 文言はすべて日本語。既存の言い回し(「未設定」「〜できませんでした」など)に合わせる
- ESLint は無い。lint = `tsc --noEmit` なので `eslint-disable` コメントを書かない
- 集計タブ(`SummaryScreen`)には予算を出さない。予算表示はホームのみ
- 旅行の並び順は既存の `listTrips()`(`createdAt` の降順 = 新しい順)に従う。並べ替え機能は作らない
- コミットメッセージは日本語・現在形の既存スタイル(`fix: 通信が戻ったらレートを取り直す` など)に合わせる

---

## File Structure

| ファイル | 役割 | タスク |
|---|---|---|
| `src/domain/currency.ts` | 通貨マスタ。ISO 順に並べ替える | 1 |
| `src/domain/types.ts` | `Trip` の予算フィールドを 2 つに割る | 2 |
| `src/data/migrateTrip.ts`(新規) | v1 → v2 の予算変換の唯一の実装 | 2 |
| `src/data/db.ts` | Dexie version 2 の upgrade | 2 |
| `src/data/tripRepo.ts` | `TripInput` の予算フィールド | 2 |
| `src/domain/summary.ts` | 予算・残額を個別/共有の 2 系統に | 2 |
| `src/data/backup.ts` | 書き出し v2、取り込み v1/v2 | 2 |
| `src/ui/TripForm.tsx` | 初期通貨・placeholder・予算欄 2 つ | 1, 2, 3 |
| `src/ui/HomeScreen.tsx` | 予算バー 2 本 | 2, 3 |
| `src/ui/SettingsScreen.tsx` | 旅行メタの予算表示 | 2, 3 |
| `src/ui/WheelPicker.tsx`(新規) | 汎用ホイール。旅行のことは知らない | 4 |
| `src/ui/TripPickerSheet.tsx`(新規) | `Sheet` + `WheelPicker`。DB に触らない | 5 |
| `src/app/App.tsx` | ヘッダーのボタン化とシートの開閉 | 6 |
| `src/styles.css` | ホイール・2 本バー・ヘッダーボタン | 2, 3, 4, 6 |
| `README.md` | 対応通貨の順、手動確認チェックリスト | 1, 7 |
| `package.json` | 1.0.3 | 7 |

**タスクの順序について:** 予算のデータモデル変更(タスク 2)を先に済ませる。`Trip` 型が変わると全テストのフィクスチャが影響を受けるので、後続タスクで書くフィクスチャを一度で新形式にするため。

---

### Task 1: 通貨の並び順と旅行名の入力例

`CURRENCIES` を ISO 4217 コードの昇順に並べ替え、新規旅行の初期通貨を USD に、旅行名の placeholder を `NY 2026-09` にする。

**Files:**
- Modify: `src/domain/currency.ts:12-22`
- Modify: `src/ui/TripForm.tsx:17`(初期通貨)、`src/ui/TripForm.tsx:67`(placeholder)
- Modify: `README.md:35`
- Test: `src/domain/currency.test.ts`、`src/ui/TripForm.test.tsx`

**Interfaces:**
- Consumes: なし(最初のタスク)
- Produces: `CURRENCIES` の要素の型・件数は変えない。`CurrencyInfo = { code: string; decimals: number; label: string; symbol: string }` のまま。順序だけが変わる

- [ ] **Step 1: 失敗するテストを書く**

`src/domain/currency.test.ts` の `describe('CURRENCIES', ...)` の中、「コードが重複しない」の直後に足す:

```ts
  it('ISO 4217 コードの昇順に並んでいる', () => {
    const codes = CURRENCIES.map((c) => c.code);
    expect(codes).toEqual([...codes].sort());
  });
```

`src/ui/TripForm.test.tsx` の `describe('TripForm', ...)` の先頭に足す:

```ts
  it('新規作成時の初期通貨は米ドル', () => {
    render(<TripForm onDone={() => {}} onCancel={() => {}} />);
    expect(screen.getByLabelText('通貨')).toHaveValue('USD');
  });

  it('旅行名の入力例は NY 2026-09', () => {
    render(<TripForm onDone={() => {}} onCancel={() => {}} />);
    expect(screen.getByLabelText('旅行名')).toHaveAttribute('placeholder', 'NY 2026-09');
  });
```

- [ ] **Step 2: 失敗を確認する**

```bash
npx vitest run src/domain/currency.test.ts src/ui/TripForm.test.tsx
```

Expected: FAIL 3 件。`ISO 4217 コードの昇順` は `['CNY','KRW',...]` と `['AUD','CNY',...]` の不一致、`初期通貨は米ドル` は value が `CNY`、`入力例` は placeholder が `上海 2026-09`。

- [ ] **Step 3: 実装する**

`src/domain/currency.ts:12-22` の配列を丸ごと置き換える(冒頭のコメントブロックは残す):

```ts
export const CURRENCIES: CurrencyInfo[] = [
  { code: 'AUD', decimals: 2, label: '豪ドル', symbol: 'A$' },
  { code: 'CNY', decimals: 2, label: '中国元', symbol: '元' },
  { code: 'EUR', decimals: 2, label: 'ユーロ', symbol: '€' },
  { code: 'GBP', decimals: 2, label: '英ポンド', symbol: '£' },
  { code: 'HKD', decimals: 2, label: '香港ドル', symbol: 'HK$' },
  { code: 'KRW', decimals: 0, label: '韓国ウォン', symbol: '₩' },
  { code: 'SGD', decimals: 2, label: 'シンガポールドル', symbol: 'S$' },
  { code: 'THB', decimals: 2, label: 'タイバーツ', symbol: '฿' },
  { code: 'USD', decimals: 2, label: '米ドル', symbol: '$' },
];
```

`src/ui/TripForm.tsx:17`:

```tsx
  const [currency, setCurrency] = useState(trip?.currency ?? 'USD');
```

`src/ui/TripForm.tsx:67`:

```tsx
        placeholder="NY 2026-09"
```

`README.md:35` の一文を置き換える:

```markdown
Frankfurter が使う ECB 参照レートの対象通貨のみ: AUD / CNY / EUR / GBP / HKD / KRW / SGD / THB / USD。TWD など ECB が公表していない通貨は自動取得できないため入れていない。
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
npm run test
```

Expected: PASS(全ファイル)。既存の「入力した内容で旅行を作る」は `selectOptions(通貨, 'CNY')` で明示的に選ぶので初期値の変更に影響されない。

- [ ] **Step 5: ビルドを確認する**

```bash
npm run build
```

Expected: 成功。

- [ ] **Step 6: コミットする**

```bash
git add src/domain/currency.ts src/domain/currency.test.ts src/ui/TripForm.tsx src/ui/TripForm.test.tsx README.md && git commit -m "feat: 通貨を ISO 4217 順に並べ、旅行の入力例を NY にする"
```

---

### Task 2: 予算フィールドの分割と移行

`Trip.budgetJpy` を `personalBudgetJpy` / `sharedBudgetJpy` に置き換える。DB を version 2 に上げ、バックアップ形式を v2 にする。**このタスクでは画面の見た目は変えない**(TripForm の予算欄は 1 つのまま個別予算に書き込み、ホームの予算バーは 1 本のまま個別予算を表示する)。UI の 2 系統化はタスク 3 で行う。型を変えると全参照が同時に壊れるため、ここは 1 タスクにまとめる。

**Files:**
- Modify: `src/domain/types.ts:22-23`
- Create: `src/data/migrateTrip.ts`
- Modify: `src/data/db.ts:12-18`
- Modify: `src/data/tripRepo.ts:6-13, 23, 36`
- Modify: `src/domain/summary.ts:15-17, 54-55`
- Modify: `src/data/backup.ts:9, 45, 74, 112, 126`
- Modify: `src/ui/TripForm.tsx:20, 36-47`(型合わせのみ)
- Modify: `src/ui/HomeScreen.tsx:51-54, 93-105`(型合わせのみ)
- Modify: `src/ui/SettingsScreen.tsx:87`(型合わせのみ)
- Test: `src/data/migrateTrip.test.ts`(新規)、`src/domain/summary.test.ts`、`src/data/backup.test.ts`、`src/data/repos.test.ts`、`src/app/reminders.test.ts`、`src/app/useActiveTrip.test.ts`、`src/ui/HomeScreen.test.tsx`、`src/ui/TripForm.test.tsx`、`src/ui/SettingsScreen.test.tsx`

**Interfaces:**
- Consumes: タスク 1 の `CURRENCIES`(変更なし)
- Produces:
  - `Trip` に `personalBudgetJpy: number | null` と `sharedBudgetJpy: number | null`。`budgetJpy` は消える
  - `export function migrateTripBudget(trip: Record<string, unknown>): Trip`(`src/data/migrateTrip.ts`)
  - `TripInput` に `personalBudgetJpy?: number | null` と `sharedBudgetJpy?: number | null`
  - `TripSummary` に `personalBudgetJpy: number | null` / `personalRemainingJpy: number | null` / `sharedBudgetJpy: number | null` / `sharedRemainingJpy: number | null`。`budgetJpy` / `remainingJpy` は消える
  - `BackupFile['version']` は `2`

- [ ] **Step 1: 移行関数の失敗するテストを書く**

`src/data/migrateTrip.test.ts` を新規作成:

```ts
import { describe, it, expect } from 'vitest';
import { migrateTripBudget } from './migrateTrip';

const v1Trip = {
  id: 't1',
  name: '上海 2026-09',
  currency: 'CNY',
  currencyDecimals: 2,
  startDate: '2026-09-12',
  endDate: null,
  budgetJpy: 100000,
  memberCount: 2,
  createdAt: 0,
};

describe('migrateTripBudget', () => {
  it('v1 の budgetJpy を個別予算に移す', () => {
    const t = migrateTripBudget({ ...v1Trip });
    expect(t.personalBudgetJpy).toBe(100000);
    expect(t.sharedBudgetJpy).toBeNull();
  });

  it('旧フィールドを残さない', () => {
    expect('budgetJpy' in migrateTripBudget({ ...v1Trip })).toBe(false);
  });

  it('予算未設定の v1 は個別・共有とも null', () => {
    const t = migrateTripBudget({ ...v1Trip, budgetJpy: null });
    expect(t.personalBudgetJpy).toBeNull();
    expect(t.sharedBudgetJpy).toBeNull();
  });

  it('予算以外のフィールドは変えない', () => {
    const t = migrateTripBudget({ ...v1Trip });
    expect(t.id).toBe('t1');
    expect(t.name).toBe('上海 2026-09');
    expect(t.currency).toBe('CNY');
    expect(t.currencyDecimals).toBe(2);
    expect(t.startDate).toBe('2026-09-12');
    expect(t.endDate).toBeNull();
    expect(t.memberCount).toBe(2);
    expect(t.createdAt).toBe(0);
  });

  it('v2 の旅行はそのまま返す', () => {
    const t = migrateTripBudget({
      id: 't2',
      name: 'NY 2026-09',
      currency: 'USD',
      currencyDecimals: 2,
      startDate: '2026-09-12',
      endDate: null,
      personalBudgetJpy: 50000,
      sharedBudgetJpy: 30000,
      memberCount: 1,
      createdAt: 0,
    });
    expect(t.personalBudgetJpy).toBe(50000);
    expect(t.sharedBudgetJpy).toBe(30000);
  });
});
```

- [ ] **Step 2: 失敗を確認する**

```bash
npx vitest run src/data/migrateTrip.test.ts
```

Expected: FAIL。`Failed to resolve import "./migrateTrip"`。

- [ ] **Step 3: 型と移行関数を実装する**

`src/domain/types.ts:22-23` の 2 行を置き換える:

```ts
  /** 個別支出の予算(円)。未設定は null */
  personalBudgetJpy: number | null;
  /** 共有支出のうち自分の負担分の予算(円)。未設定は null */
  sharedBudgetJpy: number | null;
```

`src/data/migrateTrip.ts` を新規作成:

```ts
import type { Trip } from '../domain/types';

/**
 * v1 形式(budgetJpy を持つ)の旅行を v2 形式に直す。v2 形式ならそのまま返す。
 *
 * 引数は backup.ts の isTrip を通ったもの、または DB に入っている旅行レコードだけを
 * 想定する。予算以外のフィールドは touch しない。
 * DB の upgrade とバックアップ取り込みの両方から呼ぶので、IndexedDB に依存させない。
 */
export function migrateTripBudget(trip: Record<string, unknown>): Trip {
  const { budgetJpy, ...rest } = trip;
  const migrated = rest as unknown as Trip;
  if ('personalBudgetJpy' in trip) return migrated;

  return {
    ...migrated,
    personalBudgetJpy: typeof budgetJpy === 'number' ? budgetJpy : null,
    sharedBudgetJpy: null,
  };
}
```

- [ ] **Step 4: 移行関数のテストが通ることを確認する**

```bash
npx vitest run src/data/migrateTrip.test.ts
```

Expected: PASS 5 件。ここで `npm run build` はまだ通らない(他ファイルが `budgetJpy` を参照しているため)。次のステップで直す。

- [ ] **Step 5: DB・リポジトリ・集計・バックアップを新形式に合わせる**

`src/data/db.ts` — `import` に移行関数を足し、コンストラクタの `version(1)` の直後に version 2 を足す:

```ts
import Dexie, { type EntityTable } from 'dexie';
import type { Expense, Photo, RateCache, Trip } from '../domain/types';
import { migrateTripBudget } from './migrateTrip';
```

```ts
    // 予算フィールドはインデックス対象ではないので stores は変えず、
    // 旧 budgetJpy を個別予算へ移すだけの upgrade を足す。
    this.version(2).upgrade((tx) =>
      tx
        .table('trips')
        .toCollection()
        .modify((trip: Record<string, unknown>) => {
          const migrated = migrateTripBudget(trip);
          trip.personalBudgetJpy = migrated.personalBudgetJpy;
          trip.sharedBudgetJpy = migrated.sharedBudgetJpy;
          delete trip.budgetJpy;
        }),
    );
```

`src/data/tripRepo.ts` — `TripInput` の `budgetJpy?: number | null;`(11 行目)を 2 行に:

```ts
  personalBudgetJpy?: number | null;
  sharedBudgetJpy?: number | null;
```

`createTrip` の `budgetJpy: input.budgetJpy ?? null,`(23 行目)を:

```ts
    personalBudgetJpy: input.personalBudgetJpy ?? null,
    sharedBudgetJpy: input.sharedBudgetJpy ?? null,
```

`updateTrip` の `if (patch.budgetJpy !== undefined) ...`(36 行目)を:

```ts
  if (patch.personalBudgetJpy !== undefined) changes.personalBudgetJpy = patch.personalBudgetJpy;
  if (patch.sharedBudgetJpy !== undefined) changes.sharedBudgetJpy = patch.sharedBudgetJpy;
```

`src/domain/summary.ts` — `TripSummary` の 15-17 行目を置き換える:

```ts
  personalBudgetJpy: number | null;
  /** 個別予算 - 個別支出。予算未設定なら null */
  personalRemainingJpy: number | null;
  sharedBudgetJpy: number | null;
  /** 共有予算 - 共有支出の自己負担分。予算未設定なら null */
  sharedRemainingJpy: number | null;
```

`summarize` の return の 54-55 行目を置き換える:

```ts
    personalBudgetJpy: trip.personalBudgetJpy,
    personalRemainingJpy:
      trip.personalBudgetJpy === null ? null : trip.personalBudgetJpy - personalJpy,
    sharedBudgetJpy: trip.sharedBudgetJpy,
    sharedRemainingJpy:
      trip.sharedBudgetJpy === null ? null : trip.sharedBudgetJpy - sharedPerPersonJpy,
```

`src/data/backup.ts` — import に移行関数を足す:

```ts
import { migrateTripBudget } from './migrateTrip';
```

`BackupFile` の `version: 1;`(9 行目)を `version: 2;` に。`exportBackup` の `version: 1,`(45 行目)を `version: 2,` に。

`isTrip` の `(t.budgetJpy === null || typeof t.budgetJpy === 'number')`(74 行目)を置き換え、直前にヘルパを足す:

```ts
/**
 * 予算は v1 の budgetJpy と v2 の 2 フィールドのどちらの形でも受け入れる。
 * ここの目的は壊れた JSON を弾くことで、形式ごとの網羅は migrateTripBudget の責務。
 */
function isBudgetField(v: unknown): boolean {
  return v === undefined || v === null || typeof v === 'number';
}
```

```ts
    isBudgetField(t.budgetJpy) &&
    isBudgetField(t.personalBudgetJpy) &&
    isBudgetField(t.sharedBudgetJpy)
```

`parseBackup` のバージョン判定(112-114 行目)を置き換える。`BackupFile['version']` を `2` に狭めたので、`b.version` のままだと 1 との比較が型エラーになる:

```ts
  // 書き出しは v2 だが、v1.0.2 以前が書いた v1 も取り込めるようにする
  const version = (b as { version?: unknown }).version;
  if (version !== 1 && version !== 2) {
    throw new Error(`対応していないバージョンです: ${String(version)}`);
  }
```

`parseBackup` の return(124-131 行目)を置き換える:

```ts
  return {
    format: 'trip-wallet-backup',
    version: 2,
    exportedAt: typeof b.exportedAt === 'number' ? b.exportedAt : 0,
    trips: b.trips.map((t) => migrateTripBudget(t as unknown as Record<string, unknown>)),
    expenses: b.expenses,
    photos,
  };
```

- [ ] **Step 6: 画面側を型に合わせる(見た目は変えない)**

`src/ui/TripForm.tsx:20` — 予算欄は 1 つのまま、個別予算に読み書きする:

```tsx
  const [budget, setBudget] = useState(
    trip === undefined || trip.personalBudgetJpy === null ? '' : String(trip.personalBudgetJpy),
  );
```

`src/ui/TripForm.tsx` の `handleSubmit` 内(36-47 行目)、`budgetJpy` を組み立てている箇所を:

```tsx
    // "1.2.3" や "abc" など数値として解釈できない入力は NaN のまま保存せず null にする
    const parsedBudget = Number(budget);
    const personalBudgetJpy =
      budget.trim() === '' || !Number.isFinite(parsedBudget)
        ? null
        : Math.max(0, Math.round(parsedBudget));
    const input = {
      name: name.trim(),
      currency,
      startDate,
      endDate: endDate === '' ? null : endDate,
      personalBudgetJpy,
      memberCount: Math.max(1, Math.round(Number(memberCount) || 1)),
    };
```

`src/ui/HomeScreen.tsx:51-54` の `usedRatio`:

```tsx
  const usedRatio =
    summary.personalBudgetJpy === null || summary.personalBudgetJpy === 0
      ? 0
      : Math.min(1, summary.personalJpy / summary.personalBudgetJpy);
```

`src/ui/HomeScreen.tsx:93-105` の予算ブロックの条件と表示:

```tsx
        {summary.personalBudgetJpy !== null && summary.personalRemainingJpy !== null && (
          <div className="budget">
            <div className="budget-bar">
              <div
                className={usedRatio >= 1 ? 'budget-fill over' : 'budget-fill'}
                style={{ width: `${usedRatio * 100}%` }}
              />
            </div>
            <span className="card-sub" data-testid="remaining-jpy">
              予算 {formatJpy(summary.personalBudgetJpy)} / 残り{' '}
              {formatJpy(summary.personalRemainingJpy)}
            </span>
          </div>
        )}
```

`src/ui/SettingsScreen.tsx:87`:

```tsx
                  {trip.personalBudgetJpy !== null ? ` / 予算 ${formatJpy(trip.personalBudgetJpy)}` : ''}
```

- [ ] **Step 7: 既存テストを新形式に更新する**

`src/app/reminders.test.ts:13` — `budgetJpy: null,` を:

```ts
    personalBudgetJpy: null,
    sharedBudgetJpy: null,
```

`src/app/useActiveTrip.test.ts:13` — 同じ 2 行に置き換える。

`src/data/repos.test.ts:46-51` のテストを置き換える:

```ts
  it('既定値は 1 人・予算なし・終了日なし', async () => {
    const trip = await createTrip({ name: '上海', currency: 'CNY' });
    expect(trip.memberCount).toBe(1);
    expect(trip.personalBudgetJpy).toBeNull();
    expect(trip.sharedBudgetJpy).toBeNull();
    expect(trip.endDate).toBeNull();
  });
```

`src/domain/summary.test.ts:18` — `budgetJpy: 100000,` を:

```ts
  personalBudgetJpy: 100000,
  sharedBudgetJpy: 50000,
```

`src/domain/summary.test.ts:55-62` の「支出が無いときは〜」の `expect(s.remainingJpy).toBe(100000);` を:

```ts
    expect(s.personalRemainingJpy).toBe(100000);
    expect(s.sharedRemainingJpy).toBe(50000);
```

`src/domain/summary.test.ts:78-86` の 2 つのテストを置き換える(旧「残額は支出合計を予算から引く」は前提が変わったので破棄する):

```ts
  it('個別の残額は個別支出だけを引く', () => {
    const s = summarize(
      [expense({ amountMinor: 10000 }), expense({ amountMinor: 20000, scope: 'shared' })],
      trip,
    );
    expect(s.personalBudgetJpy).toBe(100000);
    expect(s.personalRemainingJpy).toBe(98000);
  });

  it('共有の残額は人数割り後の自己負担を引く', () => {
    // 共有 4000 円 / 2 人 = 自己負担 2000 円
    const s = summarize([expense({ amountMinor: 20000, scope: 'shared' })], trip);
    expect(s.sharedBudgetJpy).toBe(50000);
    expect(s.sharedRemainingJpy).toBe(48000);
  });

  it('予算未設定なら残額は null。片方だけ設定もできる', () => {
    const none = summarize([expense()], {
      ...trip,
      personalBudgetJpy: null,
      sharedBudgetJpy: null,
    });
    expect(none.personalRemainingJpy).toBeNull();
    expect(none.sharedRemainingJpy).toBeNull();

    const personalOnly = summarize([expense()], { ...trip, sharedBudgetJpy: null });
    expect(personalOnly.personalRemainingJpy).toBe(98000);
    expect(personalOnly.sharedRemainingJpy).toBeNull();
  });
```

`src/data/backup.test.ts:41` — `seed()` の `createTrip`:

```ts
    const trip = await createTrip({
      name: '上海',
      currency: 'CNY',
      personalBudgetJpy: 100000,
      sharedBudgetJpy: 30000,
    });
```

`src/data/backup.test.ts:63` — `expect(backup.version).toBe(1);` を `expect(backup.version).toBe(2);` に。

`src/data/backup.test.ts:83` — `expect(trips[0].budgetJpy).toBe(100000);` を:

```ts
    expect(trips[0].personalBudgetJpy).toBe(100000);
    expect(trips[0].sharedBudgetJpy).toBe(30000);
```

`src/data/backup.test.ts:162` — 「startDate を欠いた trip」の JSON 内の `budgetJpy: null,` はそのままでよい(v1 形式のまま `isTrip` で弾かれることを確認するテストなので)。変更不要。

`src/ui/HomeScreen.test.tsx:30` — `budgetJpy: 10000,` を `personalBudgetJpy: 10000,` に(このタスクでは共有予算は付けない)。

`src/ui/HomeScreen.test.tsx:66` — 個別支出 2816 円に対して個別予算 10000 円なので残額が変わる:

```ts
    expect(screen.getByTestId('remaining-jpy')).toHaveTextContent('¥7,184');
```

`src/ui/TripForm.test.tsx:34` — `expect(trips[0].budgetJpy).toBe(100000);` を `expect(trips[0].personalBudgetJpy).toBe(100000);` に。

`src/ui/TripForm.test.tsx:37, 49` — テスト名とアサーションを:

```ts
  it('予算に数値として解釈できない文字列を入れたら null になる', async () => {
```

```ts
    expect(trips[0].personalBudgetJpy).toBeNull();
```

`src/ui/SettingsScreen.test.tsx:42` — `exportBackup` のモック戻り値も `BackupFile` 型なので、`version: 1,` を `version: 2,` にする。直さないと `tsc` が型エラーで落ちる。

- [ ] **Step 8: v1 バックアップの取り込みテストを足す**

`src/data/backup.test.ts` の `describe('exportBackup / importBackup', ...)` の中、「未対応バージョンは例外」の後ろに足す:

```ts
  it('v1 のバックアップを取り込むと旧予算が個別予算に入る', async () => {
    const parsed = parseBackup(
      JSON.stringify({
        format: 'trip-wallet-backup',
        version: 1,
        exportedAt: 0,
        trips: [
          {
            id: 't1',
            name: '上海',
            currency: 'CNY',
            currencyDecimals: 2,
            startDate: '2026-09-12',
            endDate: null,
            budgetJpy: 80000,
            memberCount: 1,
            createdAt: 0,
          },
        ],
        expenses: [],
      }),
    );
    expect(parsed.version).toBe(2);

    await importBackup(parsed);
    const trips = await listTrips();
    expect(trips[0].personalBudgetJpy).toBe(80000);
    expect(trips[0].sharedBudgetJpy).toBeNull();
    expect('budgetJpy' in trips[0]).toBe(false);
  });
```

- [ ] **Step 9: テストとビルドを確認する**

```bash
npm run test
```

Expected: PASS(全ファイル)。

```bash
npm run build
```

Expected: 成功。`budgetJpy` の参照が残っていれば `tsc` が落ちる。落ちたら `grep -rn "budgetJpy" src` で残りを潰す(`migrateTrip.ts` と `backup.ts` の `isBudgetField` 周り、`backup.test.ts` の v1 JSON だけが残ってよい)。

- [ ] **Step 10: コミットする**

```bash
git add -A && git commit -m "feat: 予算を個別と共有に分け、旧データを移行する"
```

---

### Task 3: 予算 UI の 2 系統化

入力欄を 2 つに、ホームの予算バーを 2 本にし、設定の旅行メタに両方を出す。

**Files:**
- Modify: `src/ui/TripForm.tsx`(予算欄 2 つ)
- Modify: `src/ui/HomeScreen.tsx`(予算バー 2 本)
- Modify: `src/ui/SettingsScreen.tsx`(旅行メタ)
- Modify: `src/styles.css`(2 本目のバーの余白)
- Test: `src/ui/TripForm.test.tsx`、`src/ui/HomeScreen.test.tsx`、`src/ui/SettingsScreen.test.tsx`

**Interfaces:**
- Consumes: タスク 2 の `Trip.personalBudgetJpy` / `Trip.sharedBudgetJpy`、`TripInput.personalBudgetJpy` / `TripInput.sharedBudgetJpy`、`TripSummary.personalBudgetJpy` / `personalRemainingJpy` / `sharedBudgetJpy` / `sharedRemainingJpy` / `personalJpy` / `sharedPerPersonJpy`
- Produces:
  - `TripForm` のラベル文字列 `個別予算(円)` と `共有予算(円・自分の負担分)`
  - `HomeScreen` の `data-testid`: `personal-remaining-jpy` / `shared-remaining-jpy`(`remaining-jpy` は無くなる)

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/TripForm.test.tsx:15-35` の「入力した内容で旅行を作る」を置き換える:

```ts
  it('入力した内容で旅行を作る', async () => {
    const user = userEvent.setup();
    const onDone = vi.fn();
    render(<TripForm onDone={onDone} onCancel={() => {}} />);

    await user.type(screen.getByLabelText('旅行名'), '上海 2026-09');
    await user.selectOptions(screen.getByLabelText('通貨'), 'CNY');
    await user.clear(screen.getByLabelText('人数'));
    await user.type(screen.getByLabelText('人数'), '2');
    await user.type(screen.getByLabelText('個別予算(円)'), '100000');
    await user.type(screen.getByLabelText('共有予算(円・自分の負担分)'), '30000');
    await user.click(screen.getByRole('button', { name: '保存' }));

    await waitFor(() => expect(onDone).toHaveBeenCalled());
    const trips = await listTrips();
    expect(trips).toHaveLength(1);
    expect(trips[0].name).toBe('上海 2026-09');
    expect(trips[0].currency).toBe('CNY');
    expect(trips[0].currencyDecimals).toBe(2);
    expect(trips[0].memberCount).toBe(2);
    expect(trips[0].personalBudgetJpy).toBe(100000);
    expect(trips[0].sharedBudgetJpy).toBe(30000);
  });
```

同ファイルの「予算に数値として解釈できない文字列を〜」を置き換える:

```ts
  it('予算に数値として解釈できない文字列を入れたら null になる', async () => {
    const user = userEvent.setup();
    const onDone = vi.fn();
    render(<TripForm onDone={onDone} onCancel={() => {}} />);

    await user.type(screen.getByLabelText('旅行名'), 'NY 2026-09');
    await user.type(screen.getByLabelText('個別予算(円)'), 'abc');
    await user.type(screen.getByLabelText('共有予算(円・自分の負担分)'), '1.2.3');
    await user.click(screen.getByRole('button', { name: '保存' }));

    await waitFor(() => expect(onDone).toHaveBeenCalled());
    const trips = await listTrips();
    expect(trips).toHaveLength(1);
    expect(trips[0].personalBudgetJpy).toBeNull();
    expect(trips[0].sharedBudgetJpy).toBeNull();
  });

  it('片方だけ予算を入れても保存できる', async () => {
    const user = userEvent.setup();
    const onDone = vi.fn();
    render(<TripForm onDone={onDone} onCancel={() => {}} />);

    await user.type(screen.getByLabelText('旅行名'), 'NY 2026-09');
    await user.type(screen.getByLabelText('共有予算(円・自分の負担分)'), '30000');
    await user.click(screen.getByRole('button', { name: '保存' }));

    await waitFor(() => expect(onDone).toHaveBeenCalled());
    const trips = await listTrips();
    expect(trips[0].personalBudgetJpy).toBeNull();
    expect(trips[0].sharedBudgetJpy).toBe(30000);
  });
```

`src/ui/HomeScreen.test.tsx:26-31` の `createTrip` に共有予算を足す:

```ts
  trip = await createTrip({
    name: '上海',
    currency: 'CNY',
    memberCount: 2,
    personalBudgetJpy: 10000,
    sharedBudgetJpy: 3000,
  });
```

同ファイルの「サマリーを表示する」の最後の 1 行(`remaining-jpy`)を置き換える:

```ts
    expect(screen.getByTestId('personal-remaining-jpy')).toHaveTextContent('¥7,184');
    expect(screen.getByTestId('shared-remaining-jpy')).toHaveTextContent('¥1,850');
```

さらに `describe('HomeScreen', ...)` に足す:

```ts
  it('設定されている側の予算バーだけを出す', async () => {
    const personalOnly = await createTrip({
      name: 'NY',
      currency: 'USD',
      personalBudgetJpy: 5000,
    });
    render(<HomeScreen trip={personalOnly} />);

    expect(await screen.findByTestId('personal-remaining-jpy')).toBeInTheDocument();
    expect(screen.queryByTestId('shared-remaining-jpy')).not.toBeInTheDocument();
  });

  it('予算が両方とも未設定ならバーを出さない', async () => {
    const noBudget = await createTrip({ name: '香港', currency: 'HKD' });
    render(<HomeScreen trip={noBudget} />);

    await screen.findByText('まだ支出がありません。右下の + から追加してください。');
    expect(screen.queryByTestId('personal-remaining-jpy')).not.toBeInTheDocument();
    expect(screen.queryByTestId('shared-remaining-jpy')).not.toBeInTheDocument();
  });
```

`src/ui/SettingsScreen.test.tsx` — import に `createTrip` を足す(`db` の import の下):

```ts
import { createTrip } from '../data/tripRepo';
```

ファイル末尾に describe を足す(既存の `beforeEach` と `vi.mock` はそのまま効く):

```tsx
describe('SettingsScreen の旅行一覧', () => {
  it('旅行メタに個別予算と共有予算を出す', async () => {
    const trip = await createTrip({
      name: '上海',
      currency: 'CNY',
      personalBudgetJpy: 50000,
      sharedBudgetJpy: 30000,
    });
    render(<SettingsScreen trips={[trip]} activeTrip={trip} onSelectTrip={() => {}} />);

    expect(screen.getByText(/個別 ¥50,000 \/ 共有 ¥30,000/)).toBeInTheDocument();
  });

  it('片方だけ設定されていればその側だけ出す', async () => {
    const trip = await createTrip({ name: 'NY', currency: 'USD', sharedBudgetJpy: 30000 });
    render(<SettingsScreen trips={[trip]} activeTrip={trip} onSelectTrip={() => {}} />);

    expect(screen.getByText(/\/ 共有 ¥30,000/)).toBeInTheDocument();
    expect(screen.queryByText(/個別 ¥/)).not.toBeInTheDocument();
  });
});
```

`trip-meta` の中身は同じ span の直下テキストノードなので、`getByText` は連結した文字列に対して照合する。

- [ ] **Step 2: 失敗を確認する**

```bash
npx vitest run src/ui/TripForm.test.tsx src/ui/HomeScreen.test.tsx src/ui/SettingsScreen.test.tsx
```

Expected: FAIL。`Unable to find a label with the text of: 個別予算(円)`、`Unable to find an element by: [data-testid="personal-remaining-jpy"]` など。

- [ ] **Step 3: TripForm の予算欄を 2 つにする**

`src/ui/TripForm.tsx` — コンポーネントの外(`type Props` の下、`export function TripForm` の上)にパース関数を置く:

```tsx
/** "1.2.3" や "abc" など数値として解釈できない入力は NaN のまま保存せず null にする。 */
function parseBudget(text: string): number | null {
  const n = Number(text);
  return text.trim() === '' || !Number.isFinite(n) ? null : Math.max(0, Math.round(n));
}
```

タスク 2 で置き換えた `budget` の `useState` を 2 つにする:

```tsx
  const [personalBudget, setPersonalBudget] = useState(
    trip === undefined || trip.personalBudgetJpy === null ? '' : String(trip.personalBudgetJpy),
  );
  const [sharedBudget, setSharedBudget] = useState(
    trip === undefined || trip.sharedBudgetJpy === null ? '' : String(trip.sharedBudgetJpy),
  );
```

`handleSubmit` のパース部分と `input` を:

```tsx
    const input = {
      name: name.trim(),
      currency,
      startDate,
      endDate: endDate === '' ? null : endDate,
      personalBudgetJpy: parseBudget(personalBudget),
      sharedBudgetJpy: parseBudget(sharedBudget),
      memberCount: Math.max(1, Math.round(Number(memberCount) || 1)),
    };
```

予算の `label` + `input`(タスク 2 時点で 1 組)を 2 組に置き換える:

```tsx
      <label htmlFor="trip-personal-budget">個別予算(円)</label>
      <input
        id="trip-personal-budget"
        inputMode="numeric"
        value={personalBudget}
        onChange={(e) => setPersonalBudget(e.target.value)}
        placeholder="未設定"
      />

      <label htmlFor="trip-shared-budget">共有予算(円・自分の負担分)</label>
      <input
        id="trip-shared-budget"
        inputMode="numeric"
        value={sharedBudget}
        onChange={(e) => setSharedBudget(e.target.value)}
        placeholder="未設定"
      />
```

- [ ] **Step 4: HomeScreen の予算バーを 2 本にする**

`src/ui/HomeScreen.tsx` — `PhotoThumb` の下、`export function HomeScreen` の上に足す:

```tsx
function BudgetBar({
  label,
  budgetJpy,
  usedJpy,
  remainingJpy,
  testId,
}: {
  label: string;
  budgetJpy: number;
  usedJpy: number;
  remainingJpy: number;
  testId: string;
}) {
  // 予算 0 のときは 0 除算を避けて使用率 0 として扱う
  const usedRatio = budgetJpy === 0 ? 0 : Math.min(1, usedJpy / budgetJpy);

  return (
    <div className="budget">
      <div className="budget-bar">
        <div
          className={usedRatio >= 1 ? 'budget-fill over' : 'budget-fill'}
          style={{ width: `${usedRatio * 100}%` }}
        />
      </div>
      <span className="card-sub" data-testid={testId}>
        {label} {formatJpy(budgetJpy)} / 残り {formatJpy(remainingJpy)}
      </span>
    </div>
  );
}
```

`HomeScreen` 本体から `usedRatio` の計算(タスク 2 で個別予算向けに直した行)を削除し、予算ブロックを置き換える:

```tsx
        {summary.personalBudgetJpy !== null && summary.personalRemainingJpy !== null && (
          <BudgetBar
            label="個別予算"
            budgetJpy={summary.personalBudgetJpy}
            usedJpy={summary.personalJpy}
            remainingJpy={summary.personalRemainingJpy}
            testId="personal-remaining-jpy"
          />
        )}

        {summary.sharedBudgetJpy !== null && summary.sharedRemainingJpy !== null && (
          <BudgetBar
            label="共有予算"
            budgetJpy={summary.sharedBudgetJpy}
            usedJpy={summary.sharedPerPersonJpy}
            remainingJpy={summary.sharedRemainingJpy}
            testId="shared-remaining-jpy"
          />
        )}
```

- [ ] **Step 5: SettingsScreen の予算表示を 2 系統にする**

`src/ui/SettingsScreen.tsx` — `type Props` の下、`export function SettingsScreen` の上に足す:

```tsx
/** 設定されている側だけを ` / 個別 ¥50,000 / 共有 ¥30,000` の形で返す。両方未設定なら空文字。 */
function budgetLabel(trip: Trip): string {
  const parts: string[] = [];
  if (trip.personalBudgetJpy !== null) parts.push(`個別 ${formatJpy(trip.personalBudgetJpy)}`);
  if (trip.sharedBudgetJpy !== null) parts.push(`共有 ${formatJpy(trip.sharedBudgetJpy)}`);
  return parts.length === 0 ? '' : ` / ${parts.join(' / ')}`;
}
```

タスク 2 で直した 87 行目付近を:

```tsx
                  {budgetLabel(trip)}
```

- [ ] **Step 6: 2 本目のバーの余白を足す**

`src/styles.css` の `.budget { margin-top: 12px; }` の直後に足す:

```css
.budget + .budget {
  margin-top: 8px;
}
```

- [ ] **Step 7: テストとビルドを確認する**

```bash
npm run test
```

Expected: PASS(全ファイル)。共有予算 3000 円・共有自己負担 1150 円なので残り 1850 円。

```bash
npm run build
```

Expected: 成功。

- [ ] **Step 8: コミットする**

```bash
git add -A && git commit -m "feat: 予算の入力欄と残額バーを個別と共有に分ける"
```

---

### Task 4: ホイール部品

依存を足さずに `scroll-snap` でドラムを作る。スクロール位置と選択インデックスの変換は純関数に切り出す。jsdom は実レイアウトを持たないので、この分離が無いとスクロール周りをテストできない。

**Files:**
- Create: `src/ui/WheelPicker.tsx`
- Modify: `src/styles.css`(末尾に追記)
- Test: `src/ui/WheelPicker.test.tsx`(新規)

**Interfaces:**
- Consumes: なし(このファイルは旅行のことも DB のことも知らない)
- Produces:
  - `export type WheelItem = { id: string; label: string }`
  - `export const WHEEL_ITEM_HEIGHT = 44`
  - `export function indexFromScroll(scrollTop: number, itemHeight: number, count: number): number`
  - `export function offsetForIndex(index: number, itemHeight: number): number`
  - `export function WheelPicker(props: { items: WheelItem[]; selectedId: string; onChange: (id: string) => void; label: string }): JSX.Element`

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/WheelPicker.test.tsx` を新規作成:

```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import {
  WHEEL_ITEM_HEIGHT,
  WheelPicker,
  indexFromScroll,
  offsetForIndex,
} from './WheelPicker';

describe('indexFromScroll', () => {
  it('項目高の倍数はそのままインデックスになる', () => {
    expect(indexFromScroll(0, 44, 3)).toBe(0);
    expect(indexFromScroll(44, 44, 3)).toBe(1);
    expect(indexFromScroll(88, 44, 3)).toBe(2);
  });

  it('半端な位置は近い方に丸める', () => {
    expect(indexFromScroll(21, 44, 3)).toBe(0);
    expect(indexFromScroll(23, 44, 3)).toBe(1);
  });

  it('範囲外は両端にクランプする', () => {
    expect(indexFromScroll(-100, 44, 3)).toBe(0);
    expect(indexFromScroll(9999, 44, 3)).toBe(2);
  });

  it('項目が無ければ 0 を返す', () => {
    expect(indexFromScroll(44, 44, 0)).toBe(0);
  });
});

describe('offsetForIndex', () => {
  it('インデックス × 項目高', () => {
    expect(offsetForIndex(0, 44)).toBe(0);
    expect(offsetForIndex(2, 44)).toBe(88);
  });

  it('indexFromScroll と往復する', () => {
    for (const i of [0, 1, 4]) {
      expect(indexFromScroll(offsetForIndex(i, WHEEL_ITEM_HEIGHT), WHEEL_ITEM_HEIGHT, 5)).toBe(i);
    }
  });
});

describe('WheelPicker', () => {
  const items = [
    { id: 'a', label: '上海 2026-09' },
    { id: 'b', label: 'NY 2026-09' },
  ];

  it('listbox として項目を並べる', () => {
    render(<WheelPicker items={items} selectedId="a" onChange={() => {}} label="旅行" />);

    expect(screen.getByRole('listbox', { name: '旅行' })).toBeInTheDocument();
    expect(screen.getAllByRole('option').map((o) => o.textContent)).toEqual([
      '上海 2026-09',
      'NY 2026-09',
    ]);
  });

  it('選択中の項目に aria-selected を付ける', () => {
    render(<WheelPicker items={items} selectedId="b" onChange={() => {}} label="旅行" />);

    expect(screen.getByRole('option', { name: 'NY 2026-09' })).toHaveAttribute(
      'aria-selected',
      'true',
    );
    expect(screen.getByRole('option', { name: '上海 2026-09' })).toHaveAttribute(
      'aria-selected',
      'false',
    );
  });

  it('項目をタップすると onChange が呼ばれる', async () => {
    const onChange = vi.fn();
    const user = userEvent.setup();
    render(<WheelPicker items={items} selectedId="a" onChange={onChange} label="旅行" />);

    await user.click(screen.getByRole('option', { name: 'NY 2026-09' }));

    expect(onChange).toHaveBeenCalledWith('b');
  });

  it('選択中の項目をタップしても onChange は呼ばない', async () => {
    const onChange = vi.fn();
    const user = userEvent.setup();
    render(<WheelPicker items={items} selectedId="a" onChange={onChange} label="旅行" />);

    await user.click(screen.getByRole('option', { name: '上海 2026-09' }));

    expect(onChange).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 失敗を確認する**

```bash
npx vitest run src/ui/WheelPicker.test.tsx
```

Expected: FAIL。`Failed to resolve import "./WheelPicker"`。

- [ ] **Step 3: WheelPicker を実装する**

`src/ui/WheelPicker.tsx` を新規作成:

```tsx
import { useEffect, useRef } from 'react';

/** 1 項目の高さ(px)。CSS の .wheel-item と必ず揃える。 */
export const WHEEL_ITEM_HEIGHT = 44;

/** スクロールが止まったとみなすまでの待ち時間(ms)。 */
const SCROLL_SETTLE_MS = 100;

export type WheelItem = { id: string; label: string };

/** スクロール位置から選択インデックスを求める。範囲外は両端にクランプする。 */
export function indexFromScroll(scrollTop: number, itemHeight: number, count: number): number {
  if (count <= 0) return 0;
  return Math.min(count - 1, Math.max(0, Math.round(scrollTop / itemHeight)));
}

/** インデックスに対応するスクロール位置を返す。 */
export function offsetForIndex(index: number, itemHeight: number): number {
  return index * itemHeight;
}

type Props = {
  items: WheelItem[];
  selectedId: string;
  onChange: (id: string) => void;
  /** listbox の読み上げ名 */
  label: string;
};

export function WheelPicker({ items, selectedId, onChange, label }: Props) {
  const listRef = useRef<HTMLDivElement>(null);
  const timer = useRef<ReturnType<typeof setTimeout> | null>(null);

  // マウント時だけ、開いた時点の選択位置へ合わせる。以後はスクロール操作が選択を決めるので
  // selectedId の変化で位置を戻してはいけない(指の動きと喧嘩する)。
  useEffect(() => {
    const el = listRef.current;
    if (!el) return;
    const index = items.findIndex((i) => i.id === selectedId);
    el.scrollTop = offsetForIndex(index < 0 ? 0 : index, WHEEL_ITEM_HEIGHT);
  }, []);

  useEffect(
    () => () => {
      if (timer.current !== null) clearTimeout(timer.current);
    },
    [],
  );

  function handleScroll() {
    const el = listRef.current;
    if (!el) return;
    // scrollend は Safari が未対応なので、止まったとみなせるまで待ってから位置を拾う
    if (timer.current !== null) clearTimeout(timer.current);
    const scrollTop = el.scrollTop;
    timer.current = setTimeout(() => {
      const item = items[indexFromScroll(scrollTop, WHEEL_ITEM_HEIGHT, items.length)];
      if (item !== undefined && item.id !== selectedId) onChange(item.id);
    }, SCROLL_SETTLE_MS);
  }

  // 実機でスナップが効かない場合でも操作が詰まないよう、タップでも選べるようにする
  function handlePick(index: number) {
    const el = listRef.current;
    if (el) el.scrollTop = offsetForIndex(index, WHEEL_ITEM_HEIGHT);
    const item = items[index];
    if (item.id !== selectedId) onChange(item.id);
  }

  return (
    <div className="wheel">
      <div className="wheel-highlight" aria-hidden="true" />
      <div
        className="wheel-list"
        ref={listRef}
        role="listbox"
        aria-label={label}
        onScroll={handleScroll}
      >
        {items.map((item, index) => (
          <button
            key={item.id}
            type="button"
            role="option"
            aria-selected={item.id === selectedId}
            className={item.id === selectedId ? 'wheel-item selected' : 'wheel-item'}
            onClick={() => handlePick(index)}
          >
            {item.label}
          </button>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: スタイルを足す**

`src/styles.css` の末尾に追記する。`--wheel-item-h` は `WHEEL_ITEM_HEIGHT` と同じ 44px、コンテナ高は項目 5 個分の 220px、上下パディングは (220 - 44) / 2 = 88px:

```css
.wheel {
  --wheel-item-h: 44px;
  --wheel-h: 220px;
  position: relative;
  height: var(--wheel-h);
  margin: 8px 0 16px;
}

.wheel-highlight {
  position: absolute;
  left: 0;
  right: 0;
  top: calc((var(--wheel-h) - var(--wheel-item-h)) / 2);
  height: var(--wheel-item-h);
  border-radius: 10px;
  background: var(--surface-2);
  pointer-events: none;
}

.wheel-list {
  height: 100%;
  overflow-y: auto;
  scroll-snap-type: y mandatory;
  padding: calc((var(--wheel-h) - var(--wheel-item-h)) / 2) 0;
  /* iOS で慣性スクロール中もスナップさせる */
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
}

.wheel-list::-webkit-scrollbar {
  display: none;
}

.wheel-item {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: var(--wheel-item-h);
  scroll-snap-align: center;
  background: none;
  border: none;
  color: var(--text-dim);
  font-size: 16px;
  padding: 0 12px;
}

.wheel-item.selected {
  color: var(--text);
  font-weight: 600;
}
```

- [ ] **Step 5: テストが通ることを確認する**

```bash
npx vitest run src/ui/WheelPicker.test.tsx
```

Expected: PASS 8 件。

- [ ] **Step 6: 全体のテストとビルドを確認する**

```bash
npm run test
```

Expected: PASS(全ファイル)。

```bash
npm run build
```

Expected: 成功。

- [ ] **Step 7: コミットする**

```bash
git add src/ui/WheelPicker.tsx src/ui/WheelPicker.test.tsx src/styles.css && git commit -m "feat: ドラム式に選ぶホイール部品を足す"
```

---

### Task 5: 旅行選択シート

`Sheet` と `WheelPicker` を組み合わせる。DB にも `useActiveTrip` にも触らないので、単体でテストできる。

**Files:**
- Create: `src/ui/TripPickerSheet.tsx`
- Test: `src/ui/TripPickerSheet.test.tsx`(新規)

**Interfaces:**
- Consumes: タスク 4 の `WheelPicker`、既存の `Sheet`(`{ title: string; onClose: () => void; children: ReactNode }`、閉じるボタンの `aria-label` は `閉じる`)、タスク 2 の `Trip`
- Produces: `export function TripPickerSheet(props: { trips: Trip[]; activeTripId: string; onSelect: (id: string) => void; onClose: () => void }): JSX.Element`

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/TripPickerSheet.test.tsx` を新規作成:

```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import type { Trip } from '../domain/types';
import { TripPickerSheet } from './TripPickerSheet';

function trip(id: string, name: string): Trip {
  return {
    id,
    name,
    currency: 'USD',
    currencyDecimals: 2,
    startDate: '2026-09-12',
    endDate: null,
    personalBudgetJpy: null,
    sharedBudgetJpy: null,
    memberCount: 1,
    createdAt: 0,
  };
}

const trips = [trip('a', '上海 2026-09'), trip('b', 'NY 2026-09')];

describe('TripPickerSheet', () => {
  it('開いた時点では今の旅行が選ばれている', () => {
    render(
      <TripPickerSheet trips={trips} activeTripId="b" onSelect={() => {}} onClose={() => {}} />,
    );

    expect(screen.getByRole('option', { name: 'NY 2026-09' })).toHaveAttribute(
      'aria-selected',
      'true',
    );
  });

  it('決定を押すと選んだ旅行の id を返す', async () => {
    const onSelect = vi.fn();
    const user = userEvent.setup();
    render(
      <TripPickerSheet trips={trips} activeTripId="a" onSelect={onSelect} onClose={() => {}} />,
    );

    await user.click(screen.getByRole('option', { name: 'NY 2026-09' }));
    await user.click(screen.getByRole('button', { name: '決定' }));

    expect(onSelect).toHaveBeenCalledWith('b');
  });

  it('選び直さずに決定を押すと今の旅行の id を返す', async () => {
    const onSelect = vi.fn();
    const user = userEvent.setup();
    render(
      <TripPickerSheet trips={trips} activeTripId="a" onSelect={onSelect} onClose={() => {}} />,
    );

    await user.click(screen.getByRole('button', { name: '決定' }));

    expect(onSelect).toHaveBeenCalledWith('a');
  });

  it('✕ で閉じると選択は伝えない', async () => {
    const onSelect = vi.fn();
    const onClose = vi.fn();
    const user = userEvent.setup();
    render(
      <TripPickerSheet trips={trips} activeTripId="a" onSelect={onSelect} onClose={onClose} />,
    );

    await user.click(screen.getByRole('option', { name: 'NY 2026-09' }));
    await user.click(screen.getByRole('button', { name: '閉じる' }));

    expect(onClose).toHaveBeenCalled();
    expect(onSelect).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 失敗を確認する**

```bash
npx vitest run src/ui/TripPickerSheet.test.tsx
```

Expected: FAIL。`Failed to resolve import "./TripPickerSheet"`。

- [ ] **Step 3: 実装する**

`src/ui/TripPickerSheet.tsx` を新規作成:

```tsx
import { useState } from 'react';
import type { Trip } from '../domain/types';
import { Sheet } from './Sheet';
import { WheelPicker } from './WheelPicker';

type Props = {
  trips: Trip[];
  activeTripId: string;
  onSelect: (id: string) => void;
  onClose: () => void;
};

export function TripPickerSheet({ trips, activeTripId, onSelect, onClose }: Props) {
  // 「決定」を押すまでは確定させない。✕ や背景タップで閉じたら選択を捨てる。
  const [pickedId, setPickedId] = useState(activeTripId);

  return (
    <Sheet title="旅行を選ぶ" onClose={onClose}>
      <WheelPicker
        items={trips.map((t) => ({ id: t.id, label: t.name }))}
        selectedId={pickedId}
        onChange={setPickedId}
        label="旅行"
      />
      <div className="form-actions">
        <button type="button" className="btn-primary" onClick={() => onSelect(pickedId)}>
          決定
        </button>
      </div>
    </Sheet>
  );
}
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
npx vitest run src/ui/TripPickerSheet.test.tsx
```

Expected: PASS 4 件。

- [ ] **Step 5: 全体のテストとビルドを確認する**

```bash
npm run test
```

Expected: PASS(全ファイル)。

```bash
npm run build
```

Expected: 成功。

- [ ] **Step 6: コミットする**

```bash
git add src/ui/TripPickerSheet.tsx src/ui/TripPickerSheet.test.tsx && git commit -m "feat: 旅行を選ぶシートを足す"
```

---

### Task 6: ヘッダーからの旅行切り替え

ホーム/集計タブのヘッダーの旅行名をボタンにして、タップで `TripPickerSheet` を開く。設定タブのヘッダーは変えない(旅行リストが既にあるため)。旅行が 1 件以下ならボタンにしない(切り替える先が無いため)。

**Files:**
- Modify: `src/app/App.tsx:16`(state)、`:35-37`(ヘッダー)、`:75`(`</main>` の直後にシート)
- Modify: `src/styles.css`(`.trip-switch`)
- Test: `src/app/App.test.tsx`(新規)

**Interfaces:**
- Consumes: タスク 5 の `TripPickerSheet`、既存の `useActiveTrip()`(`{ trips, activeTrip, loading, selectTrip }`)
- Produces: 画面上の振る舞いのみ。他タスクが依存する新しい export は無い

- [ ] **Step 1: 失敗するテストを書く**

`src/app/App.test.tsx` を新規作成:

```tsx
import { describe, it, expect, beforeEach } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { db } from '../data/db';
import { createTrip } from '../data/tripRepo';
import { App } from './App';

beforeEach(async () => {
  await db.delete();
  await db.open();
  localStorage.clear();
});

/**
 * 旅行を 2 件作り、NY をアクティブにする。
 * どちらがアクティブかを createdAt の並びに頼ると不安定なので localStorage で固定する。
 */
async function seedTwoTrips(): Promise<void> {
  await createTrip({ name: '上海 2026-09', currency: 'CNY' });
  const ny = await createTrip({ name: 'NY 2026-09', currency: 'USD' });
  localStorage.setItem('trip-wallet:active-trip', ny.id);
}

describe('App のヘッダー', () => {
  it('旅行が 2 件以上ならヘッダーがボタンになる', async () => {
    await seedTwoTrips();
    render(<App />);

    const button = await screen.findByRole('button', { name: 'NY 2026-09' });
    expect(button).toHaveAttribute('aria-haspopup', 'dialog');
  });

  it('旅行が 1 件ならヘッダーはボタンにならない', async () => {
    await createTrip({ name: 'NY 2026-09', currency: 'USD' });
    render(<App />);

    // ▾ の span は aria-hidden なので、ボタンでもアクセシブル名は旅行名だけになる。
    // 見出しの中に button があるかどうかで判定する。
    const heading = await screen.findByRole('heading', { level: 1, name: 'NY 2026-09' });
    expect(heading.querySelector('button')).toBeNull();
  });

  it('シートで選び直して決定すると表示中の旅行が変わる', async () => {
    await seedTwoTrips();
    const user = userEvent.setup();
    render(<App />);

    await user.click(await screen.findByRole('button', { name: 'NY 2026-09' }));
    await user.click(await screen.findByRole('option', { name: '上海 2026-09' }));
    await user.click(screen.getByRole('button', { name: '決定' }));

    expect(await screen.findByRole('heading', { level: 1, name: '上海 2026-09' })).toBeInTheDocument();
    expect(screen.queryByRole('dialog')).not.toBeInTheDocument();
  });

  it('✕ で閉じると旅行は変わらない', async () => {
    await seedTwoTrips();
    const user = userEvent.setup();
    render(<App />);

    await user.click(await screen.findByRole('button', { name: 'NY 2026-09' }));
    await user.click(await screen.findByRole('option', { name: '上海 2026-09' }));
    await user.click(screen.getByRole('button', { name: '閉じる' }));

    expect(screen.getByRole('heading', { level: 1, name: 'NY 2026-09' })).toBeInTheDocument();
    expect(screen.queryByRole('dialog')).not.toBeInTheDocument();
  });

  it('設定タブではヘッダーをボタンにしない', async () => {
    await seedTwoTrips();
    const user = userEvent.setup();
    render(<App />);

    await screen.findByRole('button', { name: 'NY 2026-09' });
    await user.click(screen.getByRole('button', { name: '⚙️ 設定' }));

    const heading = screen.getByRole('heading', { level: 1, name: 'NY 2026-09' });
    expect(heading.querySelector('button')).toBeNull();
  });
});
```

`virtual:pwa-register/react` は `vite.config.ts` の `disable: mode === 'test'` により stub になるので、`App` をそのまま render できる(モックは不要)。

- [ ] **Step 2: 失敗を確認する**

```bash
npx vitest run src/app/App.test.tsx
```

Expected: FAIL。`Unable to find an accessible element with the role "button" and name "NY 2026-09"`(今のヘッダーは `<h1>` のテキストのまま)。「旅行が 1 件なら〜」だけは今の実装でも通る。

- [ ] **Step 3: App にシートを組み込む**

`src/app/App.tsx` — import を足す:

```tsx
import { TripPickerSheet } from '../ui/TripPickerSheet';
```

state を足す(`const [dismissed, setDismissed] = ...` の下):

```tsx
  const [picking, setPicking] = useState(false);
```

ヘッダー(35-37 行目)を置き換える:

```tsx
        <header className="app-header">
          {/* 設定タブには旅行リストがあるので切り替えボタンを出さない。
              旅行が 1 件以下のときも切り替える先が無いのでただの見出しにする。 */}
          {tab !== 'settings' && activeTrip !== null && trips.length > 1 ? (
            <h1>
              <button
                type="button"
                className="trip-switch"
                aria-haspopup="dialog"
                onClick={() => setPicking(true)}
              >
                {activeTrip.name}
                <span aria-hidden="true"> ▾</span>
              </button>
            </h1>
          ) : (
            <h1>{activeTrip?.name ?? 'Trip Wallet'}</h1>
          )}
        </header>
```

`</main>` の直後にシートを足す:

```tsx
      {picking && activeTrip !== null && (
        <TripPickerSheet
          trips={trips}
          activeTripId={activeTrip.id}
          onSelect={(id) => {
            selectTrip(id);
            setPicking(false);
          }}
          onClose={() => setPicking(false)}
        />
      )}
```

- [ ] **Step 4: スタイルを足す**

`src/styles.css` の `.app-header h1 { ... }` の直後に足す:

```css
.trip-switch {
  background: none;
  border: none;
  color: inherit;
  font: inherit;
  padding: 0;
}
```

- [ ] **Step 5: テストが通ることを確認する**

```bash
npx vitest run src/app/App.test.tsx
```

Expected: PASS 5 件。

- [ ] **Step 6: 全体のテストとビルドを確認する**

```bash
npm run test
```

Expected: PASS(全ファイル)。

```bash
npm run build
```

Expected: 成功。

- [ ] **Step 7: コミットする**

```bash
git add src/app/App.tsx src/app/App.test.tsx src/styles.css && git commit -m "feat: ホームと集計のヘッダーから旅行を切り替えられるようにする"
```

---

### Task 7: 手動確認チェックリストとバージョン

v1.0.3 で足した挙動を実機確認の対象に足し、バージョンを上げる。

**Files:**
- Modify: `README.md:37-52`
- Modify: `package.json`(version)

**Interfaces:**
- Consumes: タスク 1〜6 の成果すべて
- Produces: なし

- [ ] **Step 1: チェックリストを更新する**

`README.md:39` の一文を戻す(前回の実機確認より後に機能を足したため):

```markdown
リリース前に実機で確認する。チェック済みは 2026-08-11 の実機確認分(v1.0.2 まで)。
```

同ファイルのリスト末尾に未チェックの項目を足す:

```markdown
- [ ] 旅行を新規作成すると通貨が AUD → USD の ISO 順に並び、初期選択が米ドルになっている
- [ ] 旅行名の入力例が「NY 2026-09」になっている
- [ ] ホーム/集計のヘッダーをタップするとドラムが出て、回して「決定」すると旅行が切り替わる
- [ ] ドラムを回さずに ✕ で閉じると旅行は切り替わらない
- [ ] 旅行が 1 件のときはヘッダーがタップできない
- [ ] 個別予算と共有予算を別々に設定でき、ホームにバーが 2 本出てそれぞれの残額が正しい
- [ ] 片方だけ予算を設定するとバーは 1 本だけ出る
- [ ] v1.0.2 で書き出したバックアップを取り込むと、旧予算が個別予算に入る
- [ ] v1.0.2 で入れたアプリを更新すると、既存の旅行の予算が個別予算に移っている
```

- [ ] **Step 2: バージョンを上げる**

`package.json` の `"version": "1.0.2"` を `"version": "1.0.3"` にする。

- [ ] **Step 3: テストとビルドを確認する**

```bash
npm run test
```

Expected: PASS(全ファイル)。

```bash
npm run build
```

Expected: 成功。

- [ ] **Step 4: コミットする**

```bash
git add README.md package.json && git commit -m "chore: bump version to 1.0.3"
```

- [ ] **Step 5: main へのマージ承認を取る**

バージョンを上げるコミットをしたので、`feature/v1-0-3` を `main` にマージしてよいか AskUserQuestion で確認してから進める(グローバルルール)。マージ前に、README のチェックリストの未チェック項目をユーザーが実機で確認する時間を取る。

---

## 依存関係

```
Task 1(通貨・入力例)  ── 独立
Task 2(予算モデル)    ── 独立(ただし Task 3 の前提)
  └─ Task 3(予算 UI)
Task 4(ホイール)      ── 独立
  └─ Task 5(選択シート)  ※ Trip フィクスチャが Task 2 の型に依存
       └─ Task 6(ヘッダー)
Task 7(README・version)── Task 1〜6 すべての後
```

Task 1 / Task 2 / Task 4 は並行してよい。Task 5 は Task 2 と Task 4 の両方が終わってから。
