# v1.0.4 集計グラフ改善 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 集計タブのカテゴリ別を帯グラフ、日別推移を縦棒グラフに作り直し、「個別 / 共有 / 自己負担」で切り替えられるようにする。

**Architecture:** 計算は `src/domain/summary.ts` に集約する。表示スコープ `ViewScope` を引数で受け、対象の支出と 1 件あたりの金額を決める。図は計算済みの結果だけを受け取る表示部品 `CategoryChart` / `DailyChart` に切り出し、Dexie もスコープ切り替えも知らせない。`SummaryScreen` はデータ取得と `view` の state と空状態の出し分けだけを持つ。

**Tech Stack:** React 19 / TypeScript 7 / Vite 8 / Vitest 4 + @testing-library/react / Dexie 4(IndexedDB)。グラフは外部ライブラリを使わず div + CSS で描く。

**設計仕様:** [docs/superpowers/specs/2026-08-11-v1.0.4-design.md](../specs/2026-08-11-v1.0.4-design.md)

## Global Constraints

- 作業ブランチは `feature/v1.0.4-summary-charts`。`main` に直接コミットしない
- 検証コマンドは `npm test`(vitest run)と `npm run build`(tsc --noEmit && vite build)。タスクの最後は必ず `npm test` が全緑
- 円換算は「行ごとに丸めてから足す」。合計を出してから割らない
- 色は CSS カスタムプロパティ(`--bg` `--surface` `--surface-2` `--text` `--text-dim` `--accent`)を優先する。ただしカテゴリ色・平均線の桃色 `#f472b6`・最高額の `#7dd3fc`・日曜の `#fca5a5` は変数を持たないので直値で書く
- カテゴリ色は 食事 `#38bdf8` / 交通 `#a78bfa` / 観光 `#4ade80` / 買物 `#fbbf24` / 宿泊 `#f472b6` / その他 `#64748b`
- 端末内保存のみ。切り替えの選択は永続化しない
- コミットメッセージは日本語、Conventional Commits の prefix(`feat:` `refactor:` `test:` `docs:` `chore:`)
- コメントは「なぜ」を書く。何をしているかはコードで読ませる(既存コードの流儀に合わせる)

---

### Task 1: 日付ユーティリティを足す

縦棒グラフの横軸に使う 3 つの関数を `date.ts` に足す。`eachDate` が 0 円の日を埋めるのに、`dayOfMonth` / `weekdayIndex` が軸ラベルの「日」「曜日」と土日の色分けに要る。

**Files:**
- Modify: `src/domain/date.ts`
- Test: `src/domain/date.test.ts`

**Interfaces:**
- Consumes: 既存の `parseIsoDate` / `addDays` / `WEEKDAYS`(いまは非公開の定数)
- Produces:
  - `eachDate(from: string, to: string): string[]`
  - `dayOfMonth(isoDate: string): number`
  - `weekdayIndex(isoDate: string): number`(0=日 〜 6=土)
  - `WEEKDAYS` を export に変える(`DailyChart` が曜日 1 文字を出すのに使う。同じ配列を 2 か所に持たないため)

- [ ] **Step 1: 失敗するテストを書く**

`src/domain/date.test.ts` の import 行を差し替える。

```ts
import {
  todayLocal,
  toIsoDate,
  parseIsoDate,
  formatDateLabel,
  addDays,
  eachDate,
  dayOfMonth,
  weekdayIndex,
} from './date';
```

ファイル末尾に足す。

```ts
describe('eachDate', () => {
  it('月をまたいで 1 日ずつ並べる', () => {
    expect(eachDate('2026-08-30', '2026-09-02')).toEqual([
      '2026-08-30',
      '2026-08-31',
      '2026-09-01',
      '2026-09-02',
    ]);
  });

  it('同じ日を渡すと 1 件', () => {
    expect(eachDate('2026-09-12', '2026-09-12')).toEqual(['2026-09-12']);
  });

  it('終わりが始まりより前なら空', () => {
    expect(eachDate('2026-09-12', '2026-09-11')).toEqual([]);
  });
});

describe('dayOfMonth / weekdayIndex', () => {
  it('日と曜日を取り出す', () => {
    expect(dayOfMonth('2026-09-12')).toBe(12);
    expect(weekdayIndex('2026-09-12')).toBe(6); // 土
    expect(weekdayIndex('2026-09-13')).toBe(0); // 日
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npm test -- src/domain/date.test.ts`
Expected: FAIL(`eachDate is not a function` など、import が解決できない)

- [ ] **Step 3: 実装する**

`src/domain/date.ts` の冒頭の `WEEKDAYS` を export に変える(定義はそのまま)。

```ts
export const WEEKDAYS = ['日', '月', '火', '水', '木', '金', '土'] as const;
```

同じファイルの末尾に足す。

```ts
/** from〜to を 1 日ずつ並べた ISO 日付。to が from より前なら空。ISO 文字列は辞書順で日付順になる。 */
export function eachDate(from: string, to: string): string[] {
  const dates: string[] = [];
  let cur = from;
  while (cur <= to) {
    dates.push(cur);
    cur = addDays(cur, 1);
  }
  return dates;
}

export function dayOfMonth(isoDate: string): number {
  return parseIsoDate(isoDate).getDate();
}

/** 0=日 〜 6=土 */
export function weekdayIndex(isoDate: string): number {
  return parseIsoDate(isoDate).getDay();
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `npm test -- src/domain/date.test.ts`
Expected: PASS

- [ ] **Step 5: コミットする**

```bash
git add src/domain/date.ts src/domain/date.test.ts && git commit -m "feat: 日付の連続生成と日・曜日の取り出しを足す"
```

---

### Task 2: カテゴリに色を持たせる

**Files:**
- Modify: `src/domain/categories.ts`
- Test: `src/domain/categories.test.ts`(新規)

**Interfaces:**
- Produces:
  - `CATEGORIES` の各要素に `color: string` が増える
  - `categoryColor(v: Category): string`

- [ ] **Step 1: 失敗するテストを書く**

`src/domain/categories.test.ts` を新規作成する。

```ts
import { describe, it, expect } from 'vitest';
import { CATEGORIES, categoryColor } from './categories';

describe('categoryColor', () => {
  it('カテゴリごとの色を返す', () => {
    expect(categoryColor('food')).toBe('#38bdf8');
    expect(categoryColor('lodging')).toBe('#f472b6');
    expect(categoryColor('other')).toBe('#64748b');
  });

  it('すべてのカテゴリに違う色がある', () => {
    const colors = CATEGORIES.map((c) => c.color);
    expect(new Set(colors).size).toBe(CATEGORIES.length);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npm test -- src/domain/categories.test.ts`
Expected: FAIL(`categoryColor is not a function`)

- [ ] **Step 3: 実装する**

`src/domain/categories.ts` の `CATEGORIES` を差し替える。

```ts
export const CATEGORIES: { value: Category; label: string; icon: string; color: string }[] = [
  { value: 'food', label: '食事', icon: '🍜', color: '#38bdf8' },
  { value: 'transport', label: '交通', icon: '🚇', color: '#a78bfa' },
  { value: 'sightseeing', label: '観光', icon: '🎫', color: '#4ade80' },
  { value: 'shopping', label: '買物', icon: '🛍️', color: '#fbbf24' },
  { value: 'lodging', label: '宿泊', icon: '🏨', color: '#f472b6' },
  { value: 'other', label: 'その他', icon: '📝', color: '#64748b' },
];
```

`categoryIcon` の下に足す。

```ts
export function categoryColor(v: Category): string {
  return CATEGORIES.find((c) => c.value === v)?.color ?? '#64748b';
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `npm test`
Expected: PASS(既存テストも含めて全緑)

- [ ] **Step 5: コミットする**

```bash
git add src/domain/categories.ts src/domain/categories.test.ts && git commit -m "feat: カテゴリに固定色を持たせる"
```

---

### Task 3: 1 件あたりの自己負担を単一の真実にする

いまの `summarize` は共有の**合計**を人数で割って丸めている。カテゴリ別・日別で自己負担を出すには**行ごと**に割るしかないので、そのままだと同じ画面に数円ずれた数字が 2 つ並ぶ。`myShareJpy` に寄せて揃える。

**Files:**
- Modify: `src/domain/summary.ts:32-64`
- Test: `src/domain/summary.test.ts`

**Interfaces:**
- Consumes: 既存の `expenseJpy(e: Expense, trip: Trip): number`
- Produces: `myShareJpy(e: Expense, trip: Trip): number`

- [ ] **Step 1: 失敗するテストを書く**

`src/domain/summary.test.ts` の import に `myShareJpy` を足す。

```ts
import {
  expenseJpy,
  myShareJpy,
  summarize,
  breakdownByCategory,
  totalsByDate,
  groupByDate,
} from './summary';
```

`describe('expenseJpy', ...)` の直後に足す。

```ts
describe('myShareJpy', () => {
  it('個別はそのままの額', () => {
    expect(myShareJpy(expense(), trip)).toBe(2000);
  });

  it('共有は人数で割る', () => {
    expect(myShareJpy(expense({ scope: 'shared' }), trip)).toBe(1000);
  });

  it('割り切れないときは 1 件ごとに四捨五入する', () => {
    // 2000 円 / 3 人 = 666.67 → 667 円
    expect(myShareJpy(expense({ scope: 'shared' }), { ...trip, memberCount: 3 })).toBe(667);
  });

  it('人数が 0 でも 0 除算しない', () => {
    expect(myShareJpy(expense({ scope: 'shared' }), { ...trip, memberCount: 0 })).toBe(2000);
  });
});
```

`describe('summarize', ...)` の中、`it('人数が 0 でも 0 除算しない', ...)` の直後に足す。

```ts
  it('共有の自己負担は 1 件ずつ割ってから足す', () => {
    // 2000 円 / 3 人 = 667 円が 2 件で 1334 円。先に合計 4000 円を割ると 1333 円になり、
    // カテゴリ別・日別の積み上げと合わなくなる。
    const s = summarize([expense({ scope: 'shared' }), expense({ scope: 'shared' })], {
      ...trip,
      memberCount: 3,
    });
    expect(s.sharedPerPersonJpy).toBe(1334);
    expect(s.myTotalJpy).toBe(1334);
  });
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npm test -- src/domain/summary.test.ts`
Expected: FAIL。`myShareJpy` が未定義。新しい summarize のテストは `expected 1333 to be 1334`

- [ ] **Step 3: 実装する**

`src/domain/summary.ts` の `expenseJpy` の下に足す。

```ts
/**
 * 支出 1 件の自己負担。共有は人数で割る。
 * 合計を割るのではなく 1 件ごとに丸めるのは、カテゴリ別・日別の積み上げと
 * 合計カードの数字を一致させるため。
 */
export function myShareJpy(e: Expense, trip: Trip): number {
  const jpy = expenseJpy(e, trip);
  if (e.scope === 'personal') return jpy;
  return Math.round(jpy / Math.max(1, trip.memberCount));
}
```

`summarize` を差し替える(ループで `sharedPerPersonJpy` を積み上げ、`members` 変数を消す)。

```ts
export function summarize(expenses: Expense[], trip: Trip): TripSummary {
  let totalMinor = 0;
  let personalJpy = 0;
  let sharedJpy = 0;
  let sharedPerPersonJpy = 0;

  for (const e of expenses) {
    totalMinor += e.amountMinor;
    // 行ごとに丸めてから足す。画面に出る各行の合計と一致させるため。
    const jpy = expenseJpy(e, trip);
    if (e.scope === 'shared') {
      sharedJpy += jpy;
      sharedPerPersonJpy += myShareJpy(e, trip);
    } else {
      personalJpy += jpy;
    }
  }

  const totalJpy = personalJpy + sharedJpy;

  return {
    count: expenses.length,
    totalMinor,
    totalJpy,
    personalJpy,
    sharedJpy,
    sharedPerPersonJpy,
    myTotalJpy: personalJpy + sharedPerPersonJpy,
    personalBudgetJpy: trip.personalBudgetJpy,
    personalRemainingJpy:
      trip.personalBudgetJpy === null ? null : trip.personalBudgetJpy - personalJpy,
    sharedBudgetJpy: trip.sharedBudgetJpy,
    sharedRemainingJpy:
      trip.sharedBudgetJpy === null ? null : trip.sharedBudgetJpy - sharedPerPersonJpy,
  };
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `npm test`
Expected: PASS。`summarize` の既存 7 件(`¥2,000` / `¥4,000` など)と `HomeScreen.test.tsx` も落ちないこと。落ちたら丸め方の変更が想定外の場所に効いているので止めて調べる

- [ ] **Step 5: コミットする**

```bash
git add src/domain/summary.ts src/domain/summary.test.ts && git commit -m "refactor: 自己負担の丸めを 1 件ごとに揃える"
```

---

### Task 4: カテゴリ別を表示スコープで絞る

**Files:**
- Modify: `src/domain/summary.ts`
- Modify: `src/ui/SummaryScreen.tsx:25`
- Test: `src/domain/summary.test.ts`、`src/ui/SummaryScreen.test.tsx`

**Interfaces:**
- Consumes: `expenseJpy`、`myShareJpy`
- Produces:
  - `type ViewScope = 'personal' | 'shared' | 'mine'`
  - `breakdownByCategory(expenses: Expense[], trip: Trip, view: ViewScope): CategoryBreakdown[]`(第 3 引数が必須になる)
  - 非公開ヘルパー `viewJpy(e, trip, view): number | null`(対象外なら null)。Task 5 の `dailySeries` も使う

- [ ] **Step 1: 失敗するテストを書く**

`src/domain/summary.test.ts` の `describe('breakdownByCategory', ...)` を丸ごと差し替える。既存 3 件は第 3 引数 `'mine'` を足すだけ(中身は個別支出なので期待値は変わらない)。

```ts
describe('breakdownByCategory', () => {
  it('金額の多い順に並べ、構成比を付ける', () => {
    const rows = breakdownByCategory(
      [
        expense({ amountMinor: 10000, category: 'food' }),
        expense({ amountMinor: 20000, category: 'transport' }),
      ],
      trip,
      'mine',
    );
    expect(rows.map((r) => r.category)).toEqual(['transport', 'food']);
    expect(rows[0].jpy).toBe(4000);
    expect(rows[0].ratio).toBeCloseTo(2 / 3, 5);
  });

  it('同じカテゴリはまとめる', () => {
    const rows = breakdownByCategory(
      [expense({ category: 'food' }), expense({ category: 'food' })],
      trip,
      'mine',
    );
    expect(rows).toHaveLength(1);
    expect(rows[0].jpy).toBe(4000);
  });

  it('支出が無ければ空配列', () => {
    expect(breakdownByCategory([], trip, 'mine')).toEqual([]);
  });

  it('個別は共有を除く', () => {
    const rows = breakdownByCategory(
      [expense({ category: 'food' }), expense({ category: 'transport', scope: 'shared' })],
      trip,
      'personal',
    );
    expect(rows).toHaveLength(1);
    expect(rows[0].category).toBe('food');
    expect(rows[0].jpy).toBe(2000);
  });

  it('共有は人数で割らない支払総額', () => {
    const rows = breakdownByCategory(
      [expense({ category: 'food' }), expense({ category: 'transport', scope: 'shared' })],
      trip,
      'shared',
    );
    expect(rows).toHaveLength(1);
    expect(rows[0].category).toBe('transport');
    expect(rows[0].jpy).toBe(2000);
  });

  it('自己負担は共有を人数で割って個別と混ぜる', () => {
    const rows = breakdownByCategory(
      [expense({ category: 'food' }), expense({ category: 'transport', scope: 'shared' })],
      trip,
      'mine',
    );
    expect(rows.map((r) => r.category)).toEqual(['food', 'transport']);
    expect(rows[0].jpy).toBe(2000);
    expect(rows[1].jpy).toBe(1000);
    expect(rows[0].ratio).toBeCloseTo(2 / 3, 5);
  });

  it('選んだスコープに該当が無ければ空配列', () => {
    expect(breakdownByCategory([expense()], trip, 'shared')).toEqual([]);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npm test -- src/domain/summary.test.ts`
Expected: FAIL(「個別は共有を除く」などスコープ別の 4 件。第 3 引数が無視されて全件が集計される)

- [ ] **Step 3: 実装する**

`src/domain/summary.ts` の型定義に足す(`CategoryBreakdown` の並びの前)。

```ts
/** 図に出す対象。データの Scope とは別で、mine は個別 + 共有の人数割り。 */
export type ViewScope = 'personal' | 'shared' | 'mine';
```

`myShareJpy` の下に足す。

```ts
/** 表示スコープでの 1 件の円。対象外の支出なら null。 */
function viewJpy(e: Expense, trip: Trip, view: ViewScope): number | null {
  if (view === 'mine') return myShareJpy(e, trip);
  if (view === 'personal') return e.scope === 'personal' ? expenseJpy(e, trip) : null;
  return e.scope === 'shared' ? expenseJpy(e, trip) : null;
}
```

`breakdownByCategory` を差し替える。

```ts
export function breakdownByCategory(
  expenses: Expense[],
  trip: Trip,
  view: ViewScope,
): CategoryBreakdown[] {
  const totals = new Map<Category, number>();
  let total = 0;

  for (const e of expenses) {
    const jpy = viewJpy(e, trip, view);
    if (jpy === null) continue;
    totals.set(e.category, (totals.get(e.category) ?? 0) + jpy);
    total += jpy;
  }

  return [...totals.entries()]
    .map(([category, jpy]) => ({ category, jpy, ratio: total === 0 ? 0 : jpy / total }))
    .sort((a, b) => b.jpy - a.jpy);
}
```

- [ ] **Step 4: 呼び出し側を直す**

`src/ui/SummaryScreen.tsx:25` を差し替える。初期表示は自己負担なので `'mine'` を直に渡す(state 化は Task 8)。

```tsx
  const categories = breakdownByCategory(list, trip, 'mine');
```

- [ ] **Step 5: 集計画面のテストの期待値を直す**

`src/ui/SummaryScreen.test.tsx` の `it('カテゴリ別を金額の多い順に並べる', ...)` を差し替える。共有の交通 ¥2,300 が 2 人割りで ¥1,150 になり、構成比も変わる。

```tsx
  it('カテゴリ別を自己負担の多い順に並べる', async () => {
    render(<SummaryScreen trip={trip} />);

    const rows = await screen.findAllByTestId('cat-row');
    expect(rows).toHaveLength(2);
    expect(rows[0]).toHaveTextContent('食事');
    expect(rows[0]).toHaveTextContent('¥2,816');
    expect(rows[0]).toHaveTextContent('71%');
    expect(rows[1]).toHaveTextContent('交通');
    expect(rows[1]).toHaveTextContent('¥1,150');
    expect(rows[1]).toHaveTextContent('29%');
  });
```

- [ ] **Step 6: テストとビルドが通ることを確認する**

Run: `npm test`
Expected: PASS

Run: `npm run build`
Expected: 成功(型エラーなし)

- [ ] **Step 7: コミットする**

```bash
git add src/domain/summary.ts src/domain/summary.test.ts src/ui/SummaryScreen.tsx src/ui/SummaryScreen.test.tsx && git commit -m "feat: カテゴリ別を表示スコープで絞れるようにする"
```

---

### Task 5: 日別の系列を作る

縦棒グラフに要るもの(0 円の日を埋めた並び、最高額、平均、最高額の日)を 1 つの関数で返す。この時点では画面はまだ古い `totalsByDate` のままにしておく。差し替えは Task 8 でまとめて行う。

**Files:**
- Modify: `src/domain/summary.ts`
- Test: `src/domain/summary.test.ts`

**Interfaces:**
- Consumes: `viewJpy`(Task 4)、`eachDate`(Task 1)
- Produces:
  - `type DailyPoint = { date: string; jpy: number }`
  - `type DailySeries = { points: DailyPoint[]; totalJpy: number; maxJpy: number; peakDate: string | null; averageJpy: number }`
  - `dailySeries(expenses: Expense[], trip: Trip, view: ViewScope): DailySeries`

- [ ] **Step 1: 失敗するテストを書く**

`src/domain/summary.test.ts` の import に `dailySeries` を足す(`totalsByDate` はまだ残す)。

```ts
import {
  expenseJpy,
  myShareJpy,
  summarize,
  breakdownByCategory,
  dailySeries,
  totalsByDate,
  groupByDate,
} from './summary';
```

`describe('totalsByDate', ...)` の直前に足す。

```ts
describe('dailySeries', () => {
  it('支出のない日を 0 円で埋める', () => {
    const s = dailySeries(
      [expense({ date: '2026-09-12' }), expense({ date: '2026-09-15' })],
      trip,
      'mine',
    );
    expect(s.points.map((p) => p.date)).toEqual([
      '2026-09-12',
      '2026-09-13',
      '2026-09-14',
      '2026-09-15',
    ]);
    expect(s.points.map((p) => p.jpy)).toEqual([2000, 0, 0, 2000]);
  });

  it('範囲は旅行期間ではなく最初と最後の支出日', () => {
    // 旅行は 9/12〜9/15 だが支出は 9/13 の 1 件だけ。まだ来ていない日は出さない。
    const s = dailySeries([expense({ date: '2026-09-13' })], trip, 'mine');
    expect(s.points.map((p) => p.date)).toEqual(['2026-09-13']);
  });

  it('平均は 0 円の日も分母に入れる', () => {
    const s = dailySeries(
      [expense({ date: '2026-09-12' }), expense({ date: '2026-09-14' })],
      trip,
      'mine',
    );
    expect(s.totalJpy).toBe(4000);
    expect(s.averageJpy).toBe(1333); // 4000 / 3 日
  });

  it('最高額の日を返す。同額なら先の日', () => {
    const s = dailySeries(
      [
        expense({ date: '2026-09-12', amountMinor: 5000 }),
        expense({ date: '2026-09-13', amountMinor: 20000 }),
        expense({ date: '2026-09-14', amountMinor: 20000 }),
      ],
      trip,
      'mine',
    );
    expect(s.maxJpy).toBe(4000);
    expect(s.peakDate).toBe('2026-09-13');
  });

  it('表示スコープで対象を絞る', () => {
    const s = dailySeries(
      [expense({ date: '2026-09-12' }), expense({ date: '2026-09-13', scope: 'shared' })],
      trip,
      'shared',
    );
    expect(s.points).toEqual([{ date: '2026-09-13', jpy: 2000 }]);
  });

  it('選んだスコープに該当が無ければ空', () => {
    expect(dailySeries([expense()], trip, 'shared')).toEqual({
      points: [],
      totalJpy: 0,
      maxJpy: 0,
      peakDate: null,
      averageJpy: 0,
    });
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npm test -- src/domain/summary.test.ts`
Expected: FAIL(`dailySeries is not a function`)

- [ ] **Step 3: 実装する**

`src/domain/summary.ts` は `./date` を import していないので、2 行目の `import { toJpy } from './money';` の下に足す。

```ts
import { eachDate } from './date';
```

`DateTotal` の隣に型を足す。

```ts
export type DailyPoint = { date: string; jpy: number };
export type DailySeries = {
  /** 最初の支出日〜最後の支出日を 1 日ずつ。0 円の日も含む。日付の昇順 */
  points: DailyPoint[];
  totalJpy: number;
  /** 棒の高さの基準 */
  maxJpy: number;
  /** 最高額の日。同額なら先の日。points が空なら null */
  peakDate: string | null;
  /** 1 日あたりの平均。0 円の日も分母に含む */
  averageJpy: number;
};
```

`totalsByDate` の直前に足す。

```ts
export function dailySeries(expenses: Expense[], trip: Trip, view: ViewScope): DailySeries {
  const totals = new Map<string, number>();
  for (const e of expenses) {
    const jpy = viewJpy(e, trip, view);
    if (jpy === null) continue;
    totals.set(e.date, (totals.get(e.date) ?? 0) + jpy);
  }

  const dates = [...totals.keys()].sort();
  if (dates.length === 0) {
    return { points: [], totalJpy: 0, maxJpy: 0, peakDate: null, averageJpy: 0 };
  }

  // 支出のない日も 0 円で埋める。日付が飛ぶと旅程の中でのペースが読めなくなるため。
  const points = eachDate(dates[0], dates[dates.length - 1]).map((date) => ({
    date,
    jpy: totals.get(date) ?? 0,
  }));

  let totalJpy = 0;
  let maxJpy = 0;
  let peakDate = points[0].date;
  for (const p of points) {
    totalJpy += p.jpy;
    if (p.jpy > maxJpy) {
      maxJpy = p.jpy;
      peakDate = p.date;
    }
  }

  return {
    points,
    totalJpy,
    maxJpy,
    peakDate,
    averageJpy: Math.round(totalJpy / points.length),
  };
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `npm test`
Expected: PASS

- [ ] **Step 5: コミットする**

```bash
git add src/domain/summary.ts src/domain/summary.test.ts && git commit -m "feat: 0 円の日を埋めた日別の系列を作る"
```

---

### Task 6: カテゴリ別の帯グラフ部品

**Files:**
- Create: `src/ui/CategoryChart.tsx`
- Create: `src/ui/CategoryChart.test.tsx`
- Modify: `src/styles.css`(末尾に追記)

**Interfaces:**
- Consumes: `CategoryBreakdown`(`src/domain/summary.ts`)、`categoryColor` / `categoryIcon` / `categoryLabel`、`formatJpy`
- Produces: `CategoryChart({ rows }: { rows: CategoryBreakdown[] })`。`rows` が空なら `null` を返す(空状態の文言は呼び出し側の担当)

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/CategoryChart.test.tsx` を新規作成する。

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import type { CategoryBreakdown } from '../domain/summary';
import { CategoryChart } from './CategoryChart';

const rows: CategoryBreakdown[] = [
  { category: 'food', jpy: 2816, ratio: 0.71 },
  { category: 'transport', jpy: 1150, ratio: 0.29 },
];

describe('CategoryChart', () => {
  it('帯の区画を構成比の幅とカテゴリ色で並べる', () => {
    render(<CategoryChart rows={rows} />);

    const segs = screen.getAllByTestId('stack-seg');
    expect(segs).toHaveLength(2);
    expect(segs[0]).toHaveStyle({ width: '71%', backgroundColor: '#38bdf8' });
    expect(segs[1]).toHaveStyle({ width: '29%', backgroundColor: '#a78bfa' });
  });

  it('凡例を帯と同じ順・同じ色で並べる', () => {
    render(<CategoryChart rows={rows} />);

    const legend = screen.getAllByTestId('cat-row');
    expect(legend).toHaveLength(2);
    expect(legend[0]).toHaveTextContent('食事');
    expect(legend[0]).toHaveTextContent('¥2,816');
    expect(legend[0]).toHaveTextContent('71%');
    expect(legend[1]).toHaveTextContent('交通');
    expect(legend[1]).toHaveTextContent('¥1,150');
    expect(legend[1]).toHaveTextContent('29%');
    expect(screen.getAllByTestId('legend-dot')[1]).toHaveStyle({ backgroundColor: '#a78bfa' });
  });

  it('行が無ければ何も描かない', () => {
    const { container } = render(<CategoryChart rows={[]} />);
    expect(container).toBeEmptyDOMElement();
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npm test -- src/ui/CategoryChart.test.tsx`
Expected: FAIL(`Failed to resolve import './CategoryChart'`)

- [ ] **Step 3: 実装する**

`src/ui/CategoryChart.tsx` を新規作成する。

```tsx
import { categoryColor, categoryIcon, categoryLabel } from '../domain/categories';
import { formatJpy } from '../domain/money';
import type { CategoryBreakdown } from '../domain/summary';

/** 構成比の帯と、同じ色・同じ順の内訳リスト。色が凡例の役割を兼ねる。 */
export function CategoryChart({ rows }: { rows: CategoryBreakdown[] }) {
  if (rows.length === 0) return null;

  return (
    <>
      <div className="stack-bar">
        {rows.map((r) => (
          <span
            key={r.category}
            className="stack-seg"
            data-testid="stack-seg"
            style={{ width: `${r.ratio * 100}%`, backgroundColor: categoryColor(r.category) }}
          />
        ))}
      </div>
      <ul className="legend">
        {rows.map((r) => (
          <li className="legend-row" data-testid="cat-row" key={r.category}>
            <span
              className="legend-dot"
              data-testid="legend-dot"
              style={{ backgroundColor: categoryColor(r.category) }}
            />
            <span className="legend-name">
              {categoryIcon(r.category)} {categoryLabel(r.category)}
            </span>
            <span className="legend-jpy">{formatJpy(r.jpy)}</span>
            <span className="legend-pct">{Math.round(r.ratio * 100)}%</span>
          </li>
        ))}
      </ul>
    </>
  );
}
```

- [ ] **Step 4: スタイルを足す**

`src/styles.css` の末尾に足す。

```css
.stack-bar {
  display: flex;
  height: 22px;
  border-radius: 999px;
  overflow: hidden;
  background: var(--surface-2);
  margin-bottom: 12px;
}

.stack-seg {
  display: block;
}

.legend {
  list-style: none;
  margin: 0;
  padding: 0;
}

.legend-row {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 6px 0;
  border-bottom: 1px solid var(--surface);
  font-size: 13px;
}

.legend-row:last-child {
  border-bottom: none;
}

.legend-dot {
  width: 9px;
  height: 9px;
  border-radius: 3px;
  flex-shrink: 0;
}

.legend-name {
  flex: 1;
}

.legend-jpy {
  font-variant-numeric: tabular-nums;
  font-weight: 600;
}

.legend-pct {
  width: 34px;
  text-align: right;
  color: var(--text-dim);
  font-variant-numeric: tabular-nums;
  font-size: 12px;
}
```

- [ ] **Step 5: テストが通ることを確認する**

Run: `npm test -- src/ui/CategoryChart.test.tsx`
Expected: PASS

- [ ] **Step 6: コミットする**

```bash
git add src/ui/CategoryChart.tsx src/ui/CategoryChart.test.tsx src/styles.css && git commit -m "feat: カテゴリ別の帯グラフ部品を足す"
```

---

### Task 7: 日別推移の縦棒グラフ部品

**Files:**
- Create: `src/ui/DailyChart.tsx`
- Create: `src/ui/DailyChart.test.tsx`
- Modify: `src/styles.css`(末尾に追記)

**Interfaces:**
- Consumes: `DailySeries`(Task 5)、`formatDateLabel` / `dayOfMonth` / `weekdayIndex`(Task 1)、`formatJpy`
- Produces: `DailyChart({ series }: { series: DailySeries })`。`series.points` が空なら `null` を返す

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/DailyChart.test.tsx` を新規作成する。

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { eachDate } from '../domain/date';
import type { DailySeries } from '../domain/summary';
import { DailyChart } from './DailyChart';

const threeDays: DailySeries = {
  points: [
    { date: '2026-09-12', jpy: 2816 },
    { date: '2026-09-13', jpy: 0 },
    { date: '2026-09-14', jpy: 1150 },
  ],
  totalJpy: 3966,
  maxJpy: 2816,
  peakDate: '2026-09-12',
  averageJpy: 1322,
};

describe('DailyChart', () => {
  it('0 円の日にも棒と日付を出す', () => {
    render(<DailyChart series={threeDays} />);

    const cols = screen.getAllByTestId('day-col');
    expect(cols).toHaveLength(3);
    expect(cols[1]).toHaveAttribute('aria-label', '9/13(日) ¥0');
    expect(screen.getAllByTestId('day-label')).toHaveLength(3);
  });

  it('最高額の日と 0 円の日で棒の見た目を変える', () => {
    render(<DailyChart series={threeDays} />);

    const cols = screen.getAllByTestId('day-col');
    expect(cols[0].firstChild).toHaveClass('peak');
    expect(cols[1].firstChild).toHaveClass('zero');
    expect(cols[2].firstChild).not.toHaveClass('peak');
  });

  it('平均と最高を図の下に出す', () => {
    render(<DailyChart series={threeDays} />);

    expect(screen.getByTestId('daily-average')).toHaveTextContent('1日あたり平均 ¥1,322');
    expect(screen.getByTestId('daily-peak')).toHaveTextContent('最高 9/12(土) ¥2,816');
  });

  it('31 日分では日付ラベルを間引く', () => {
    const points = eachDate('2026-09-01', '2026-10-01').map((date) => ({ date, jpy: 1000 }));
    render(
      <DailyChart
        series={{
          points,
          totalJpy: 31000,
          maxJpy: 1000,
          peakDate: '2026-09-01',
          averageJpy: 1000,
        }}
      />,
    );

    expect(screen.getAllByTestId('day-col')).toHaveLength(31);
    // step = ceil(31 / 7) = 5 → 0, 5, 10, 15, 20, 25, 30 の 7 個
    expect(screen.getAllByTestId('day-label')).toHaveLength(7);
  });

  it('points が空なら何も描かない', () => {
    const { container } = render(
      <DailyChart
        series={{ points: [], totalJpy: 0, maxJpy: 0, peakDate: null, averageJpy: 0 }}
      />,
    );
    expect(container).toBeEmptyDOMElement();
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npm test -- src/ui/DailyChart.test.tsx`
Expected: FAIL(`Failed to resolve import './DailyChart'`)

- [ ] **Step 3: 実装する**

`src/ui/DailyChart.tsx` を新規作成する。

```tsx
import { WEEKDAYS, dayOfMonth, formatDateLabel, weekdayIndex } from '../domain/date';
import { formatJpy } from '../domain/money';
import type { DailySeries } from '../domain/summary';

const BAR_HEIGHT = 110;

/** 日付ラベルは 7 個くらいまでに間引く。長い旅行でも横スクロールさせないため。 */
function labelStep(days: number): number {
  return Math.max(1, Math.ceil(days / 7));
}

function tickClass(isoDate: string): string {
  const wd = weekdayIndex(isoDate);
  if (wd === 6) return 'daily-tick sat';
  if (wd === 0) return 'daily-tick sun';
  return 'daily-tick';
}

function barClass(jpy: number, isPeak: boolean): string {
  if (jpy === 0) return 'daily-bar zero';
  return isPeak ? 'daily-bar peak' : 'daily-bar';
}

export function DailyChart({ series }: { series: DailySeries }) {
  const { points, maxJpy, peakDate, averageJpy } = series;
  if (points.length === 0) return null;

  const step = labelStep(points.length);
  // 日数が多いと棒が潰れるので間隔を詰める。横スクロールはさせない。
  const gap = points.length >= 15 ? 2 : 6;
  const scale = Math.max(1, maxJpy);

  return (
    <div className="daily">
      <div className="daily-bars" style={{ gap: `${gap}px`, height: `${BAR_HEIGHT}px` }}>
        {averageJpy > 0 && (
          <div className="daily-avg" style={{ bottom: `${(averageJpy / scale) * BAR_HEIGHT}px` }}>
            <span>平均 {formatJpy(averageJpy)}</span>
          </div>
        )}
        {points.map((p) => (
          <div
            className="daily-col"
            data-testid="day-col"
            key={p.date}
            role="img"
            aria-label={`${formatDateLabel(p.date)} ${formatJpy(p.jpy)}`}
          >
            <span
              className={barClass(p.jpy, p.date === peakDate && maxJpy > 0)}
              style={p.jpy === 0 ? undefined : { height: `${(p.jpy / scale) * BAR_HEIGHT}px` }}
            />
          </div>
        ))}
      </div>

      <div className="daily-axis" style={{ gap: `${gap}px` }}>
        {points.map((p, i) => (
          <span className={tickClass(p.date)} key={p.date}>
            {i % step === 0 && (
              <span data-testid="day-label">
                {dayOfMonth(p.date)}
                <br />
                {WEEKDAYS[weekdayIndex(p.date)]}
              </span>
            )}
          </span>
        ))}
      </div>

      <div className="daily-foot">
        <span data-testid="daily-average">
          1日あたり平均 <b>{formatJpy(averageJpy)}</b>
        </span>
        {maxJpy > 0 && peakDate && (
          <span data-testid="daily-peak">
            最高 {formatDateLabel(peakDate)} <b>{formatJpy(maxJpy)}</b>
          </span>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: スタイルを足す**

`src/styles.css` の末尾に足す。

```css
.daily-bars {
  position: relative;
  display: flex;
  align-items: flex-end;
  border-bottom: 1px solid var(--surface-2);
}

.daily-col {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  height: 100%;
}

.daily-bar {
  display: block;
  background: var(--accent);
  border-radius: 4px 4px 0 0;
}

.daily-bar.peak {
  background: #7dd3fc;
}

.daily-bar.zero {
  height: 2px;
  background: var(--surface-2);
}

.daily-avg {
  position: absolute;
  left: 0;
  right: 0;
  border-top: 1px dashed #f472b6;
}

.daily-avg span {
  position: absolute;
  right: 0;
  top: -14px;
  font-size: 10px;
  color: #f472b6;
  background: var(--bg);
  padding: 0 3px;
}

.daily-axis {
  display: flex;
  margin-top: 5px;
}

.daily-tick {
  flex: 1;
  text-align: center;
  font-size: 10px;
  line-height: 1.3;
  color: var(--text-dim);
}

.daily-tick.sat {
  color: #7dd3fc;
}

.daily-tick.sun {
  color: #fca5a5;
}

.daily-foot {
  display: flex;
  justify-content: space-between;
  margin-top: 10px;
  font-size: 11px;
  color: var(--text-dim);
}

.daily-foot b {
  color: var(--text);
  font-variant-numeric: tabular-nums;
}
```

- [ ] **Step 5: テストが通ることを確認する**

Run: `npm test -- src/ui/DailyChart.test.tsx`
Expected: PASS

- [ ] **Step 6: コミットする**

```bash
git add src/ui/DailyChart.tsx src/ui/DailyChart.test.tsx src/styles.css && git commit -m "feat: 日別推移の縦棒グラフ部品を足す"
```

---

### Task 8: 集計画面に切り替えと 2 つの図を組み込む

**Files:**
- Modify: `src/ui/SummaryScreen.tsx`(全面)
- Modify: `src/ui/SummaryScreen.test.tsx`(全面)
- Modify: `src/domain/summary.ts`(`totalsByDate` と `DateTotal` を削除)
- Modify: `src/domain/summary.test.ts`(`totalsByDate` の describe と import を削除)
- Modify: `src/styles.css:549-581`(`.cat-row` 系を削除)、末尾に `.chart-head` を追記

**Interfaces:**
- Consumes: `CategoryChart`(Task 6)、`DailyChart`(Task 7)、`breakdownByCategory` / `dailySeries` / `ViewScope`
- Produces: なし(画面の終端)

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/SummaryScreen.test.tsx` の `describe('SummaryScreen', ...)` を丸ごと差し替える。import に `userEvent` を足す。

```tsx
import userEvent from '@testing-library/user-event';
```

```tsx
describe('SummaryScreen', () => {
  it('個別・共有・人数割りを表示する', async () => {
    render(<SummaryScreen trip={trip} />);

    expect(await screen.findByTestId('summary-total')).toHaveTextContent('¥5,116');
    expect(screen.getByTestId('summary-personal')).toHaveTextContent('¥2,816');
    expect(screen.getByTestId('summary-shared')).toHaveTextContent('¥2,300');
    expect(screen.getByTestId('summary-share-note')).toHaveTextContent('自分の負担 ¥1,150(2人)');
    expect(screen.getByTestId('summary-mine')).toHaveTextContent('¥3,966');
  });

  it('初期表示は自己負担で、カテゴリ別を多い順に並べる', async () => {
    render(<SummaryScreen trip={trip} />);

    const rows = await screen.findAllByTestId('cat-row');
    expect(rows).toHaveLength(2);
    expect(rows[0]).toHaveTextContent('食事');
    expect(rows[0]).toHaveTextContent('¥2,816');
    expect(rows[0]).toHaveTextContent('71%');
    expect(rows[1]).toHaveTextContent('交通');
    expect(rows[1]).toHaveTextContent('¥1,150');
    expect(rows[1]).toHaveTextContent('29%');
    expect(screen.getByTestId('category-head-note')).toHaveTextContent('自己負担 ¥3,966');
    expect(screen.getByTestId('daily-head-note')).toHaveTextContent('自己負担');
  });

  it('日別推移を古い順に並べ、日付と金額を読み上げられる', async () => {
    render(<SummaryScreen trip={trip} />);

    const cols = await screen.findAllByTestId('day-col');
    expect(cols.map((c) => c.getAttribute('aria-label'))).toEqual([
      '9/11(金) ¥1,150',
      '9/12(土) ¥2,816',
    ]);
  });

  it('共有に切り替えると共有だけの図になる', async () => {
    render(<SummaryScreen trip={trip} />);
    await screen.findAllByTestId('cat-row');

    await userEvent.click(screen.getByRole('button', { name: '共有' }));

    const rows = screen.getAllByTestId('cat-row');
    expect(rows).toHaveLength(1);
    expect(rows[0]).toHaveTextContent('交通');
    expect(rows[0]).toHaveTextContent('¥2,300');
    expect(screen.getByTestId('category-head-note')).toHaveTextContent('共有 ¥2,300');
    expect(screen.getAllByTestId('day-col')).toHaveLength(1);
  });

  it('個別に切り替えると個別だけの図になる', async () => {
    render(<SummaryScreen trip={trip} />);
    await screen.findAllByTestId('cat-row');

    await userEvent.click(screen.getByRole('button', { name: '個別' }));

    const rows = screen.getAllByTestId('cat-row');
    expect(rows).toHaveLength(1);
    expect(rows[0]).toHaveTextContent('食事');
    expect(rows[0]).toHaveTextContent('¥2,816');
    expect(screen.getByTestId('category-head-note')).toHaveTextContent('個別 ¥2,816');
  });

  it('切り替えても合計カードは変わらない', async () => {
    render(<SummaryScreen trip={trip} />);
    expect(await screen.findByTestId('summary-total')).toHaveTextContent('¥5,116');

    await userEvent.click(screen.getByRole('button', { name: '個別' }));

    expect(screen.getByTestId('summary-total')).toHaveTextContent('¥5,116');
    expect(screen.getByTestId('summary-mine')).toHaveTextContent('¥3,966');
  });

  it('選んだスコープに支出が無ければその旨を出す', async () => {
    const solo = await createTrip({ name: '香港', currency: 'HKD' });
    await addExpense({
      tripId: solo.id,
      date: '2026-09-12',
      amountMinor: 10000,
      scope: 'personal',
      category: 'food',
      payment: 'cash',
      memo: '',
      rate: 20,
      rateSource: 'api',
      photoId: null,
    });
    render(<SummaryScreen trip={solo} />);
    await screen.findAllByTestId('cat-row');

    await userEvent.click(screen.getByRole('button', { name: '共有' }));

    expect(screen.getByText('共有の支出はまだありません。')).toBeInTheDocument();
    expect(screen.queryAllByTestId('cat-row')).toHaveLength(0);
  });

  it('支出が無ければ案内を出す', async () => {
    const empty = await createTrip({ name: '台北', currency: 'USD' });
    render(<SummaryScreen trip={empty} />);

    expect(await screen.findByText('集計する支出がまだありません。')).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npm test -- src/ui/SummaryScreen.test.tsx`
Expected: FAIL(`day-col` も `category-head-note` も存在せず、「共有」ボタンも無い)

- [ ] **Step 3: 集計画面を書き直す**

`src/ui/SummaryScreen.tsx` を全面的に差し替える。

```tsx
import { useState } from 'react';
import { useLiveQuery } from 'dexie-react-hooks';
import { listExpenses } from '../data/expenseRepo';
import { formatJpy, formatWithCurrency } from '../domain/money';
import { breakdownByCategory, dailySeries, summarize } from '../domain/summary';
import type { ViewScope } from '../domain/summary';
import type { Trip } from '../domain/types';
import { CategoryChart } from './CategoryChart';
import { DailyChart } from './DailyChart';

const VIEWS: { value: ViewScope; label: string }[] = [
  { value: 'personal', label: '個別' },
  { value: 'shared', label: '共有' },
  { value: 'mine', label: '自己負担' },
];

function viewLabel(v: ViewScope): string {
  return VIEWS.find((x) => x.value === v)?.label ?? '自己負担';
}

export function SummaryScreen({ trip }: { trip: Trip }) {
  const expenses = useLiveQuery(() => listExpenses(trip.id), [trip.id]);
  // 切り替えはセッション内だけ保持する。保存する値を増やさないため、開き直すと自己負担に戻る。
  const [view, setView] = useState<ViewScope>('mine');

  // useLiveQuery は初回レンダーで undefined を返す(IndexedDB への問い合わせは非同期のため)。
  // ここで早期returnしないと、支出が存在する trip でも一瞬「集計する支出がまだありません。」
  // が表示されてしまう(HomeScreen.tsx と同じ理由・同じ対処)。
  if (expenses === undefined) {
    return (
      <div className="summary">
        <p className="empty">読み込み中…</p>
      </div>
    );
  }

  const list = expenses;
  const summary = summarize(list, trip);
  const categories = breakdownByCategory(list, trip, view);
  const days = dailySeries(list, trip, view);
  const viewTotalJpy = categories.reduce((sum, c) => sum + c.jpy, 0);

  if (list.length === 0) {
    return <p className="empty">集計する支出がまだありません。</p>;
  }

  return (
    <div className="summary">
      <section>
        <h3>合計</h3>
        <div className="card">
          <span className="card-jpy" data-testid="summary-total">
            {formatJpy(summary.totalJpy)}
          </span>
          <span className="card-foreign">
            {formatWithCurrency(summary.totalMinor, trip.currency)} / {summary.count}件
          </span>
          <div className="card-split">
            <div>
              <span className="card-label">個別</span>
              <span data-testid="summary-personal">{formatJpy(summary.personalJpy)}</span>
            </div>
            <div>
              <span className="card-label">共有</span>
              <span data-testid="summary-shared">{formatJpy(summary.sharedJpy)}</span>
              <span className="card-sub" data-testid="summary-share-note">
                自分の負担 {formatJpy(summary.sharedPerPersonJpy)}({trip.memberCount}人)
              </span>
            </div>
          </div>
          <p className="card-sub">
            自分の負担合計:{' '}
            <strong data-testid="summary-mine">{formatJpy(summary.myTotalJpy)}</strong>
            (個別 + 共有の人数割り)
          </p>
        </div>
      </section>

      <div className="segment">
        {VIEWS.map((v) => (
          <button
            key={v.value}
            type="button"
            className={v.value === view ? 'seg active' : 'seg'}
            onClick={() => setView(v.value)}
          >
            {v.label}
          </button>
        ))}
      </div>

      {categories.length === 0 ? (
        <p className="empty">{viewLabel(view)}の支出はまだありません。</p>
      ) : (
        <>
          <section>
            <h3 className="chart-head">
              <span>カテゴリ別</span>
              <em data-testid="category-head-note">
                {viewLabel(view)} {formatJpy(viewTotalJpy)}
              </em>
            </h3>
            <CategoryChart rows={categories} />
          </section>

          <section>
            <h3 className="chart-head">
              <span>日別推移</span>
              <em data-testid="daily-head-note">{viewLabel(view)}</em>
            </h3>
            <DailyChart series={days} />
          </section>
        </>
      )}
    </div>
  );
}
```

- [ ] **Step 4: 見出しのスタイルを足す**

`src/styles.css` の末尾に足す。

```css
.chart-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 6px;
}

.chart-head em {
  font-style: normal;
  font-size: 12px;
  font-weight: 400;
  color: var(--text-dim);
  font-variant-numeric: tabular-nums;
}
```

- [ ] **Step 5: テストが通ることを確認する**

Run: `npm test -- src/ui/SummaryScreen.test.tsx`
Expected: PASS

- [ ] **Step 6: 使わなくなったものを消す**

`src/domain/summary.ts` から `DateTotal` 型(24 行目)と `totalsByDate` 関数を削除する。

`src/domain/summary.test.ts` から import の `totalsByDate` と `describe('totalsByDate', ...)` を削除する。

`src/styles.css` から `.cat-row` / `.cat-row-name` / `.cat-row-value` / `.cat-bar` / `.cat-fill` の 5 ブロック(549〜581 行目付近)を削除する。次の 2 つは名前が似ているが用途が別なので残す。

- `.cat-icon`(344 行目付近)— 支出入力のカテゴリ選択グリッド
- `.summary .card-jpy`(583 行目付近)— 合計カードの金額

削除後、`grep -n "cat-row\|cat-bar\|cat-fill" src/` で残りがテストの `data-testid="cat-row"` だけになっていることを確かめる。

- [ ] **Step 7: 全部通ることを確認する**

Run: `npm test`
Expected: PASS

Run: `npm run build`
Expected: 成功(未使用 import などの型エラーが出たら直す)

- [ ] **Step 8: コミットする**

```bash
git add src/ui/SummaryScreen.tsx src/ui/SummaryScreen.test.tsx src/domain/summary.ts src/domain/summary.test.ts src/styles.css && git commit -m "feat: 集計の 2 つの図を個別/共有/自己負担で切り替えられるようにする"
```

---

### Task 9: ドキュメントとバージョン

**Files:**
- Modify: `README.md:11`、`README.md:49`、手動確認チェックリストの末尾
- Modify: `package.json:4`

**Interfaces:** なし

- [ ] **Step 1: README の機能説明を直す**

`README.md:11` を差し替える。

```markdown
- 合計 / 個別 / 共有(人数割り)の集計、カテゴリ別の帯グラフと日別推移の棒グラフ(個別 / 共有 / 自己負担で切り替え)、予算の残額表示
```

- [ ] **Step 2: 手動確認チェックリストを更新する**

`README.md:49` の既存項目を差し替える。

```markdown
- [x] 集計タブで個別/共有/人数割りが正しく出る
```

チェックリストの末尾に未確認の項目として足す。

```markdown
- [ ] 集計タブを開くと「自己負担」が選ばれていて、カテゴリ別が帯 + 内訳、日別が縦棒になっている
- [ ] 「個別」「共有」を押すと 2 つの図が同時に切り替わり、見出しの右の金額も変わる
- [ ] 切り替えても合計カードの数字は変わらない
- [ ] カテゴリ別の内訳の金額を足すと、見出しの右の合計と一致する
- [ ] 支出のない日が 0 円の細い線で残り、日付が飛んでいない
- [ ] 平均線と「1日あたり平均」「最高」の数字が図と合っている
- [ ] 2 週間以上の旅行で日付ラベルが間引かれ、横スクロールが出ない
- [ ] 共有支出が 1 件もない旅行で「共有」を押すと案内が出る
- [ ] アプリを開き直すと「自己負担」に戻っている
```

- [ ] **Step 3: バージョンを上げる**

`package.json:4` を `"version": "1.0.4",` にする。

- [ ] **Step 4: 最終確認**

Run: `npm test`
Expected: PASS

Run: `npm run build`
Expected: 成功

- [ ] **Step 5: コミットする**

```bash
git add README.md package.json && git commit -m "chore: bump version to 1.0.4"
```

- [ ] **Step 6: main へのマージ可否を確認する**

バージョンを上げるコミットをしたので、`main` へマージしてよいかユーザーに確認する(AskUserQuestion)。承認が出るまでマージしない。

---

## 実装後に残ること

- 実機での手動確認(README のチェックリストの未チェック項目)。ここは人が実機で見ないと埋められない
