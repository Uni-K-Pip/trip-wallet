# v1.0.6(先送り事項の対応と全体の整理)実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v1.0.5 の最終レビューで先送りした 8 項目を解消し、依存更新・README 整理・軽い掃除まで含めて v1.0.6 としてリリースする。新機能は足さない。

**Architecture:** 既存構成のまま、足場(依存・CI)→ 挙動の修正(丸め・再フォーマット・文言・日付書式)→ 命名整理(CSS)→ テスト追加(非 JPY 表示・DB 移行)→ ドキュメント整理 → リリースの順で進む。ドメイン層(`src/domain`)は i18n を import しない方針を維持し、日付書式は辞書を構造的型で受け取って解決する。

**Tech Stack:** React 19 / Vite 8 / TypeScript 7 / Dexie 4(IndexedDB)/ vitest 4 + Testing Library + fake-indexeddb 6 / vite-plugin-pwa / GitHub Pages(GitHub Actions デプロイ)

**承認済みスペック:** [docs/superpowers/specs/2026-08-12-v1.0.6-design.md](../specs/2026-08-12-v1.0.6-design.md)

## Global Constraints

- ブランチは `feature/v1.0.6-cleanup`。`main` はリリース版専用で、Task 12 でユーザー承認を得てからマージする。
- 1 項目 = 1 コミット。各コミットの前に `npm run test` を通す。
- 用語: `homeCurrency` = 換算先通貨、`currency` = 現地通貨。金額はすべて最小単位の整数(minor)で扱う。
- 辞書の型は `src/i18n/ja.ts` から作る(`Dictionary = typeof ja`)。**`ja.ts` に `as const` を付けないこと。**
- 改名禁止: `budgetJpy` / `personalBudgetJpy` / `sharedBudgetJpy`(旧 DB とバックアップ v1/v2 のレガシー名)。
- 触らない: `src/rates/resolveRate.ts` の直近キャッシュフォールバックで `stale: true` を固定にしている点(意図的な挙動)。
- 触らない: `src/i18n/zh.ts` 87 行目 `rateAsk` の `` `1${from} = ? ${to}` ``(値を入れる場所を表す全言語共通の意匠)。
- 日付書式の本格的なロケール対応(`Intl.DateTimeFormat` 化・年表示・月名表記)はしない。今回は en の並び替えのみ。
- GUI の刷新は v1.0.7、収益化は v1.0.8 以降。この計画では扱わない。
- 検証コマンド: `npm run test`(vitest run)/ `npm run build`(tsc --noEmit && vite build)/ `npm run lint`(tsc --noEmit)。
- コミットメッセージは日本語 + Conventional Commits の prefix(既存履歴に合わせる)。
- シェルは PowerShell。コマンドはすべてリポジトリルート `D:\work_D\0\wk_trip-wallet` で実行する。

## 変更するファイルの全体像

| ファイル | 役割 | 触る Task |
|---|---|---|
| `package.json` / `package-lock.json` | 依存の版・アプリの版 | 1, 12 |
| `.github/workflows/deploy.yml` | ビルドと GitHub Pages デプロイ | 2 |
| `src/domain/money.ts` | 金額の変換・整形(換算はここだけを通す) | 3 |
| `src/domain/money.test.ts` | 同上のテスト | 3 |
| `src/ui/TripForm.tsx` | 旅行の作成・編集フォーム | 4 |
| `src/ui/TripForm.test.tsx` | 同上のテスト | 4 |
| `src/i18n/zh.ts` | 中国語辞書 | 5, 6 |
| `src/i18n/ja.ts` / `en.ts` / `ko.ts` | 各言語辞書(`ja.ts` が型の源) | 6 |
| `src/domain/date.ts` | 日付の計算と表示ラベル | 6 |
| `src/domain/date.test.ts` | 同上のテスト | 6 |
| `src/i18n/dictionaries.test.ts` | 全言語の辞書の整合テスト | 6 |
| `src/ui/DailyChart.tsx` | 日別推移グラフ | 6 |
| `src/ui/ExpenseSheet.tsx` | 支出の入力シート | 6, 7 |
| `src/ui/HomeScreen.tsx` | ホーム画面 | 6, 7 |
| `src/ui/SettingsScreen.tsx` | 設定画面 | 6 |
| `src/ui/SummaryScreen.tsx` / `CategoryChart.tsx` | 集計画面・カテゴリ帯 | 7 |
| `src/styles.css` | 全画面のスタイル(単一ファイル) | 7 |
| `src/ui/DailyChart.test.tsx` / `SummaryScreen.test.tsx` | 画面テスト | 8 |
| `src/data/db.migration.test.ts` | **新規**。Dexie の upgrade を DB レベルで検証 | 9 |
| `README.md` | 手動確認チェックリスト | 11, 12 |

---

### Task 1: 依存パッケージの patch 更新(A)

**Files:**
- Modify: `package.json`(必要なら)
- Modify: `package-lock.json`

**Interfaces:**
- Consumes: なし(先行タスクなし)
- Produces: なし(以降のタスクはテスト実行環境としてのみ依存する)

- [ ] **Step 1: 更新可能なものを確認する**

```powershell
npm outdated
```

期待: `@testing-library/jest-dom`(7.0.0 → 7.0.1)と `@testing-library/user-event`(14.6.3 → 14.6.4)の 2 行だけが出る。更新可能なパッケージがあると `npm outdated` は exit code 1 を返すが、これは仕様であってエラーではない。3 件目以上が出た場合は patch 更新かどうかを確認し、minor / major が混ざっていたらこの Task では上げずに報告する(スペックのスコープ外)。

- [ ] **Step 2: patch を上げる**

```powershell
npm update @testing-library/jest-dom @testing-library/user-event
```

`package.json` の範囲指定は `^7.0.0` / `^14.6.3` のままで patch を吸収できるため、差分は `package-lock.json` だけになる可能性が高い。`git status` で実際の差分を確認する。

- [ ] **Step 3: 更新結果を確認する**

```powershell
npm ls @testing-library/jest-dom @testing-library/user-event
```

期待: `jest-dom@7.0.1` と `user-event@14.6.4` が出る。

- [ ] **Step 4: テストを流す**

```powershell
npm run test
```

期待: PASS(全テストファイル)。Testing Library の更新でアサーション挙動が変わっていないことの確認。

- [ ] **Step 5: コミット**

```bash
git add package.json package-lock.json
git commit -m "chore: テスト用依存の patch を更新する(jest-dom 7.0.1 / user-event 14.6.4)"
```

---

### Task 2: GitHub Actions のメジャー更新(B)

**Files:**
- Modify: `.github/workflows/deploy.yml`(23・24・31・44 行)

**Interfaces:**
- Consumes: なし
- Produces: なし(アプリのコードには影響しない)

**背景(実装前に把握しておくこと):** スペック執筆時の想定は checkout v5 / setup-node v5 / upload-pages-artifact v4 だったが、2026-08 時点の実測ではさらに新しいメジャーが出ている(checkout v7 / setup-node v7 / upload-pages-artifact v5 / deploy-pages v5)。スペック自身が「実装時に最新を確認して上げる」と定めているため、最新メジャーへ上げるのが方針どおり。破壊的変更の調査結果は以下。

- **checkout**: v5 = Node 24 ランタイム化 / v6 = 認証情報の格納先変更 / v7 = ESM 化 + `pull_request_target`・`workflow_run` での fork PR チェックアウト遮断。本ワークフローのトリガーは `push` と `pull_request` のみなので影響なし。
- **setup-node**: v5 = Node 24 + `packageManager` フィールドがあれば自動キャッシュ / v6 = 自動キャッシュを npm に限定 / v7 = ESM 化 + 出力追加。`package.json` に `packageManager` は無く、`cache: npm` を明示済みなので影響なし。
- **upload-pages-artifact**: **v4 で隠しファイル(dotfile)がアーティファクトから除外される潜在的破壊的変更**があり、v5 で `include-hidden-files` 入力が追加された。`dist` に dotfile が入らないことを Step 2 で確認する。
- **deploy-pages**: v5 は Node 24 への更新のみで API 変更なし。

- [ ] **Step 1: 各アクションの最新リリースタグを確認する**

```powershell
'checkout','setup-node','upload-pages-artifact','deploy-pages' | ForEach-Object { "$_ = " + (gh api "repos/actions/$_/releases/latest" --jq '.tag_name') }
```

期待: `checkout = v7.x` / `setup-node = v7.x` / `upload-pages-artifact = v5.x` / `deploy-pages = v5.x`。ここで上の想定と違うメジャーが出たら、そのアクションのリリースノート(`gh api repos/actions/<name>/releases/latest --jq '.body'`)で破壊的変更を確認してから採用する。

- [ ] **Step 2: dist に隠しファイルが無いことを確認する**

```powershell
npm run build; if ($?) { Get-ChildItem -Recurse -Force dist | Where-Object { $_.Name -like '.*' } }
```

期待: ビルドが成功し、隠しファイルの一覧は空(何も出力されない)。もし出た場合は upload-pages-artifact に `include-hidden-files: true` を足す必要があるので、その場で Step 3 の記述に加える。

- [ ] **Step 3: 4 つのアクションのタグを上げる**

`.github/workflows/deploy.yml` の該当行を置き換える。

23 行目:

```yaml
      - uses: actions/checkout@v7
```

24 行目:

```yaml
      - uses: actions/setup-node@v7
```

31 行目:

```yaml
      - uses: actions/upload-pages-artifact@v5
```

44 行目:

```yaml
        uses: actions/deploy-pages@v5
```

`with:` ブロック・`node-version: 22`・`cache: npm`・`path: dist`・ジョブ構成(build → deploy、PR ではテストのみ)はいずれも変えない。

- [ ] **Step 4: YAML が壊れていないことを確認する**

```powershell
gh workflow list
```

期待: `Deploy` ワークフローが一覧に出る(パースエラーがあれば GitHub 側に反映されず気づけないため、ここでは構文の目視確認も併せて行う)。ワークフロー自体は `main` への push か PR でしか走らないため、実際の成功確認は Task 12 の main マージ後になる。

- [ ] **Step 5: テストを流す**

```powershell
npm run test
```

期待: PASS(ワークフロー変更なので影響は無いが、コミット前の決まりとして流す)。

- [ ] **Step 6: コミット**

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: GitHub Actions を最新メジャーへ上げる(Node 20 非推奨警告の解消)"
```

---

### Task 3: `toHomeMinor` の丸めガード(C・TDD)

**Files:**
- Modify: `src/domain/money.ts:36-43`
- Test: `src/domain/money.test.ts`(`describe('toHomeMinor')` = 61-84 行の中に追加)

**Interfaces:**
- Consumes: `minorToMajor(amountMinor: number, decimals: number): number`(同ファイル 7-9 行、既存)
- Produces: `toHomeMinor(amountMinor: number, decimals: number, rate: number, homeDecimals: number): number` — シグネチャは変わらない。戻り値が「積が .5 のわずか下に落ちる誤差ケース」でのみ +1 変わる。

- [ ] **Step 1: 失敗するテストを書く**

`src/domain/money.test.ts` の 83 行目(`describe('toHomeMinor')` の最後の `});` の直前)に追加する:

```ts
  it('積が「.5 のわずか下」に落ちる二進浮動小数の誤差を取り込まない', () => {
    // 100.00 × 1.005 = 100.5(数学上)だが JS では 100.49999999999999 になる
    expect(toHomeMinor(10000, 2, 1.005, 0)).toBe(101);
    // 27.00 × 23.465 × 100 = 63355.5 だが JS では 63355.49999999999 になる
    expect(toHomeMinor(2700, 2, 23.465, 2)).toBe(63356);
  });
```

- [ ] **Step 2: テストが失敗することを確認する**

```powershell
npm run test -- src/domain/money.test.ts
```

期待: FAIL。`expected 100 to be 101`(1 つ目)で落ちる。

- [ ] **Step 3: 実装する**

`src/domain/money.ts` の 32-43 行を次で置き換える:

```ts
/**
 * 外貨の最小単位を換算先通貨の最小単位にする。換算はこの関数だけを通す。
 * 桁数は旅行に保存した値を使う(通貨マスタを後から変えても過去の数字が動かないため)。
 * 小数の丸め誤差(1.005 → 100.49999…)は parseMajorToMinor と同じく文字列経由で潰す。
 */
export function toHomeMinor(
  amountMinor: number,
  decimals: number,
  rate: number,
  homeDecimals: number,
): number {
  return Math.round(
    Number((minorToMajor(amountMinor, decimals) * rate * 10 ** homeDecimals).toFixed(4)),
  );
}
```

- [ ] **Step 4: テストが通ることを確認する**

```powershell
npm run test -- src/domain/money.test.ts
```

期待: PASS。既存 5 ケース(2816 / 1686 / 1085 / 0 / 0)も通ったままであること。

- [ ] **Step 5: 全テストで回帰が無いことを確認する**

```powershell
npm run test
```

期待: PASS。`SummaryScreen` や `HomeScreen` の金額アサーション(`¥5,116` など)が変わっていないこと。もし変わった場合は「通常ケースにも影響が出た」ことを意味するので、変更を戻して報告する。

- [ ] **Step 6: コミット**

```bash
git add src/domain/money.ts src/domain/money.test.ts
git commit -m "fix: toHomeMinor の丸めを parseMajorToMinor と対称にする"
```

---

### Task 4: 換算先切替時の予算欄再フォーマット(D・TDD)

**Files:**
- Modify: `src/ui/TripForm.tsx`(19-24 行の近くに関数追加、107-118 行の `onChange` を差し替え)
- Test: `src/ui/TripForm.test.tsx`(170 行目、`inputmode` テストの直後に追加)

**Interfaces:**
- Consumes: `formatMajor(amountMinor: number, decimals: number): string` と `parseMajorToMinor(input: string, decimals: number): number`(`src/domain/money.ts`、8 行目で import 済み)、`currencyDecimals(code: string): number`(`src/domain/currency.ts`、6 行目で import 済み)
- Produces: `reformatBudget(text: string, decimals: number): string` — `TripForm.tsx` 内のモジュールローカル関数。export しない。

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/TripForm.test.tsx` の 170 行目(`換算先通貨に応じて予算欄の inputmode が変わる` の `});` の直後)に 3 件追加する:

```tsx
  it('換算先を小数 2 桁から 0 桁へ切り替えると予算欄が四捨五入される', async () => {
    const user = userEvent.setup();
    renderWithLang(<TripForm onDone={() => {}} onCancel={() => {}} />);

    await user.selectOptions(screen.getByLabelText('換算先通貨'), 'EUR');
    await user.type(screen.getByLabelText('個別予算(EUR)'), '500.55');
    await user.selectOptions(screen.getByLabelText('換算先通貨'), 'JPY');

    expect(screen.getByLabelText('個別予算(JPY)')).toHaveValue('501');
  });

  it('換算先を小数 0 桁から 2 桁へ切り替えると小数部が付く', async () => {
    const user = userEvent.setup();
    renderWithLang(<TripForm onDone={() => {}} onCancel={() => {}} />);

    await user.type(screen.getByLabelText('個別予算(JPY)'), '50000');
    await user.selectOptions(screen.getByLabelText('換算先通貨'), 'EUR');

    // カンマは付けない(初期値の作り方と揃える)
    expect(screen.getByLabelText('個別予算(EUR)')).toHaveValue('50000.00');
  });

  it('空欄と数値として読めない入力はそのまま残す', async () => {
    const user = userEvent.setup();
    renderWithLang(<TripForm onDone={() => {}} onCancel={() => {}} />);

    await user.type(screen.getByLabelText('共有予算(JPY・自分の負担分)'), 'abc');
    await user.selectOptions(screen.getByLabelText('換算先通貨'), 'EUR');

    expect(screen.getByLabelText('個別予算(EUR)')).toHaveValue('');
    expect(screen.getByLabelText('共有予算(EUR・自分の負担分)')).toHaveValue('abc');
  });
```

- [ ] **Step 2: テストが失敗することを確認する**

```powershell
npm run test -- src/ui/TripForm.test.tsx
```

期待: FAIL。1 件目は `expected '500.55' to equal '501'`、2 件目は `expected '50000' to equal '50000.00'` で落ちる。3 件目は現状でも通る(触らない挙動の固定)。

- [ ] **Step 3: 再フォーマット関数を追加する**

`src/ui/TripForm.tsx` の 24 行目(`parseBudget` の閉じ括弧)の直後に追加する:

```tsx
/** 数値として読める入力だけ、新しい通貨の桁数に表示し直す。それ以外は触らない。 */
function reformatBudget(text: string, decimals: number): string {
  const trimmed = text.trim();
  if (trimmed === '' || !Number.isFinite(Number(trimmed))) return text;
  return formatMajor(parseMajorToMinor(trimmed, decimals), decimals).replace(/,/g, '');
}
```

負値は特別扱いしない。`parseMajorToMinor` が負のまま `Math.round` を通し、`formatMajor` が負号を先頭に置くので「-500.55」→ JPY 切替は「-501」となり符号が保たれる。保存時に `parseBudget`(19-24 行)が負値を 0 に丸める従来の挙動も変えない。

- [ ] **Step 4: 換算先セレクトの onChange を差し替える**

`src/ui/TripForm.tsx` 110 行目の

```tsx
        onChange={(e) => setHomeCurrency(e.target.value)}
```

を次で置き換える:

```tsx
        onChange={(e) => {
          const next = e.target.value;
          const nextDecimals = currencyDecimals(next);
          setHomeCurrency(next);
          setPersonalBudget((b) => reformatBudget(b, nextDecimals));
          setSharedBudget((b) => reformatBudget(b, nextDecimals));
        }}
```

`homeDecimals`(34 行目)は `homeCurrency` state から毎レンダー計算しているが、この onChange の中では state 更新が反映される前なので、新しい通貨から `currencyDecimals(next)` を直接求める。

- [ ] **Step 5: テストが通ることを確認する**

```powershell
npm run test -- src/ui/TripForm.test.tsx
```

期待: PASS(既存 12 件 + 新規 3 件)。特に `換算先通貨を選んで保存できる`(140-157 行)が通ったままであること — USD へ切り替えたあと `300` と入力する流れなので、空欄への再フォーマットが悪さをしないことの確認になる。

- [ ] **Step 6: 全テストと型チェック**

```powershell
npm run test; if ($?) { npm run lint }
```

期待: どちらも PASS。

- [ ] **Step 7: コミット**

```bash
git add src/ui/TripForm.tsx src/ui/TripForm.test.tsx
git commit -m "fix: 換算先通貨の切替で予算欄を新しい桁数に整え直す"
```

---

### Task 5: `zh.ts` の半角疑問符を全角に直す(E)

**Files:**
- Modify: `src/i18n/zh.ts:56`, `src/i18n/zh.ts:116`

**Interfaces:**
- Consumes: なし
- Produces: なし(文言のみ)

- [ ] **Step 1: 現状の半角疑問符を洗い出す**

```powershell
Select-String -Path src\i18n\zh.ts -Pattern '\?' | ForEach-Object { "$($_.LineNumber): $($_.Line.Trim())" }
```

期待: 56 行目・87 行目・116 行目の 3 件。87 行目(`rateAsk` の `` `1${from} = ? ${to}` ``)は全言語共通の意匠なので**触らない**。

- [ ] **Step 2: 2 箇所を全角に直す**

56 行目:

```ts
    confirmDelete: '要删除这笔支出吗？',
```

116 行目:

```ts
    confirmDelete: (name) => `将删除「${name}」及其全部支出。确定吗？`,
```

- [ ] **Step 3: 残りが 87 行目だけであることを確認する**

```powershell
Select-String -Path src\i18n\zh.ts -Pattern '\?' | ForEach-Object { "$($_.LineNumber): $($_.Line.Trim())" }
```

期待: 87 行目の `rateAsk` の 1 件のみ。

- [ ] **Step 4: テストを流す**

```powershell
npm run test
```

期待: PASS。

- [ ] **Step 5: コミット**

```bash
git add src/i18n/zh.ts
git commit -m "fix: 中国語の削除確認文の疑問符を全角にする"
```

---

### Task 6: 日付ラベルの言語別書式(F)

**Files:**
- Modify: `src/domain/date.ts:20-24`
- Modify: `src/i18n/ja.ts`(6 行目の直後)、`src/i18n/en.ts`(5 行目の直後)、`src/i18n/ko.ts`(5 行目の直後)、`src/i18n/zh.ts`(5 行目の直後)
- Modify: `src/ui/DailyChart.tsx:50`, `:80`
- Modify: `src/ui/ExpenseSheet.tsx:131-132`
- Modify: `src/ui/HomeScreen.tsx:162`
- Modify: `src/ui/SettingsScreen.tsx:145-146`
- Test: `src/domain/date.test.ts:13-14`, `:41-56`
- Test: `src/i18n/dictionaries.test.ts`(29 行目、最後の `it` の直後)

**Interfaces:**
- Consumes: `parseIsoDate(isoDate: string): Date`(`src/domain/date.ts` 15-18 行、既存)
- Produces:
  - `export type DateLabelDict = { weekdays: readonly string[]; dateLabel: (month: number, day: number, weekday: string) => string }`(`src/domain/date.ts`)
  - `export function formatDateLabel(isoDate: string, dict: DateLabelDict): string`(第 2 引数が `readonly string[]` から `DateLabelDict` に変わる)
  - 辞書に `dateLabel: (m: number, d: number, w: string) => string` がトップレベルで加わる(`Dictionary` = `typeof ja` なので `ja.ts` の記述が型になる)

呼び出し側は `formatDateLabel(date, t.weekdays)` を `formatDateLabel(date, t)` に変える。`Dictionary` の `weekdays: string[]` は `readonly string[]` に代入可能なので、`t` をそのまま渡せる。

- [ ] **Step 1: 失敗するテストを書く(date.test.ts)**

`src/domain/date.test.ts` の 13-14 行を次で置き換える:

```ts
const JA_DICT = {
  weekdays: ['日', '月', '火', '水', '木', '金', '土'],
  dateLabel: (m: number, d: number, w: string) => `${m}/${d}(${w})`,
};
const EN_DICT = {
  weekdays: ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'],
  dateLabel: (m: number, d: number, w: string) => `${w}, ${m}/${d}`,
};
```

続けて、41-56 行の 2 つの `describe` を次で置き換える:

```ts
describe('formatDateLabel', () => {
  it('曜日つきの短い表記にする', () => {
    expect(formatDateLabel('2026-09-12', JA_DICT)).toBe('9/12(土)');
  });

  it('1 桁の月日をそのまま表示する', () => {
    expect(formatDateLabel('2026-01-05', JA_DICT)).toBe('1/5(月)');
  });
});

describe('formatDateLabel の言語別書式', () => {
  it('曜日名も並びも辞書が決める', () => {
    expect(formatDateLabel('2026-09-12', JA_DICT)).toBe('9/12(土)');
    expect(formatDateLabel('2026-09-12', EN_DICT)).toBe('Sat, 9/12');
  });
});
```

- [ ] **Step 2: テストが失敗することを確認する**

```powershell
npm run test -- src/domain/date.test.ts
```

期待: FAIL。`dict.dateLabel is not a function` か、`expected '9/12(Sat)' to be 'Sat, 9/12'` で落ちる。

- [ ] **Step 3: `formatDateLabel` を書き換える**

`src/domain/date.ts` の 20-24 行を次で置き換える:

```ts
/** 日付ラベルの組み立てに要る辞書の一部。date.ts は i18n を import せず構造的型で受ける。 */
export type DateLabelDict = {
  weekdays: readonly string[];
  dateLabel: (month: number, day: number, weekday: string) => string;
};

/** "2026-09-12" → ja なら "9/12(土)"、en なら "Sat, 9/12"。曜日名も並びも辞書が決める。 */
export function formatDateLabel(isoDate: string, dict: DateLabelDict): string {
  const d = parseIsoDate(isoDate);
  return dict.dateLabel(d.getMonth() + 1, d.getDate(), dict.weekdays[d.getDay()]);
}
```

- [ ] **Step 4: date.test.ts が通ることを確認する**

```powershell
npm run test -- src/domain/date.test.ts
```

期待: PASS。

- [ ] **Step 5: 4 言語の辞書に `dateLabel` を足す**

`src/i18n/ja.ts` の 6 行目(`weekdays: [...]`)の直後に追加する。**ja.ts が型の源なので引数の型注釈を明示する**:

```ts
  dateLabel: (m: number, d: number, w: string) => `${m}/${d}(${w})`,
```

`src/i18n/ko.ts` の 5 行目の直後(`Dictionary` から型が付くので注釈は不要):

```ts
  dateLabel: (m, d, w) => `${m}/${d}(${w})`,
```

`src/i18n/zh.ts` の 5 行目の直後:

```ts
  dateLabel: (m, d, w) => `${m}/${d}(${w})`,
```

`src/i18n/en.ts` の 5 行目の直後(**英語だけ曜日が先頭**):

```ts
  dateLabel: (m, d, w) => `${w}, ${m}/${d}`,
```

- [ ] **Step 6: 辞書テストを足す**

`src/i18n/dictionaries.test.ts` の 29 行目(`人数は言語ごとの数え方になる` の `});` の直後)に追加する:

```ts
  it('日付書式は全言語にあり、英語だけ曜日が先頭に来る', () => {
    for (const lang of LANGS) {
      expect(typeof DICTIONARIES[lang].dateLabel).toBe('function');
    }
    expect(DICTIONARIES.ja.dateLabel(9, 12, '土')).toBe('9/12(土)');
    expect(DICTIONARIES.ko.dateLabel(9, 12, '토')).toBe('9/12(토)');
    expect(DICTIONARIES.zh.dateLabel(9, 12, '六')).toBe('9/12(六)');
    expect(DICTIONARIES.en.dateLabel(9, 12, 'Sat')).toBe('Sat, 9/12');
  });
```

- [ ] **Step 7: 呼び出し 7 箇所を追随させる**

`src/ui/DailyChart.tsx` 50 行目:

```tsx
            aria-label={`${formatDateLabel(p.date, t)} ${fmt(p.home)}`}
```

`src/ui/DailyChart.tsx` 80 行目:

```tsx
            {t.summary.peak} {formatDateLabel(peakDate, t)} <b>{fmt(maxHome)}</b>
```

`src/ui/ExpenseSheet.tsx` 130-132 行:

```tsx
              note: r.stale
                ? t.expense.noteStale(formatDateLabel(r.effectiveDate, t))
                : t.expense.noteOn(formatDateLabel(r.effectiveDate, t)),
```

`src/ui/HomeScreen.tsx` 162 行目:

```tsx
            <h3>{formatDateLabel(group.date, t)}</h3>
```

`src/ui/SettingsScreen.tsx` 145-146 行:

```tsx
                  {trip.currency} / {formatDateLabel(trip.startDate, t)}
                  {trip.endDate ? `〜${formatDateLabel(trip.endDate, t)}` : ''} /{' '}
```

**`src/ui/DailyChart.tsx` の軸ラベル(60-72 行)は変えない。** `dayOfMonth(p.date)` と `t.weekdays[weekdayIndex(p.date)]` を上下 2 段で出しており `formatDateLabel` を通していないため、この Task の対象外(日付書式の並びは関係しない)。

- [ ] **Step 8: 追随漏れが無いことを型チェックで確認する**

```powershell
npm run lint
```

期待: PASS(エラーなし)。`t.weekdays` を渡したままの箇所が残っていれば `string[]` は `DateLabelDict` に代入できずコンパイルエラーになる。念のため grep でも確認する:

```powershell
Get-ChildItem -Recurse src -Include *.ts,*.tsx | Select-String -Pattern 'formatDateLabel\([^)]*weekdays'
```

期待: 出力なし。

- [ ] **Step 9: 全テストを流す**

```powershell
npm run test
```

期待: PASS。ja 表記に依存する既存アサーション(`src/ui/HomeScreen.test.tsx:98`・`:105` の `'9/12(土)'`、`src/ui/SummaryScreen.test.tsx:75` の `'9/12(土) ¥2,816'`、`src/ui/DailyChart.test.tsx:26`・`:43`)は書式が変わらないのでそのまま通る。

- [ ] **Step 10: コミット**

```bash
git add src/domain/date.ts src/domain/date.test.ts src/i18n/ja.ts src/i18n/en.ts src/i18n/ko.ts src/i18n/zh.ts src/i18n/dictionaries.test.ts src/ui/DailyChart.tsx src/ui/ExpenseSheet.tsx src/ui/HomeScreen.tsx src/ui/SettingsScreen.tsx
git commit -m "feat: 日付ラベルの書式を辞書で切り替える(英語は Sat, 9/12 形式)"
```

---

### Task 7: CSS クラス名から「jpy / foreign」を排除する(G)

**Files:**
- Modify: `src/styles.css:246`, `:385`, `:391`, `:549`, `:671`
- Modify: `src/ui/ExpenseSheet.tsx:279`
- Modify: `src/ui/HomeScreen.tsx:105`, `:108`
- Modify: `src/ui/SummaryScreen.tsx:47`, `:50`
- Modify: `src/ui/CategoryChart.tsx:40`

**Interfaces:**
- Consumes: なし
- Produces: なし(クラス名のみ。見た目は変わらない)

リネーム表:

| 旧 | 新 |
|----|----|
| `amount-jpy` | `amount-home` |
| `card-jpy` | `card-home` |
| `card-foreign` | `card-local` |
| `legend-jpy` | `legend-home` |

**注意:** テストコードは `data-testid` で要素を取っておりクラス名を参照していない(Step 1 で確認する)。`docs/superpowers/plans/` 配下の過去の計画書には旧名が出てくるが、これは履歴なので**変更しない**。

- [ ] **Step 1: 現在の使用箇所を洗い出す**

```powershell
Get-ChildItem -Recurse src -Include *.ts,*.tsx,*.css | Select-String -Pattern 'amount-jpy|card-jpy|card-foreign|legend-jpy' | ForEach-Object { "$($_.Path):$($_.LineNumber): $($_.Line.Trim())" }
```

期待: 11 件(`styles.css` 5 件 / `ExpenseSheet.tsx` 1 件 / `HomeScreen.tsx` 2 件 / `SummaryScreen.tsx` 2 件 / `CategoryChart.tsx` 1 件)。テストファイル(`*.test.tsx`)が 1 件も出ないことを確認する。

- [ ] **Step 2: CSS を書き換える**

`src/styles.css` 246 行目:

```css
.amount-home {
```

385 行目:

```css
.card-home {
```

391 行目(392 行目の `.card-sub {` はそのまま。共通ブロックの 1 つ目のセレクタだけを変える):

```css
.card-local,
```

549 行目:

```css
.summary .card-home { display: block; }
```

671 行目:

```css
.legend-home {
```

- [ ] **Step 3: コンポーネントを書き換える**

`src/ui/ExpenseSheet.tsx` 279 行目:

```tsx
              <span className="amount-home" data-testid="home-preview">
```

`src/ui/HomeScreen.tsx` 105・108 行:

```tsx
          <span className="card-home" data-testid="total-home">
```

```tsx
          <span className="card-local">
```

`src/ui/SummaryScreen.tsx` 47・50 行:

```tsx
          <span className="card-home" data-testid="summary-total">
```

```tsx
          <span className="card-local">
```

`src/ui/CategoryChart.tsx` 40 行目:

```tsx
            <span className="legend-home">{formatWithCurrency(r.home, homeCurrency)}</span>
```

- [ ] **Step 4: 旧名が 1 つも残っていないことを確認する**

```powershell
Get-ChildItem -Recurse src -Include *.ts,*.tsx,*.css | Select-String -Pattern 'amount-jpy|card-jpy|card-foreign|legend-jpy'
```

期待: 出力なし。

新名が想定どおりの数だけ入ったことも確認する:

```powershell
Get-ChildItem -Recurse src -Include *.ts,*.tsx,*.css | Select-String -Pattern 'amount-home|card-home|card-local|legend-home' | Measure-Object | Select-Object -ExpandProperty Count
```

期待: `11`。

- [ ] **Step 5: テストとビルド**

```powershell
npm run test; if ($?) { npm run build }
```

期待: どちらも PASS。

- [ ] **Step 6: コミット**

```bash
git add src/styles.css src/ui/ExpenseSheet.tsx src/ui/HomeScreen.tsx src/ui/SummaryScreen.tsx src/ui/CategoryChart.tsx
git commit -m "refactor: CSS クラス名を換算先/現地の用語に合わせる"
```

---

### Task 8: 非 JPY 換算先の表示テスト(H)

**Files:**
- Test: `src/ui/DailyChart.test.tsx`(104 行目、最後の `it` の直後)
- Test: `src/ui/SummaryScreen.test.tsx`(149 行目、最後の `it` の直後)

**Interfaces:**
- Consumes: `formatDateLabel(isoDate, dict)`(Task 6 の新シグネチャ)、`toHomeMinor`(Task 3 の丸めガード入り)、`createTrip({ name, currency, homeCurrency, memberCount? })` / `addExpense(input)`(`src/data/tripRepo.ts` / `expenseRepo.ts`、既存)
- Produces: なし(テストのみ)

これは既存の正しい挙動を固定するテストなので、初回から PASS する。空振りでないことを Step 3 で確かめる。EUR は小数 2 桁・記号 `€` 前置(`src/domain/currency.ts:24`)。

- [ ] **Step 1: DailyChart に EUR のテストを足す**

`src/ui/DailyChart.test.tsx` の 104 行目(`英語なら曜日と平均が英語になる` の `});` の直後)に追加する:

```tsx
  it('換算先が EUR なら記号つき小数 2 桁で出す', () => {
    const euro: DailySeries = {
      points: [
        { date: '2026-09-12', home: 123456 },
        { date: '2026-09-13', home: 0 },
      ],
      totalHome: 123456,
      maxHome: 123456,
      peakDate: '2026-09-12',
      averageHome: 61728,
    };

    renderWithLang(<DailyChart series={euro} homeCurrency="EUR" />);

    expect(screen.getAllByTestId('day-col')[0]).toHaveAttribute('aria-label', '9/12(土) €1,234.56');
    expect(screen.getByTestId('daily-average')).toHaveTextContent('1日あたり平均 €617.28');
    expect(screen.getByTestId('daily-peak')).toHaveTextContent('最高 9/12(土) €1,234.56');
  });
```

- [ ] **Step 2: SummaryScreen に EUR のテストを足す**

`src/ui/SummaryScreen.test.tsx` の 149 行目(`英語なら切り替えボタンが英語になる` の `});` の直後)に追加する:

```tsx
  it('換算先が EUR の旅行は記号つき小数 2 桁で集計する', async () => {
    const paris = await createTrip({ name: 'Paris', currency: 'USD', homeCurrency: 'EUR' });
    await addExpense({
      tripId: paris.id,
      date: '2026-09-12',
      amountMinor: 10000, // 100.00 ドル × 0.85 = 85.00 ユーロ
      scope: 'personal',
      category: 'food',
      payment: 'card',
      memo: 'Bistro',
      rate: 0.85,
      rateSource: 'api',
      photoId: null,
    });

    renderWithLang(<SummaryScreen trip={paris} />);

    expect(await screen.findByTestId('summary-total')).toHaveTextContent('€85.00');
    expect(screen.getByTestId('summary-personal')).toHaveTextContent('€85.00');
    const cols = await screen.findAllByTestId('day-col');
    expect(cols[0]).toHaveAttribute('aria-label', '9/12(土) €85.00');
  });
```

- [ ] **Step 3: テストが通ること、かつ空振りでないことを確認する**

```powershell
npm run test -- src/ui/DailyChart.test.tsx src/ui/SummaryScreen.test.tsx
```

期待: PASS。

続けて、一時的に `expect(screen.getByTestId('summary-total')).toHaveTextContent('€85.00')` の `€85.00` を `€99.99` に書き換えて同じコマンドを流し、FAIL することを確認してから元に戻す(アサーションが実際に評価されていることの確認)。

- [ ] **Step 4: 全テストを流す**

```powershell
npm run test
```

期待: PASS。

- [ ] **Step 5: コミット**

```bash
git add src/ui/DailyChart.test.tsx src/ui/SummaryScreen.test.tsx
git commit -m "test: 換算先が EUR のときの表示を検証する"
```

---

### Task 9: DB v1/v2 → v3 移行テスト(I)

**Files:**
- Create: `src/data/db.migration.test.ts`

**Interfaces:**
- Consumes: `TripWalletDb`(`src/data/db.ts` 5 行目で export 済みのクラス)、`Dexie`(`dexie` パッケージ)
- Produces: なし(テストのみ)

現行スキーマ(`src/data/db.ts`)は version(1) → version(2) upgrade → version(3) stores + upgrade。Dexie は宣言した版番号を 10 倍して IndexedDB の版にするため、v1 相当(IDB 10)から現行(IDB 30)を開くと version(2)・version(3) の upgrade が順に走る。

テストでは**モジュールの `db` シングルトンではなく `new TripWalletDb()`** を使う(旧スキーマの接続を閉じてから開く順序を自分で制御するため)。`db.ts` を import した時点でシングルトンも生成されるが、Dexie は最初の操作まで開かないので干渉しない。

- [ ] **Step 1: テストファイルを作る**

`src/data/db.migration.test.ts` を新規作成する:

```ts
import Dexie from 'dexie';
import { describe, it, expect, afterEach } from 'vitest';
import { TripWalletDb } from './db';

const DB_NAME = 'trip-wallet';

/** 移行前の形のデータを書き込むためだけの、旧スキーマの Dexie。 */
function openLegacy(version: 1 | 2): Dexie {
  const legacy = new Dexie(DB_NAME);
  const v1Stores = {
    trips: 'id, createdAt',
    expenses: 'id, tripId, [tripId+date], date, createdAt',
    photos: 'id',
    rates: 'key, base, [base+date], date',
  };
  legacy.version(1).stores(v1Stores);
  // v2 はインデックスを変えていないので、同じ定義のまま版番号だけ進める
  if (version === 2) legacy.version(2).stores(v1Stores);
  return legacy;
}

afterEach(async () => {
  await Dexie.delete(DB_NAME);
});

describe('DB の移行', () => {
  it('v1 の budgetJpy が個別予算になり、旧キーが消える', async () => {
    const legacy = openLegacy(1);
    await legacy.table('trips').put({
      id: 't1',
      name: '上海 2026-09',
      currency: 'CNY',
      currencyDecimals: 2,
      startDate: '2026-09-12',
      endDate: null,
      budgetJpy: 50000,
      memberCount: 2,
      createdAt: 0,
    });
    legacy.close();

    const db = new TripWalletDb();
    const trip = (await db.trips.get('t1')) as unknown as Record<string, unknown>;
    db.close();

    expect(trip.personalBudgetHome).toBe(50000);
    expect(trip.sharedBudgetHome).toBeNull();
    expect(trip.homeCurrency).toBe('JPY');
    expect(trip.homeCurrencyDecimals).toBe(0);
    expect('budgetJpy' in trip).toBe(false);
  });

  it('v2 の円建て予算が換算先通貨の予算に移り、旧キーが消える', async () => {
    const legacy = openLegacy(2);
    await legacy.table('trips').put({
      id: 't2',
      name: 'NY 2026-09',
      currency: 'USD',
      currencyDecimals: 2,
      startDate: '2026-09-12',
      endDate: null,
      personalBudgetJpy: 50000,
      sharedBudgetJpy: 30000,
      memberCount: 1,
      createdAt: 0,
    });
    legacy.close();

    const db = new TripWalletDb();
    const trip = (await db.trips.get('t2')) as unknown as Record<string, unknown>;
    db.close();

    expect(trip.personalBudgetHome).toBe(50000);
    expect(trip.sharedBudgetHome).toBe(30000);
    expect(trip.homeCurrency).toBe('JPY');
    expect('personalBudgetJpy' in trip).toBe(false);
    expect('sharedBudgetJpy' in trip).toBe(false);
  });

  it('v2 までのレートに quote が付き、新しいインデックスで引ける', async () => {
    const legacy = openLegacy(2);
    await legacy.table('rates').put({
      key: 'CNY:JPY:2026-09-12',
      base: 'CNY',
      date: '2026-09-12',
      rate: 23.465,
      effectiveDate: '2026-09-11',
      fetchedAt: 0,
      source: 'frankfurter',
    });
    legacy.close();

    const db = new TripWalletDb();
    const rows = await db.rates.where('[base+quote]').equals(['CNY', 'JPY']).toArray();
    db.close();

    expect(rows).toHaveLength(1);
    expect(rows[0].quote).toBe('JPY');
    expect(rows[0].rate).toBe(23.465);
  });
});
```

- [ ] **Step 2: テストが通ることを確認する**

```powershell
npm run test -- src/data/db.migration.test.ts
```

期待: PASS(3 件)。移行コードは既にあるので初回から通る。

- [ ] **Step 3: テストが空振りでないことを確認する**

`v1 の budgetJpy が個別予算になり、旧キーが消える` の `expect(trip.personalBudgetHome).toBe(50000);` を一時的に `toBe(1)` に書き換えて Step 2 のコマンドを流し、`expected 50000 to be 1` で FAIL することを確認してから元に戻す。これで「upgrade が実際に走り、値が読めている」ことが確かめられる。

- [ ] **Step 4: 他のテストと同一実行で干渉しないことを確認する**

```powershell
npm run test
```

期待: PASS。特に `src/data/repos.test.ts` と `src/ui/*.test.tsx`(いずれも `db.delete()` → `db.open()` を beforeEach で行う)が影響を受けていないこと。もし DB 名の衝突で落ちる場合は、`afterEach` の `Dexie.delete` が効いているかを最初に疑う。

- [ ] **Step 5: コミット**

```bash
git add src/data/db.migration.test.ts
git commit -m "test: Dexie の v1/v2 から v3 への移行を DB レベルで検証する"
```

---

### Task 10: src 一巡の軽い掃除(J)

**Files:**
- Modify: 検出結果しだい(下記 2 つのコマンドが出したものだけ)

**Interfaces:**
- Consumes: なし
- Produces: なし

大規模なリファクタや構造変更はしない。**下の 2 つの検査で出たものだけ**を扱い、何も出なければコミットせず次の Task に進む。

- [ ] **Step 1: 未使用 export の候補を洗い出す**

```powershell
Get-ChildItem -Recurse src -Include *.ts,*.tsx | Select-String -Pattern '^export (?:async )?function (\w+)' | ForEach-Object { $_.Matches[0].Groups[1].Value } | Sort-Object -Unique | ForEach-Object { $n = $_; $c = (Get-ChildItem -Recurse src -Include *.ts,*.tsx | Select-String -Pattern "\b$n\b" | Measure-Object).Count; if ($c -le 1) { "未使用の可能性: $n" } }
```

出現回数が 1(定義行のみ)の export 関数を列挙する。テストからだけ使われている関数は 2 回以上出るので候補には入らない。候補が出たら、本当に参照ゼロかを個別に確認したうえで削除する。`src/main.tsx` や `App.tsx` のエントリなど、外部から参照される可能性があるものは残す。

- [ ] **Step 2: 置き去りのマーカーを洗い出す**

```powershell
Get-ChildItem -Recurse src -Include *.ts,*.tsx,*.css | Select-String -Pattern 'TODO|FIXME|XXX' | ForEach-Object { "$($_.Path):$($_.LineNumber): $($_.Line.Trim())" }
```

期待: 出力なし。出た場合は、その場で直せるものは直し、直せないものはコメントごと削除して内容を最終報告に含める(先送り事項の記録が二重にならないよう、`docs/superpowers/specs` 側に寄せる)。

- [ ] **Step 3: 検出結果に応じて対応する**

Step 1 と Step 2 の出力が両方とも空なら、この Task は「対象なし」としてコミットせずに Task 11 へ進む。何か出た場合のみ Step 4 に進む。

- [ ] **Step 4: テストと型チェック**

```powershell
npm run test; if ($?) { npm run lint }
```

期待: どちらも PASS。

- [ ] **Step 5: コミット(対象があった場合のみ)**

```bash
git add -A src
git commit -m "refactor: 未使用コードと置き去りのコメントを整理する"
```

---

### Task 11: README 手動確認チェックリストの整理(K)

**Files:**
- Modify: `README.md:49` から末尾(98 行目)まで

**Interfaces:**
- Consumes: なし
- Produces: なし(Task 12 でこの節の「最終確認」行を更新する)

現状 45 項目に肥大化し、過去リリース固有の確認(「旅行名の入力例が『NY 2026-09』になっている」など)が混ざっている。恒常的に確認する価値のある 12 項目に絞る。運用も変える: チェックボックスは常に未チェック(`- [ ]`)で置き、実機確認の記録は「最終確認」の 1 行だけで持つ。過去項目の履歴は git と `docs/superpowers/specs` に残るので README からは消してよい。

- [ ] **Step 1: 現状の行数を確認する**

```powershell
(Get-Content README.md | Measure-Object -Line).Lines
```

期待: `98`。違っていたら、置き換える範囲(`## 手動確認チェックリスト` の見出しから末尾まで)を実際のファイルに合わせる。

- [ ] **Step 2: 49 行目から末尾までを置き換える**

```markdown
## 手動確認チェックリスト

リリース前に実機で確認する。最終確認: 2026-08-12(v1.0.5)

- [ ] iPhone / Android のブラウザで開き、ホーム画面に追加してアプリとして起動できる
- [ ] 旅行を作成し、テンキーで金額を入れて保存できる(3 タップで完了する)
- [ ] レート行に「1元 = 23.47円」が出て、タップで手動上書きできる
- [ ] レシート写真を撮って保存し、アプリを再起動しても一覧にサムネイルが出る
- [ ] 機内モードにして支出を保存でき、直近レートを使っている旨の表示が出る
- [ ] 集計タブで個別/共有/自己負担の切り替え・カテゴリ別・日別推移・人数割りが正しく出る
- [ ] 予算を設定した旅行で残額の帯が減り、超過すると赤くなる
- [ ] 設定からバックアップを書き出し、別のブラウザで取り込むとデータが復元される
- [ ] 旧バージョン(v1.0.2 / v1.0.4)のバックアップを取り込むと、予算・換算先が正しく移行される
- [ ] 言語を切り替えると全画面の文言・曜日・日付書式が変わり、再起動しても残る
- [ ] 換算先を EUR にした旅行で「€1,234.56」の形で出て、現地通貨と換算先が同じ旅行ではレート行が出ない
- [ ] 旅行の終了日を過ぎた状態で起動するとエクスポート促しバナーが出る
```

「最終確認」は v1.0.6 の実機確認がまだ済んでいないため、この時点では v1.0.5 のままにする(Task 12 で更新する)。

- [ ] **Step 3: 結果を確認する**

```powershell
Get-Content README.md | Select-Object -Skip 48
```

期待: 見出し + 「最終確認: 2026-08-12(v1.0.5)」+ 未チェックの 12 項目のみ。`- [x]` が 1 つも残っていないこと:

```powershell
Select-String -Path README.md -Pattern '\- \[x\]' | Measure-Object | Select-Object -ExpandProperty Count
```

期待: `0`。

- [ ] **Step 4: コミット**

```bash
git add README.md
git commit -m "docs: 手動確認チェックリストを恒常項目 12 件に絞る"
```

---

### Task 12: リリース(L)

**Files:**
- Modify: `package.json:4`
- Modify: `README.md:51`(実機確認後)

**Interfaces:**
- Consumes: Task 1〜11 のすべて
- Produces: `main` ブランチ上の v1.0.6

- [ ] **Step 1: バージョンを上げる**

`package.json` 4 行目:

```json
  "version": "1.0.6",
```

- [ ] **Step 2: 全検証を流して出力を控える**

```powershell
npm run test
```

```powershell
npm run build
```

```powershell
npm run lint
```

3 つとも PASS であること。**出力(テスト件数・ビルドサイズ・エラーなしの表示)は最終報告にそのまま含める**(グローバルルール)。

- [ ] **Step 3: バージョンのコミット**

```bash
git add package.json
git commit -m "chore: bump version to 1.0.6"
```

- [ ] **Step 4: 実機確認をユーザーに依頼する**

README の恒常チェックリスト 12 項目に加えて、今回の変更点を確認してもらう:

- en に切り替えると日付が「Sat, 9/12」形式になる(ホームの日付見出し・支出シートのレート注記・日別推移の最高日・設定の旅行一覧)
- ja / ko / zh は従来どおり「9/12(土)」形式のまま
- 旅行フォームで予算「500.55」(EUR)を入れてから換算先を JPY に切り替えると「501」に変わる
- zh の削除確認ダイアログの疑問符が全角になっている
- 全画面の見た目に崩れがない(CSS リネームの取り残し検出)

- [ ] **Step 5: 実機確認の結果を README に反映する**

ユーザーの確認が取れたら `README.md` 51 行目を更新する(日付は実際に確認した日):

```markdown
リリース前に実機で確認する。最終確認: 2026-08-12(v1.0.6)
```

```bash
git add README.md
git commit -m "docs: 手動確認の最終確認日を v1.0.6 に更新する"
```

- [ ] **Step 6: main マージの承認を取る**

AskUserQuestion で `feature/v1.0.6-cleanup` を `main` へマージしてよいか確認する(グローバルルール: version を上げるコミットの後は承認必須)。承認が得られなければここで止める。

- [ ] **Step 7: main へマージする**

```bash
git checkout main && git merge --no-ff feature/v1.0.6-cleanup -m "Merge branch 'feature/v1.0.6-cleanup' into main" && git push origin main
```

- [ ] **Step 8: デプロイの成功を確認する**

```powershell
gh run list --branch main --limit 1
```

完了まで待ってから、Node 20 非推奨の警告が消えたことを確認する:

```powershell
gh run view --log | Select-String -Pattern 'deprecat|Node.js 20'
```

期待: 該当なし(出力が空)。ワークフローが失敗した場合は、`.github/workflows/deploy.yml` だけを直して push すればよい(アプリ本体には影響しない)。

---

## リスクと対策

| リスク | 対策 |
|---|---|
| CSS リネームの取り残しでスタイルが静かに外れる | Task 7 Step 4 の grep で新旧両方を数え、Task 12 の実機確認で見た目も確認する |
| `formatDateLabel` の追随漏れ | 引数の型が変わるので `npm run lint`(tsc)がコンパイルエラーで捕まえる(Task 6 Step 8) |
| Actions メジャー更新の破壊的変更 | Task 2 Step 1 でリリースノートを確認、Step 2 で dotfile を確認。ワークフローは main への push でしか走らないため最終確認は Task 12 Step 8 |
| `toHomeMinor` の丸め変更で既存表示が動く | Task 3 Step 5 で全テストを流し、既存の金額アサーションが変わらないことを確認する |
| DB 移行テストの DB 名衝突 | `afterEach` で `Dexie.delete('trip-wallet')`。Task 9 Step 4 で全テスト同時実行での干渉が無いことを確認する |
| npm update が想定外のパッケージまで上げる | Task 1 Step 1 で `npm outdated` の内容を先に確認し、patch 2 件以外が出たら報告して止める |
