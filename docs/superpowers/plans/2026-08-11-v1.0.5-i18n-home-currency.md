# v1.0.5 多言語対応と換算先通貨 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 日本語・日本円に固定されている Trip Wallet を、日本語/英語/韓国語/中国語の 4 言語で使え、旅行ごとに換算先通貨を選べるアプリにする。

**Architecture:** 依存を増やさず、`src/i18n/` に「ネストしたプレーンオブジェクトの辞書 + React Context」だけの最小 i18n を置く。`Dictionary = typeof ja` とすることで、他言語のキー抜けや引数違いを `tsc --noEmit` が落とす。通貨は `Trip.currency`(現地通貨)と `Trip.homeCurrency`(換算先通貨)の 2 本立てにし、レートは `base`/`quote` の組で扱う。既存データは Dexie の v3 upgrade と `migrateTrip` で移行する(JPY は小数 0 桁なので予算の値変換は不要、純粋な改名)。

**Tech Stack:** React 19 / TypeScript 7 / Vite 8 / Dexie 4 / Vitest 4 + Testing Library / `Intl.DisplayNames`(通貨名の現地語表示)

## Global Constraints

- 設計仕様は `docs/superpowers/specs/2026-08-11-v1.0.5-design.md`。判断に迷ったら仕様が優先。
- 作業ブランチは `feature/v1.0.5-i18n-home-currency`(作成済み)。`main` へは最後にユーザー承認を得てからマージする。
- 検証コマンドは `npm run lint`(= `tsc --noEmit`)、`npm test`(= `vitest run`)、`npm run build`。**各タスクの最後のコミット時点で 3 つとも通る状態にする。**
- 新しい依存は入れない。i18n ライブラリも使わない。
- ソースコードのコメントは日本語のまま。「なぜ」を書く。UI に出る文言だけを辞書に移す。
- コード上の識別子は `Jpy` を使わず `Home` にする(`totalJpy` → `totalHome`)。`JPY` はデータの値としてだけ登場する。
- **`src/i18n/ja.ts` の辞書オブジェクトに `as const` を付けない。** 付けると `typeof ja` が文字列リテラル型になり、`const en: Dictionary = {…}` が代入できなくなる。
- 通貨は「計算は旅行に保存した桁数(`Trip.currencyDecimals` / `Trip.homeCurrencyDecimals`)、表示は通貨マスタの桁数」を守る。
- 日付は `9/12(土)` 形式のまま。曜日名だけ翻訳する。数値は 4 言語ともカンマ区切り + ピリオド小数のまま。
- コミットメッセージは日本語の Conventional Commits(`feat:` / `fix:` / `refactor:` / `test:` / `docs:` / `chore:`)。
- 対応言語は `ja` / `en` / `ko` / `zh` の 4 つ。対応通貨は ECB(Frankfurter)が返す 30 通貨。
- `localStorage` へ保存するキーは `trip-wallet:lang` と `trip-wallet:home-currency`。読み書きは必ず try/catch で囲む。

---

### Task 1: 言語の型・検出と設定の保存

**Files:**
- Create: `src/i18n/index.ts`
- Create: `src/i18n/index.test.ts`
- Create: `src/app/settings.ts`
- Create: `src/app/settings.test.ts`
- Modify: `src/test/setup.ts`

**Interfaces:**
- Consumes: `findCurrency` from `src/domain/currency.ts`
- Produces:
  - `type Lang = 'ja' | 'en' | 'ko' | 'zh'`
  - `LANGS: readonly Lang[]`
  - `LANG_LABELS: Record<Lang, string>`
  - `detectLang(tags?: readonly string[]): Lang`
  - `defaultHomeCurrency(lang: Lang): string`
  - `loadLang(): Lang | null` / `saveLang(lang: Lang): void`
  - `loadHomeCurrency(): string | null` / `saveHomeCurrency(code: string): void`

- [ ] **Step 1: テストの既定言語を日本語に固定する**

`src/test/setup.ts` の末尾に追記する。

```ts
// テストの既定言語を日本語に固定する。CI と手元で navigator.languages が違うと
// 画面の文言アサーションが環境依存になるため。
Object.defineProperty(navigator, 'languages', { value: ['ja-JP', 'ja'], configurable: true });
```

- [ ] **Step 2: 言語検出の失敗するテストを書く**

`src/i18n/index.test.ts` を作る。

```ts
import { describe, it, expect } from 'vitest';
import { LANGS, LANG_LABELS, detectLang, defaultHomeCurrency } from './index';

describe('LANGS', () => {
  it('4 言語を並べる', () => {
    expect(LANGS).toEqual(['ja', 'en', 'ko', 'zh']);
  });

  it('ラベルはその言語自身の表記にする', () => {
    expect(LANGS.map((l) => LANG_LABELS[l])).toEqual(['日本語', 'English', '한국어', '中文']);
  });
});

describe('detectLang', () => {
  it('先頭から対応言語を探す', () => {
    expect(detectLang(['ja-JP', 'en-US'])).toBe('ja');
    expect(detectLang(['ko-KR'])).toBe('ko');
  });

  it('地域や表記が付いた中国語も zh にまとめる', () => {
    expect(detectLang(['zh-Hant-TW'])).toBe('zh');
    expect(detectLang(['zh-CN'])).toBe('zh');
  });

  it('大文字小文字を無視する', () => {
    expect(detectLang(['JA'])).toBe('ja');
  });

  it('対応外しかなければ英語にする', () => {
    expect(detectLang(['fr-FR', 'de-DE'])).toBe('en');
    expect(detectLang([])).toBe('en');
  });

  it('引数を省くと端末の設定を見る', () => {
    expect(detectLang()).toBe('ja');
  });
});

describe('defaultHomeCurrency', () => {
  it('言語ごとの既定の換算先を返す', () => {
    expect(defaultHomeCurrency('ja')).toBe('JPY');
    expect(defaultHomeCurrency('en')).toBe('USD');
    expect(defaultHomeCurrency('ko')).toBe('KRW');
    expect(defaultHomeCurrency('zh')).toBe('CNY');
  });
});
```

- [ ] **Step 3: テストが落ちることを確認する**

Run: `npx vitest run src/i18n/index.test.ts`
Expected: FAIL(`Failed to resolve import "./index"`)

- [ ] **Step 4: `src/i18n/index.ts` を実装する**

```ts
export type Lang = 'ja' | 'en' | 'ko' | 'zh';

/** 設定画面に並べる順。 */
export const LANGS: readonly Lang[] = ['ja', 'en', 'ko', 'zh'];

/** 言語名はその言語自身の表記で固定する(読めない言語に迷い込まないため)。だから辞書には入れない。 */
export const LANG_LABELS: Record<Lang, string> = {
  ja: '日本語',
  en: 'English',
  ko: '한국어',
  zh: '中文',
};

const SUPPORTED: readonly string[] = LANGS;

/**
 * 端末の言語設定から対応言語を選ぶ。
 * zh-Hant / zh-TW のような下位タグは見ず、先頭 2 文字だけで判定する。
 */
export function detectLang(tags: readonly string[] = navigator.languages ?? []): Lang {
  for (const tag of tags) {
    const head = tag.slice(0, 2).toLowerCase();
    if (SUPPORTED.includes(head)) return head as Lang;
  }
  return 'en';
}

/** 言語ごとの換算先通貨の既定値。設定でも旅行ごとでも上書きできる。 */
export function defaultHomeCurrency(lang: Lang): string {
  return { ja: 'JPY', en: 'USD', ko: 'KRW', zh: 'CNY' }[lang];
}
```

- [ ] **Step 5: テストが通ることを確認する**

Run: `npx vitest run src/i18n/index.test.ts`
Expected: PASS(10 tests)

- [ ] **Step 6: 設定の保存の失敗するテストを書く**

`src/app/settings.test.ts` を作る。

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { loadLang, saveLang, loadHomeCurrency, saveHomeCurrency } from './settings';

describe('言語の保存', () => {
  beforeEach(() => localStorage.clear());

  it('未保存なら null', () => {
    expect(loadLang()).toBeNull();
  });

  it('保存した言語を読み戻す', () => {
    saveLang('ko');
    expect(loadLang()).toBe('ko');
  });

  it('壊れた値は null 扱いにする', () => {
    localStorage.setItem('trip-wallet:lang', 'xx');
    expect(loadLang()).toBeNull();
  });
});

describe('換算先通貨の保存', () => {
  beforeEach(() => localStorage.clear());

  it('未保存なら null', () => {
    expect(loadHomeCurrency()).toBeNull();
  });

  it('保存した通貨を読み戻す', () => {
    saveHomeCurrency('USD');
    expect(loadHomeCurrency()).toBe('USD');
  });

  it('対応していない通貨は null 扱いにする', () => {
    localStorage.setItem('trip-wallet:home-currency', 'XXX');
    expect(loadHomeCurrency()).toBeNull();
  });
});
```

- [ ] **Step 7: テストが落ちることを確認する**

Run: `npx vitest run src/app/settings.test.ts`
Expected: FAIL(`Failed to resolve import "./settings"`)

- [ ] **Step 8: `src/app/settings.ts` を実装する**

```ts
import { findCurrency } from '../domain/currency';
import type { Lang } from '../i18n';

const LANG_KEY = 'trip-wallet:lang';
const HOME_CURRENCY_KEY = 'trip-wallet:home-currency';

// プライベートブラウジングや容量超過で localStorage は例外を投げる。
// 設定が読み書きできなくても起動と操作は止めない。
function read(key: string): string | null {
  try {
    return localStorage.getItem(key);
  } catch {
    return null;
  }
}

function write(key: string, value: string): void {
  try {
    localStorage.setItem(key, value);
  } catch {
    // 保存できなくても今回の操作は続行する
  }
}

export function loadLang(): Lang | null {
  const v = read(LANG_KEY);
  return v === 'ja' || v === 'en' || v === 'ko' || v === 'zh' ? v : null;
}

export function saveLang(lang: Lang): void {
  write(LANG_KEY, lang);
}

/** 新しい旅行の換算先の既定値。対応表にない通貨は保存済みでも捨てる。 */
export function loadHomeCurrency(): string | null {
  const v = read(HOME_CURRENCY_KEY);
  return v !== null && findCurrency(v) !== undefined ? v : null;
}

export function saveHomeCurrency(code: string): void {
  write(HOME_CURRENCY_KEY, code);
}
```

- [ ] **Step 9: テストが通ることを確認する**

Run: `npx vitest run src/app/settings.test.ts`
Expected: PASS(6 tests)

- [ ] **Step 10: 全体の検証とコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/i18n src/app/settings.ts src/app/settings.test.ts src/test/setup.ts
git commit -m "feat: 言語の型・端末言語の検出と設定の保存を追加する"
```

---

### Task 2: 通貨マスタを ECB 対象の 30 通貨にする

**Files:**
- Modify: `src/domain/currency.ts`
- Modify: `src/ui/TripForm.tsx:96-100`(通貨 select の option 文言)
- Test: `src/domain/currency.test.ts`

**Interfaces:**
- Consumes: `Lang` from `src/i18n`
- Produces:
  - `type CurrencyInfo = { code: string; decimals: number; symbol: string; symbolAfter?: true; flag: string }`
  - `CURRENCIES: CurrencyInfo[]`(30 件、ISO 4217 昇順)
  - `currencyName(code: string, lang: Lang): string`
  - `findCurrency` / `currencyDecimals` / `currencySymbol` は従来どおり

- [ ] **Step 1: 失敗するテストを書く**

`src/domain/currency.test.ts` を次の内容に置き換える。

```ts
import { describe, it, expect } from 'vitest';
import {
  CURRENCIES,
  findCurrency,
  currencyDecimals,
  currencySymbol,
  currencyName,
} from './currency';

describe('CURRENCIES', () => {
  it('ECB が公表する 30 通貨を載せる', () => {
    expect(CURRENCIES).toHaveLength(30);
  });

  it('主要な通貨を含む', () => {
    const codes = CURRENCIES.map((c) => c.code);
    expect(codes).toContain('CNY');
    expect(codes).toContain('JPY');
    expect(codes).toContain('IDR');
  });

  it('ECB 非対応の通貨を含まない', () => {
    const codes = CURRENCIES.map((c) => c.code);
    expect(codes).not.toContain('TWD');
    expect(codes).not.toContain('VND');
  });

  it('コードが重複しない', () => {
    const codes = CURRENCIES.map((c) => c.code);
    expect(new Set(codes).size).toBe(codes.length);
  });

  it('ISO 4217 コードの昇順に並んでいる', () => {
    const codes = CURRENCIES.map((c) => c.code);
    expect(codes).toEqual([...codes].sort());
  });

  it('すべてに国旗を持たせる', () => {
    expect(CURRENCIES.every((c) => c.flag.length > 0)).toBe(true);
  });
});

describe('currencyDecimals', () => {
  it('中国元は 2 桁', () => {
    expect(currencyDecimals('CNY')).toBe(2);
  });

  it('韓国ウォン・日本円は 0 桁', () => {
    expect(currencyDecimals('KRW')).toBe(0);
    expect(currencyDecimals('JPY')).toBe(0);
  });

  it('実務で小数を使わない通貨は 0 桁に寄せる', () => {
    expect(currencyDecimals('IDR')).toBe(0);
    expect(currencyDecimals('HUF')).toBe(0);
  });

  it('未知の通貨は 2 桁とみなす', () => {
    expect(currencyDecimals('XXX')).toBe(2);
  });
});

describe('findCurrency / currencySymbol', () => {
  it('未知のコードは undefined', () => {
    expect(findCurrency('XXX')).toBeUndefined();
  });

  it('記号を返す。未知ならコードをそのまま返す', () => {
    expect(currencySymbol('CNY')).toBe('元');
    expect(currencySymbol('XXX')).toBe('XXX');
  });

  it('記号を後ろに置く通貨には symbolAfter が立つ', () => {
    expect(findCurrency('CNY')?.symbolAfter).toBe(true);
    expect(findCurrency('SEK')?.symbolAfter).toBe(true);
    expect(findCurrency('USD')?.symbolAfter).toBeUndefined();
  });
});

describe('currencyName', () => {
  it('言語ごとの通貨名を返す', () => {
    expect(currencyName('USD', 'ja')).not.toBe(currencyName('USD', 'ko'));
  });

  it('空文字は返さない', () => {
    for (const c of CURRENCIES) {
      expect(currencyName(c.code, 'en').length).toBeGreaterThan(0);
    }
  });

  it('未知のコードはコードをそのまま返す', () => {
    expect(currencyName('XXX', 'ja')).toBe('XXX');
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/domain/currency.test.ts`
Expected: FAIL(`currencyName` が export されていない)

- [ ] **Step 3: `src/domain/currency.ts` を書き換える**

```ts
import type { Lang } from '../i18n';

export type CurrencyInfo = {
  code: string;
  /** 最小単位の桁数。計算ではなく表示に使う */
  decimals: number;
  symbol: string;
  /** 記号を数字の後ろに置く通貨。前置が多数派なので後置だけ印を付ける */
  symbolAfter?: true;
  flag: string;
};

// レート取得は Frankfurter(ECB 参照レート)が主なので、ECB が公表する通貨だけを載せる。
// 台湾ドル(TWD)・ベトナムドン(VND)は ECB の対象外でレートを取得できないため追加してはいけない。
// ブルガリアレフ(BGN)はユーロ導入で公表が終わるため入れない。
export const CURRENCIES: CurrencyInfo[] = [
  { code: 'AUD', decimals: 2, symbol: 'A$', flag: '🇦🇺' },
  { code: 'BRL', decimals: 2, symbol: 'R$', flag: '🇧🇷' },
  { code: 'CAD', decimals: 2, symbol: 'C$', flag: '🇨🇦' },
  { code: 'CHF', decimals: 2, symbol: 'CHF', flag: '🇨🇭' },
  { code: 'CNY', decimals: 2, symbol: '元', symbolAfter: true, flag: '🇨🇳' },
  { code: 'CZK', decimals: 2, symbol: 'Kč', symbolAfter: true, flag: '🇨🇿' },
  { code: 'DKK', decimals: 2, symbol: 'kr', symbolAfter: true, flag: '🇩🇰' },
  { code: 'EUR', decimals: 2, symbol: '€', flag: '🇪🇺' },
  { code: 'GBP', decimals: 2, symbol: '£', flag: '🇬🇧' },
  { code: 'HKD', decimals: 2, symbol: 'HK$', flag: '🇭🇰' },
  // HUF と IDR は ISO 上は小数 2 桁だが、現地で小数を使わないので 0 桁にする
  { code: 'HUF', decimals: 0, symbol: 'Ft', symbolAfter: true, flag: '🇭🇺' },
  { code: 'IDR', decimals: 0, symbol: 'Rp', flag: '🇮🇩' },
  { code: 'ILS', decimals: 2, symbol: '₪', flag: '🇮🇱' },
  { code: 'INR', decimals: 2, symbol: '₹', flag: '🇮🇳' },
  { code: 'ISK', decimals: 0, symbol: 'kr', symbolAfter: true, flag: '🇮🇸' },
  { code: 'JPY', decimals: 0, symbol: '¥', flag: '🇯🇵' },
  { code: 'KRW', decimals: 0, symbol: '₩', flag: '🇰🇷' },
  { code: 'MXN', decimals: 2, symbol: 'MX$', flag: '🇲🇽' },
  { code: 'MYR', decimals: 2, symbol: 'RM', flag: '🇲🇾' },
  { code: 'NOK', decimals: 2, symbol: 'kr', symbolAfter: true, flag: '🇳🇴' },
  { code: 'NZD', decimals: 2, symbol: 'NZ$', flag: '🇳🇿' },
  { code: 'PHP', decimals: 2, symbol: '₱', flag: '🇵🇭' },
  { code: 'PLN', decimals: 2, symbol: 'zł', symbolAfter: true, flag: '🇵🇱' },
  { code: 'RON', decimals: 2, symbol: 'lei', symbolAfter: true, flag: '🇷🇴' },
  { code: 'SEK', decimals: 2, symbol: 'kr', symbolAfter: true, flag: '🇸🇪' },
  { code: 'SGD', decimals: 2, symbol: 'S$', flag: '🇸🇬' },
  { code: 'THB', decimals: 2, symbol: '฿', flag: '🇹🇭' },
  { code: 'TRY', decimals: 2, symbol: '₺', flag: '🇹🇷' },
  { code: 'USD', decimals: 2, symbol: '$', flag: '🇺🇸' },
  { code: 'ZAR', decimals: 2, symbol: 'R', flag: '🇿🇦' },
];

export function findCurrency(code: string): CurrencyInfo | undefined {
  return CURRENCIES.find((c) => c.code === code);
}

export function currencyDecimals(code: string): number {
  return findCurrency(code)?.decimals ?? 2;
}

export function currencySymbol(code: string): string {
  return findCurrency(code)?.symbol ?? code;
}

/**
 * 通貨名をその言語で返す。30 通貨 × 4 言語を手で訳さずに済ませるため
 * Intl に任せる。未対応の環境やコードでは ISO コードをそのまま出す。
 */
export function currencyName(code: string, lang: Lang): string {
  try {
    return new Intl.DisplayNames([lang], { type: 'currency' }).of(code) ?? code;
  } catch {
    return code;
  }
}
```

- [ ] **Step 4: TripForm の通貨 option を `label` 無しに直す**

`CurrencyInfo.label` が無くなったので `src/ui/TripForm.tsx` の option を差し替える。通貨名は Task 16 で言語対応させる。

```tsx
{CURRENCIES.map((c) => (
  <option key={c.code} value={c.code}>
    {c.flag} {c.code}
  </option>
))}
```

- [ ] **Step 5: テストと型検査が通ることを確認する**

Run: `npm run lint && npx vitest run src/domain/currency.test.ts src/ui/TripForm.test.tsx`
Expected: PASS。TripForm のテストが `中国元(CNY)` のような文字列に依存していたら `🇨🇳 CNY` に直す。

- [ ] **Step 6: 全体の検証とコミット**

Run: `npm test`
Expected: 成功

```bash
git add src/domain/currency.ts src/domain/currency.test.ts src/ui/TripForm.tsx src/ui/TripForm.test.tsx
git commit -m "feat: 対応通貨を ECB の 30 通貨に広げ、国旗と通貨名の多言語化を入れる"
```

---

### Task 3: 金額フォーマッタを換算先通貨に対応させる

**Files:**
- Modify: `src/domain/money.ts`
- Test: `src/domain/money.test.ts`

**Interfaces:**
- Consumes: `findCurrency` from `src/domain/currency.ts`(Task 2 で `symbolAfter` が生えている)
- Produces:
  - `toHomeMinor(amountMinor: number, decimals: number, rate: number, homeDecimals: number): number`
  - `formatWithCurrency(amountMinor: number, currency: string): string`(後置記号と負号に対応)
- 注意: `toJpy` は Task 10、`formatJpy` は Task 17 で消す。このタスクでは残す。

- [ ] **Step 1: 失敗するテストを書く**

`src/domain/money.test.ts` の import に `toHomeMinor` を足し、`formatWithCurrency` の describe を差し替え、`toHomeMinor` の describe を追加する。

```ts
import {
  minorToMajor,
  formatMajor,
  parseMajorToMinor,
  toJpy,
  toHomeMinor,
  formatJpy,
  formatWithCurrency,
} from './money';
```

```ts
describe('toHomeMinor', () => {
  it('小数 0 桁の換算先(円)はそのまま整数になる', () => {
    // 120.00 元 × 23.465 = 2815.8 → 2816
    expect(toHomeMinor(12000, 2, 23.465, 0)).toBe(2816);
  });

  it('小数 2 桁の換算先(ドル)はセントまで持つ', () => {
    // 120.00 元 × 0.1405 = 16.86 ドル → 1686 セント
    expect(toHomeMinor(12000, 2, 0.1405, 2)).toBe(1686);
  });

  it('小数 0 桁どうしも扱える', () => {
    // 10000 ウォン × 0.1085 = 1085 円
    expect(toHomeMinor(10000, 0, 0.1085, 0)).toBe(1085);
  });

  it('金額 0 は 0', () => {
    expect(toHomeMinor(0, 2, 23.465, 2)).toBe(0);
  });
});

describe('formatWithCurrency', () => {
  it('記号を前に置く通貨', () => {
    expect(formatWithCurrency(15000, 'KRW')).toBe('₩15,000');
    expect(formatWithCurrency(147242, 'USD')).toBe('$1,472.42');
  });

  it('記号を後ろに置く通貨', () => {
    expect(formatWithCurrency(12000, 'CNY')).toBe('120.00元');
    expect(formatWithCurrency(120000, 'SEK')).toBe('1,200.00kr');
  });

  it('負の値は記号より前に符号を置く', () => {
    expect(formatWithCurrency(-1200, 'JPY')).toBe('-¥1,200');
    expect(formatWithCurrency(-120000, 'CNY')).toBe('-1,200.00元');
  });

  it('未知の通貨はコードを前に置き小数 2 桁で出す', () => {
    expect(formatWithCurrency(1234, 'XXX')).toBe('XXX12.34');
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/domain/money.test.ts`
Expected: FAIL(`toHomeMinor` 未定義、`-1,200.00元` が `¥-…` 側の実装と食い違う)

- [ ] **Step 3: `src/domain/money.ts` を直す**

先頭の import を差し替える。

```ts
import { findCurrency } from './currency';
```

`toJpy` の下に `toHomeMinor` を足す。

```ts
/**
 * 外貨の最小単位を換算先通貨の最小単位にする。換算はこの関数だけを通す。
 * 桁数は旅行に保存した値を使う(通貨マスタを後から変えても過去の数字が動かないため)。
 */
export function toHomeMinor(
  amountMinor: number,
  decimals: number,
  rate: number,
  homeDecimals: number,
): number {
  return Math.round(minorToMajor(amountMinor, decimals) * rate * 10 ** homeDecimals);
}
```

`formatWithCurrency` を差し替える。

```ts
/** 通貨記号を添えた表示。後置記号の通貨は末尾に付け、負号は必ず先頭に置く。 */
export function formatWithCurrency(amountMinor: number, currency: string): string {
  const info = findCurrency(currency);
  const sign = amountMinor < 0 ? '-' : '';
  const body = formatMajor(Math.abs(amountMinor), info?.decimals ?? 2);
  const symbol = info?.symbol ?? currency;
  return info?.symbolAfter ? `${sign}${body}${symbol}` : `${sign}${symbol}${body}`;
}
```

`currencyDecimals` / `currencySymbol` の import が未使用になるので消す。

- [ ] **Step 4: テストが通ることを確認する**

Run: `npx vitest run src/domain/money.test.ts`
Expected: PASS

- [ ] **Step 5: 全体の検証とコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/domain/money.ts src/domain/money.test.ts
git commit -m "feat: 換算先通貨の桁数を扱う toHomeMinor と後置記号の表示を追加する"
```

---

### Task 4: Trip に換算先通貨を持たせる

**Files:**
- Modify: `src/domain/types.ts`
- Modify: `src/data/migrateTrip.ts`(`migrateTripBudget` → `migrateTrip`)
- Modify: `src/data/tripRepo.ts`
- Modify: `src/data/db.ts:26`(v2 upgrade の呼び先を改名に追随)
- Modify: `src/data/backup.ts`(`isBudgetField`・`isTrip`)
- Modify: `src/domain/summary.ts:93-98`(予算フィールドの参照)
- Modify: `src/ui/TripForm.tsx`, `src/ui/SettingsScreen.tsx`(予算フィールドの参照)
- Test: `src/data/migrateTrip.test.ts`, `src/data/repos.test.ts`, `src/data/backup.test.ts`

**Interfaces:**
- Produces:
  - `Trip` に `homeCurrency: string` / `homeCurrencyDecimals: number` を追加、`personalBudgetJpy` → `personalBudgetHome`、`sharedBudgetJpy` → `sharedBudgetHome`
  - `RateSource` に `'same'` を追加、`RateCache` に `quote: string` を追加
  - `migrateTrip(trip: Record<string, unknown>): Trip`
  - `TripInput` に `homeCurrency: string` を追加、予算フィールドを `*Home` に改名

- [ ] **Step 1: `src/domain/types.ts` を直す**

`Trip` を次の形にする。

```ts
export type Trip = {
  id: string;
  name: string;
  /** 現地通貨。ISO 4217。"CNY" など */
  currency: string;
  /** 現地通貨の最小単位の桁数。旅行を作った時点の値を保存する */
  currencyDecimals: number;
  /** 換算先通貨。ISO 4217。"JPY" など */
  homeCurrency: string;
  /** 換算先通貨の最小単位の桁数 */
  homeCurrencyDecimals: number;
  startDate: string;
  endDate: string | null;
  /** 個別支出の予算(換算先通貨の最小単位)。未設定は null */
  personalBudgetHome: number | null;
  /** 共有支出のうち自分の負担分の予算(換算先通貨の最小単位)。未設定は null */
  sharedBudgetHome: number | null;
  memberCount: number;
  createdAt: number;
};
```

`RateSource` と `RateCache` を直す。

```ts
/** same は現地通貨と換算先通貨が同じでレートが 1 の場合 */
export type RateSource = 'api' | 'cache' | 'manual' | 'same';

export type RateCache = {
  /** "CNY:JPY:2026-09-12" */
  key: string;
  base: string;
  /** 換算先通貨 */
  quote: string;
  date: string;
  rate: number;
  effectiveDate: string;
  fetchedAt: number;
  source: 'frankfurter' | 'er-api';
};
```

- [ ] **Step 2: migrateTrip の失敗するテストを書く**

`src/data/migrateTrip.test.ts` を次の内容に置き換える。

```ts
import { describe, it, expect } from 'vitest';
import { migrateTrip } from './migrateTrip';

const v1Trip = {
  id: 't1',
  name: '上海 2026-09',
  currency: 'CNY',
  currencyDecimals: 2,
  startDate: '2026-09-12',
  endDate: null,
  budgetJpy: 100000,
  memberCount: 2,
  createdAt: 0,
};

const v2Trip = {
  id: 't2',
  name: 'NY 2026-09',
  currency: 'USD',
  currencyDecimals: 2,
  startDate: '2026-09-12',
  endDate: null,
  personalBudgetJpy: 50000,
  sharedBudgetJpy: 30000,
  memberCount: 1,
  createdAt: 0,
};

describe('migrateTrip', () => {
  it('v1 の budgetJpy を個別予算に移す', () => {
    const t = migrateTrip({ ...v1Trip });
    expect(t.personalBudgetHome).toBe(100000);
    expect(t.sharedBudgetHome).toBeNull();
  });

  it('予算未設定の v1 は個別・共有とも null', () => {
    const t = migrateTrip({ ...v1Trip, budgetJpy: null });
    expect(t.personalBudgetHome).toBeNull();
    expect(t.sharedBudgetHome).toBeNull();
  });

  it('v2 の予算を換算先通貨の予算として引き継ぐ(円は小数 0 桁なので値は変えない)', () => {
    const t = migrateTrip({ ...v2Trip });
    expect(t.personalBudgetHome).toBe(50000);
    expect(t.sharedBudgetHome).toBe(30000);
  });

  it('換算先通貨が無ければ円にする', () => {
    const t = migrateTrip({ ...v2Trip });
    expect(t.homeCurrency).toBe('JPY');
    expect(t.homeCurrencyDecimals).toBe(0);
  });

  it('v3 の旅行はそのまま返す', () => {
    const t = migrateTrip({
      ...v2Trip,
      personalBudgetJpy: undefined,
      sharedBudgetJpy: undefined,
      homeCurrency: 'USD',
      homeCurrencyDecimals: 2,
      personalBudgetHome: 150050,
      sharedBudgetHome: null,
    });
    expect(t.homeCurrency).toBe('USD');
    expect(t.homeCurrencyDecimals).toBe(2);
    expect(t.personalBudgetHome).toBe(150050);
    expect(t.sharedBudgetHome).toBeNull();
  });

  it('旧フィールドを残さない', () => {
    const t = migrateTrip({ ...v1Trip }) as unknown as Record<string, unknown>;
    expect('budgetJpy' in t).toBe(false);
    expect('personalBudgetJpy' in t).toBe(false);
    expect('sharedBudgetJpy' in t).toBe(false);
  });

  it('予算と通貨以外のフィールドは変えない', () => {
    const t = migrateTrip({ ...v1Trip });
    expect(t.id).toBe('t1');
    expect(t.name).toBe('上海 2026-09');
    expect(t.currency).toBe('CNY');
    expect(t.currencyDecimals).toBe(2);
    expect(t.startDate).toBe('2026-09-12');
    expect(t.endDate).toBeNull();
    expect(t.memberCount).toBe(2);
    expect(t.createdAt).toBe(0);
  });
});
```

- [ ] **Step 3: テストが落ちることを確認する**

Run: `npx vitest run src/data/migrateTrip.test.ts`
Expected: FAIL(`migrateTrip` が export されていない)

- [ ] **Step 4: `src/data/migrateTrip.ts` を書き換える**

```ts
import type { Trip } from '../domain/types';

/**
 * 旧バージョンの旅行を今の形に直す。DB の upgrade とバックアップ取り込みの両方から使う。
 * v1: 予算が budgetJpy 1 本 / v2: 個別・共有の 2 本(円固定) / v3: 換算先通貨つき
 */
export function migrateTrip(trip: Record<string, unknown>): Trip {
  const t: Record<string, unknown> = { ...trip };

  // v1 → v2: 1 本だった予算は個別予算として引き継ぐ
  if (!('personalBudgetJpy' in t) && !('personalBudgetHome' in t)) {
    t.personalBudgetJpy = t.budgetJpy ?? null;
    t.sharedBudgetJpy = null;
  }

  // v2 → v3: 予算は円建てだった。JPY は小数 0 桁なので値の変換は要らず、名前だけ移す
  if (!('personalBudgetHome' in t)) {
    t.personalBudgetHome = t.personalBudgetJpy ?? null;
    t.sharedBudgetHome = t.sharedBudgetJpy ?? null;
  }

  t.homeCurrency ??= 'JPY';
  t.homeCurrencyDecimals ??= 0;

  delete t.budgetJpy;
  delete t.personalBudgetJpy;
  delete t.sharedBudgetJpy;

  return t as unknown as Trip;
}
```

- [ ] **Step 5: テストが通ることを確認する**

Run: `npx vitest run src/data/migrateTrip.test.ts`
Expected: PASS(7 tests)

- [ ] **Step 6: 呼び出し側を改名に追随させる**

`src/data/db.ts` の v2 upgrade。

```ts
import { migrateTrip } from './migrateTrip';
```

```ts
      await tx
        .table('trips')
        .toCollection()
        .modify((trip: Record<string, unknown>) => {
          Object.assign(trip, migrateTrip(trip));
          delete trip.budgetJpy;
        });
```

`src/data/tripRepo.ts` の `TripInput` と `createTrip` / `updateTrip`。

```ts
export type TripInput = {
  name: string;
  currency: string;
  homeCurrency: string;
  startDate: string;
  endDate?: string | null;
  personalBudgetHome?: number | null;
  sharedBudgetHome?: number | null;
  memberCount?: number;
};
```

`createTrip` の生成部分。

```ts
  const trip: Trip = {
    id: newId(),
    name: input.name.trim(),
    currency: input.currency,
    currencyDecimals: currencyDecimals(input.currency),
    homeCurrency: input.homeCurrency,
    homeCurrencyDecimals: currencyDecimals(input.homeCurrency),
    startDate: input.startDate,
    endDate: input.endDate ?? null,
    personalBudgetHome: input.personalBudgetHome ?? null,
    sharedBudgetHome: input.sharedBudgetHome ?? null,
    memberCount: input.memberCount ?? 1,
    createdAt: Date.now(),
  };
```

`updateTrip` の差分反映部分。既存の `currency` と同じ扱いを `homeCurrency` にも足す。

```ts
  // 小数桁数は通貨から必ず導く。手で食い違わせない。
  if (patch.currency !== undefined) {
    next.currency = patch.currency;
    next.currencyDecimals = currencyDecimals(patch.currency);
  }
  if (patch.homeCurrency !== undefined) {
    next.homeCurrency = patch.homeCurrency;
    next.homeCurrencyDecimals = currencyDecimals(patch.homeCurrency);
  }
  if (patch.personalBudgetHome !== undefined) next.personalBudgetHome = patch.personalBudgetHome;
  if (patch.sharedBudgetHome !== undefined) next.sharedBudgetHome = patch.sharedBudgetHome;
```

`src/data/backup.ts` の `isBudgetField` と `isTrip`。

```ts
/** 予算は v1〜v3 で名前が違う。どれも「数値または null、あるいは未設定」を許す。 */
function isBudgetField(v: unknown): boolean {
  return v === undefined || v === null || typeof v === 'number';
}
```

```ts
    isBudgetField(t.budgetJpy) &&
    isBudgetField(t.personalBudgetJpy) &&
    isBudgetField(t.sharedBudgetJpy) &&
    isBudgetField(t.personalBudgetHome) &&
    isBudgetField(t.sharedBudgetHome) &&
    // v1.0.4 以前のバックアップには換算先通貨が無い
    (t.homeCurrency === undefined || typeof t.homeCurrency === 'string') &&
```

`src/domain/summary.ts` の予算参照(フィールド名は Task 10 で改名するのでここでは参照先だけ直す)。

```ts
    personalBudgetJpy: trip.personalBudgetHome,
    personalRemainingJpy:
      trip.personalBudgetHome === null ? null : trip.personalBudgetHome - personalJpy,
    sharedBudgetJpy: trip.sharedBudgetHome,
    sharedRemainingJpy:
      trip.sharedBudgetHome === null ? null : trip.sharedBudgetHome - sharedPerPersonJpy,
```

`src/ui/TripForm.tsx` は `personalBudgetJpy` / `sharedBudgetJpy` を渡している箇所を `personalBudgetHome` / `sharedBudgetHome` に、初期値の読み出しも `trip?.personalBudgetHome` / `trip?.sharedBudgetHome` に直す。さらに `createTrip` / `updateTrip` に `homeCurrency` を渡す必要があるので、暫定で `homeCurrency: trip?.homeCurrency ?? 'JPY'` を渡す(選択 UI は Task 16 で入れる)。

`src/ui/SettingsScreen.tsx` の `budgetLabel` も参照名を直す。

```tsx
function budgetLabel(trip: Trip): string {
  const parts: string[] = [];
  if (trip.personalBudgetHome !== null) parts.push(`個別 ${formatJpy(trip.personalBudgetHome)}`);
  if (trip.sharedBudgetHome !== null) parts.push(`共有 ${formatJpy(trip.sharedBudgetHome)}`);
  return parts.length === 0 ? '' : ` / ${parts.join(' / ')}`;
}
```

- [ ] **Step 7: 型検査を通し、落ちたテストを直す**

Run: `npm run lint`
Expected: エラーが出た箇所(主にテストの Trip リテラルと `createTrip` 呼び出し)に `homeCurrency` を足し、予算フィールドを改名する。`createTrip({ …, homeCurrency: 'JPY' })` を既定にする。

Run: `npm test`
Expected: PASS

- [ ] **Step 8: コミット**

```bash
git add src/domain/types.ts src/domain/summary.ts src/data src/ui/TripForm.tsx src/ui/SettingsScreen.tsx
git commit -m "feat: 旅行ごとの換算先通貨と予算フィールドの改名を入れる"
```

---

### Task 5: DB を v3 に上げる

**Files:**
- Modify: `src/data/db.ts`
- Test: `src/data/repos.test.ts`(既存の通し確認のみ。v2→v3 の移行そのものは `migrateTrip` の単体テストで担保する)

**Interfaces:**
- Consumes: `migrateTrip` from `src/data/migrateTrip.ts`
- Produces: `rates` テーブルに `quote` と `[base+quote]` の索引

- [ ] **Step 1: `src/data/db.ts` に v3 を足す**

v2 の定義の下に追記する。`trips` は索引の付くフィールドが増えないので `stores()` に書かない(v2 と同じ考え方)。

```ts
    // v3: 換算先通貨を旅行ごとに持たせる。レートも base だけでなく quote で引けるようにする。
    this.version(3)
      .stores({ rates: 'key, base, quote, [base+quote], [base+date], date' })
      .upgrade(async (tx) => {
        await tx
          .table('trips')
          .toCollection()
          .modify((trip: Record<string, unknown>) => {
            Object.assign(trip, migrateTrip(trip));
            delete trip.budgetJpy;
            delete trip.personalBudgetJpy;
            delete trip.sharedBudgetJpy;
          });
        // v2 までに貯めたキャッシュはすべて円建てだった
        await tx
          .table('rates')
          .toCollection()
          .modify((r: Record<string, unknown>) => {
            r.quote = 'JPY';
          });
      });
```

- [ ] **Step 2: 検証してコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/data/db.ts
git commit -m "feat: DB を v3 に上げて旅行の移行とレートの quote 索引を追加する"
```

---

### Task 6: バックアップを v3 にする

**Files:**
- Modify: `src/data/backup.ts`
- Modify: `src/ui/SettingsScreen.tsx:handleImport`
- Test: `src/data/backup.test.ts`

**Interfaces:**
- Consumes: `migrateTrip` from `src/data/migrateTrip.ts`
- Produces:
  - `BackupFile.version: 3`
  - `type BackupErrorCode = 'invalid-json' | 'not-backup' | 'unsupported-version' | 'broken'`
  - `class BackupError extends Error { readonly code: BackupErrorCode; readonly detail?: string }`
  - `parseBackup` は v1 / v2 / v3 を受け付け、旅行を `migrateTrip` に通して返す

- [ ] **Step 1: 失敗するテストを書く**

`src/data/backup.test.ts` に追記する。

```ts
import { BackupError, parseBackup, serializeBackup } from './backup';

describe('parseBackup のエラー', () => {
  it('JSON でなければ invalid-json', () => {
    expect(() => parseBackup('{')).toThrowError(
      expect.objectContaining({ code: 'invalid-json' }),
    );
  });

  it('形が違えば not-backup', () => {
    expect(() => parseBackup('{"foo":1}')).toThrowError(
      expect.objectContaining({ code: 'not-backup' }),
    );
  });

  it('未知のバージョンは unsupported-version とその値', () => {
    const err = (() => {
      try {
        parseBackup('{"app":"trip-wallet","version":9,"trips":[],"expenses":[],"photos":[]}');
        return null;
      } catch (e) {
        return e as BackupError;
      }
    })();
    expect(err?.code).toBe('unsupported-version');
    expect(err?.detail).toBe('9');
  });

  it('中身が壊れていれば broken', () => {
    expect(() =>
      parseBackup('{"app":"trip-wallet","version":3,"trips":[{"id":1}],"expenses":[],"photos":[]}'),
    ).toThrowError(expect.objectContaining({ code: 'broken' }));
  });
});

describe('parseBackup のバージョン互換', () => {
  it('v2 の旅行に換算先通貨を補う', () => {
    const json = JSON.stringify({
      app: 'trip-wallet',
      version: 2,
      exportedAt: 0,
      trips: [
        {
          id: 't1',
          name: 'NY',
          currency: 'USD',
          currencyDecimals: 2,
          startDate: '2026-09-12',
          endDate: null,
          personalBudgetJpy: 50000,
          sharedBudgetJpy: null,
          memberCount: 1,
          createdAt: 0,
        },
      ],
      expenses: [],
      photos: [],
    });
    const parsed = parseBackup(json);
    expect(parsed.trips[0].homeCurrency).toBe('JPY');
    expect(parsed.trips[0].personalBudgetHome).toBe(50000);
  });

  it('書き出しは v3', () => {
    const json = serializeBackup({ trips: [], expenses: [], photos: [] });
    expect(JSON.parse(json).version).toBe(3);
  });
});
```

> `serializeBackup` の引数は既存のシグネチャに合わせる。既存テストの呼び出し方をそのまま真似ること。

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/data/backup.test.ts`
Expected: FAIL(`BackupError` 未定義、version が 2)

- [ ] **Step 3: `src/data/backup.ts` を直す**

`BackupFile` の `version` を `3` にし、エラー型を追加する。

```ts
export type BackupErrorCode = 'invalid-json' | 'not-backup' | 'unsupported-version' | 'broken';

/** 取り込みの失敗理由。文言は画面側で言語に合わせて組み立てる。 */
export class BackupError extends Error {
  constructor(
    readonly code: BackupErrorCode,
    readonly detail?: string,
  ) {
    super(code);
    this.name = 'BackupError';
  }
}
```

`parseBackup` を差し替える。

```ts
export function parseBackup(text: string): BackupPayload {
  let raw: unknown;
  try {
    raw = JSON.parse(text);
  } catch {
    throw new BackupError('invalid-json');
  }

  if (typeof raw !== 'object' || raw === null) throw new BackupError('not-backup');
  const obj = raw as Record<string, unknown>;
  if (obj.app !== 'trip-wallet') throw new BackupError('not-backup');

  // 書き出しは v3 だが、v1.0.2 以前が書いた v1 と v1.0.4 までの v2 も取り込めるようにする
  const version = obj.version;
  if (version !== 1 && version !== 2 && version !== 3) {
    throw new BackupError('unsupported-version', String(version));
  }

  if (!Array.isArray(obj.trips) || !Array.isArray(obj.expenses) || !Array.isArray(obj.photos)) {
    throw new BackupError('broken');
  }
  if (!obj.trips.every(isTrip) || !obj.expenses.every(isExpense) || !obj.photos.every(isBackupPhoto)) {
    throw new BackupError('broken');
  }

  return {
    trips: obj.trips.map((t) => migrateTrip(t as Record<string, unknown>)),
    expenses: obj.expenses as Expense[],
    photos: obj.photos as BackupPhoto[],
  };
}
```

`serializeBackup` / `exportBackup` が書く `version` も `3` にする。

- [ ] **Step 4: SettingsScreen のエラー表示を code から組み立てる**

`src/ui/SettingsScreen.tsx` の `handleImport` の `catch`。文言は Task 17 で辞書に移すので、ここでは日本語のまま関数に切り出しておく。

```tsx
// Task 17 で t.backup.error に置き換える
function importErrorMessage(err: unknown): string {
  if (!(err instanceof BackupError)) return '取り込みに失敗しました';
  switch (err.code) {
    case 'invalid-json':
      return 'JSON として読み込めませんでした';
    case 'not-backup':
      return 'Trip Wallet のバックアップファイルではありません';
    case 'unsupported-version':
      return `対応していないバージョンです: ${err.detail ?? ''}`;
    case 'broken':
      return 'バックアップの中身が壊れています';
  }
}
```

```tsx
      setMessage(importErrorMessage(err));
```

- [ ] **Step 5: テストが通ることを確認する**

Run: `npx vitest run src/data/backup.test.ts src/ui/SettingsScreen.test.tsx`
Expected: PASS

- [ ] **Step 6: 全体の検証とコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/data/backup.ts src/data/backup.test.ts src/ui/SettingsScreen.tsx
git commit -m "feat: バックアップを v3 にし、取り込みエラーをコードで返すようにする"
```

---

### Task 7: レート取得を任意の通貨ペアに対応させる

**Files:**
- Modify: `src/rates/frankfurter.ts`
- Modify: `src/rates/erApi.ts`
- Modify: `src/rates/resolveRate.ts:defaultDeps`(引数の橋渡しのみ)
- Test: `src/rates/frankfurter.test.ts`, `src/rates/erApi.test.ts`

**Interfaces:**
- Produces:
  - `fetchFrankfurterRate(base: string, quote: string, date: string, fetchImpl?: typeof fetch): Promise<FetchedRate>`
  - `fetchErApiRate(base: string, quote: string, fallbackDate: string, fetchImpl?: typeof fetch): Promise<FetchedRate>`
  - `FetchedRate = { rate: number; effectiveDate: string }`(変更なし)
- Note: `ResolveRateDeps` を広げるのは Task 9。ここでは `defaultDeps()` の中で `'JPY'` を渡して型を保つ。

- [ ] **Step 1: Frankfurter の失敗するテストを書く**

`src/rates/frankfurter.test.ts` を次の内容に置き換える。

```ts
import { describe, it, expect, vi } from 'vitest';
import { fetchFrankfurterRate } from './frankfurter';

function jsonResponse(body: unknown, ok = true, status = 200): Response {
  return { ok, status, json: async () => body } as Response;
}

describe('fetchFrankfurterRate', () => {
  it('日付と通貨ペアを指定してレートを取り出す', async () => {
    // amount=1000000 で問い合わせるので応答も 100 万倍で返る
    const fetchImpl = vi.fn(async () => jsonResponse({ rates: { JPY: 23465000 }, date: '2026-09-11' }));
    const r = await fetchFrankfurterRate('CNY', 'JPY', '2026-09-12', fetchImpl as unknown as typeof fetch);

    expect(r).toEqual({ rate: 23.465, effectiveDate: '2026-09-11' });
    expect(fetchImpl).toHaveBeenCalledWith(
      'https://api.frankfurter.dev/v1/2026-09-12?base=CNY&symbols=JPY&amount=1000000',
    );
  });

  it('1 を大きく下回るレートでも桁を落とさない', async () => {
    // 1 円 = 0.0056306 ドル。amount=1 だと応答が 0.01 に丸められてしまう
    const fetchImpl = vi.fn(async () => jsonResponse({ rates: { USD: 5630.6 }, date: '2026-09-12' }));
    const r = await fetchFrankfurterRate('JPY', 'USD', '2026-09-12', fetchImpl as unknown as typeof fetch);

    expect(r.rate).toBeCloseTo(0.0056306, 9);
  });

  it('HTTP エラーは例外にする', async () => {
    const fetchImpl = vi.fn(async () => jsonResponse({}, false, 404));
    await expect(
      fetchFrankfurterRate('CNY', 'JPY', '2026-09-12', fetchImpl as unknown as typeof fetch),
    ).rejects.toThrow();
  });

  it('相手通貨が含まれない応答は例外にする', async () => {
    const fetchImpl = vi.fn(async () => jsonResponse({ rates: {}, date: '2026-09-12' }));
    await expect(
      fetchFrankfurterRate('CNY', 'JPY', '2026-09-12', fetchImpl as unknown as typeof fetch),
    ).rejects.toThrow();
  });

  it('レートが 0 以下なら例外にする', async () => {
    const fetchImpl = vi.fn(async () => jsonResponse({ rates: { JPY: 0 }, date: '2026-09-12' }));
    await expect(
      fetchFrankfurterRate('CNY', 'JPY', '2026-09-12', fetchImpl as unknown as typeof fetch),
    ).rejects.toThrow();
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/rates/frankfurter.test.ts`
Expected: FAIL(引数が 1 つ多い / URL に amount が無い)

- [ ] **Step 3: `src/rates/frankfurter.ts` を直す**

```ts
import type { FetchedRate } from './types';

const BASE_URL = 'https://api.frankfurter.dev/v1';

// 応答は結果を丸めて返す。1 円 = 0.01 ドルのように桁が落ちるのを避けるため、
// 100 万倍で問い合わせてから割り戻す。
const AMOUNT = 1_000_000;

export async function fetchFrankfurterRate(
  base: string,
  quote: string,
  date: string,
  fetchImpl: typeof fetch = fetch,
): Promise<FetchedRate> {
  const res = await fetchImpl(`${BASE_URL}/${date}?base=${base}&symbols=${quote}&amount=${AMOUNT}`);
  if (!res.ok) throw new Error(`frankfurter: HTTP ${res.status}`);

  const json = (await res.json()) as { rates?: Record<string, number>; date?: string };
  const scaled = json.rates?.[quote];
  if (typeof scaled !== 'number' || scaled <= 0) throw new Error(`frankfurter: ${quote} のレートがありません`);

  return { rate: scaled / AMOUNT, effectiveDate: json.date ?? date };
}
```

> `FetchedRate` は現状 `frankfurter.ts` で定義されている。`erApi.ts` からも使うので `src/rates/types.ts` に移し、`frankfurter.ts` は `export type { FetchedRate } from './types';` を残して既存の import を壊さないこと。

- [ ] **Step 4: er-api の失敗するテストを書く**

`src/rates/erApi.test.ts` を次の内容に置き換える。

```ts
import { describe, it, expect, vi } from 'vitest';
import { fetchErApiRate } from './erApi';

function jsonResponse(body: unknown, ok = true, status = 200): Response {
  return { ok, status, json: async () => body } as Response;
}

describe('fetchErApiRate', () => {
  it('最新レートと更新日を取り出す', async () => {
    const fetchImpl = vi.fn(async () =>
      jsonResponse({
        result: 'success',
        time_last_update_unix: Date.UTC(2026, 8, 12, 0, 0, 0) / 1000,
        rates: { JPY: 23.5 },
      }),
    );
    const r = await fetchErApiRate('CNY', 'JPY', '2026-09-12', fetchImpl as unknown as typeof fetch);

    expect(r.rate).toBe(23.5);
    expect(r.effectiveDate).toMatch(/^2026-09-1[12]$/);
    expect(fetchImpl).toHaveBeenCalledWith('https://open.er-api.com/v6/latest/CNY');
  });

  it('更新時刻が無ければ渡された日付を使う', async () => {
    const fetchImpl = vi.fn(async () => jsonResponse({ result: 'success', rates: { JPY: 23.5 } }));
    const r = await fetchErApiRate('CNY', 'JPY', '2026-09-12', fetchImpl as unknown as typeof fetch);

    expect(r.effectiveDate).toBe('2026-09-12');
  });

  it('1 を大きく下回るレートは逆方向を引いて精度を上げる', async () => {
    // 直接引くと 1 円 = 0.0056 ドル(4 桁で丸め)。逆方向は 1 ドル = 177.6 円と細かい
    const fetchImpl = vi.fn(async (url: string) =>
      url.endsWith('/JPY')
        ? jsonResponse({ result: 'success', rates: { USD: 0.0056 } })
        : jsonResponse({ result: 'success', rates: { JPY: 177.6 } }),
    );
    const r = await fetchErApiRate('JPY', 'USD', '2026-09-12', fetchImpl as unknown as typeof fetch);

    expect(r.rate).toBeCloseTo(1 / 177.6, 9);
    expect(fetchImpl).toHaveBeenCalledTimes(2);
  });

  it('逆方向の取得に失敗したら直接のレートを使う', async () => {
    const fetchImpl = vi.fn(async (url: string) => {
      if (url.endsWith('/JPY')) return jsonResponse({ result: 'success', rates: { USD: 0.0056 } });
      throw new Error('offline');
    });
    const r = await fetchErApiRate('JPY', 'USD', '2026-09-12', fetchImpl as unknown as typeof fetch);

    expect(r.rate).toBe(0.0056);
  });

  it('result が error なら例外にする', async () => {
    const fetchImpl = vi.fn(async () => jsonResponse({ result: 'error' }));
    await expect(
      fetchErApiRate('CNY', 'JPY', '2026-09-12', fetchImpl as unknown as typeof fetch),
    ).rejects.toThrow();
  });

  it('HTTP エラーは例外にする', async () => {
    const fetchImpl = vi.fn(async () => jsonResponse({}, false, 500));
    await expect(
      fetchErApiRate('CNY', 'JPY', '2026-09-12', fetchImpl as unknown as typeof fetch),
    ).rejects.toThrow();
  });
});
```

- [ ] **Step 5: テストが落ちることを確認する**

Run: `npx vitest run src/rates/erApi.test.ts`
Expected: FAIL

- [ ] **Step 6: `src/rates/erApi.ts` を直す**

```ts
import { toIsoDate } from '../domain/date';
import type { FetchedRate } from './types';

const BASE_URL = 'https://open.er-api.com/v6/latest';

// 応答は小数 4〜5 桁で丸められる。この閾値を下回るレートは有効桁が足りないので
// 逆方向(quote → base)を引いて逆数を取る。
const PRECISION_FLOOR = 0.1;

async function fetchLatest(
  base: string,
  quote: string,
  fallbackDate: string,
  fetchImpl: typeof fetch,
): Promise<FetchedRate> {
  const res = await fetchImpl(`${BASE_URL}/${base}`);
  if (!res.ok) throw new Error(`er-api: HTTP ${res.status}`);

  const json = (await res.json()) as {
    result?: string;
    time_last_update_unix?: number;
    rates?: Record<string, number>;
  };
  if (json.result === 'error') throw new Error('er-api: エラー応答');

  const rate = json.rates?.[quote];
  if (typeof rate !== 'number' || rate <= 0) throw new Error(`er-api: ${quote} のレートがありません`);

  const unix = json.time_last_update_unix;
  return {
    rate,
    effectiveDate: typeof unix === 'number' ? toIsoDate(new Date(unix * 1000)) : fallbackDate,
  };
}

export async function fetchErApiRate(
  base: string,
  quote: string,
  fallbackDate: string,
  fetchImpl: typeof fetch = fetch,
): Promise<FetchedRate> {
  const direct = await fetchLatest(base, quote, fallbackDate, fetchImpl);
  if (direct.rate >= PRECISION_FLOOR) return direct;

  try {
    const reverse = await fetchLatest(quote, base, fallbackDate, fetchImpl);
    return { rate: 1 / reverse.rate, effectiveDate: reverse.effectiveDate };
  } catch {
    // 2 回目が落ちても 1 回目の値は使える。精度は落ちるが取得できないよりよい。
    return direct;
  }
}
```

- [ ] **Step 7: `defaultDeps` の型を保つ**

`src/rates/resolveRate.ts` の `defaultDeps()` で、まだ `quote` を持たない `ResolveRateDeps` に合わせて `'JPY'` を渡す。Task 9 でこの橋渡しを外す。

```ts
    // quote を通すのは Task 9。ここでは従来どおり円建てで呼ぶ。
    fetchFrankfurter: (base, date) => fetchFrankfurterRate(base, 'JPY', date),
    fetchErApi: (base, fallbackDate) => fetchErApiRate(base, 'JPY', fallbackDate),
```

- [ ] **Step 8: 検証してコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/rates
git commit -m "feat: レート取得を任意の通貨ペアに対応させ、丸めで桁が落ちるのを防ぐ"
```

---

### Task 8: レートキャッシュを通貨ペアで引けるようにする

**Files:**
- Modify: `src/data/rateCacheRepo.ts`
- Modify: `src/rates/resolveRate.ts:defaultDeps`(引数の橋渡しのみ)
- Test: `src/data/repos.test.ts`

**Interfaces:**
- Produces:
  - `rateKey(base: string, quote: string, date: string): string` → `` `${base}:${quote}:${date}` ``
  - `getCachedRate(base: string, quote: string, date: string): Promise<RateCache | undefined>`
  - `latestCachedRate(base: string, quote: string): Promise<RateCache | undefined>`
  - `putCachedRate(entry: RateCache): Promise<void>` / `countCachedRates(): Promise<number>` は変更なし

- [ ] **Step 1: 失敗するテストを書く**

`src/data/repos.test.ts` に追記する。

```ts
import { rateKey, getCachedRate, putCachedRate, latestCachedRate } from './rateCacheRepo';
import type { RateCache } from '../domain/types';

function rate(over: Partial<RateCache> = {}): RateCache {
  return {
    key: rateKey('CNY', 'JPY', '2026-09-12'),
    base: 'CNY',
    quote: 'JPY',
    date: '2026-09-12',
    rate: 23.4,
    effectiveDate: '2026-09-12',
    fetchedAt: 0,
    source: 'frankfurter',
    ...over,
  };
}

describe('レートキャッシュ', () => {
  it('キーに換算先通貨を含める', () => {
    expect(rateKey('CNY', 'USD', '2026-09-12')).toBe('CNY:USD:2026-09-12');
  });

  it('同じ base でも quote が違えば別のレートとして扱う', async () => {
    await putCachedRate(rate());
    await putCachedRate(
      rate({ key: rateKey('CNY', 'USD', '2026-09-12'), quote: 'USD', rate: 0.14 }),
    );

    expect((await getCachedRate('CNY', 'JPY', '2026-09-12'))?.rate).toBe(23.4);
    expect((await getCachedRate('CNY', 'USD', '2026-09-12'))?.rate).toBe(0.14);
  });

  it('直近のキャッシュは同じ通貨ペアの中から選ぶ', async () => {
    await putCachedRate(rate({ key: rateKey('CNY', 'JPY', '2026-09-10'), date: '2026-09-10', rate: 23.0 }));
    await putCachedRate(rate({ key: rateKey('CNY', 'JPY', '2026-09-12'), date: '2026-09-12', rate: 23.4 }));
    await putCachedRate(
      rate({ key: rateKey('CNY', 'USD', '2026-09-20'), quote: 'USD', date: '2026-09-20', rate: 0.14 }),
    );

    const latest = await latestCachedRate('CNY', 'JPY');
    expect(latest?.date).toBe('2026-09-12');
  });

  it('キャッシュが無ければ undefined', async () => {
    expect(await latestCachedRate('THB', 'JPY')).toBeUndefined();
  });
});
```

> 既存テストと同じ `beforeEach` の DB クリア方法に合わせること。

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/data/repos.test.ts`
Expected: FAIL(引数の数が違う)

- [ ] **Step 3: `src/data/rateCacheRepo.ts` を直す**

```ts
import { db } from './db';
import type { RateCache } from '../domain/types';

export function rateKey(base: string, quote: string, date: string): string {
  return `${base}:${quote}:${date}`;
}

export async function getCachedRate(
  base: string,
  quote: string,
  date: string,
): Promise<RateCache | undefined> {
  return db.rates.get(rateKey(base, quote, date));
}

export async function putCachedRate(entry: RateCache): Promise<void> {
  await db.rates.put(entry);
}

/** 通信できないときの最後の手段。通貨ペアをまたいで拾わないよう [base+quote] で絞る。 */
export async function latestCachedRate(base: string, quote: string): Promise<RateCache | undefined> {
  const rows = await db.rates.where('[base+quote]').equals([base, quote]).toArray();
  if (rows.length === 0) return undefined;
  return rows.sort((a, b) => b.date.localeCompare(a.date))[0];
}

export async function countCachedRates(): Promise<number> {
  return db.rates.count();
}
```

- [ ] **Step 4: `defaultDeps` の型を保つ**

`src/rates/resolveRate.ts`。Task 9 でこの橋渡しを外す。

```ts
    getCachedRate: (base, date) => getCachedRate(base, 'JPY', date),
    latestCachedRate: (base) => latestCachedRate(base, 'JPY'),
```

- [ ] **Step 5: 検証してコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/data/rateCacheRepo.ts src/data/repos.test.ts src/rates/resolveRate.ts
git commit -m "feat: レートキャッシュを通貨ペア単位で引けるようにする"
```

---

### Task 9: resolveRate を通貨ペアに対応させる

**Files:**
- Modify: `src/rates/resolveRate.ts`
- Modify: `src/main.tsx`(プリフェッチ)
- Modify: `src/ui/ExpenseSheet.tsx`(`resolveRate` の呼び出し)
- Test: `src/rates/resolveRate.test.ts`

**Interfaces:**
- Consumes: Task 7 / Task 8 の関数
- Produces:
  - `resolveRate(base: string, quote: string, date: string, overrides?: Partial<ResolveRateDeps>): Promise<ResolvedRate | null>`
  - `ResolveRateDeps` の全メンバーが `quote` を受け取る
  - `prefetchTodayRate(base: string, quote: string): Promise<void>`
  - `base === quote` のとき `{ rate: 1, effectiveDate: date, source: 'same', stale: false }`

- [ ] **Step 1: 失敗するテストを書く**

`src/rates/resolveRate.test.ts` のヘルパーと呼び出しを直し、同一通貨のテストを足す。

```ts
function cacheEntry(over: Partial<RateCache> = {}): RateCache {
  return {
    key: 'CNY:JPY:2026-09-12',
    base: 'CNY',
    quote: 'JPY',
    date: '2026-09-12',
    rate: 23.4,
    effectiveDate: '2026-09-12',
    fetchedAt: 0,
    source: 'frankfurter',
    ...over,
  };
}
```

既存 7 件の `resolveRate('CNY', date, d)` をすべて `resolveRate('CNY', 'JPY', date, d)` に直し、`getCachedRate` / `latestCachedRate` / `fetchFrankfurter` / `fetchErApi` のモックが第 2 引数に `quote` を受ける形にする。加えて次を追加する。

```ts
it('現地通貨と換算先が同じならレート 1 を返し、通信もキャッシュもしない', async () => {
  const d = deps();
  const r = await resolveRate('JPY', 'JPY', '2026-09-12', d);

  expect(r).toEqual({ rate: 1, effectiveDate: '2026-09-12', source: 'same', stale: false });
  expect(d.getCachedRate).not.toHaveBeenCalled();
  expect(d.putCachedRate).not.toHaveBeenCalled();
  expect(d.fetchFrankfurter).not.toHaveBeenCalled();
});

it('保存するキャッシュに換算先通貨を持たせる', async () => {
  const put = vi.fn(async () => undefined);
  const d = deps({
    putCachedRate: put,
    fetchFrankfurter: vi.fn(async () => ({ rate: 0.1405, effectiveDate: '2026-09-12' })),
  });
  await resolveRate('CNY', 'USD', '2026-09-12', d);

  expect(put).toHaveBeenCalledWith(
    expect.objectContaining({ key: 'CNY:USD:2026-09-12', base: 'CNY', quote: 'USD' }),
  );
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/rates/resolveRate.test.ts`
Expected: FAIL

- [ ] **Step 3: `src/rates/resolveRate.ts` を直す**

型と `defaultDeps` を差し替える。

```ts
export type ResolveRateDeps = {
  getCachedRate: (base: string, quote: string, date: string) => Promise<RateCache | undefined>;
  putCachedRate: (entry: RateCache) => Promise<void>;
  latestCachedRate: (base: string, quote: string) => Promise<RateCache | undefined>;
  fetchFrankfurter: (base: string, quote: string, date: string) => Promise<FetchedRate>;
  fetchErApi: (base: string, quote: string, fallbackDate: string) => Promise<FetchedRate>;
  today: string;
};

function defaultDeps(): ResolveRateDeps {
  return {
    getCachedRate,
    putCachedRate,
    latestCachedRate,
    fetchFrankfurter: (base, quote, date) => fetchFrankfurterRate(base, quote, date),
    fetchErApi: (base, quote, fallbackDate) => fetchErApiRate(base, quote, fallbackDate),
    today: todayLocal(),
  };
}
```

`resolveRate` の本体。既存の流れ(キャッシュ → Frankfurter → er-api → 直近キャッシュ)は変えず、先頭に短絡を足して `quote` を配る。

```ts
export async function resolveRate(
  base: string,
  quote: string,
  date: string,
  overrides: Partial<ResolveRateDeps> = {},
): Promise<ResolvedRate | null> {
  // 同じ通貨なら換算しない。通信もキャッシュもせず 1 を返す。
  if (base === quote) {
    return { rate: 1, effectiveDate: date, source: 'same', stale: false };
  }

  const d = { ...defaultDeps(), ...overrides };

  const cached = await d.getCachedRate(base, quote, date);
  if (cached) {
    return {
      rate: cached.rate,
      effectiveDate: cached.effectiveDate,
      source: 'cache',
      stale: cached.effectiveDate !== date,
    };
  }

  const save = async (
    fetched: FetchedRate,
    source: RateCache['source'],
  ): Promise<ResolvedRate> => {
    await d.putCachedRate({
      key: rateKey(base, quote, date),
      base,
      quote,
      date,
      rate: fetched.rate,
      effectiveDate: fetched.effectiveDate,
      fetchedAt: Date.now(),
      source,
    });
    return {
      rate: fetched.rate,
      effectiveDate: fetched.effectiveDate,
      source: 'api',
      stale: fetched.effectiveDate !== date,
    };
  };

  try {
    return await save(await d.fetchFrankfurter(base, quote, date), 'frankfurter');
  } catch {
    // 続けて er-api を試す
  }

  // er-api は当日レートしか返さない。当日以外(過去日・未来日とも)に使うと嘘の値になる。
  if (date === d.today) {
    try {
      return await save(await d.fetchErApi(base, quote, date), 'er-api');
    } catch {
      // 続けて直近キャッシュを探す
    }
  }

  const latest = await d.latestCachedRate(base, quote);
  if (latest) {
    return { rate: latest.rate, effectiveDate: latest.effectiveDate, source: 'cache', stale: true };
  }

  return null;
}

/** 起動直後に当日レートを温めておく。失敗しても何もしない。 */
export async function prefetchTodayRate(base: string, quote: string): Promise<void> {
  await resolveRate(base, quote, todayLocal());
}
```

- [ ] **Step 4: 呼び出し側を直す**

`src/main.tsx` のプリフェッチ。旅行ごとにペアが違うので、ペア単位で重複を除く。

```tsx
void listTrips()
  .then((trips) => {
    // 旅行ごとに換算先が違うので、通貨ペア単位で重複を除く
    for (const pair of new Set(trips.map((t) => `${t.currency}:${t.homeCurrency}`))) {
      const [base, quote] = pair.split(':');
      void prefetchTodayRate(base, quote).catch(() => {});
    }
  })
  .catch(() => {});
```

`src/ui/ExpenseSheet.tsx` の取得。

```tsx
const r = await resolveRate(trip.currency, trip.homeCurrency, date);
```

副作用フックの依存配列に `trip.homeCurrency` を足す。

- [ ] **Step 5: 検証してコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/rates src/main.tsx src/ui/ExpenseSheet.tsx
git commit -m "feat: resolveRate を通貨ペア対応にし、同一通貨は換算せず 1 を返す"
```

---

### Task 10: 集計と画面の Jpy を Home に改名する

**Files:**
- Modify: `src/domain/summary.ts`
- Modify: `src/ui/HomeScreen.tsx`, `src/ui/SummaryScreen.tsx`, `src/ui/CategoryChart.tsx`, `src/ui/DailyChart.tsx`, `src/ui/ExpenseSheet.tsx`
- Test: `src/domain/summary.test.ts`, `src/ui/HomeScreen.test.tsx`, `src/ui/SummaryScreen.test.tsx`, `src/ui/CategoryChart.test.tsx`, `src/ui/DailyChart.test.tsx`

**Interfaces:**
- Consumes: `toHomeMinor` from `src/domain/money.ts`
- Produces:
  - `TripSummary = { count; totalMinor; totalHome; personalHome; sharedHome; sharedPerPersonHome; myTotalHome; personalBudgetHome; personalRemainingHome; sharedBudgetHome; sharedRemainingHome }`
  - `CategoryBreakdown = { category; home; ratio }`
  - `DailyPoint = { date; home }`
  - `DailySeries = { points; totalHome; maxHome; peakDate; averageHome }`
  - `DateGroup = { date; expenses; home }`
  - `expenseHome(e: Expense, trip: Trip): number` / `myShareHome(e: Expense, trip: Trip): number`
  - `toJpy` は削除

- [ ] **Step 1: 集計テストの名前を直す**

`src/domain/summary.test.ts` の `*Jpy` をすべて `*Home` に、`expenseJpy` → `expenseHome`、`myShareJpy` → `myShareHome`、`CategoryBreakdown.jpy` / `DailyPoint.jpy` / `DateGroup.jpy` → `.home` に置き換える。Trip のフィクスチャに `homeCurrency: 'JPY'` / `homeCurrencyDecimals: 0` を足す(Task 4 で型エラーとして出ているはず)。

換算先が円以外の場合のテストを 1 件足す。

```ts
it('換算先が小数 2 桁ならセント単位で集計する', () => {
  const trip: Trip = {
    ...baseTrip,
    currency: 'CNY',
    currencyDecimals: 2,
    homeCurrency: 'USD',
    homeCurrencyDecimals: 2,
  };
  // 120.00 元 × 0.1405 = 16.86 ドル
  const s = summarize([expense({ amountMinor: 12000, rate: 0.1405, scope: 'personal' })], trip);
  expect(s.totalHome).toBe(1686);
  expect(s.personalHome).toBe(1686);
});
```

> `baseTrip` / `expense` は既存テストのヘルパー名に合わせる。無ければ既存のフィクスチャの書き方をそのまま真似る。

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/domain/summary.test.ts`
Expected: FAIL

- [ ] **Step 3: `src/domain/summary.ts` を改名する**

import と 2 つの公開関数。

```ts
import { toHomeMinor } from './money';
```

```ts
/** 1 件の支出を換算先通貨の最小単位にする。 */
export function expenseHome(e: Expense, trip: Trip): number {
  return toHomeMinor(e.amountMinor, trip.currencyDecimals, e.rate, trip.homeCurrencyDecimals);
}

/** 共有支出は人数で割る。個別支出はそのまま自分の負担。 */
export function myShareHome(e: Expense, trip: Trip): number {
  const home = expenseHome(e, trip);
  return e.scope === 'shared' ? Math.round(home / Math.max(1, trip.memberCount)) : home;
}
```

型と内部の `viewJpy` → `viewHome`、`TripSummary` / `CategoryBreakdown` / `DailyPoint` / `DailySeries` / `DateGroup` のフィールドを上の Interfaces のとおりに改名する。計算式とコメント(`// 行ごとに丸めてから足す。画面に出る各行の合計と一致させるため。` など)はそのまま残す。

- [ ] **Step 4: 参照側 4 画面を改名に追随させる**

`src/ui/SummaryScreen.tsx`。

```tsx
  const viewTotalHome = categories.reduce((sum, c) => sum + c.home, 0);
```

`summary.totalJpy` → `summary.totalHome`、`summary.personalJpy` → `summary.personalHome`、`summary.sharedJpy` → `summary.sharedHome`、`summary.sharedPerPersonJpy` → `summary.sharedPerPersonHome`、`summary.myTotalJpy` → `summary.myTotalHome` に置き換える(`data-testid` は `summary-*` のままでよい)。

`src/ui/CategoryChart.tsx`。

```tsx
            <span className="legend-jpy">{formatJpy(r.home)}</span>
```

`src/ui/DailyChart.tsx`。`maxJpy` / `averageJpy` / `p.jpy` を `maxHome` / `averageHome` / `p.home` に置き換え、`barClass(jpy: number, …)` の引数名も `home` にする。

`src/ui/HomeScreen.tsx`。`BudgetBar` の props を `{ label, budgetHome, usedHome, remainingHome, testId }` に改名し、`summary.*Jpy` の参照と `data-testid` を次のとおり直す。

| 旧 testid | 新 testid |
| --- | --- |
| `total-jpy` | `total-home` |
| `personal-jpy` | `personal-home` |
| `shared-jpy` | `shared-home` |
| `personal-remaining-jpy` | `personal-remaining-home` |
| `shared-remaining-jpy` | `shared-remaining-home` |

`shared-per-person` は変えない。

`src/ui/ExpenseSheet.tsx` は `toJpy` の最後の呼び出し元なので、ここで `toHomeMinor` に移す。

```tsx
  const home = rate === null ? null : toHomeMinor(amountMinor, decimals, rate, trip.homeCurrencyDecimals);
```

以降の `jpy` の参照を `home` に直す。`data-testid="jpy-preview"` と表示文言は Task 15 で扱うので、このタスクでは変えない。

- [ ] **Step 5: 画面テストの testid を直す**

`src/ui/HomeScreen.test.tsx` の `getByTestId('total-jpy')` などを上の表に従って直す。`src/ui/CategoryChart.test.tsx` / `src/ui/DailyChart.test.tsx` / `src/ui/SummaryScreen.test.tsx` のフィクスチャは `jpy` → `home`、`maxJpy` → `maxHome` などに直す。

- [ ] **Step 6: `toJpy` を消す**

`src/domain/money.ts` から `toJpy` を削除し、`src/domain/money.test.ts` の `toJpy` の describe も消す(挙動は `toHomeMinor` のテストが引き継いでいる)。

- [ ] **Step 7: 検証してコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/domain src/ui
git commit -m "refactor: 集計と画面の Jpy を換算先通貨を表す Home に改名する"
```

---

### Task 11: i18n の骨格と共通辞書を入れる

**Files:**
- Create: `src/i18n/ja.ts`, `src/i18n/en.ts`, `src/i18n/ko.ts`, `src/i18n/zh.ts`, `src/i18n/dictionaries.ts`, `src/i18n/LangContext.tsx`, `src/test/renderWithLang.tsx`
- Modify: `index.html`, `src/main.tsx`, `src/domain/categories.ts`, `src/domain/date.ts`, `src/ui/HomeScreen.tsx`, `src/ui/CategoryChart.tsx`, `src/ui/DailyChart.tsx`, `src/ui/ExpenseSheet.tsx`, `src/ui/SettingsScreen.tsx`
- Test: `src/i18n/dictionaries.test.ts`, `src/i18n/LangContext.test.tsx`, `src/domain/date.test.ts`, 画面テスト 11 ファイル(下記)

**Interfaces:**
- Consumes: `Lang` / `LANGS` / `detectLang`(Task 1)、`loadLang` / `saveLang`(Task 1)
- Produces:
  - `type Dictionary = typeof ja`、`DICTIONARIES: Record<Lang, Dictionary>`(`src/i18n/dictionaries.ts`)
  - `LangProvider({ children, initial? })`、`useI18n(): { lang: Lang; t: Dictionary; setLang: (lang: Lang) => void }`(`src/i18n/LangContext.tsx`)
  - `renderWithLang(ui: ReactElement, lang?: Lang)`(`src/test/renderWithLang.tsx`)
  - `formatDateLabel(isoDate: string, weekdays: readonly string[]): string`(第 2 引数は必須)
  - `CATEGORIES: { value: Category; icon: string; color: string }[]`、`PAYMENTS: { value: Payment; icon: string }[]`、`SCOPES: Scope[]`

> このタスクは辞書の**共通部分だけ**を作る。画面ごとの文言は Task 12〜17 でそれぞれの辞書サブオブジェクトを足しながら移す。
> `src/i18n/index.ts`(Task 1)には辞書を置かない。`currency.ts` が `Lang` だけを import できるようにするため、辞書は `dictionaries.ts` に分ける。

- [ ] **Step 1: 失敗するテストを書く**

`src/i18n/dictionaries.test.ts` を新規作成する。

```ts
import { describe, expect, it } from 'vitest';
import { DICTIONARIES } from './dictionaries';
import { LANGS } from './index';

describe('DICTIONARIES', () => {
  it('4 言語すべてに辞書がある', () => {
    for (const lang of LANGS) {
      expect(DICTIONARIES[lang].appName).toBe('Trip Wallet');
    }
  });

  it('曜日は 7 個ある', () => {
    for (const lang of LANGS) {
      expect(DICTIONARIES[lang].weekdays).toHaveLength(7);
    }
  });

  it('言語ごとに文言が違う', () => {
    expect(DICTIONARIES.ja.category.food).toBe('食事');
    expect(DICTIONARIES.en.category.food).toBe('Food');
    expect(DICTIONARIES.ko.category.food).toBe('식사');
    expect(DICTIONARIES.zh.category.food).toBe('餐饮');
  });

  it('人数は言語ごとの数え方になる', () => {
    expect(DICTIONARIES.ja.common.people(3)).toBe('3人');
    expect(DICTIONARIES.en.common.people(1)).toBe('1 person');
    expect(DICTIONARIES.en.common.people(3)).toBe('3 people');
  });
});
```

`src/i18n/LangContext.test.tsx` を新規作成する。

```tsx
import { describe, expect, it, beforeEach, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { LangProvider, useI18n } from './LangContext';

/** 辞書を引くだけの確認用コンポーネント。切り替えボタンも自前で持つ。 */
function Probe() {
  const { t, setLang } = useI18n();
  return (
    <div>
      <span data-testid="label">{t.category.food}</span>
      <button type="button" onClick={() => setLang('en')}>
        en
      </button>
    </div>
  );
}

beforeEach(() => {
  localStorage.clear();
  document.documentElement.lang = '';
});

describe('LangProvider', () => {
  it('言語を切り替えると文言が変わり localStorage に残る', async () => {
    const user = userEvent.setup();
    render(
      <LangProvider initial="ja">
        <Probe />
      </LangProvider>,
    );

    expect(screen.getByTestId('label')).toHaveTextContent('食事');

    await user.click(screen.getByRole('button', { name: 'en' }));

    expect(screen.getByTestId('label')).toHaveTextContent('Food');
    expect(localStorage.getItem('trip-wallet:lang')).toBe('en');
  });

  it('document.documentElement.lang を追随させる', async () => {
    const user = userEvent.setup();
    render(
      <LangProvider initial="ja">
        <Probe />
      </LangProvider>,
    );

    expect(document.documentElement.lang).toBe('ja');

    await user.click(screen.getByRole('button', { name: 'en' }));

    expect(document.documentElement.lang).toBe('en');
  });

  it('保存値があればそれを使う', () => {
    localStorage.setItem('trip-wallet:lang', 'ko');
    render(
      <LangProvider>
        <Probe />
      </LangProvider>,
    );

    expect(screen.getByTestId('label')).toHaveTextContent('식사');
  });

  it('LangProvider の外で useI18n を呼ぶと落ちる', () => {
    // render が投げたエラーを React がコンソールに出すので黙らせる
    const spy = vi.spyOn(console, 'error').mockImplementation(() => {});
    expect(() => render(<Probe />)).toThrow('useI18n は LangProvider の中で使ってください');
    spy.mockRestore();
  });
});
```

`src/domain/date.test.ts` に追記する。

```ts
const JA_WEEKDAYS = ['日', '月', '火', '水', '木', '金', '土'];
const EN_WEEKDAYS = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'];

describe('formatDateLabel の曜日名', () => {
  it('渡した配列から曜日名を取る', () => {
    expect(formatDateLabel('2026-09-12', JA_WEEKDAYS)).toBe('9/12(土)');
    expect(formatDateLabel('2026-09-12', EN_WEEKDAYS)).toBe('9/12(Sat)');
  });
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/i18n/dictionaries.test.ts src/i18n/LangContext.test.tsx src/domain/date.test.ts`
Expected: FAIL(`./dictionaries` と `./LangContext` が無い / `formatDateLabel` の引数が 1 つ)

- [ ] **Step 3: 4 言語の辞書を作る**

`src/i18n/ja.ts` を新規作成する。**`as const` は付けない。**付けるとリテラル型になり、他の 3 言語が `Dictionary` を満たせなくなる。

```ts
import type { Category, Payment, Scope } from '../domain/types';

// 辞書の型はこのファイルから作る(Dictionary = typeof ja)。as const は付けないこと。
export const ja = {
  appName: 'Trip Wallet',
  weekdays: ['日', '月', '火', '水', '木', '金', '土'],
  common: {
    loading: '読み込み中…',
    cancel: 'キャンセル',
    save: '保存',
    add: '追加',
    edit: '編集',
    delete: '削除',
    close: '閉じる',
    confirm: '決定',
    unset: '未設定',
    deleteDigit: '1 文字削除',
    people: (n: number) => `${n}人`,
    items: (n: number) => `${n}件`,
  },
  category: {
    food: '食事',
    transport: '交通',
    sightseeing: '観光',
    shopping: '買物',
    lodging: '宿泊',
    other: 'その他',
  } satisfies Record<Category, string>,
  payment: {
    cash: '現金',
    mobile: 'QR決済',
    card: 'カード',
  } satisfies Record<Payment, string>,
  scope: {
    personal: '個別',
    shared: '共有',
  } satisfies Record<Scope, string>,
  error: {
    title: 'エラーが発生しました',
    reload: '再読み込み',
  },
};
```

`src/i18n/dictionaries.ts` を新規作成する。

```ts
import { en } from './en';
import { ja } from './ja';
import { ko } from './ko';
import { zh } from './zh';
import type { Lang } from './index';

/** 辞書の型は日本語版から作る。他の 3 言語は形が違うと tsc が落ちる。 */
export type Dictionary = typeof ja;

export const DICTIONARIES: Record<Lang, Dictionary> = { ja, en, ko, zh };
```

`src/i18n/en.ts` を新規作成する。

```ts
import type { Dictionary } from './dictionaries';

export const en: Dictionary = {
  appName: 'Trip Wallet',
  weekdays: ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'],
  common: {
    loading: 'Loading…',
    cancel: 'Cancel',
    save: 'Save',
    add: 'Add',
    edit: 'Edit',
    delete: 'Delete',
    close: 'Close',
    confirm: 'OK',
    unset: 'Not set',
    deleteDigit: 'Delete one character',
    people: (n) => `${n} ${n === 1 ? 'person' : 'people'}`,
    items: (n) => `${n} ${n === 1 ? 'item' : 'items'}`,
  },
  category: {
    food: 'Food',
    transport: 'Transit',
    sightseeing: 'Sights',
    shopping: 'Shopping',
    lodging: 'Lodging',
    other: 'Other',
  },
  payment: { cash: 'Cash', mobile: 'Mobile pay', card: 'Card' },
  scope: { personal: 'Personal', shared: 'Shared' },
  error: { title: 'Something went wrong', reload: 'Reload' },
};
```

`src/i18n/ko.ts` を新規作成する。

```ts
import type { Dictionary } from './dictionaries';

export const ko: Dictionary = {
  appName: 'Trip Wallet',
  weekdays: ['일', '월', '화', '수', '목', '금', '토'],
  common: {
    loading: '불러오는 중…',
    cancel: '취소',
    save: '저장',
    add: '추가',
    edit: '편집',
    delete: '삭제',
    close: '닫기',
    confirm: '확인',
    unset: '미설정',
    deleteDigit: '한 글자 삭제',
    people: (n) => `${n}명`,
    items: (n) => `${n}건`,
  },
  category: {
    food: '식사',
    transport: '교통',
    sightseeing: '관광',
    shopping: '쇼핑',
    lodging: '숙박',
    other: '기타',
  },
  payment: { cash: '현금', mobile: 'QR결제', card: '카드' },
  scope: { personal: '개별', shared: '공유' },
  error: { title: '오류가 발생했습니다', reload: '새로 고침' },
};
```

`src/i18n/zh.ts` を新規作成する(簡体字)。

```ts
import type { Dictionary } from './dictionaries';

export const zh: Dictionary = {
  appName: 'Trip Wallet',
  weekdays: ['日', '一', '二', '三', '四', '五', '六'],
  common: {
    loading: '加载中…',
    cancel: '取消',
    save: '保存',
    add: '添加',
    edit: '编辑',
    delete: '删除',
    close: '关闭',
    confirm: '确定',
    unset: '未设置',
    deleteDigit: '删除一个字符',
    people: (n) => `${n}人`,
    items: (n) => `${n}笔`,
  },
  category: {
    food: '餐饮',
    transport: '交通',
    sightseeing: '观光',
    shopping: '购物',
    lodging: '住宿',
    other: '其他',
  },
  payment: { cash: '现金', mobile: '扫码支付', card: '刷卡' },
  scope: { personal: '个人', shared: '共同' },
  error: { title: '发生错误', reload: '重新加载' },
};
```

- [ ] **Step 4: LangProvider と useI18n を作ってアプリの入口に配線する**

`src/i18n/LangContext.tsx` を新規作成する。

```tsx
import { createContext, useContext, useEffect, useState } from 'react';
import type { ReactNode } from 'react';
import { loadLang, saveLang } from '../app/settings';
import { DICTIONARIES } from './dictionaries';
import type { Dictionary } from './dictionaries';
import { detectLang } from './index';
import type { Lang } from './index';

type LangValue = { lang: Lang; t: Dictionary; setLang: (lang: Lang) => void };

const LangCtx = createContext<LangValue | null>(null);

/** initial はテスト用。実行時は 保存値 → 端末の言語 の順で決める。 */
export function LangProvider({ children, initial }: { children: ReactNode; initial?: Lang }) {
  const [lang, setLangState] = useState<Lang>(() => initial ?? loadLang() ?? detectLang());

  // <html lang> はスクリーンリーダーの読み上げ言語に効くので追随させる
  useEffect(() => {
    document.documentElement.lang = lang;
  }, [lang]);

  const setLang = (next: Lang) => {
    saveLang(next);
    setLangState(next);
  };

  return <LangCtx.Provider value={{ lang, t: DICTIONARIES[lang], setLang }}>{children}</LangCtx.Provider>;
}

export function useI18n(): LangValue {
  const value = useContext(LangCtx);
  if (value === null) throw new Error('useI18n は LangProvider の中で使ってください');
  return value;
}
```

`src/main.tsx` の `render` を `LangProvider` で包む。`ErrorBoundary` は Provider の外に置く(Provider 自体が落ちても最後の砦が残るようにするため)。

```tsx
createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <ErrorBoundary>
      <LangProvider>
        <App />
      </LangProvider>
    </ErrorBoundary>
  </StrictMode>,
);
```

`index.html` の `<html lang="ja">` を `<html lang="en">` にする。ここは JS が動く前の初期値でしかなく、
`LangProvider` が起動直後に `document.documentElement.lang` を上書きする。日本語決め打ちをやめて、
`detectLang` のフォールバックと同じ `en` に合わせる。

```html
<html lang="en">
```

- [ ] **Step 5: テスト用の renderWithLang を作って画面テストを切り替える**

`src/test/renderWithLang.tsx` を新規作成する。

```tsx
import { render } from '@testing-library/react';
import type { ReactElement } from 'react';
import { LangProvider } from '../i18n/LangContext';
import type { Lang } from '../i18n';

/** 画面テストは LangProvider の中で描画する。既定は日本語(既存テストの期待文字列に合わせる)。 */
export function renderWithLang(ui: ReactElement, lang: Lang = 'ja') {
  return render(ui, {
    wrapper: ({ children }) => <LangProvider initial={lang}>{children}</LangProvider>,
  });
}
```

次の 11 ファイルで、`render` の import を `renderWithLang` に差し替え、`render(` の呼び出しを `renderWithLang(` に置換する(`screen` / `fireEvent` / `waitFor` は `@testing-library/react` から import したままにする)。

```
src/app/App.test.tsx
src/ui/HomeScreen.test.tsx
src/ui/SummaryScreen.test.tsx
src/ui/CategoryChart.test.tsx
src/ui/DailyChart.test.tsx
src/ui/ExpenseSheet.test.tsx
src/ui/SettingsScreen.test.tsx
src/ui/TripForm.test.tsx
src/ui/TripPickerSheet.test.tsx
src/ui/Numpad.test.tsx
src/ui/WheelPicker.test.tsx
```

例(`src/app/App.test.tsx`):

```tsx
import { screen } from '@testing-library/react';
import { renderWithLang } from '../test/renderWithLang';
```

`src/app/ErrorBoundary.test.tsx` は**そのまま `render` を使う**。`ErrorBoundary` は Provider の外で動く必要があり、Provider 無しでも描画できることをこのテストで担保する。

- [ ] **Step 6: `categories.ts` からラベルを外す**

`src/domain/categories.ts` を次の内容に置き換える。ラベルは辞書が持つので、`categoryLabel` / `scopeLabel` / `paymentLabel` / `paymentIcon` は消す(`paymentLabel` と `paymentIcon` はもともと呼び出し元が無い)。

```ts
import type { Category, Payment, Scope } from './types';

export const CATEGORIES: { value: Category; icon: string; color: string }[] = [
  { value: 'food', icon: '🍜', color: '#38bdf8' },
  { value: 'transport', icon: '🚇', color: '#a78bfa' },
  { value: 'sightseeing', icon: '🎫', color: '#4ade80' },
  { value: 'shopping', icon: '🛍️', color: '#fbbf24' },
  { value: 'lodging', icon: '🏨', color: '#f472b6' },
  { value: 'other', icon: '📝', color: '#64748b' },
];

export const PAYMENTS: { value: Payment; icon: string }[] = [
  { value: 'cash', icon: '💴' },
  { value: 'mobile', icon: '📱' },
  { value: 'card', icon: '💳' },
];

export const SCOPES: Scope[] = ['personal', 'shared'];

export function categoryIcon(v: Category): string {
  return CATEGORIES.find((c) => c.value === v)?.icon ?? '📝';
}
export function categoryColor(v: Category): string {
  return CATEGORIES.find((c) => c.value === v)?.color ?? '#64748b';
}
```

`src/domain/categories.test.ts` は色の重複を見ているだけなので直さなくてよい。

- [ ] **Step 7: `formatDateLabel` に曜日名を渡す形にする**

`src/domain/date.ts` から `WEEKDAYS` を消し、`formatDateLabel` を 2 引数にする。既定値は付けない(付けると差し替え忘れが日本語のまま出てしまい、tsc も気づけない)。

```ts
/** "2026-09-12" → "9/12(土)"。曜日名は辞書から渡す。 */
export function formatDateLabel(isoDate: string, weekdays: readonly string[]): string {
  const d = parseIsoDate(isoDate);
  return `${d.getMonth() + 1}/${d.getDate()}(${weekdays[d.getDay()]})`;
}
```

呼び出し元は Task 12〜17 で `t.weekdays` に変わるまで、暫定で `ja.weekdays` を渡す。差し替え忘れを防ぐため、暫定の import には必ずコメントを付ける。

```tsx
// Task 13 で useI18n の t に差し替える
import { ja } from '../i18n/ja';
```

| ファイル | 直す箇所 | 暫定 | 最終 |
| --- | --- | --- | --- |
| `src/ui/HomeScreen.tsx` | 日付見出し | `formatDateLabel(group.date, ja.weekdays)` | Task 13 |
| `src/ui/HomeScreen.tsx` | `categoryLabel(e.category)` / `scopeLabel(e.scope)` | `ja.category[e.category]` / `ja.scope[e.scope]` | Task 13 |
| `src/ui/CategoryChart.tsx` | `categoryLabel(r.category)` | `ja.category[r.category]` | Task 14 |
| `src/ui/DailyChart.tsx` | `aria-label` と `最高` の日付 | `formatDateLabel(…, ja.weekdays)` | Task 14 |
| `src/ui/DailyChart.tsx` | `WEEKDAYS[weekdayIndex(p.date)]` | `ja.weekdays[weekdayIndex(p.date)]` | Task 14 |
| `src/ui/ExpenseSheet.tsx` | レート注記の日付 2 か所 | `formatDateLabel(…, ja.weekdays)` | Task 15 |
| `src/ui/ExpenseSheet.tsx` | `SCOPES` / `CATEGORIES` / `PAYMENTS` の map | 下記 | Task 15 |
| `src/ui/SettingsScreen.tsx` | 旅行のメタ行の日付 2 か所 | `formatDateLabel(…, ja.weekdays)` | Task 17 |

`src/ui/ExpenseSheet.tsx` の 3 つの map は要素の形が変わるので次のように直す。

```tsx
        {SCOPES.map((s) => (
          <button
            key={s}
            type="button"
            className={s === scope ? 'seg active' : 'seg'}
            onClick={() => setScope(s)}
          >
            {ja.scope[s]}
          </button>
        ))}
```

```tsx
            {c.icon} {ja.category[c.value]}
```

```tsx
            {p.icon} {ja.payment[p.value]}
```

> `ja.category[e.category]` が型エラーにならないのは、`Category` / `Payment` / `Scope` が閉じたユニオンで、辞書側を `satisfies Record<Category, string>` で網羅させているため。だから `?? 'その他'` のような既定値はもう要らない。

`src/domain/date.test.ts` の既存の `formatDateLabel` の呼び出し 2 か所にも `JA_WEEKDAYS` を渡す。

- [ ] **Step 8: テストが通ることを確認する**

Run: `npx vitest run src/i18n/dictionaries.test.ts src/i18n/LangContext.test.tsx src/domain/date.test.ts`
Expected: PASS

- [ ] **Step 9: 検証してコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功(画面テストの期待文字列は日本語のままなので変化しないはず)

```bash
git add index.html src/i18n src/test src/main.tsx src/domain src/ui src/app
git commit -m "feat: 4 言語の辞書と LangProvider を追加する"
```

---

### Task 12: 土台の画面(App / ErrorBoundary / Sheet / Numpad / 旅行ピッカー)を多言語にする

**Files:**
- Modify: `src/i18n/ja.ts`, `src/i18n/en.ts`, `src/i18n/ko.ts`, `src/i18n/zh.ts`
- Modify: `src/app/App.tsx`, `src/app/ErrorBoundary.tsx`, `src/ui/Sheet.tsx`, `src/ui/Numpad.tsx`, `src/ui/TripPickerSheet.tsx`
- Test: `src/app/App.test.tsx`

**Interfaces:**
- Consumes: `useI18n`(Task 11)、`DICTIONARIES` / `loadLang` / `detectLang`(Task 11 / Task 1)
- Produces: 辞書に `app` と `tripPicker` を追加

- [ ] **Step 1: 失敗するテストを書く**

`src/app/App.test.tsx` に追記する。既存の describe と同じ組み立て(旅行を 1 件入れてから描画する形)を真似ること。

```tsx
it('英語ならタブが英語で出る', async () => {
  renderWithLang(<App />, 'en');
  expect(await screen.findByRole('button', { name: /Summary/ })).toBeInTheDocument();
  expect(screen.getByRole('button', { name: /Settings/ })).toBeInTheDocument();
});
```

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/app/App.test.tsx`
Expected: FAIL(タブが「📊 集計」のまま)

- [ ] **Step 3: 辞書に `app` と `tripPicker` を足す**

`src/i18n/ja.ts`。

```ts
  app: {
    noTrip: 'まず「設定」タブで旅行を作成してください。',
    exportReminder: '旅行が終わりました。設定からデータを書き出しておきましょう。',
    toSettings: '設定へ',
    update: '新しいバージョンがあります。タップで更新',
    tabHome: 'ホーム',
    tabSummary: '集計',
    tabSettings: '設定',
  },
  tripPicker: {
    title: '旅行を選ぶ',
    label: '旅行',
  },
```

`src/i18n/en.ts`。

```ts
  app: {
    noTrip: 'Create a trip from the Settings tab first.',
    exportReminder: 'Your trip has ended. Export your data from Settings.',
    toSettings: 'Settings',
    update: 'A new version is available. Tap to update',
    tabHome: 'Home',
    tabSummary: 'Summary',
    tabSettings: 'Settings',
  },
  tripPicker: {
    title: 'Choose a trip',
    label: 'Trip',
  },
```

`src/i18n/ko.ts`。

```ts
  app: {
    noTrip: '먼저 「설정」 탭에서 여행을 만들어 주세요.',
    exportReminder: '여행이 끝났습니다. 설정에서 데이터를 내보내 두세요.',
    toSettings: '설정으로',
    update: '새 버전이 있습니다. 탭하여 업데이트',
    tabHome: '홈',
    tabSummary: '집계',
    tabSettings: '설정',
  },
  tripPicker: {
    title: '여행 선택',
    label: '여행',
  },
```

`src/i18n/zh.ts`。

```ts
  app: {
    noTrip: '请先在「设置」标签中创建行程。',
    exportReminder: '行程已结束。请在设置中导出数据。',
    toSettings: '前往设置',
    update: '有新版本，点击更新',
    tabHome: '首页',
    tabSummary: '统计',
    tabSettings: '设置',
  },
  tripPicker: {
    title: '选择行程',
    label: '行程',
  },
```

- [ ] **Step 4: `App.tsx` を辞書から引くようにする**

先頭で `const { t } = useI18n();` を呼ぶ(早期 return より前)。差し替えは次のとおり。

| 旧 | 新 |
| --- | --- |
| `読み込み中…` | `{t.common.loading}` |
| ヘッダの `'Trip Wallet'` フォールバック | `t.appName` |
| `旅行が終わりました。…` | `{t.app.exportReminder}` |
| `設定へ` | `{t.app.toSettings}` |
| `閉じる` | `{t.common.close}` |
| `まず「設定」タブで旅行を作成してください。`(2 か所) | `{t.app.noTrip}` |
| `新しいバージョンがあります。タップで更新` | `{t.app.update}` |
| `🏠 ホーム` | `{`🏠 ${t.app.tabHome}`}` |
| `📊 集計` | `{`📊 ${t.app.tabSummary}`}` |
| `⚙️ 設定` | `{`⚙️ ${t.app.tabSettings}`}` |

- [ ] **Step 5: `ErrorBoundary.tsx` を辞書から引くようにする**

`ErrorBoundary` はクラスコンポーネントで、しかも `LangProvider` の外にいるのでフックが使えない。`render()` の中で保存値(無ければ端末の言語)から辞書を直に引く。

```tsx
import { DICTIONARIES } from '../i18n/dictionaries';
import { detectLang } from '../i18n';
import { loadLang } from './settings';
```

```tsx
  render() {
    if (this.state.error === null) return this.props.children;
    // Provider の外なので自前で言語を決める。ここが動くのは既に何かが壊れている場面なので、
    // 保存値が読めなければ端末の言語にフォールバックする。
    const t = DICTIONARIES[loadLang() ?? detectLang()];
    return (
      <div className="screen">
        <p className="empty">{t.error.title}</p>
        <p className="error">{this.state.error.message}</p>
        <button type="button" className="btn-primary" onClick={() => location.reload()}>
          {t.error.reload}
        </button>
      </div>
    );
  }
```

> 既存の JSX の構造(クラス名・要素)は変えない。文字列だけ差し替える。

- [ ] **Step 6: `Sheet` / `Numpad` / `TripPickerSheet` を直す**

`src/ui/Sheet.tsx`: `const { t } = useI18n();` を足し、閉じるボタンを `aria-label={t.common.close}` にする(表示している `✕` はそのまま)。

`src/ui/Numpad.tsx`: `const { t } = useI18n();` を足し、`aria-label={key === 'del' ? t.common.deleteDigit : key}` にする。`pressKey` は純粋関数のままにする(テストが直接呼んでいるため)。

`src/ui/TripPickerSheet.tsx`:

```tsx
  const { t } = useI18n();
```

```tsx
    <Sheet title={t.tripPicker.title} onClose={onClose}>
```

`WheelPicker` の `label` を `t.tripPicker.label` に、決定ボタンを `{t.common.confirm}` にする。

> `src/ui/Numpad.test.tsx` は `aria-label="1 文字削除"` で引いている箇所があれば、既定が日本語のままなので変更不要。

- [ ] **Step 7: テストが通ることを確認する**

Run: `npx vitest run src/app src/ui`
Expected: PASS

- [ ] **Step 8: 検証してコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/i18n src/app src/ui
git commit -m "feat: アプリ枠とシートの文言を辞書から引くようにする"
```

---

### Task 13: ホーム画面を多言語と換算先通貨に対応させる

**Files:**
- Modify: `src/i18n/ja.ts`, `src/i18n/en.ts`, `src/i18n/ko.ts`, `src/i18n/zh.ts`
- Modify: `src/ui/HomeScreen.tsx`
- Test: `src/ui/HomeScreen.test.tsx`

**Interfaces:**
- Consumes: `useI18n`(Task 11)、`formatWithCurrency`(Task 3)、`expenseHome` / `summarize`(Task 10)、`Trip.homeCurrency`(Task 4)
- Produces: 辞書に `home` を追加

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/HomeScreen.test.tsx` に追記する。旅行のフィクスチャは既存のものに `homeCurrency` / `homeCurrencyDecimals` が入っている前提(Task 4 で追加済み)。

```tsx
it('換算先通貨で合計を表示する', async () => {
  // homeCurrency: 'EUR'、homeCurrencyDecimals: 2 の旅行を作る(既存フィクスチャを上書き)
  renderWithLang(<HomeScreen trip={{ ...trip, homeCurrency: 'EUR', homeCurrencyDecimals: 2 }} onEdit={() => {}} />);
  expect(await screen.findByTestId('total-home')).toHaveTextContent('€');
});

it('英語ならカードの見出しが英語になる', async () => {
  renderWithLang(<HomeScreen trip={trip} onEdit={() => {}} />, 'en');
  expect(await screen.findByText('Total')).toBeInTheDocument();
});
```

> `HomeScreen` の props は既存のテストの呼び出しをそのまま真似ること(この計画では `trip` 以外の props 名を決め打ちしない)。

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/ui/HomeScreen.test.tsx`
Expected: FAIL(`¥` のまま / `合計` のまま)

- [ ] **Step 3: 辞書に `home` を足す**

`src/i18n/ja.ts`。

```ts
  home: {
    total: '合計',
    receipt: 'レシート',
    personalBudget: '個別予算',
    sharedBudget: '共有予算',
    remaining: (v: string) => `残り ${v}`,
    myShare: (v: string, n: number) => `自分の負担 ${v}(${n}人)`,
    empty: 'まだ支出がありません。右下の + から追加してください。',
    addExpense: '支出を追加',
    confirmDelete: 'この支出を削除しますか?',
    deleted: '削除しました',
    deleteFailed: '削除できませんでした',
  },
```

`src/i18n/en.ts`。

```ts
  home: {
    total: 'Total',
    receipt: 'Receipt',
    personalBudget: 'Personal budget',
    sharedBudget: 'Shared budget',
    remaining: (v) => `${v} left`,
    myShare: (v, n) => `Your share ${v} (${n} people)`,
    empty: 'No expenses yet. Tap + at the bottom right to add one.',
    addExpense: 'Add expense',
    confirmDelete: 'Delete this expense?',
    deleted: 'Deleted',
    deleteFailed: 'Could not delete',
  },
```

`src/i18n/ko.ts`。

```ts
  home: {
    total: '합계',
    receipt: '영수증',
    personalBudget: '개별 예산',
    sharedBudget: '공유 예산',
    remaining: (v) => `남은 금액 ${v}`,
    myShare: (v, n) => `내 부담 ${v}(${n}명)`,
    empty: '아직 지출이 없습니다. 오른쪽 아래 + 로 추가하세요.',
    addExpense: '지출 추가',
    confirmDelete: '이 지출을 삭제할까요?',
    deleted: '삭제했습니다',
    deleteFailed: '삭제하지 못했습니다',
  },
```

`src/i18n/zh.ts`。

```ts
  home: {
    total: '合计',
    receipt: '收据',
    personalBudget: '个人预算',
    sharedBudget: '共同预算',
    remaining: (v) => `剩余 ${v}`,
    myShare: (v, n) => `我的分摊 ${v}(${n}人)`,
    empty: '还没有支出。点击右下角的 + 添加。',
    addExpense: '添加支出',
    confirmDelete: '要删除这笔支出吗?',
    deleted: '已删除',
    deleteFailed: '删除失败',
  },
```

- [ ] **Step 4: `HomeScreen.tsx` を直す**

Task 11 で入れた `import { ja } from '../i18n/ja';` を消し、`const { t } = useI18n();` にする。`formatJpy(x)` は**すべて** `formatWithCurrency(x, trip.homeCurrency)` に置き換える。

`BudgetBar` は通貨を知らないので props を 1 つ増やす。

```tsx
function BudgetBar({
  label,
  budgetHome,
  usedHome,
  remainingHome,
  currency,
  testId,
}: {
  label: string;
  budgetHome: number;
  usedHome: number;
  remainingHome: number;
  currency: string;
  testId: string;
}) {
  const { t } = useI18n();
  const usedRatio = budgetHome === 0 ? 0 : Math.min(1, usedHome / budgetHome);
  // …バーの JSX は既存のまま…
  return (
    // …
    <span className="budget-label">
      {label} {formatWithCurrency(budgetHome, currency)} /{' '}
      {t.home.remaining(formatWithCurrency(remainingHome, currency))}
    </span>
  );
}
```

呼び出し側は `currency={trip.homeCurrency}`、`label={t.home.personalBudget}` / `label={t.home.sharedBudget}` を渡す。

その他の差し替え。

| 旧 | 新 |
| --- | --- |
| `alt="レシート"` | `alt={t.home.receipt}` |
| `読み込み中…` | `{t.common.loading}` |
| `confirm('この支出を削除しますか?')` | `confirm(t.home.confirmDelete)` |
| `削除しました` / `削除できませんでした` | `t.home.deleted` / `t.home.deleteFailed` |
| カードの `合計` | `{t.home.total}` |
| カードの `個別` / `共有` | `{t.scope.personal}` / `{t.scope.shared}` |
| `自分の負担 …(…人)` | `{t.home.myShare(formatWithCurrency(summary.sharedPerPersonHome, trip.homeCurrency), trip.memberCount)}` |
| `まだ支出がありません。…` | `{t.home.empty}` |
| `formatDateLabel(group.date, ja.weekdays)` | `formatDateLabel(group.date, t.weekdays)` |
| `ja.category[e.category]` / `ja.scope[e.scope]` | `t.category[e.category]` / `t.scope[e.scope]` |
| `aria-label="削除"` | `aria-label={t.common.delete}` |
| `aria-label="支出を追加"` | `aria-label={t.home.addExpense}` |

明細の行は、現地通貨と換算先が同じときに「$12.00 → $12.00」と二度出るのを避ける。

```tsx
              <span className="row-sub">
                {formatWithCurrency(e.amountMinor, trip.currency)}
                {trip.currency !== trip.homeCurrency &&
                  ` → ${formatWithCurrency(expenseHome(e, trip), trip.homeCurrency)}`}
              </span>
```

日付見出しの金額も `formatWithCurrency(group.home, trip.homeCurrency)` にする。

- [ ] **Step 5: テストが通ることを確認する**

Run: `npx vitest run src/ui/HomeScreen.test.tsx`
Expected: PASS

- [ ] **Step 6: 検証してコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/i18n src/ui/HomeScreen.tsx src/ui/HomeScreen.test.tsx
git commit -m "feat: ホーム画面を多言語と換算先通貨の表示に対応させる"
```

---

### Task 14: 集計画面とグラフを多言語と換算先通貨に対応させる

**Files:**
- Modify: `src/i18n/ja.ts`, `src/i18n/en.ts`, `src/i18n/ko.ts`, `src/i18n/zh.ts`
- Modify: `src/ui/SummaryScreen.tsx`, `src/ui/CategoryChart.tsx`, `src/ui/DailyChart.tsx`
- Test: `src/ui/SummaryScreen.test.tsx`, `src/ui/CategoryChart.test.tsx`, `src/ui/DailyChart.test.tsx`

**Interfaces:**
- Consumes: `useI18n`(Task 11)、`formatWithCurrency`(Task 3)、`CategoryBreakdown.home` / `DailySeries`(Task 10)
- Produces:
  - 辞書に `summary` と `view` を追加
  - `CategoryChart({ rows, homeCurrency }: { rows: CategoryBreakdown[]; homeCurrency: string })`
  - `DailyChart({ series, homeCurrency }: { series: DailySeries; homeCurrency: string })`

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/SummaryScreen.test.tsx` に追記する。

```tsx
it('英語なら切り替えボタンが英語になる', async () => {
  renderWithLang(<SummaryScreen trip={trip} />, 'en');
  expect(await screen.findByRole('button', { name: 'My share' })).toBeInTheDocument();
});
```

`src/ui/CategoryChart.test.tsx` に追記する。

```tsx
it('換算先通貨の記号で金額を出す', () => {
  renderWithLang(<CategoryChart rows={rows} homeCurrency="EUR" />);
  expect(screen.getAllByTestId('cat-row')[0]).toHaveTextContent('€');
});
```

`src/ui/DailyChart.test.tsx` に追記する。

```tsx
it('英語なら曜日と平均が英語になる', () => {
  renderWithLang(<DailyChart series={series} homeCurrency="JPY" />, 'en');
  expect(screen.getByTestId('day-label')).toHaveTextContent('Sat');
});
```

> `rows` / `series` / `trip` は各テストの既存フィクスチャを使う。フィクスチャ名は既存ファイルに合わせること。

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/ui/SummaryScreen.test.tsx src/ui/CategoryChart.test.tsx src/ui/DailyChart.test.tsx`
Expected: FAIL(`homeCurrency` が props に無い / 日本語のまま)

- [ ] **Step 3: 辞書に `summary` と `view` を足す**

`src/i18n/ja.ts`。

```ts
  view: {
    personal: '個別',
    shared: '共有',
    mine: '自己負担',
  },
  summary: {
    empty: '集計する支出がまだありません。',
    viewEmpty: (view: string) => `${view}の支出はまだありません。`,
    myTotal: '自分の負担合計:',
    myTotalNote: '(個別 + 共有の人数割り)',
    byCategory: 'カテゴリ別',
    daily: '日別推移',
    average: (v: string) => `平均 ${v}`,
    dailyAverage: '1日あたり平均',
    peak: '最高',
  },
```

`src/i18n/en.ts`。

```ts
  view: {
    personal: 'Personal',
    shared: 'Shared',
    mine: 'My share',
  },
  summary: {
    empty: 'No expenses to summarize yet.',
    viewEmpty: (view) => `No ${view.toLowerCase()} expenses yet.`,
    myTotal: 'Your total:',
    myTotalNote: ' (personal + your split of shared)',
    byCategory: 'By category',
    daily: 'Daily trend',
    average: (v) => `Avg ${v}`,
    dailyAverage: 'Daily average',
    peak: 'Peak',
  },
```

`src/i18n/ko.ts`。

```ts
  view: {
    personal: '개별',
    shared: '공유',
    mine: '내 부담',
  },
  summary: {
    empty: '집계할 지출이 아직 없습니다.',
    viewEmpty: (view) => `${view} 지출이 아직 없습니다.`,
    myTotal: '내 부담 합계:',
    myTotalNote: '(개별 + 공유 인원 분할)',
    byCategory: '카테고리별',
    daily: '일별 추이',
    average: (v) => `평균 ${v}`,
    dailyAverage: '하루 평균',
    peak: '최고',
  },
```

`src/i18n/zh.ts`。

```ts
  view: {
    personal: '个人',
    shared: '共同',
    mine: '我的分摊',
  },
  summary: {
    empty: '还没有可统计的支出。',
    viewEmpty: (view) => `还没有${view}支出。`,
    myTotal: '我的分摊合计:',
    myTotalNote: '(个人 + 共同按人数分摊)',
    byCategory: '分类占比',
    daily: '每日趋势',
    average: (v) => `平均 ${v}`,
    dailyAverage: '日均',
    peak: '最高',
  },
```

- [ ] **Step 4: `SummaryScreen.tsx` を直す**

`VIEWS` からラベルを外し、`viewLabel` を消す。

```tsx
const VIEWS: ViewScope[] = ['personal', 'shared', 'mine'];
```

```tsx
export function SummaryScreen({ trip }: { trip: Trip }) {
  const { t } = useI18n();
```

金額は `const fmt = (v: number) => formatWithCurrency(v, trip.homeCurrency);` を先に置いて、`formatJpy(...)` を `fmt(...)` に置き換えると差分が小さい。

| 旧 | 新 |
| --- | --- |
| `読み込み中…` | `{t.common.loading}` |
| `集計する支出がまだありません。` | `{t.summary.empty}` |
| `合計` | `{t.home.total}` |
| `{summary.count}件` | `{t.common.items(summary.count)}` |
| `個別` / `共有` | `{t.scope.personal}` / `{t.scope.shared}` |
| `自分の負担 …(…人)` | `{t.home.myShare(fmt(summary.sharedPerPersonHome), trip.memberCount)}` |
| `自分の負担合計:` | `{t.summary.myTotal}` |
| `(個別 + 共有の人数割り)` | `{t.summary.myTotalNote}` |
| `{v.label}` | `{t.view[v]}`(`key={v}` / `v === view` に合わせる) |
| `{viewLabel(view)}の支出はまだありません。` | `{t.summary.viewEmpty(t.view[view])}` |
| `カテゴリ別` | `{t.summary.byCategory}` |
| `日別推移` | `{t.summary.daily}` |
| `category-head-note` の中身 | `{t.view[view]} {fmt(viewTotalHome)}` |
| `daily-head-note` の中身 | `{t.view[view]}` |

グラフには通貨を渡す。

```tsx
            <CategoryChart rows={categories} homeCurrency={trip.homeCurrency} />
```

```tsx
            <DailyChart series={days} homeCurrency={trip.homeCurrency} />
```

- [ ] **Step 5: `CategoryChart.tsx` を直す**

```tsx
export function CategoryChart({ rows, homeCurrency }: { rows: CategoryBreakdown[]; homeCurrency: string }) {
  const { t } = useI18n();
  if (rows.length === 0) return null;
```

`ja.category[r.category]` を `t.category[r.category]` に、`formatJpy(r.home)` を `formatWithCurrency(r.home, homeCurrency)` にする。`className="legend-jpy"` などの CSS クラス名は変えない(スタイル定義を触らないため)。

- [ ] **Step 6: `DailyChart.tsx` を直す**

```tsx
export function DailyChart({ series, homeCurrency }: { series: DailySeries; homeCurrency: string }) {
  const { t } = useI18n();
  const { points, maxHome, peakDate, averageHome } = series;
  if (points.length === 0) return null;
```

`const fmt = (v: number) => formatWithCurrency(v, homeCurrency);` を置き、次のように直す。

| 旧 | 新 |
| --- | --- |
| `平均 {formatJpy(averageHome)}` | `{t.summary.average(fmt(averageHome))}` |
| `aria-label={`${formatDateLabel(p.date, ja.weekdays)} ${formatJpy(p.home)}`}` | `aria-label={`${formatDateLabel(p.date, t.weekdays)} ${fmt(p.home)}`}` |
| `{ja.weekdays[weekdayIndex(p.date)]}` | `{t.weekdays[weekdayIndex(p.date)]}` |
| `1日あたり平均 <b>…</b>` | `{t.summary.dailyAverage} <b>{fmt(averageHome)}</b>` |
| `最高 {formatDateLabel(peakDate, ja.weekdays)} <b>…</b>` | `{t.summary.peak} {formatDateLabel(peakDate, t.weekdays)} <b>{fmt(maxHome)}</b>` |

`BAR_HEIGHT` / `labelStep` / `tickClass` / `barClass` / `gap` の計算は変えない。

- [ ] **Step 7: テストが通ることを確認する**

Run: `npx vitest run src/ui/SummaryScreen.test.tsx src/ui/CategoryChart.test.tsx src/ui/DailyChart.test.tsx`
Expected: PASS(既存テストの `homeCurrency` 未指定の呼び出しは props 追加に合わせて直す)

- [ ] **Step 8: 検証してコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/i18n src/ui
git commit -m "feat: 集計画面とグラフを多言語と換算先通貨の表示に対応させる"
```

---

### Task 15: 支出シートを多言語にし、同じ通貨ならレート行を出さない

**Files:**
- Modify: `src/i18n/ja.ts`, `src/i18n/en.ts`, `src/i18n/ko.ts`, `src/i18n/zh.ts`
- Modify: `src/ui/ExpenseSheet.tsx`
- Test: `src/ui/ExpenseSheet.test.tsx`

**Interfaces:**
- Consumes: `useI18n`(Task 11)、`formatWithCurrency` / `currencySymbol`(Task 3 / Task 2)、`resolveRate`(Task 9)、`toHomeMinor`(Task 3)
- Produces: 辞書に `expense` を追加。`data-testid="jpy-preview"` → `data-testid="home-preview"`

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/ExpenseSheet.test.tsx` に追記する。旅行のフィクスチャは既存のものを使う。

```tsx
it('現地通貨と換算先が同じならレート行を出さない', async () => {
  renderWithLang(
    <ExpenseSheet trip={{ ...trip, currency: 'JPY', currencyDecimals: 0, homeCurrency: 'JPY', homeCurrencyDecimals: 0 }} onClose={() => {}} />,
  );
  await waitFor(() => expect(screen.queryByTestId('home-preview')).not.toBeInTheDocument());
  expect(screen.queryByText(/1¥ =/)).not.toBeInTheDocument();
});

it('換算先が円以外ならレート行にその通貨コードが出る', async () => {
  renderWithLang(
    <ExpenseSheet trip={{ ...trip, currency: 'CNY', currencyDecimals: 2, homeCurrency: 'USD', homeCurrencyDecimals: 2 }} onClose={() => {}} />,
  );
  expect(await screen.findByText(/1元 = .*USD/)).toBeInTheDocument();
});
```

> `ExpenseSheet` の props は既存テストの呼び出しをそのまま真似ること。

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/ui/ExpenseSheet.test.tsx`
Expected: FAIL(`jpy-preview` のまま / `円` 固定のまま)

- [ ] **Step 3: 辞書に `expense` を足す**

`src/i18n/ja.ts`。

```ts
  expense: {
    addTitle: '支出を追加',
    editTitle: '支出を編集',
    memo: 'メモ',
    memoPlaceholder: '店名・内容',
    date: '日付',
    photoTake: 'レシート写真を撮る',
    photoRetake: 'レシート写真: あり(撮り直す)',
    noRate: 'レート未設定',
    rateLoading: 'レートを取得中…',
    rateFailed: 'レートを取得できません。手動で入力してください',
    rateAsk: (from: string, to: string) => `1${from} = ? ${to}`,
    rateLine: (from: string, value: string, to: string, note: string) =>
      `1${from} = ${value} ${to}(${note})`,
    rateConfirm: 'レートを確定',
    rateEdit: 'レートを編集',
    rateAuto: '自動に戻す',
    rateInvalid: 'レートは 0 より大きい数で入力してください',
    noteSaved: '記録時のレート',
    noteManual: '手動',
    noteCarried: '前回入力した手動レート',
    noteOn: (date: string) => `${date}のレート`,
    noteStale: (date: string) => `${date}時点のレートを使用中`,
    needAmount: '金額を入力してください',
    needRate: 'レートを入力してください',
    savedWithoutPhoto: '端末の空き容量が足りず、写真なしで保存しました',
    photoFailed: '写真は保存できませんでした',
    saved: '保存しました',
    saveFailed: '保存できませんでした',
  },
```

`src/i18n/en.ts`。

```ts
  expense: {
    addTitle: 'Add expense',
    editTitle: 'Edit expense',
    memo: 'Note',
    memoPlaceholder: 'Store or item',
    date: 'Date',
    photoTake: 'Take a receipt photo',
    photoRetake: 'Receipt photo: attached (retake)',
    noRate: 'No rate set',
    rateLoading: 'Fetching the rate…',
    rateFailed: 'Could not fetch the rate. Enter it manually',
    rateAsk: (from, to) => `1${from} = ? ${to}`,
    rateLine: (from, value, to, note) => `1${from} = ${value} ${to} (${note})`,
    rateConfirm: 'Set rate',
    rateEdit: 'Edit rate',
    rateAuto: 'Back to auto',
    rateInvalid: 'Enter a rate greater than 0',
    noteSaved: 'rate when recorded',
    noteManual: 'manual',
    noteCarried: 'last manual rate',
    noteOn: (date) => `rate on ${date}`,
    noteStale: (date) => `using the rate from ${date}`,
    needAmount: 'Enter an amount',
    needRate: 'Enter a rate',
    savedWithoutPhoto: 'Not enough storage, so it was saved without the photo',
    photoFailed: 'Could not save the photo',
    saved: 'Saved',
    saveFailed: 'Could not save',
  },
```

`src/i18n/ko.ts`。

```ts
  expense: {
    addTitle: '지출 추가',
    editTitle: '지출 편집',
    memo: '메모',
    memoPlaceholder: '가게·내용',
    date: '날짜',
    photoTake: '영수증 사진 찍기',
    photoRetake: '영수증 사진: 있음(다시 찍기)',
    noRate: '환율 미설정',
    rateLoading: '환율을 가져오는 중…',
    rateFailed: '환율을 가져올 수 없습니다. 직접 입력해 주세요',
    rateAsk: (from, to) => `1${from} = ? ${to}`,
    rateLine: (from, value, to, note) => `1${from} = ${value} ${to}(${note})`,
    rateConfirm: '환율 확정',
    rateEdit: '환율 수정',
    rateAuto: '자동으로 되돌리기',
    rateInvalid: '환율은 0보다 큰 수로 입력해 주세요',
    noteSaved: '기록 당시 환율',
    noteManual: '수동',
    noteCarried: '이전에 입력한 수동 환율',
    noteOn: (date) => `${date}의 환율`,
    noteStale: (date) => `${date} 기준 환율 사용 중`,
    needAmount: '금액을 입력해 주세요',
    needRate: '환율을 입력해 주세요',
    savedWithoutPhoto: '저장 공간이 부족해 사진 없이 저장했습니다',
    photoFailed: '사진을 저장하지 못했습니다',
    saved: '저장했습니다',
    saveFailed: '저장하지 못했습니다',
  },
```

`src/i18n/zh.ts`。

```ts
  expense: {
    addTitle: '添加支出',
    editTitle: '编辑支出',
    memo: '备注',
    memoPlaceholder: '店名·内容',
    date: '日期',
    photoTake: '拍摄收据照片',
    photoRetake: '收据照片: 已有(重拍)',
    noRate: '未设置汇率',
    rateLoading: '正在获取汇率…',
    rateFailed: '无法获取汇率，请手动输入',
    rateAsk: (from, to) => `1${from} = ? ${to}`,
    rateLine: (from, value, to, note) => `1${from} = ${value} ${to}(${note})`,
    rateConfirm: '确定汇率',
    rateEdit: '修改汇率',
    rateAuto: '恢复自动',
    rateInvalid: '汇率请输入大于 0 的数',
    noteSaved: '记录时的汇率',
    noteManual: '手动',
    noteCarried: '上次输入的手动汇率',
    noteOn: (date) => `${date}的汇率`,
    noteStale: (date) => `正在使用${date}的汇率`,
    needAmount: '请输入金额',
    needRate: '请输入汇率',
    savedWithoutPhoto: '存储空间不足，已在没有照片的情况下保存',
    photoFailed: '照片保存失败',
    saved: '已保存',
    saveFailed: '保存失败',
  },
```

- [ ] **Step 4: `ExpenseSheet.tsx` の文言を辞書から引く**

Task 11 で入れた `import { ja } from '../i18n/ja';` を消し、`const { t } = useI18n();` にする。

レート注記(`AutoRate.note`)を組み立てている箇所。

| 旧 | 新 |
| --- | --- |
| `'記録時のレート'` | `t.expense.noteSaved` |
| `` `${formatDateLabel(r.effectiveDate, ja.weekdays)}時点のレートを使用中` `` | `t.expense.noteStale(formatDateLabel(r.effectiveDate, t.weekdays))` |
| `` `${formatDateLabel(r.effectiveDate, ja.weekdays)}のレート` `` | `t.expense.noteOn(formatDateLabel(r.effectiveDate, t.weekdays))` |
| `'前回入力した手動レート'` | `t.expense.noteCarried` |
| `'手動'` | `t.expense.noteManual` |

レート行と換算プレビュー。

| 旧 | 新 |
| --- | --- |
| `` `1${symbol} = ? 円` `` | `t.expense.rateAsk(symbol, trip.homeCurrency)` |
| `` `1${symbol} = ${rate}円(${rateNote})` `` | `t.expense.rateLine(symbol, String(rate), trip.homeCurrency, rateNote)` |
| `data-testid="jpy-preview"` | `data-testid="home-preview"` |
| `formatJpy(home)` | `formatWithCurrency(home, trip.homeCurrency)` |
| `レート未設定` | `{t.expense.noRate}` |
| `レートを確定` / `レートを編集` / `自動に戻す` | `t.expense.rateConfirm` / `t.expense.rateEdit` / `t.expense.rateAuto` |
| `レートを取得できません。手動で入力してください` | `{t.expense.rateFailed}` |
| `レートを取得中…` | `{t.expense.rateLoading}` |

その他。

| 旧 | 新 |
| --- | --- |
| `レートは 0 より大きい数で入力してください` | `t.expense.rateInvalid` |
| `金額を入力してください` / `レートを入力してください` | `t.expense.needAmount` / `t.expense.needRate` |
| `端末の空き容量が足りず、写真なしで保存しました` | `t.expense.savedWithoutPhoto` |
| `写真は保存できませんでした` | `t.expense.photoFailed` |
| `保存しました` / `保存できませんでした` | `t.expense.saved` / `t.expense.saveFailed` |
| シート見出し `支出を編集` / `支出を追加` | `t.expense.editTitle` / `t.expense.addTitle` |
| `メモ` / placeholder `店名・内容` / `日付` | `t.expense.memo` / `t.expense.memoPlaceholder` / `t.expense.date` |
| `レシート写真: あり(撮り直す)` / `レシート写真を撮る` | `t.expense.photoRetake` / `t.expense.photoTake` |
| `キャンセル` / `保存` | `t.common.cancel` / `t.common.save` |
| `ja.scope[s]` / `ja.category[c.value]` / `ja.payment[p.value]` | `t.scope[s]` / `t.category[c.value]` / `t.payment[p.value]` |

- [ ] **Step 5: 同じ通貨ならレート UI を隠す**

`resolveRate` は同一通貨なら `{ rate: 1, source: 'same' }` を返す(Task 9)ので、保存の流れは触らなくてよい。画面から「1¥ = 1 JPY」と「換算後 = 入力額」という無意味な 2 行を消すだけにする。

```tsx
  // 現地通貨と換算先が同じ旅行(国内旅行や、円で使う日本人が JPY 建ての旅行を作った場合)では
  // レートは常に 1 なので、レート行と換算プレビューを出さない
  const sameCurrency = trip.currency === trip.homeCurrency;
```

レート行のブロックと `home-preview` のブロックをそれぞれ `{!sameCurrency && ( … )}` で囲む。`resolveRate` を呼ぶ effect と保存処理はそのままにする(`rate` は 1 が入る)。

- [ ] **Step 6: テストが通ることを確認する**

Run: `npx vitest run src/ui/ExpenseSheet.test.tsx`
Expected: PASS(既存テストの `getByTestId('jpy-preview')` は `home-preview` に直す)

- [ ] **Step 7: 検証してコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/i18n src/ui/ExpenseSheet.tsx src/ui/ExpenseSheet.test.tsx
git commit -m "feat: 支出シートを多言語にし、同一通貨ではレート行を省く"
```

---

### Task 16: 旅行フォームに換算先通貨を足す

**Files:**
- Modify: `src/i18n/ja.ts`, `src/i18n/en.ts`, `src/i18n/ko.ts`, `src/i18n/zh.ts`
- Modify: `src/ui/TripForm.tsx`
- Test: `src/ui/TripForm.test.tsx`

**Interfaces:**
- Consumes: `CURRENCIES` / `currencyDecimals` / `currencyName`(Task 2)、`parseMajorToMinor` / `formatMajor`(既存の `src/domain/money.ts`)、`loadHomeCurrency` / `defaultHomeCurrency`(Task 1)、`useI18n`(Task 11)
- Produces: 辞書に `trip` を追加。`TripInput` に `homeCurrency` / `homeCurrencyDecimals` / `personalBudgetHome` / `sharedBudgetHome` を渡す

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/TripForm.test.tsx` に追記する。

```tsx
it('換算先通貨を選んで保存できる', async () => {
  const onSaved = vi.fn();
  renderWithLang(<TripForm onSaved={onSaved} onCancel={() => {}} />);
  fireEvent.change(screen.getByLabelText('旅行名'), { target: { value: 'Seoul' } });
  fireEvent.change(screen.getByLabelText('通貨'), { target: { value: 'KRW' } });
  fireEvent.change(screen.getByLabelText('換算先通貨'), { target: { value: 'USD' } });
  fireEvent.change(screen.getByLabelText(/個別予算/), { target: { value: '300' } });
  fireEvent.click(screen.getByRole('button', { name: '保存' }));
  await waitFor(() => expect(onSaved).toHaveBeenCalled());
  const trips = await listTrips();
  expect(trips[0].homeCurrency).toBe('USD');
  expect(trips[0].homeCurrencyDecimals).toBe(2);
  // 換算先がドルなので予算はセント単位で入る
  expect(trips[0].personalBudgetHome).toBe(30000);
});

it('支出があると通貨も換算先も変えられない', async () => {
  // 支出が 1 件ある旅行を作ってから開く(既存の lockCurrency のテストと同じ組み立て)
  renderWithLang(<TripForm trip={tripWithExpense} onSaved={() => {}} onCancel={() => {}} />);
  expect(await screen.findByLabelText('通貨')).toBeDisabled();
  expect(screen.getByLabelText('換算先通貨')).toBeDisabled();
});
```

> `TripForm` の props 名・`listTrips` の import・支出付き旅行の作り方は、既存テストの書き方をそのまま真似ること。

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/ui/TripForm.test.tsx`
Expected: FAIL(`換算先通貨` のラベルが無い)

- [ ] **Step 3: 辞書に `trip` を足す**

`src/i18n/ja.ts`。

```ts
  trip: {
    name: '旅行名',
    namePlaceholder: 'NY 2026-09',
    currency: '通貨',
    homeCurrency: '換算先通貨',
    lockedByExpenses: '支出があるため通貨は変更できません。',
    start: '開始日',
    end: '終了日',
    personalBudget: (code: string) => `個別予算(${code})`,
    sharedBudget: (code: string) => `共有予算(${code}・自分の負担分)`,
    members: '人数',
    needName: '旅行名を入力してください',
    saveFailed: '保存できませんでした',
  },
```

`src/i18n/en.ts`。

```ts
  trip: {
    name: 'Trip name',
    namePlaceholder: 'NY 2026-09',
    currency: 'Currency',
    homeCurrency: 'Convert to',
    lockedByExpenses: 'Currency is locked because this trip already has expenses.',
    start: 'Start date',
    end: 'End date',
    personalBudget: (code) => `Personal budget (${code})`,
    sharedBudget: (code) => `Shared budget (${code}, your share)`,
    members: 'People',
    needName: 'Enter a trip name',
    saveFailed: 'Could not save',
  },
```

`src/i18n/ko.ts`。

```ts
  trip: {
    name: '여행 이름',
    namePlaceholder: 'NY 2026-09',
    currency: '통화',
    homeCurrency: '환산 통화',
    lockedByExpenses: '지출이 있어 통화를 변경할 수 없습니다.',
    start: '시작일',
    end: '종료일',
    personalBudget: (code) => `개별 예산(${code})`,
    sharedBudget: (code) => `공유 예산(${code}·내 부담분)`,
    members: '인원',
    needName: '여행 이름을 입력해 주세요',
    saveFailed: '저장하지 못했습니다',
  },
```

`src/i18n/zh.ts`。

```ts
  trip: {
    name: '行程名称',
    namePlaceholder: 'NY 2026-09',
    currency: '货币',
    homeCurrency: '换算货币',
    lockedByExpenses: '已有支出，无法更改货币。',
    start: '开始日期',
    end: '结束日期',
    personalBudget: (code) => `个人预算(${code})`,
    sharedBudget: (code) => `共同预算(${code}·我的分摊)`,
    members: '人数',
    needName: '请输入行程名称',
    saveFailed: '保存失败',
  },
```

- [ ] **Step 4: `TripForm.tsx` に換算先通貨を足す**

`const { t, lang } = useI18n();` を足す。状態を 1 つ増やす。

```tsx
  // 換算先の初期値は 既存の旅行 → アプリ設定 → 端末の言語からの推定 の順
  const [homeCurrency, setHomeCurrency] = useState(
    trip?.homeCurrency ?? loadHomeCurrency() ?? defaultHomeCurrency(lang),
  );
```

予算は換算先通貨の最小単位で持つので、桁数を見て解釈する。

```tsx
const homeDecimals = currencyDecimals(homeCurrency);
```

```tsx
/** 空欄は未設定(null)。マイナスは 0 に丸める(`parseMajorToMinor` は壊れた入力を 0 にする)。 */
function parseBudget(text: string, decimals: number): number | null {
  if (text.trim() === '') return null;
  return Math.max(0, parseMajorToMinor(text, decimals));
}
```

既存の旅行を開いたときの初期表示は最小単位から主単位に戻す。`formatMajor` はカンマを入れるので取り除く(`<input type="number">` はカンマを受け付けない)。

```tsx
  const [personalBudget, setPersonalBudget] = useState(
    trip?.personalBudgetHome == null
      ? ''
      : formatMajor(trip.personalBudgetHome, trip.homeCurrencyDecimals).replace(/,/g, ''),
  );
```

`sharedBudget` も同じ形にする。保存時に渡す値。

```tsx
      homeCurrency,
      homeCurrencyDecimals: homeDecimals,
      personalBudgetHome: parseBudget(personalBudget, homeDecimals),
      sharedBudgetHome: parseBudget(sharedBudget, homeDecimals),
```

通貨の `<select>` は 2 つとも同じ選択肢にする。国旗・コード・言語別の通貨名を並べる。

```tsx
        <label>
          {t.trip.currency}
          <select value={currency} onChange={(e) => setCurrency(e.target.value)} disabled={lockCurrency}>
            {CURRENCIES.map((c) => (
              <option key={c.code} value={c.code}>
                {c.flag} {c.code} — {currencyName(c.code, lang)}
              </option>
            ))}
          </select>
        </label>

        <label>
          {t.trip.homeCurrency}
          <select
            value={homeCurrency}
            onChange={(e) => setHomeCurrency(e.target.value)}
            disabled={lockCurrency}
          >
            {CURRENCIES.map((c) => (
              <option key={c.code} value={c.code}>
                {c.flag} {c.code} — {currencyName(c.code, lang)}
              </option>
            ))}
          </select>
        </label>
```

> 換算先も `lockCurrency` で止める。支出には記録時のレート(現地通貨 → 換算先)が焼き付いているので、あとから換算先だけ変えると既存の支出の金額が意味を失う。

残りの文言を差し替える。

| 旧 | 新 |
| --- | --- |
| `旅行名` / placeholder `NY 2026-09` | `{t.trip.name}` / `t.trip.namePlaceholder` |
| `支出があるため通貨は変更できません。` | `{t.trip.lockedByExpenses}` |
| `開始日` / `終了日` | `{t.trip.start}` / `{t.trip.end}` |
| `個別予算(円)` | `{t.trip.personalBudget(homeCurrency)}` |
| `共有予算(円・自分の負担分)` | `{t.trip.sharedBudget(homeCurrency)}` |
| placeholder `未設定` | `t.common.unset` |
| `人数` | `{t.trip.members}` |
| `旅行名を入力してください` / `保存できませんでした` | `t.trip.needName` / `t.trip.saveFailed` |
| `キャンセル` / `保存` | `t.common.cancel` / `t.common.save` |

`memberCount` の丸め(`Math.max(1, Math.round(Number(memberCount) || 1))`)は変えない。

- [ ] **Step 5: テストが通ることを確認する**

Run: `npx vitest run src/ui/TripForm.test.tsx`
Expected: PASS

- [ ] **Step 6: 検証してコミット**

Run: `npm run lint && npm test`
Expected: どちらも成功

```bash
git add src/i18n src/ui/TripForm.tsx src/ui/TripForm.test.tsx
git commit -m "feat: 旅行フォームで換算先通貨を選べるようにする"
```

---

### Task 17: 設定画面に言語と換算先の設定を足す

**Files:**
- Modify: `src/i18n/ja.ts`, `src/i18n/en.ts`, `src/i18n/ko.ts`, `src/i18n/zh.ts`
- Modify: `src/ui/SettingsScreen.tsx`, `src/domain/money.ts`, `src/domain/money.test.ts`
- Test: `src/ui/SettingsScreen.test.tsx`

**Interfaces:**
- Consumes: `useI18n` / `setLang`(Task 11)、`LANGS` / `LANG_LABELS` / `loadHomeCurrency` / `saveHomeCurrency` / `defaultHomeCurrency`(Task 1)、`BackupError`(Task 6)、`CURRENCIES` / `currencyName`(Task 2)
- Produces:
  - 辞書に `settings` と `backup` を追加
  - `formatJpy` は削除

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/SettingsScreen.test.tsx` に追記する。既存の `beforeEach` に `localStorage.clear();` を 1 行足しておく(言語の設定がテスト間で漏れないようにする)。

```tsx
describe('SettingsScreen の表示設定', () => {
  it('言語を切り替えると画面の文言が変わる', async () => {
    const user = userEvent.setup();
    renderWithLang(<SettingsScreen trips={[]} activeTrip={null} onSelectTrip={() => {}} />);

    await user.selectOptions(screen.getByLabelText('言語'), 'en');

    expect(await screen.findByLabelText('Language')).toBeInTheDocument();
    expect(localStorage.getItem('trip-wallet:lang')).toBe('en');
  });

  it('換算先通貨の既定値を保存する', async () => {
    const user = userEvent.setup();
    renderWithLang(<SettingsScreen trips={[]} activeTrip={null} onSelectTrip={() => {}} />);

    await user.selectOptions(screen.getByLabelText('換算先通貨'), 'EUR');

    expect(localStorage.getItem('trip-wallet:home-currency')).toBe('EUR');
  });

  it('取り込みエラーは選んでいる言語で出る', async () => {
    const user = userEvent.setup();
    renderWithLang(<SettingsScreen trips={[]} activeTrip={null} onSelectTrip={() => {}} />, 'en');

    const file = new File(['not json'], 'broken.json', { type: 'application/json' });
    await user.upload(screen.getByLabelText('Import'), file);

    expect(await screen.findByText('Could not read the file as JSON')).toBeInTheDocument();
  });
});
```

> 既存の「壊れた JSON を取り込むとエラー文言が出る」はそのまま残す(日本語のときの経路を守る)。3 つ目は同じ組み立てを英語で通すテスト。

- [ ] **Step 2: テストが落ちることを確認する**

Run: `npx vitest run src/ui/SettingsScreen.test.tsx`
Expected: FAIL(`言語` のラベルが無い)

- [ ] **Step 3: 辞書に `settings` と `backup` を足す**

`src/i18n/ja.ts`。

```ts
  settings: {
    display: '表示',
    language: '言語',
    homeCurrency: '換算先通貨',
    homeCurrencyHint: 'ここで選んだ通貨は新しい旅行の初期値です。旅行ごとの換算先は各旅行の編集から変えられます。',
    trips: '旅行',
    tripsEmpty: '旅行がまだありません。「追加」から作成してください。',
    tripAdd: '旅行を追加',
    tripEdit: '旅行を編集',
    budgetPersonal: (v: string) => ` / 個別 ${v}`,
    budgetShared: (v: string) => ` / 共有 ${v}`,
    confirmDelete: (name: string) => `「${name}」と、その支出をすべて削除します。よろしいですか?`,
    deleted: (name: string) => `「${name}」を削除しました`,
    data: 'データ',
    dataHint: '端末内にだけ保存されます。ブラウザのデータを消すと失われるので、旅行のあとは書き出しておいてください。',
    export: 'バックアップを書き出す',
    exported: 'バックアップを書き出しました',
    exportFailed: '書き出せませんでした',
    import: '取り込む',
    importHint: '取り込みは追加(マージ)です。同じ記録があればファイル側で上書きします。',
    imported: (trips: number, expenses: number) => `旅行 ${trips} 件・支出 ${expenses} 件を取り込みました`,
    rateCache: 'レートキャッシュ',
    rateCacheHint: (n: number) => `保存済み ${n} 件。オフライン時はここから直近のレートを使います。`,
  },
  backup: {
    error: {
      unknown: '取り込みに失敗しました',
      'invalid-json': 'JSON として読み込めませんでした',
      'not-backup': 'Trip Wallet のバックアップファイルではありません',
      'unsupported-version': (detail: string) => `対応していないバージョンです: ${detail}`,
      broken: 'バックアップの中身が壊れています',
    },
  },
```

`src/i18n/en.ts`。

```ts
  settings: {
    display: 'Display',
    language: 'Language',
    homeCurrency: 'Convert to',
    homeCurrencyHint: 'This is the default for new trips. Each trip keeps its own conversion currency, editable from that trip.',
    trips: 'Trips',
    tripsEmpty: 'No trips yet. Create one from “Add”.',
    tripAdd: 'Add trip',
    tripEdit: 'Edit trip',
    budgetPersonal: (v) => ` / personal ${v}`,
    budgetShared: (v) => ` / shared ${v}`,
    confirmDelete: (name) => `Delete “${name}” and all of its expenses?`,
    deleted: (name) => `Deleted “${name}”`,
    data: 'Data',
    dataHint: 'Everything is stored on this device only. Clearing browser data erases it, so export after a trip.',
    export: 'Export a backup',
    exported: 'Backup exported',
    exportFailed: 'Could not export',
    import: 'Import',
    importHint: 'Importing merges. Records with the same id are overwritten by the file.',
    imported: (trips, expenses) => `Imported ${trips} trips and ${expenses} expenses`,
    rateCache: 'Rate cache',
    rateCacheHint: (n) => `${n} saved. Offline, the latest cached rate is used.`,
  },
  backup: {
    error: {
      unknown: 'Import failed',
      'invalid-json': 'Could not read the file as JSON',
      'not-backup': 'This is not a Trip Wallet backup file',
      'unsupported-version': (detail) => `Unsupported version: ${detail}`,
      broken: 'The backup contents are broken',
    },
  },
```

`src/i18n/ko.ts`。

```ts
  settings: {
    display: '표시',
    language: '언어',
    homeCurrency: '환산 통화',
    homeCurrencyHint: '여기서 고른 통화는 새 여행의 기본값입니다. 여행별 환산 통화는 각 여행의 편집에서 바꿀 수 있습니다.',
    trips: '여행',
    tripsEmpty: '여행이 아직 없습니다. 「추가」에서 만들어 주세요.',
    tripAdd: '여행 추가',
    tripEdit: '여행 편집',
    budgetPersonal: (v) => ` / 개별 ${v}`,
    budgetShared: (v) => ` / 공유 ${v}`,
    confirmDelete: (name) => `「${name}」과 그 지출을 모두 삭제합니다. 계속할까요?`,
    deleted: (name) => `「${name}」을(를) 삭제했습니다`,
    data: '데이터',
    dataHint: '기기 안에만 저장됩니다. 브라우저 데이터를 지우면 사라지므로 여행 후에는 내보내 두세요.',
    export: '백업 내보내기',
    exported: '백업을 내보냈습니다',
    exportFailed: '내보내지 못했습니다',
    import: '가져오기',
    importHint: '가져오기는 병합입니다. 같은 기록이 있으면 파일 쪽으로 덮어씁니다.',
    imported: (trips, expenses) => `여행 ${trips}건·지출 ${expenses}건을 가져왔습니다`,
    rateCache: '환율 캐시',
    rateCacheHint: (n) => `저장됨 ${n}건. 오프라인일 때 최근 환율을 여기서 사용합니다.`,
  },
  backup: {
    error: {
      unknown: '가져오기에 실패했습니다',
      'invalid-json': 'JSON으로 읽을 수 없었습니다',
      'not-backup': 'Trip Wallet 백업 파일이 아닙니다',
      'unsupported-version': (detail) => `지원하지 않는 버전입니다: ${detail}`,
      broken: '백업 내용이 손상되었습니다',
    },
  },
```

`src/i18n/zh.ts`。

```ts
  settings: {
    display: '显示',
    language: '语言',
    homeCurrency: '换算货币',
    homeCurrencyHint: '这里选择的货币是新行程的默认值。每个行程的换算货币可在该行程的编辑中修改。',
    trips: '行程',
    tripsEmpty: '还没有行程。请从「添加」创建。',
    tripAdd: '添加行程',
    tripEdit: '编辑行程',
    budgetPersonal: (v) => ` / 个人 ${v}`,
    budgetShared: (v) => ` / 共同 ${v}`,
    confirmDelete: (name) => `将删除「${name}」及其全部支出。确定吗?`,
    deleted: (name) => `已删除「${name}」`,
    data: '数据',
    dataHint: '数据只保存在本机。清除浏览器数据会丢失，行程结束后请导出。',
    export: '导出备份',
    exported: '已导出备份',
    exportFailed: '导出失败',
    import: '导入',
    importHint: '导入为合并。若存在相同记录，以文件为准覆盖。',
    imported: (trips, expenses) => `已导入 ${trips} 个行程、${expenses} 笔支出`,
    rateCache: '汇率缓存',
    rateCacheHint: (n) => `已保存 ${n} 条。离线时会使用其中最近的汇率。`,
  },
  backup: {
    error: {
      unknown: '导入失败',
      'invalid-json': '无法作为 JSON 读取',
      'not-backup': '这不是 Trip Wallet 的备份文件',
      'unsupported-version': (detail) => `不支持的版本: ${detail}`,
      broken: '备份内容已损坏',
    },
  },
```

- [ ] **Step 4: `SettingsScreen.tsx` に「表示」セクションを足す**

`const { t, lang, setLang } = useI18n();` を足し、換算先の既定値を状態に持つ。

```tsx
  const [homeCurrency, setHomeCurrency] = useState(() => loadHomeCurrency() ?? defaultHomeCurrency(lang));
```

「旅行」セクションの前に置く。

```tsx
      <section className="settings-section">
        <h3>{t.settings.display}</h3>
        <label>
          {t.settings.language}
          <select value={lang} onChange={(e) => setLang(e.target.value as Lang)}>
            {LANGS.map((l) => (
              <option key={l} value={l}>
                {LANG_LABELS[l]}
              </option>
            ))}
          </select>
        </label>
        <label>
          {t.settings.homeCurrency}
          <select
            value={homeCurrency}
            onChange={(e) => {
              setHomeCurrency(e.target.value);
              saveHomeCurrency(e.target.value);
            }}
          >
            {CURRENCIES.map((c) => (
              <option key={c.code} value={c.code}>
                {c.flag} {c.code} — {currencyName(c.code, lang)}
              </option>
            ))}
          </select>
        </label>
        <p className="hint">{t.settings.homeCurrencyHint}</p>
      </section>
```

> `<label>` の中に `<select>` を入れる書き方は既存の `TripForm` と同じ。`getByLabelText` が引けるのはこの入れ子のおかげなので崩さない。

- [ ] **Step 5: 残りの文言を辞書から引く**

`budgetLabel` は通貨を知る必要があるのでコンポーネントの中に入れる。

```tsx
  const budgetLabel = (trip: Trip) => {
    const parts: string[] = [];
    if (trip.personalBudgetHome != null) {
      parts.push(t.settings.budgetPersonal(formatWithCurrency(trip.personalBudgetHome, trip.homeCurrency)));
    }
    if (trip.sharedBudgetHome != null) {
      parts.push(t.settings.budgetShared(formatWithCurrency(trip.sharedBudgetHome, trip.homeCurrency)));
    }
    return parts.join('');
  };
```

> 既存の `budgetLabel(trip)` が返していた文字列の形(先頭の ` / ` 込み)をそのまま踏襲する。分岐の条件も既存のものに合わせること。

Task 6 で入れた `importErrorMessage` を辞書版に差し替える。

```tsx
  const importErrorMessage = (err: unknown): string => {
    if (!(err instanceof BackupError)) return t.backup.error.unknown;
    if (err.code === 'unsupported-version') return t.backup.error['unsupported-version'](err.detail ?? '');
    return t.backup.error[err.code];
  };
```

その他の差し替え。

| 旧 | 新 |
| --- | --- |
| `旅行` / `追加` / `編集` / `削除` | `{t.settings.trips}` / `{t.common.add}` / `{t.common.edit}` / `{t.common.delete}` |
| `旅行がまだありません。…` | `{t.settings.tripsEmpty}` |
| メタ行の `{trip.memberCount}人` | `{t.common.people(trip.memberCount)}` |
| メタ行の日付 | `formatDateLabel(trip.startDate, t.weekdays)`(終了日も同じ) |
| `データ` / その説明 | `{t.settings.data}` / `{t.settings.dataHint}` |
| `バックアップを書き出す` | `{t.settings.export}` |
| `バックアップを書き出しました` / `書き出せませんでした` | `t.settings.exported` / `t.settings.exportFailed` |
| `取り込む` / その説明 | `{t.settings.import}` / `{t.settings.importHint}` |
| `旅行 n 件・支出 m 件を取り込みました` | `t.settings.imported(result.trips, result.expenses)` |
| `レートキャッシュ` / その説明 | `{t.settings.rateCache}` / `{t.settings.rateCacheHint(rateCount ?? 0)}` |
| `confirm(\`「…」と、その支出を…\`)` | `confirm(t.settings.confirmDelete(trip.name))` |
| `「…」を削除しました` | `t.settings.deleted(trip.name)` |
| Sheet の `旅行を追加` / `旅行を編集` | `t.settings.tripAdd` / `t.settings.tripEdit` |

`backupFileName()` と `URL.revokeObjectURL` の後始末は変えない。

- [ ] **Step 6: `formatJpy` を消す**

これで `formatJpy` の呼び出し元が無くなる。`src/domain/money.ts` から `formatJpy` を削除し、`src/domain/money.test.ts` の `formatJpy` の describe も消す(`formatWithCurrency` のテストが役割を引き継いでいる)。

Run: `npx tsc --noEmit`
Expected: 成功(まだ `formatJpy` を参照している箇所があればここで落ちる)

- [ ] **Step 7: テストが通ることを確認する**

Run: `npx vitest run src/ui/SettingsScreen.test.tsx src/domain/money.test.ts`
Expected: PASS

- [ ] **Step 8: 検証してコミット**

Run: `npm run lint && npm test && npm run build`
Expected: 3 つとも成功

```bash
git add src/i18n src/ui/SettingsScreen.tsx src/ui/SettingsScreen.test.tsx src/domain/money.ts src/domain/money.test.ts
git commit -m "feat: 設定画面に言語と換算先通貨の設定を追加する"
```

---

### Task 18: ドキュメントを更新してバージョンを上げる

**Files:**
- Modify: `README.md`, `package.json`

**Interfaces:**
- Consumes: なし(コードの変更は無い)
- Produces: `package.json` の `version` が `1.0.5`

- [ ] **Step 1: README の説明を直す**

冒頭の 1 文。

```markdown
海外旅行で使った外貨を、その日の為替レートで自分の通貨に換算して記録する PWA。データは端末内(IndexedDB)にだけ保存する。
```

「できること」に 2 行足す(既存の行はそのまま)。

```markdown
- 換算先の通貨は旅行ごとに選べる(既定値は設定タブ / 初回は端末の言語から推定)
- 表示言語は日本語・English・한국어・中文の 4 つ。初回は端末の言語に合わせ、設定タブで変えられる
```

「対応通貨」の節の直前に「対応言語」の節を足す。翻訳の素性を隠さないために書く。

```markdown
## 対応言語

日本語 / English / 한국어 / 中文 の 4 つ。文言は `src/i18n/<lang>.ts` の 1 ファイルに閉じてあるので後から差し替えやすい。

韓国語(`ko.ts`)と中国語(`zh.ts`)はネイティブのレビューを受けていない。おかしな表現を見つけたら該当ファイルの 1 行を直すだけで済む。
```

「対応通貨」の節を差し替える。

```markdown
## 対応通貨

Frankfurter が使う ECB 参照レートの対象 30 通貨: AUD / BRL / CAD / CHF / CNY / CZK / DKK / EUR / GBP / HKD / HUF / IDR / ILS / INR / ISK / JPY / KRW / MXN / MYR / NOK / NZD / PHP / PLN / RON / SEK / SGD / THB / TRY / USD / ZAR。

台湾ドル(TWD)・ベトナムドン(VND)は ECB が公表しておらず自動取得できないため入れていない。ブルガリアレフ(BGN)はユーロ導入で公表が終わるため入れていない。

現地通貨と換算先が同じ旅行では換算せず、レート行も出さない。
```

- [ ] **Step 2: 手動確認チェックリストに v1.0.5 の項目を足す**

既存の行は触らず、末尾に未チェックで追記する。

```markdown
- [ ] 端末の言語が英語の状態で初回起動すると、画面が英語で出る
- [ ] 設定 → 表示 → 言語で 4 言語を切り替えると、全画面(ホーム・集計・設定・支出シート)の文言が即座に変わる
- [ ] アプリを再起動しても選んだ言語が残っている
- [ ] 曜日名が言語に合わせて変わる(日別推移の軸とホームの日付見出し)
- [ ] カテゴリ・支払い方法・個別/共有の表示が言語に合わせて変わる
- [ ] 設定 → 表示 → 換算先通貨を変えると、新しく作る旅行の初期値がその通貨になる
- [ ] 旅行の編集で換算先通貨を変えられ、支出を 1 件でも入れると通貨・換算先の両方が変更できなくなる
- [ ] 換算先をユーロにした旅行で、金額が「€1,234.56」の形で出る(記号が前、小数 2 桁)
- [ ] 換算先をスウェーデンクローナにすると記号が数字の後ろに出る
- [ ] 通貨の選択肢に国旗と、表示言語に合わせた通貨名が出る
- [ ] 現地通貨と換算先を同じにした旅行では、支出シートにレート行と換算プレビューが出ない
- [ ] 韓国ウォン → 日本円のように小数の無い通貨どうしでも、レートが 0.1 未満にならず桁が落ちない
- [ ] 米ドル → 韓国ウォンのようにレートが大きい組み合わせでも金額が合っている
- [ ] v1.0.4 で書き出したバックアップを取り込むと、換算先が円の旅行として復元される
- [ ] v1.0.4 で入れたアプリを更新すると、既存の旅行が換算先=円のまま開ける
```

- [ ] **Step 3: バージョンを上げる**

`package.json` の `version` を `1.0.5` にする。

- [ ] **Step 4: 検証してコミット**

Run: `npm run lint && npm test && npm run build`
Expected: 3 つとも成功

```bash
git add README.md package.json
git commit -m "chore: bump version to 1.0.5"
```

- [ ] **Step 5: main へのマージを確認する**

バージョンを上げるコミットをしたので、`main` へマージしてよいか AskUserQuestion で確認する。承認されたら `feature/v1.0.5-i18n-home-currency` を `main` にマージする。承認が得られなければブランチのまま止める。
