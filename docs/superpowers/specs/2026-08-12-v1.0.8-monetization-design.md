# v1.0.8 以降 概要設計: 品質対応と収益化

日付: 2026-08-12(2026-08-18 に v1.0.8 の作業範囲を確定して完成。同日、着手時 brainstorming の結果を「収益化 Phase 1 詳細設計」として追記)
対象バージョン: 1.0.8 以降(収益化は複数バージョンにまたがる想定)
位置づけ: 概要スペック。v1.0.8 の作業範囲(品質対応 8 件+収益化 Phase 1)と、収益化の手段比較・推奨シナリオをここで確定する。価格・実装方式などの細部は各フェーズ着手時の brainstorming で決める。数値・規約は 2026-08 時点の調査であり、**着手時に必ず最新を再確認する**(ストア規約と手数料は変動が激しい)。

## 目的

Trip Wallet の収益化手段を検討し、現実的な順序で着手する。候補は iPhone / Android のストアアプリ化、広告、買い切りプラン、寄付。あわせて、v1.0.7 のレビュー・実機確認で先送りにした品質項目 8 件を v1.0.8 で解消し、ストアに出す成果物の品質を先に固める。

## v1.0.8 の作業範囲(2026-08-18 確定)

v1.0.8 は次の 2 本立てで構成し、この順に進める。

1. **品質対応**: v1.0.7 で先送りにした 8 件(下表)
2. **収益化 Phase 1**: TWA で Google Play に無料公開+寄付リンク設置

品質対応を先にするのは、ストア審査・テスターに見せる成果物の品質を固めるためと、実機確認が必要な項目(#7・#8)を Play 公開前の実機確認にまとめて消化するため。

### 品質対応: 先送り 8 件(2026-08-18 に現行コードで全件検証済み)

**不具合修正(v1.0.7 実機確認で発覚。未修正・優先)**

| # | 項目 | 現状と原因 | 対応方針 |
|---|---|---|---|
| 1 | 「旅行を追加」シートの保存ボタンがタブバーのアイコンに重なって見える | シート(`.sheet-backdrop` z-index 20)はタブバー(`.tabbar` z-index 15)より前面にあるが、`.sheet` の背景が半透明ガラス+blur(`--glass` は不透明度 0.6)のため、真下にあるタブバーのアイコンが透けて保存ボタン(`.form-actions`)に重なって見える。シートは fixed で画面下端からタブバーに被さる配置なので、シート内をスクロールしても位置関係は変わらず解消しない。横向き・ワイド表示でも同じ。 | シート表示中はタブバーを非表示にする(視覚的に消す)のを第一候補とする。シート下端だけ不透明にする・タブバー高さ分の下余白を足す、は代替案。実機で見て決める。 |
| 2 | 集計タブの日別グラフで平均線が棒の裏に隠れることがある | `src/ui/DailyChart.tsx` が `.daily-avg` を棒(`.daily-col`)より先に描画しており、z-index 指定も無いため、DOM 順で後の棒が前面になる。 | `.daily-avg` を最前面に出す(z-index 指定または描画順の変更)。 |

**改善(v1.0.7 最終レビューの先送り)**

| # | 項目 | 現状 | 対応方針 |
|---|---|---|---|
| 3 | `AVG_LABEL_HEIGHT` の二重管理 | `src/ui/DailyChart.tsx` の定数 `14` と `styles.css` の `.daily-avg span { top: -14px }` を手で揃えている | CSS 変数化して一元管理する |
| 4 | `scripts/generate-icons.mjs` の `</g>` 貪欲マッチ | `/<g id="motif">[\s\S]*<\/g>/` は最後の `</g>` までマッチするため、将来 `icon.svg` のモチーフより後ろに `<g>` を足すと取り込んで壊れる | モチーフ抽出を構造変更に耐える形にする(単純な非貪欲化は入れ子の `<g>` に負けるので、終端の特定方法は実装時に決める) |
| 5 | 生成 PNG のファイルサイズ | `sharp` の `.png()` に圧縮指定が無い | `compressionLevel: 9` を指定して生成 4 枚を削る |
| 6 | 未使用トークン | `--accent-2` と `--ok` は定義のみで参照ゼロ(グラデーションはリテラル色で記述されている) | `src/styles.css` から削除する |
| 7 | `.daily-avg span` の背景が板に見える | `background: var(--bg)` の単色。ボディはグラデーション背景なので、位置によっては四角い板に見える可能性 | 実機で確認し、気になるなら周囲に馴染む表現へ変える |
| 8 | iOS の `background-attachment: fixed` | `body` に指定。iOS Safari は `fixed` を無視してスクロールで背景が動く既知挙動がある | 実機で確認し、問題があれば iOS でも成立する背景手法へ変える |

- #7・#8 は「実機確認の結果次第」の条件付き項目。確認して問題が無かった場合も、その結果を記録して閉じることを v1.0.8 の完了条件に含める(8 件すべてに決着をつける)。
- #1 の余白仮説(`env(safe-area-inset-bottom)`+タブバー高さ分の不足)は今回の分析で「透け」に更新した。ただし最終判定は実機で行う。

#### 対応結果(2026-08-18)

| # | 項目 | 結果 |
|---|---|---|
| 1 | シートの保存ボタンにタブバーが重なる | 修正。シート表示中は `body.sheet-open` でタブバーと FAB を非表示にした |
| 2 | 平均線が棒の裏に隠れる | 修正。JSX の描画順を棒の後に変え、`.daily-avg` に `z-index: 1` を足した |
| 3 | `AVG_LABEL_HEIGHT` の二重管理 | 解消。JS からインラインの `--avg-label-h` で渡し、CSS は `calc()` で受ける |
| 4 | `</g>` 貪欲マッチ | 解消。`scripts/svg.mjs` の `extractGroup` で深さを数える方式にし、単体テストを付けた |
| 5 | PNG の圧縮 | 対応。sharp に compressionLevel: 9 を指定。4 ファイル計 302,979 → 300,324 バイト(−2,655 / 0.88%) |
| 6 | 未使用トークン | 削除。`--accent-2` / `--ok` を `:root` から外した |
| 7 | 平均ラベルの背景が板に見える | 修正。グラフは .card の外でグラデーションの上に載るため不透明な --bg の背景板は馴染まない。背景塗りをやめ --bg 色の text-shadow 2 重に置き換えた。破線と重なったときの可読性の最終判断は README のチェックリストに送った |
| 8 | iOS の `background-attachment: fixed` | 修正。body::before(position: fixed)の固定レイヤーに分離し、html に --bg を敷いた。background-attachment: fixed の宣言は削除。iPhone 実機でのスクロール追従の最終確認は README のチェックリストに送った |

### 収益化 Phase 1 の作業項目(v1.0.8 の本体)

- Digital Asset Links 用の `assetlinks.json` を GitHub Pages(`/.well-known/`)に配置する
- Bubblewrap か PWABuilder で AAB を生成する(ツール選定は着手時)
- Google Play 開発者登録($25)とクローズドテスト設定(テスター 12 人×14 日)
- 寄付リンクの設置(設定タブ想定。Buy Me a Coffee / Ko-fi 等の外部リンク。文言は 4 言語対応)
- ストア掲載素材の用意(説明文・スクリーンショット。アイコンは v1.0.7 の刷新済みを使用)

詳細は「収益化 Phase 1 詳細設計(2026-08-18 確定)」を参照。

### 時間軸とリリースの切り方

- アプリ側の変更(品質 8 件+寄付リンク+TWA 対応の Web 側作業)を v1.0.8 として main へマージし、GitHub Pages へデプロイする。
- Play 公開プロセス(開発者登録 → クローズドテスト 12 人×14 日 → 本番審査 7 日程度)は Web 側リリース後も続く外部プロセスであり、v1.0.8 のリリースをブロックしない。
- TWA はアプリ本体を GitHub Pages から配信するため、Play 公開後も Web 側の更新がそのまま反映される。

## 収益化 Phase 1 詳細設計(2026-08-18 確定)

着手時の brainstorming で確定した詳細。作業は 3 つに分類する。

- **A. アプリのコード変更**(このリポジトリ): 寄付リンクと TWA 判定。writing-plans → SDD の通常フローで進める。
- **B. リポジトリ・成果物の作業**(Claude が実施、要所でユーザー承認): TWA パッケージング・assetlinks.json・ストア掲載素材。
- **C. アカウント・ストア操作**(ユーザーが手動で実施): アカウント作成・支払い・Play Console 操作は代行できないため、末尾のチェックリストにまとめる。

### 決定事項

| 論点 | 決定 |
|---|---|
| Google Play 開発者アカウント | 未登録。個人アカウントを新規登録する($25・本人確認あり)。新規個人アカウントのため、本番公開前にテスター 12 人×14 日のクローズドテストが必須 |
| テスターの集め方 | テスター交換コミュニティ(相互にテスターになり合う場)で募る。離脱で 12 人を割るとカウンターがリセットされるため多めに確保する |
| 寄付サービス | Ko-fi(寄付への手数料 0%。受け取りは PayPal / Stripe) |
| TWA での寄付リンク | 非表示にする(下記のポリシー再調査を受けた決定)。Web / PWA 版でのみ表示する |
| TWA 生成ツール | Bubblewrap CLI。PWABuilder は Bubblewrap を中身に使う Web UI であり、CLI なら設定(`twa-manifest.json`)をリポジトリにコミットして再生成を再現できる |
| packageId | `io.github.unikpip.tripwallet`(Java パッケージ規約でハイフンが使えないため username のハイフンを除去) |

### 着手時再調査の結果(2026-08-18)

- **assetlinks.json はオリジンのルート必須**: Digital Asset Links は `https://uni-k-pip.github.io/.well-known/assetlinks.json` だけを見る。プロジェクトページ(`/trip-wallet/`)配下には置けない。現在ルートは 404 で、ユーザーサイトリポジトリ `Uni-K-Pip/uni-k-pip.github.io` は未作成であることを確認した。→ 新規作成が必要(B-2)。
- **寄付の外部リンクは Play 決済ポリシー上グレー**: ポリシーは Google Play Billing 以外への誘導を原則禁止し、明文の例外は慈善団体(tax exempt)への寄付など。開発者個人へのチップは例外に明記されていない。よってリスク欄が用意していた「TWA 版ではリンクを出し分けて隠す」を採用する。
- テスター要件(12 人×14 日)・登録費 $25 は不変(同日再確認)。

### A. アプリ側の設計

- **寄付 URL の定数**: `src/app/donation.ts` に `DONATION_URL` を 1 箇所で定義する。**空文字のあいだはサポートセクションを描画しない**。これにより Ko-fi アカウントの作成を待たずにコードをマージでき、URL は後から 1 行のコミットで設定できる。
- **設定タブ「サポート」セクション**: `SettingsScreen` のデータセクションの後に追加する。Ko-fi への外部リンク(`target="_blank"` + `rel="noopener noreferrer"`)。文言は 4 言語(ja / en / ko / zh)。
- **TWA 判定**: TWA の初回ナビゲーションでは `document.referrer` が `android-app://<packageId>` になることを検知し、localStorage に永続化する(SPA 内の遷移や再訪で referrer が失われるため)。`display-mode: standalone` は PWA インストールと区別できないため使わない。TWA と判定された環境ではサポートセクションを描画しない。
- テストは jsdom で referrer / localStorage をモックし、表示条件(URL 空・TWA 判定)をユニットテストで固定する。

### B-1. TWA パッケージング

- `twa/` ディレクトリに Bubblewrap の設定 `twa-manifest.json` をコミットする。生成される Android プロジェクトと keystore(署名鍵)は gitignore する。**keystore とそのパスワードはユーザーが端末外にもバックアップ保管する**(紛失すると同じアプリとして更新できなくなる)。
- `bubblewrap init` は `https://uni-k-pip.github.io/trip-wallet/manifest.webmanifest` を起点にし、`bubblewrap build` で AAB(と検証用 APK)を生成する。初回実行時に JDK / Android SDK の自動ダウンロード提案がある。
- アプリ本体は GitHub Pages 配信のままで、Play 公開後も Web 側の更新がそのまま反映される。

### B-2. assetlinks.json(2 段階)

1. `Uni-K-Pip/uni-k-pip.github.io` を public で新規作成し、`.nojekyll`(GitHub Pages の Jekyll が dot ディレクトリを無視するのを防ぐ)と `.well-known/assetlinks.json` を置く。まず Bubblewrap の**アップロード鍵**の SHA-256 fingerprint を載せ、検証用 APK を実機に入れて URL バーが消えることを確認する。
2. Play へのアップロード後、**Play App Signing の署名鍵**の SHA-256(Play Console「アプリの整合性」画面)を追記し、以後は両方を並記する。

このリポジトリ新規作成はユーザーの GitHub アカウントに公開リポジトリを作る操作のため、実行前に改めて承認を取る。

### B-3. ストア掲載素材

- 説明文(短い説明 80 字以内・詳細 4000 字以内)を 4 言語で起草する
- スマホ用スクリーンショット(最低 2 枚): ブラウザのスマホビューポートで撮影する
- フィーチャーグラフィック 1024×500(必須): 既存の `public/icon.svg` のモチーフと背景グラデーションから sharp で生成する
- ストアアイコン 512×512: 既存の `public/icon-maskable-512.png` を流用する

### C. ユーザー作業チェックリスト(代行できない操作)

- [ ] Google Play 開発者登録($25・本人確認)
- [ ] Ko-fi アカウント作成と URL の共有(共有後に `DONATION_URL` を設定する)
- [ ] Play Console でのアプリ作成・AAB アップロード・クローズドテスト設定
- [ ] テスター交換コミュニティへの投稿と 14 日間の人数維持(12 人を割るとリセット)
- [ ] 14 日完走後、本番審査への提出

### リリースの切り方(確定)

- アプリ側の変更(A)+ `package.json` の version を 1.0.8 へ bump + README の「v1.0.8 だけの確認」セクション削除を main へマージ・デプロイした時点で **v1.0.8 リリース**とする。
- Ko-fi URL の設定・assetlinks への fingerprint 追記・Play 公開プロセスは後続作業とし、v1.0.8 をブロックしない。

### Phase 1 の検証

- コード変更(A): `npm run test` / `npm run build`。表示条件はユニットテストで固定する。
- TWA(B): 検証用 APK を実機にインストールし、起動・URL バー非表示・既存 PWA からの IndexedDB データ引き継ぎ・オフライン動作を確認する。
- 掲載素材(B): Play Console が受理する(形式チェックを通る)ことをもって確認とする。

## 前提(2026-08 時点の調査結果)

### ストア配布の方式

- **Google Play**: PWA を TWA(Trusted Web Activity)で包むのが Google 公式サポートの方法。Bubblewrap CLI か PWABuilder で APK/AAB を生成し、Digital Asset Links でサイト所有権を検証する。Lighthouse の PWA スコア 80 以上が目安。アプリ本体は今の GitHub Pages 配信のまま(ストアの審査を経ずに Web 側の更新がそのまま反映される)。
- **App Store**: TWA 相当の仕組みが無く、Capacitor(または Cordova)でのラップが必須。Web 技術ラップだけのアプリは Guideline 4.2(minimum functionality)でリジェクトされるリスクがあり、ネイティブ寄りの付加価値(ウィジェット・共有機能等)を足して申請するのが定石。
- **開発者登録費用**: Google Play は $25(買い切り)、Apple は $99/年。
- **Google Play の個人アカウント要件**: 2023-11-13 以降に作成した個人アカウントは、本番公開の前に「12 人のテスターが 14 日間連続でオプトイン+インストール済み」のクローズドテストが必須(2024-12-11 に 20 人から 12 人へ緩和)。人数を割るとカウンターがリセットされる。本番申請後の審査は 7 日程度。

### 手数料と日本のスマホ新法

- 日本のスマホソフトウェア競争促進法(MSCA)が 2025-12-18 に施行され、日本では外部課金リンクが解禁、代替ストアも許可された。
- Apple(日本): IAP 手数料は 15〜26%(基本 21%+決済 5%。Small Business Program 対象なら 10%+5%=15%)。外部リンク経由は 15%(SBP なら 10%)。SBP は年商 $1M 未満が対象。
- Google(日本): 手数料を 20% へ引き下げ、外部決済も許可。
- 本アプリの規模なら SBP / 小規模枠に確実に収まる。

### 2026-08-18 の再確認結果

- Google Play のテスター要件(12 人×14 日)は不変であることを確認した。
- 手数料は情報源間で数字が食い違う(Apple 26% 説・Google 据え置き説など)。MSCA 施行後の運用は流動的で、2026-02 には業界団体が外部決済手数料への緊急共同声明を出すなど係争が続いている。上記の数字は目安に留め、**Phase 2(課金)着手時の再調査を必須とする**。Phase 1 は無料公開+寄付リンクのみのため手数料の影響を受けない。

### 広告・寄付の相場

- AdMob バナーの eCPM は Tier1 国で $0.50〜1.50、グローバル平均 $0.20〜0.80。小規模ユーティリティアプリでは**月数百円オーダー**が現実的。UX 悪化と釣り合うかが論点。
- 寄付は Buy Me a Coffee / Ko-fi 等の外部リンクを置くだけで実装コストほぼゼロ。収益は期待しないが、反応の指標になる。

### 参考リンク(着手時の再確認の起点)

- PWA のストア公開手法: https://www.mobiloud.com/blog/publishing-pwa-app-store
- 日本の App Store 変更(MSCA 対応): https://www.macrumors.com/2025/12/17/japan-app-store-feature-updates/
- Google Play クローズドテスト要件: https://support.google.com/googleplay/android-developer/answer/14151465
- Google Play 外部決済リンク: https://developer.android.com/google/play/billing/externalpaymentlinks
- AdMob 収益の目安: https://www.monetizemore.com/blog/admob-monetization/

## 手段の比較

| 手段 | 初期コスト | 継続コスト | 収益見込み | 主なリスク |
|---|---|---|---|---|
| Google Play(TWA) | $25 + 実装小 | ほぼ無し | 直接収益は無し(配布チャネル) | 12 人×14 日のテスター集め |
| App Store(Capacitor) | $99/年 + 実装大 | $99/年 | 同上 | Guideline 4.2 リジェクト、年会費の回収 |
| 広告(AdMob) | 実装中 | UX 悪化 | 月数百円オーダー | 割に合わない可能性が高い |
| 買い切り Pro | 実装中〜大 | 決済管理 | 単価×少数。SBP 15% / Google 20% | 無料版との線引き設計 |
| 寄付リンク | 実装ほぼゼロ | 無し | ほぼゼロ(指標として有用) | 特に無し |

## 推奨シナリオ(段階方式)— この文書で確定

一度に全部やらず、反応を見ながら段階的に進める。

- **Phase 1(= v1.0.8 の本体): Android を TWA で Google Play に無料公開+寄付リンク設置。**
  目的は収益ではなく、配布実績とフィードバックの獲得。費用 $25。クローズドテスト(12 人×14 日)の完走が最大のハードルなので、テスター集めを最初に計画する。作業項目は「v1.0.8 の作業範囲」を参照。
- **Phase 2: 反応を見て、買い切り Pro か広告かを判断。**
  ダウンロード数・寄付の有無が判断材料。価格・無料版との線引き・決済方式(ストア IAP か外部リンクか)はこの時点の brainstorming で決める。
- **Phase 3: iOS は回収見込みが立ってから。**
  $99/年を回収できる見通し(Phase 2 の実績)ができてから Capacitor 化に着手する。Guideline 4.2 対策の付加価値もこの時点で設計する。

## 技術的な考慮点(実装時に効く)

- **TWA は既存 PWA ユーザーのデータを引き継げる**: TWA は端末の Chrome と同一オリジン・同一ストレージで動くため、既に PWA として使っているユーザーが Play 版を入れても IndexedDB のデータはそのまま見える。
- **Capacitor(iOS)は引き継げない**: WebView のオリジンが変わるため IndexedDB は空から始まる。既存のバックアップ(JSON エクスポート/インポート)機能が移行手段になる。Phase 3 の設計時に導線を整える。
- TWA 化には HTTPS・マニフェスト・Service Worker が必要 — すべて既に満たしている(`vite-plugin-pwa` で生成)。Digital Asset Links 用の `assetlinks.json` を GitHub Pages(`/.well-known/`)に置く作業が加わる。
- ストア掲載素材(アイコン・スクリーンショット・説明文)は v1.0.7 の GUI 刷新の成果物を使う。

## 今決めないこと(各フェーズ着手時に決める)

Phase 1 の各論点(TWA 生成ツール・寄付サービス・開発者アカウント名義・テスターの集め方)は 2026-08-18 の着手時 brainstorming で決定した。「収益化 Phase 1 詳細設計」を参照。

- Pro 版の価格と無料版との機能の線引き(Phase 2)
- 決済方式: ストア IAP か、MSCA で解禁された外部課金リンクか(Phase 2)
- 広告を入れる場合の位置・頻度(Phase 2。入れない判断も含めて)
- iOS 版の付加価値(Guideline 4.2 対策)の中身(Phase 3)

## リスク

- **テスター 12 人×14 日**が集められないと Google Play の本番公開自体ができない。Phase 1 最大の外部依存。
- **規約・手数料の変動**: MSCA 施行(2025-12-18)から日が浅く、両ストアの運用が今後も変わりうる。2026-08-18 の再確認でも情報源間で手数料の数字が食い違っており、着手時の再調査を必須とする。
- **寄付リンクと Play ポリシー**: 開発者個人への寄付の外部リンクは Play の決済ポリシー上の扱いが曖昧。実装時に最新ポリシーを確認し、抵触しそうなら「TWA 版ではリンクを出し分けて隠す」等の逃げ道を用意する。→ 2026-08-18 に最新ポリシーを確認し、この逃げ道(TWA では非表示)を採用した。
- **収益の期待値は低い**: 広告は月数百円オーダー、買い切りも単価×少数。「収益化の仕組みを一通り経験する」ことを主目的と割り切る。
- **Apple Guideline 4.2**: iOS 版は付加価値を足してもリジェクトされる可能性が残る。Phase 3 を最後に置くのはこのため。

## 検証

- 品質 8 件: `npm run test` と `npm run build` を通す。グラフ関連(#2・#3)は `DailyChart` のテストを更新して挙動を固定する。#1・#7・#8 は実機(iPhone Safari)で判定する。
- アイコン生成(#4・#5): `npm run icons` を実行し、生成 PNG の見た目が変わらないこととファイルサイズが減ることを確認する。
- Phase 1: TWA を実機にインストールして起動・データ引き継ぎ・オフライン動作を確認する。クローズドテスト参加者のフィードバックが実質の検証となる。

## 次のステップ

1. 品質対応 8 件 — 2026-08-18 完了(「対応結果(2026-08-18)」の表を参照)。
2. Phase 1 — 2026-08-18 の着手時 brainstorming で詳細化済み(「収益化 Phase 1 詳細設計」)。writing-plans で実装プラン化し、実装へ進む。
