# v1.0.7 GUI プレミアム化 実装計画

**Goal:** Trip Wallet の見た目を Indigo × Violet の配色へ全面刷新し、すりガラス・グラデーション・マイクロアニメーションで「プレミアムな仕上がり」にしたうえで、UI の不具合 2 件を直して v1.0.7 として出す。

**Architecture:** 変更のほとんどは `src/styles.css` のトークンとクラス定義に閉じる。TSX を触るのは「CSS だけでは表現できない箇所」に限る — セグメントのスライドインジケーター(要素追加)、タブ切替アニメーション(`key` による再マウント)、金額のカウントアップ(`useCountUp` フック)、シートの閉じアニメーション(180ms 遅延)、平均ラベルの位置判定。「視差効果を減らす」設定への追従は、CSS 側は `@media (prefers-reduced-motion: reduce)` の一括無効化、JS 側は共通ヘルパー `src/ui/motion.ts` の `prefersReducedMotion()` で行う。アイコンは `public/icon.svg` を差し替え、maskable 専用 PNG は `scripts/generate-icons.mjs` がラッパー SVG を組み立てて生成する(SVG ファイルは 1 枚のまま)。

**Tech Stack:** React 19 / TypeScript 7 / Vite 8 / vite-plugin-pwa / Dexie 4 / vitest 4 + jsdom + @testing-library/react / sharp(アイコン生成)。**新規依存ライブラリはゼロ。**

**Spec:** [docs/dev/specs/2026-08-12-v1.0.7-premium-gui-design.md](../specs/2026-08-12-v1.0.7-premium-gui-design.md)

## Global Constraints

- 作業ブランチは `feature/v1.0.7-premium-gui`(作成済み)。`main` へは Task 12 でユーザーの承認を得てからマージする。
- 1 Task = 1 コミット。コミットメッセージは日本語、Conventional Commits の prefix を付ける(`feat:` / `fix:` / `style:` / `docs:` / `chore:`)。
- シェルは PowerShell。リポジトリルート `D:\work_D\0\wk_trip-wallet` で実行する。
- 検証コマンド: `npm run test`(vitest run) / `npm run build`(tsc --noEmit && vite build) / `npm run lint`(tsc --noEmit) / `npm run icons`(PNG 生成)。
- **新規依存を追加しない。** アニメーションライブラリも Web フォントも入れない。
- **ライトテーマは追加しない。** `color-scheme: dark` のまま。
- i18n の新キーは `ja` / `en` / `ko` / `zh` の 4 言語すべてに追加する(`Dictionary = typeof ja` なので 1 つでも欠けると `npm run lint` が落ちる)。
- 本文テキストのコントラストは WCAG AA(4.5:1)を維持する。
- 全アニメーションは `prefers-reduced-motion: reduce` で止まること。CSS・JS の両方。
- 既存機能の回帰・オフライン動作の劣化を起こさない。データモデルは一切変更しない。
- **触ってはいけない箇所(意図的なコード。直さない):**
  - `src/rates/resolveRate.ts` の `stale: true` 固定
  - `src/data/` の `budgetJpy` / `personalBudgetJpy` / `sharedBudgetJpy` というレガシーなフィールド名
  - `src/i18n/ja.ts` に `as const` を付けない
  - `src/i18n/zh.ts` の `rateAsk` の `` `1${from} = ? ${to}` ``
  - `src/ui/DailyChart.tsx` の軸ラベル 2 段表示
  - `src/domain/date.ts` の簡易ロケール対応

---

## 変更するファイルの全体像

| ファイル | 役割 | 触る Task |
|---|---|---|
| `src/styles.css` | デザイントークン・全クラス定義・キーフレーム。今回の変更の主戦場 | 1, 2, 3, 5, 6, 7, 9 |
| `vite.config.ts` | PWA manifest の色と maskable アイコンの指定 | 1, 11 |
| `index.html` | `theme-color` メタタグと favicon | 1, 11 |
| `src/ui/motion.ts` | **新規。** `prefersReducedMotion()` だけを持つ共通ヘルパー | 4 |
| `src/ui/useCountUp.ts` | **新規。** 金額を 500ms でカウントアップするフック | 4 |
| `src/ui/useCountUp.test.ts` | **新規。** `useCountUp` のユニットテスト | 4 |
| `src/test/setup.ts` | `window.matchMedia` のモックを追加(テスト既定は reduced-motion) | 4 |
| `src/ui/HomeScreen.tsx` | 合計カードに `glass` クラス、合計金額にカウントアップ | 2, 4 |
| `src/ui/SummaryScreen.tsx` | 同上 + セグメントのスライドインジケーター + グラフの `key={view}` | 2, 3, 4, 6 |
| `src/app/App.tsx` | タブ内容を `key={tab}` のラッパーで包む + バナーの設定タブ分岐 | 5, 10 |
| `src/ui/Sheet.tsx` | 閉じアニメーション(180ms 遅延)を持たせる | 7 |
| `src/ui/DailyChart.tsx` | 平均ラベルの上下判定 | 9 |
| `src/ui/DailyChart.test.tsx` | 平均ラベル位置のテスト 2 件を追加 | 9 |
| `src/ui/SummaryScreen.test.tsx` | セグメントインジケーターのテスト 1 件を追加 | 3 |
| `src/app/App.test.tsx` | 設定タブでのバナー表記のテスト 2 件を追加 | 10 |
| `src/i18n/ja.ts` `en.ts` `ko.ts` `zh.ts` | `app.exportReminderSettings` を追加 | 10 |
| `public/icon.svg` | アイコンを新デザインへ差し替え(モチーフを `<g id="motif">` で囲う) | 11 |
| `scripts/generate-icons.mjs` | maskable 用ラッパー SVG の組み立てを追加 | 11 |
| `public/icon-192.png` `icon-512.png` `apple-touch-icon.png` `icon-maskable-512.png` | `npm run icons` の生成物。commit する | 11 |
| `README.md` | 手動確認チェックリストに v1.0.7 の一時項目 5 件 | 12 |
| `package.json` | version を 1.0.7 へ | 12 |

**CSS 中心の Task(1, 2, 3 の一部, 5, 6, 7)は自動テストの対象外。** jsdom は CSS を評価しないため、見た目の検証は `npm run build` / `npm run test`(回帰が無いこと)と、Task 12 の手動確認チェックリストで担保する。DOM 構造を変える部分(インジケーター要素・ラッパー要素・クラス付与)だけはテストを書く。

---

## Task 1: デザイントークンの刷新と背景

配色を Indigo × Violet に切り替える。この Task では色とトークンの定義だけを行い、質感やアニメーションは後続の Task で足す。

**Files:**
- Modify: `src/styles.css:1-12`(`:root`)、`src/styles.css:19-27`(`body`)
- Modify: `vite.config.ts:16-32`(manifest の色)
- Modify: `index.html:6`(`theme-color`)

**Interfaces:**
- Consumes: なし(最初の Task)
- Produces: CSS カスタムプロパティ `--bg` `--surface` `--surface-2` `--text` `--text-dim` `--accent` `--accent-2` `--on-accent` `--danger` `--ok` `--radius` `--grad-accent` `--glass` `--glass-border` `--shadow-card` `--dur-fast` `--dur-base` `--dur-slow` `--ease-swift`。以降の全 Task がこれらを参照する。

- [ ] **Step 1: `:root` を新トークンへ置き換える**

`src/styles.css` の 1〜12 行目をまるごと以下に置き換える。

```css
:root {
  --bg: #0c0a1d;
  --surface: #1e1b3a;
  --surface-2: #312e50;
  --text: #ede9fe;
  --text-dim: #8b87ad;
  --accent: #818cf8;
  --accent-2: #a855f7;
  /* アクセント面(FAB / primary ボタン / バッジ)に乗せる文字色。背景色と同じ深い紫。 */
  --on-accent: #0c0a1d;
  --danger: #f87171;
  --ok: #4ade80;
  --radius: 14px;

  --grad-accent: linear-gradient(150deg, #818cf8, #a855f7);
  --glass: rgba(30, 27, 58, 0.6);
  --glass-border: rgba(165, 180, 252, 0.14);
  --shadow-card: 0 1px 2px rgba(0, 0, 0, 0.3), 0 8px 24px rgba(10, 8, 30, 0.5);

  --dur-fast: 180ms;
  --dur-base: 280ms;
  --dur-slow: 420ms;
  --ease-swift: cubic-bezier(0.2, 0.8, 0.2, 1);

  color-scheme: dark;
}
```

- [ ] **Step 2: `body` の背景をグラデーション + 固定の紫グローにする**

`src/styles.css` の `body` ルール(19〜27 行目)を以下に置き換える。`filter: blur()` は使わない — モバイルで常時合成コストがかかるため、`radial-gradient` の固定レイヤーでにじみを表現する。

```css
body {
  margin: 0;
  /* 紫のグローは radial-gradient の固定レイヤーで出す。blur フィルターは
     常時合成が走ってモバイルで重くなるので使わない。 */
  background:
    radial-gradient(60% 40% at 85% 0%, rgba(168, 85, 247, 0.18), transparent 70%),
    radial-gradient(50% 35% at 0% 20%, rgba(129, 140, 248, 0.14), transparent 70%),
    linear-gradient(160deg, #0c0a1d, #12102a 55%, #1a1533);
  background-attachment: fixed;
  color: var(--text);
  font-family: system-ui, -apple-system, 'Hiragino Sans', 'Noto Sans JP', sans-serif;
  /* iOS のホーム画面起動時にセーフエリアへ潜り込まないようにする */
  padding: env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom)
    env(safe-area-inset-left);
}
```

- [ ] **Step 3: PWA manifest とメタタグの色を合わせる**

`vite.config.ts` の manifest 内、`background_color` と `theme_color` をどちらも `'#0c0a1d'` にする。

```ts
        background_color: '#0c0a1d',
        theme_color: '#0c0a1d',
```

`index.html` の 6 行目を以下にする。

```html
    <meta name="theme-color" content="#0c0a1d" />
```

- [ ] **Step 4: 回帰が無いことを確認する**

Run:

```bash
npm run test
```

Expected: 全テスト PASS(CSS 変更のみなので既存テストは全通過するはず)

Run:

```bash
npm run build
```

Expected: エラーなく `dist/` が生成される

- [ ] **Step 5: Commit**

```bash
git add src/styles.css vite.config.ts index.html
git commit -m "style: デザイントークンを Indigo x Violet に刷新する"
```

---

## Task 2: 質感の適用(すりガラス・グラデーション・シャドウ)

面の質感を作る。**すりガラス(`backdrop-filter`)は常時見えている少数の面だけ** — 合計カード・タブバー・シートのパネル。スクロールで量産される行(支出行・カテゴリ行・設定行)には使わず「半透明 surface + 1px 縁 + 軽いシャドウ」で済ませる。

`.app-header` は `position: sticky` ではない(`src/styles.css:43` は `.app-header h1` のフォント指定のみ)ので、スペックの「ヘッダーは sticky である場合のみ」という条件に当てはまらない。**ヘッダーにはグラスを適用しない。**

**Files:**
- Modify: `src/styles.css`(`.card` / `.ex-row` / `.badge` / `.fab` / `.tabbar` / `.tab.active` / `.banner` / `.toast.update` / `.btn-primary` / `.btn-ghost` / `.sheet` / `.sheet-backdrop` / `.budget-fill` / `.daily-bar` / `.daily-tick.sat` / `.seg.active`、末尾に `:focus-visible`)
- Modify: `src/ui/HomeScreen.tsx:102`
- Modify: `src/ui/SummaryScreen.tsx:46`

**Interfaces:**
- Consumes: Task 1 のトークン(`--glass` `--glass-border` `--shadow-card` `--grad-accent` `--on-accent` `--accent` `--accent-2`)
- Produces: CSS クラス `.card.glass`(合計カード用のすりガラス面)。Task 4 以降は追加しない。

- [ ] **Step 1: 合計カードに `glass` クラスを付ける**

`src/ui/HomeScreen.tsx` の 102 行目:

```tsx
      <div className="card glass">
```

`src/ui/SummaryScreen.tsx` の 46 行目:

```tsx
        <div className="card glass">
```

- [ ] **Step 2: カード・行・バッジの質感を書き換える**

`src/styles.css` の `.card` ルールに枠線と影を足す(`background` / `border-radius` / `padding` / `margin-bottom` は現状のまま)。

```css
.card {
  background: var(--surface);
  border: 1px solid rgba(165, 180, 252, 0.08);
  box-shadow: var(--shadow-card);
  border-radius: var(--radius);
  padding: 14px;
  margin-bottom: 16px;
}

/* 常時見えている少数の面だけすりガラスにする。スクロールで量産される行には使わない。 */
.card.glass {
  background:
    linear-gradient(150deg, rgba(129, 140, 248, 0.18), rgba(168, 85, 247, 0.06)),
    var(--glass);
  border: 1px solid var(--glass-border);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
}
```

`.ex-row`(454 行目付近)の `background` 行を置き換え、枠線と影を足す。

```css
.ex-row {
  display: flex;
  align-items: center;
  gap: 6px;
  background: rgba(30, 27, 58, 0.72);
  border: 1px solid rgba(165, 180, 252, 0.08);
  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.25);
  border-radius: var(--radius);
  padding: 8px 10px;
  margin-bottom: 6px;
}
```

`.badge.shared`(501 行目付近)の文字色をトークンに置き換える。

```css
.badge.shared {
  background: var(--grad-accent);
  color: var(--on-accent);
}
```

- [ ] **Step 3: アクセント面にグラデーションを流す**

`src/styles.css` の該当ルールを以下のとおり書き換える。`#062033` というハードコードは全て `var(--on-accent)` に置き換わる。

```css
.btn-primary {
  background: var(--grad-accent);
  color: var(--on-accent);
  font-weight: 600;
}

.btn-ghost {
  background: var(--surface-2);
  border: 1px solid rgba(165, 180, 252, 0.1);
}

.fab {
  position: fixed;
  right: 16px;
  bottom: calc(72px + env(safe-area-inset-bottom));
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: var(--grad-accent);
  color: var(--on-accent);
  font-size: 28px;
  z-index: 10;
}

.toast.update {
  background: var(--grad-accent);
  color: var(--on-accent);
  font-weight: 600;
}

.budget-fill {
  height: 100%;
  background: var(--grad-accent);
}

.budget-fill.over {
  background: linear-gradient(150deg, #f87171, #fb7185);
}

.daily-bar {
  display: block;
  background: var(--grad-accent);
  border-radius: 4px 4px 0 0;
}

.daily-bar.peak {
  background: linear-gradient(150deg, #c4b5fd, #a855f7);
}

/* 土曜の水色は新配色から浮くので藤色にする。日曜の赤系はそのまま。 */
.daily-tick.sat {
  color: #a5b4fc;
}
```

- [ ] **Step 4: タブバーとシートをすりガラスにする**

```css
.tabbar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  background: var(--glass);
  border-top: 1px solid var(--glass-border);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
  padding-bottom: env(safe-area-inset-bottom);
  z-index: 15;
}
```

`.sheet-backdrop` の `background` は暗幕だけにする(オーバーレイはぼかさない — シートのパネルとの二重ぼかしで重くなるため)。

```css
.sheet-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(12, 10, 29, 0.6);
  display: flex;
  align-items: flex-end;
  justify-content: center;
  z-index: 20;
}
```

`.sheet` の `background` を差し替え、上辺に縁を足す。

```css
.sheet {
  background: var(--glass);
  border: 1px solid var(--glass-border);
  border-bottom: none;
  backdrop-filter: blur(18px);
  -webkit-backdrop-filter: blur(18px);
  width: 100%;
  max-width: 640px;
  max-height: 92vh;
  overflow-y: auto;
  border-radius: 20px 20px 0 0;
  padding: 12px 16px calc(16px + env(safe-area-inset-bottom));
}
```

- [ ] **Step 5: バナーとフォーカスリング**

`.banner`(551 行目付近)を書き換える。

```css
.banner {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
  background: rgba(49, 46, 80, 0.72);
  border: 1px solid var(--glass-border);
  border-left: 4px solid var(--accent);
  box-shadow: var(--shadow-card);
  border-radius: 10px;
  padding: 10px 12px;
  font-size: 13px;
  margin-bottom: 12px;
}
```

`src/styles.css` の末尾に、キーボード操作時のフォーカスリングを追加する。

```css
/* キーボード操作時だけリングを出す。ポインタ操作では出さない。 */
:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
  border-radius: 4px;
}
```

- [ ] **Step 6: 回帰が無いことを確認する**

Run:

```bash
npm run test
```

Expected: 全テスト PASS

Run:

```bash
npm run build
```

Expected: エラーなし

- [ ] **Step 7: Commit**

```bash
git add src/styles.css src/ui/HomeScreen.tsx src/ui/SummaryScreen.tsx
git commit -m "style: すりガラスとグラデーションで面の質感を作る"
```

---

## Task 3: セグメントコントロールをスライドインジケーター方式にする

集計タブの「自己負担 / 個別 / 共有」を、アクティブなボタンを塗る方式から、背後のインジケーターが横にスライドする方式へ変える。`VIEWS` は常に 3 要素なので、インジケーターの幅は 1/3 固定、`translateX(index * 100%)` で移動させる。

**Files:**
- Modify: `src/ui/SummaryScreen.tsx:74-85`
- Modify: `src/styles.css`(`.segment` / `.seg` / `.seg.active`、`.seg-indicator` を追加)
- Test: `src/ui/SummaryScreen.test.tsx`

**Interfaces:**
- Consumes: Task 1 のトークン(`--grad-accent` `--on-accent` `--dur-base` `--ease-swift`)、Task 2 の `.card.glass`
- Produces: DOM 要素 `<span class="seg-indicator" data-testid="seg-indicator">`。`style.transform` が `translateX(0%)` / `translateX(100%)` / `translateX(200%)` のいずれかになる。

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/SummaryScreen.test.tsx` の `describe('SummaryScreen', ...)` の中、`it('切り替えても合計カードは変わらない', ...)` の直後に以下を追加する。

```tsx
  it('セグメントのインジケーターが選択位置へスライドする', async () => {
    renderWithLang(<SummaryScreen trip={trip} />);
    // 初期表示は自己負担(VIEWS の 3 番目)
    expect(await screen.findByTestId('seg-indicator')).toHaveStyle({
      transform: 'translateX(200%)',
    });

    await userEvent.click(screen.getByRole('button', { name: '個別' }));

    expect(screen.getByTestId('seg-indicator')).toHaveStyle({ transform: 'translateX(0%)' });
  });
```

- [ ] **Step 2: テストが失敗することを確認する**

Run:

```bash
npm run test -- SummaryScreen
```

Expected: FAIL — `Unable to find an element by: [data-testid="seg-indicator"]`

- [ ] **Step 3: インジケーター要素を追加する**

`src/ui/SummaryScreen.tsx` の 74〜85 行目を以下に置き換える。

```tsx
      <div className="segment">
        {/* アクティブなボタンを塗る代わりに、背後のインジケーターを横にスライドさせる。
            VIEWS は常に 3 要素なので幅 1/3 固定で位置を出せる。 */}
        <span
          className="seg-indicator"
          data-testid="seg-indicator"
          style={{ transform: `translateX(${VIEWS.indexOf(view) * 100}%)` }}
        />
        {VIEWS.map((v) => (
          <button
            key={v}
            type="button"
            className={v === view ? 'seg active' : 'seg'}
            onClick={() => setView(v)}
          >
            {t.view[v]}
          </button>
        ))}
      </div>
```

- [ ] **Step 4: CSS を書く**

`src/styles.css` の `.segment` / `.seg` / `.seg.active`(299〜319 行目付近)を以下に置き換える。

```css
.segment {
  position: relative;
  display: flex;
  background: var(--surface-2);
  border-radius: 12px;
  padding: 4px;
  margin-bottom: 12px;
}

.seg-indicator {
  position: absolute;
  top: 4px;
  bottom: 4px;
  left: 4px;
  width: calc((100% - 8px) / 3);
  border-radius: 9px;
  background: var(--grad-accent);
  transition: transform var(--dur-base) var(--ease-swift);
  pointer-events: none;
}

.seg {
  position: relative;
  z-index: 1;
  flex: 1;
  padding: 10px;
  border-radius: 9px;
  font-size: 15px;
  color: var(--text-dim);
  transition: color var(--dur-base) var(--ease-swift);
}

.seg.active {
  color: var(--on-accent);
  font-weight: 600;
}
```

- [ ] **Step 5: テストが通ることを確認する**

Run:

```bash
npm run test
```

Expected: 全テスト PASS(新規 1 件を含む)

- [ ] **Step 6: Commit**

```bash
git add src/ui/SummaryScreen.tsx src/ui/SummaryScreen.test.tsx src/styles.css
git commit -m "feat: セグメントをスライドインジケーター方式にする"
```

---

## Task 4: 金額のカウントアップ

ホームと集計の合計金額を、値が変わるたびに 500ms かけて前回値から新しい値へ動かす。初回マウント時は 0 から始める。イージングは ease-out(三乗)。

**「視差効果を減らす」設定の扱いがこの Task の肝。** `window.matchMedia` は jsdom に存在しないため、`src/test/setup.ts` にモックを足す。**テスト環境の既定は reduced-motion = true** とする。こうしないと既存の `SummaryScreen.test.tsx` / `HomeScreen.test.tsx` の合計金額アサーション(`¥5,116` など)がアニメーション途中の値を拾って壊れる。アニメーションする側の挙動は `useCountUp.test.ts` がモックを上書きして検証する。

**Files:**
- Create: `src/ui/motion.ts`
- Create: `src/ui/useCountUp.ts`
- Create: `src/ui/useCountUp.test.ts`
- Modify: `src/test/setup.ts`
- Modify: `src/ui/HomeScreen.tsx`(import と 105-107 行目)
- Modify: `src/ui/SummaryScreen.tsx`(import と 47-49 行目)

**Interfaces:**
- Consumes: なし(CSS トークンには依存しない)
- Produces:
  - `src/ui/motion.ts` → `export function prefersReducedMotion(): boolean`
  - `src/ui/useCountUp.ts` → `export function useCountUp(value: number): number`(引数は表示したい最終値、戻り値は今フレームで表示すべき整数)
  - Task 7 が `prefersReducedMotion()` を使う。

- [ ] **Step 1: テスト環境に `matchMedia` のモックを足す**

`src/test/setup.ts` の末尾に以下を追加する。

```ts
// jsdom には matchMedia が無い。テストの既定は「視差効果を減らす」= 有効にして、
// カウントアップなどの JS アニメーションを即座に最終値へ飛ばす。こうしないと
// 画面テストの金額アサーションがアニメーション途中の値を拾って不安定になる。
// アニメーションする側の挙動は useCountUp.test.ts がこのモックを上書きして検証する。
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  configurable: true,
  value: (query: string) => ({
    matches: query.includes('prefers-reduced-motion'),
    media: query,
    onchange: null,
    addEventListener: () => {},
    removeEventListener: () => {},
    addListener: () => {},
    removeListener: () => {},
    dispatchEvent: () => false,
  }),
});
```

- [ ] **Step 2: 失敗するテストを書く**

`src/ui/useCountUp.test.ts` を新規作成する。

```ts
import { describe, it, expect, afterEach, vi } from 'vitest';
import { act, renderHook } from '@testing-library/react';
import { useCountUp } from './useCountUp';

/** setup.ts の既定(reduced-motion = true)を、このテストの中だけ上書きする。 */
function setReducedMotion(reduced: boolean): void {
  Object.defineProperty(window, 'matchMedia', {
    writable: true,
    configurable: true,
    value: (query: string) => ({
      matches: reduced && query.includes('prefers-reduced-motion'),
      media: query,
      onchange: null,
      addEventListener: () => {},
      removeEventListener: () => {},
      addListener: () => {},
      removeListener: () => {},
      dispatchEvent: () => false,
    }),
  });
}

afterEach(() => {
  vi.useRealTimers();
  setReducedMotion(true);
});

describe('useCountUp', () => {
  it('視差効果を減らす設定なら最初から最終値を返す', () => {
    setReducedMotion(true);
    const { result } = renderHook(() => useCountUp(5116));
    expect(result.current).toBe(5116);
  });

  it('初回は 0 から始まり、500ms 後に最終値へ届く', () => {
    setReducedMotion(false);
    vi.useFakeTimers();
    const { result } = renderHook(() => useCountUp(5116));

    expect(result.current).toBe(0);

    act(() => {
      vi.advanceTimersByTime(600);
    });
    expect(result.current).toBe(5116);
  });

  it('値が変わると前回値から新しい値へ動く', () => {
    setReducedMotion(false);
    vi.useFakeTimers();
    const { result, rerender } = renderHook(({ v }) => useCountUp(v), {
      initialProps: { v: 1000 },
    });
    act(() => {
      vi.advanceTimersByTime(600);
    });
    expect(result.current).toBe(1000);

    rerender({ v: 2000 });
    act(() => {
      vi.advanceTimersByTime(16);
    });
    // 途中経過は前回値と新しい値のあいだにいる(0 に戻らない)
    expect(result.current).toBeGreaterThan(1000);
    expect(result.current).toBeLessThan(2000);

    act(() => {
      vi.advanceTimersByTime(600);
    });
    expect(result.current).toBe(2000);
  });
});
```

- [ ] **Step 3: テストが失敗することを確認する**

Run:

```bash
npm run test -- useCountUp
```

Expected: FAIL — `Failed to resolve import "./useCountUp"`

- [ ] **Step 4: `prefersReducedMotion()` を実装する**

`src/ui/motion.ts` を新規作成する。

```ts
/**
 * OS の「視差効果を減らす」設定が有効かどうか。
 * CSS 側は styles.css の @media (prefers-reduced-motion: reduce) が一括で止めるので、
 * ここは JS で動かすアニメーション(カウントアップ・シートの閉じ待ち)専用。
 */
export function prefersReducedMotion(): boolean {
  if (typeof window.matchMedia !== 'function') return false;
  return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
}
```

- [ ] **Step 5: `useCountUp` を実装する**

`src/ui/useCountUp.ts` を新規作成する。

```ts
import { useEffect, useRef, useState } from 'react';
import { prefersReducedMotion } from './motion';

const DURATION = 500;

/** ease-out(三乗)。終盤ほどゆっくり止まって「数え上げた」感じになる。 */
function easeOut(t: number): number {
  return 1 - (1 - t) ** 3;
}

/**
 * 金額を前回値から今の値へ 500ms でカウントアップさせる。
 * 初回マウント時は 0 から始める。「視差効果を減らす」設定なら即座に最終値を返す。
 *
 * 経過時間は performance.now() ではなく requestAnimationFrame が渡すタイムスタンプの
 * 差分で測る。テストのフェイクタイマー下でも同じ時計を使えるようにするため。
 */
export function useCountUp(value: number): number {
  const [shown, setShown] = useState(() => (prefersReducedMotion() ? value : 0));
  // 今表示している値。途中で目標が変わっても、見えている値から続けて動かすために保持する。
  const fromRef = useRef(shown);

  useEffect(() => {
    if (prefersReducedMotion()) {
      fromRef.current = value;
      setShown(value);
      return;
    }

    const from = fromRef.current;
    if (from === value) return;

    let raf = 0;
    let start: number | null = null;
    const step = (now: number) => {
      if (start === null) start = now;
      const t = Math.min(1, (now - start) / DURATION);
      const current = Math.round(from + (value - from) * easeOut(t));
      fromRef.current = current;
      setShown(current);
      if (t < 1) raf = requestAnimationFrame(step);
    };
    raf = requestAnimationFrame(step);
    return () => cancelAnimationFrame(raf);
  }, [value]);

  return shown;
}
```

- [ ] **Step 6: テストが通ることを確認する**

Run:

```bash
npm run test -- useCountUp
```

Expected: PASS(3 件)

- [ ] **Step 7: 表示用コンポーネント `CountUpAmount` を作る**

> **なぜコンポーネントに切り出すか:** `useCountUp` はフックなので、コンポーネントの先頭で無条件に呼ぶ必要がある。ところが合計金額を表示している場所は、`HomeScreen.tsx:105`(早期 return は 78 行目)も `SummaryScreen.tsx:47`(早期 return は 23 行目と 38 行目)も、どちらも早期 return より**後ろ**にある。画面コンポーネントの中で直接 `useCountUp` を呼ぶとフックのルールに違反するので、合計金額の表示だけを子コンポーネントに切り出す。両画面から使うので独立したファイルに置く。

`src/ui/CountUpAmount.tsx` を新規作成する。

```tsx
import { formatWithCurrency } from '../domain/money';
import { useCountUp } from './useCountUp';

/**
 * 合計金額を 0 から数え上げて表示する。
 * useCountUp はフックなので、画面コンポーネントの早期 return より後ろでは呼べない。
 * 表示だけを切り出してフックの呼び出し位置を安全にしている。
 */
export function CountUpAmount({
  value,
  currency,
  testId,
}: {
  value: number;
  currency: string;
  testId: string;
}) {
  const shown = useCountUp(value);
  return (
    <span className="card-home" data-testid={testId}>
      {formatWithCurrency(shown, currency)}
    </span>
  );
}
```

- [ ] **Step 8: ホームと集計の合計を `CountUpAmount` に置き換える**

`src/ui/HomeScreen.tsx` の import に 1 行足す(`./ExpenseSheet` の import の下)。

```tsx
import { CountUpAmount } from './CountUpAmount';
```

105〜107 行目(`<span className="card-home" data-testid="total-home">` の 3 行)を以下に置き換える。

```tsx
          <CountUpAmount
            value={summary.totalHome}
            currency={trip.homeCurrency}
            testId="total-home"
          />
```

`src/ui/SummaryScreen.tsx` の import にも同じ行を足す(`./DailyChart` の import の下)。

```tsx
import { CountUpAmount } from './CountUpAmount';
```

47〜49 行目(`<span className="card-home" data-testid="summary-total">` の 3 行)を以下に置き換える。

```tsx
          <CountUpAmount
            value={summary.totalHome}
            currency={trip.homeCurrency}
            testId="summary-total"
          />
```

> **確認:** 置き換え後、`HomeScreen.tsx` と `SummaryScreen.tsx` の両方で `formatWithCurrency` が他の箇所でも使われているかを見る。使われていなければ import を消す(`npm run lint` が未使用 import を検出する)。`HomeScreen.tsx` は支出一覧などで使っているので残る見込み。

- [ ] **Step 9: 全テストと型が通ることを確認する**

Run:

```bash
npm run test
```

Expected: 全テスト PASS。既存の `total-home` / `summary-total` のアサーションは、テスト環境の既定が reduced-motion なので即値が入り、そのまま通る。

Run:

```bash
npm run lint
```

Expected: エラーなし

- [ ] **Step 10: Commit**

```bash
git add src/ui/motion.ts src/ui/useCountUp.ts src/ui/useCountUp.test.ts src/ui/CountUpAmount.tsx src/ui/HomeScreen.tsx src/ui/SummaryScreen.tsx src/test/setup.ts
git commit -m "feat: 合計金額をカウントアップで表示する"
```

---

## Task 5: 画面遷移とリスト入場のアニメーション + reduced-motion の一括無効化

タブを切り替えたときに内容がフェードしながら 8px 上がってくる。支出行とカテゴリ行は 70ms ずつずれて入場する(**遅延は先頭 8 行で頭打ち** — 長いリストで最後の行がいつまでも出てこないのを防ぐ)。そして「視差効果を減らす」設定時に全部止まるようにする。

**Files:**
- Modify: `src/app/App.tsx:41-113`(タブ内容を `key={tab}` のラッパーで包む)
- Modify: `src/styles.css`(キーフレームと `.tab-panel` / `.ex-row` / `.legend-row` のアニメーション、末尾に reduced-motion ブロック)

**Interfaces:**
- Consumes: Task 1 のトークン(`--dur-base` `--ease-swift`)
- Produces: CSS クラス `.tab-panel`(タブ内容のラッパー)。Task 6・7 が同じ reduced-motion ブロックに守られる。

- [ ] **Step 1: タブ内容をラッパーで包む**

`src/app/App.tsx` の `<main className="screen">` の中、バナー・エラー・トーストの後にある 3 つのタブ内容(`{tab === 'home' && ...}` など)を、1 つのラッパー要素の中に入れる。`key={tab}` を付けることでタブ切替のたびに再マウントされ、CSS アニメーションが再生される。

置き換え前後の対応が分かるよう、`</main>` 直前までの該当部分を以下の形にする。

```tsx
        {/* key={tab} でタブ切替のたびに再マウントし、入場アニメーションを再生させる */}
        <div className="tab-panel" key={tab}>
          {tab === 'home' && activeTrip !== null && <HomeScreen trip={activeTrip} />}
          {tab === 'summary' && activeTrip !== null && <SummaryScreen trip={activeTrip} />}
          {tab === 'settings' && <SettingsScreen />}
        </div>
```

> 既存の条件式・コンポーネント・props はそのまま。`activeTrip === null` のときの案内文(`t.app.noTrip`)など、この 3 行以外の要素はラッパーの外に残す。

- [ ] **Step 2: キーフレームと入場アニメーションを書く**

`src/styles.css` の末尾(Task 2 で足した `:focus-visible` の後)に追加する。

```css
/* ---- マイクロアニメーション ---- */

@keyframes rise-in {
  from {
    opacity: 0;
    transform: translateY(8px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.tab-panel {
  animation: rise-in var(--dur-base) var(--ease-swift) both;
}

.ex-row,
.legend-row {
  animation: rise-in var(--dur-base) var(--ease-swift) both;
}

/* 行ごとに 70ms ずつずらす。9 行目以降は同時に出す(長いリストで待たされないため)。 */
.ex-row:nth-child(1),
.legend-row:nth-child(1) {
  animation-delay: 0ms;
}
.ex-row:nth-child(2),
.legend-row:nth-child(2) {
  animation-delay: 70ms;
}
.ex-row:nth-child(3),
.legend-row:nth-child(3) {
  animation-delay: 140ms;
}
.ex-row:nth-child(4),
.legend-row:nth-child(4) {
  animation-delay: 210ms;
}
.ex-row:nth-child(5),
.legend-row:nth-child(5) {
  animation-delay: 280ms;
}
.ex-row:nth-child(6),
.legend-row:nth-child(6) {
  animation-delay: 350ms;
}
.ex-row:nth-child(7),
.legend-row:nth-child(7) {
  animation-delay: 420ms;
}
.ex-row:nth-child(n + 8),
.legend-row:nth-child(n + 8) {
  animation-delay: 490ms;
}
```

- [ ] **Step 3: reduced-motion で全部止める**

`src/styles.css` の**いちばん末尾**に追加する。以降の Task で足すアニメーションもここで一括して止まる。

```css
/* OS の「視差効果を減らす」が有効なら、全てのアニメーションと遷移を無効化する。
   0 ではなく 0.01ms にするのは、animationend / transitionend を待つコードが
   止まらないようにするため(標準的な指定)。 */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    animation-delay: 0ms !important;
    transition-duration: 0.01ms !important;
    transition-delay: 0ms !important;
    scroll-behavior: auto !important;
  }
}
```

- [ ] **Step 4: 回帰が無いことを確認する**

Run:

```bash
npm run test
```

Expected: 全テスト PASS(ラッパー `div` の追加は `getByRole` / `getByTestId` に影響しない)

Run:

```bash
npm run build
```

Expected: エラーなし

- [ ] **Step 5: Commit**

```bash
git add src/app/App.tsx src/styles.css
git commit -m "feat: 画面遷移とリスト入場のアニメーションを追加する"
```

---

## Task 6: バーの伸長・FAB の呼吸グロー・ボタンの押下

グラフと予算バーが伸びる、FAB がゆっくり明滅する、ボタンを押すと少し縮む。集計のセグメントを切り替えたときにグラフを再生させるため、`CategoryChart` と `DailyChart` に `key={view}` を付ける。

**Files:**
- Modify: `src/ui/SummaryScreen.tsx:98`、`src/ui/SummaryScreen.tsx:106`
- Modify: `src/styles.css`(キーフレームと `.daily-bar` / `.stack-bar` / `.budget-fill` / `.fab` / ボタン `:active`)

**Interfaces:**
- Consumes: Task 1 のトークン(`--dur-fast` `--dur-slow` `--ease-swift` `--accent` `--accent-2`)、Task 5 の reduced-motion ブロック
- Produces: なし(以降の Task は参照しない)

- [ ] **Step 1: セグメント切替でグラフを再マウントさせる**

`src/ui/SummaryScreen.tsx` の 98 行目:

```tsx
            <CategoryChart key={view} rows={categories} homeCurrency={trip.homeCurrency} />
```

同 106 行目:

```tsx
            <DailyChart key={view} series={days} homeCurrency={trip.homeCurrency} />
```

- [ ] **Step 2: バーの伸長アニメーションを書く**

`src/styles.css` の末尾(reduced-motion ブロックの**手前**)に追加する。棒の高さ・幅はインライン style で決まっているので、`transform` のスケールで伸長を表現する。既存のレイアウトも既存テストの `toHaveStyle({ height: '110px' })` も壊さない。

```css
@keyframes grow-up {
  from {
    transform: scaleY(0);
  }
  to {
    transform: scaleY(1);
  }
}

@keyframes grow-right {
  from {
    transform: scaleX(0);
  }
  to {
    transform: scaleX(1);
  }
}

.daily-bar {
  transform-origin: bottom;
  animation: grow-up var(--dur-slow) var(--ease-swift) both;
}

.stack-bar {
  transform-origin: left;
  animation: grow-right var(--dur-slow) var(--ease-swift) both;
}

.budget-fill {
  transform-origin: left;
  animation: grow-right var(--dur-slow) var(--ease-swift) both;
  /* マウント後に予算や支出が変わったときは幅の変化そのものを補間する */
  transition: width var(--dur-slow) var(--ease-swift);
}

/* 日別グラフの平均線はフェードだけ。位置が動くと目盛りに見えてしまうため。 */
.daily-avg {
  animation: fade-in var(--dur-base) var(--ease-swift) both;
}

@keyframes fade-in {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}
```

- [ ] **Step 3: FAB の呼吸グローとボタンの押下**

`src/styles.css` の末尾(reduced-motion ブロックの**手前**)に追加する。

```css
/* 常時動く演出はこれだけ。3.6s とゆっくりにしてうるさくならないようにする。 */
@keyframes fab-breathe {
  0%,
  100% {
    box-shadow: 0 6px 20px rgba(129, 140, 248, 0.32);
  }
  50% {
    box-shadow: 0 6px 28px rgba(168, 85, 247, 0.55);
  }
}

.fab {
  animation: fab-breathe 3.6s ease-in-out infinite;
  transition: transform var(--dur-fast) var(--ease-swift);
}

.btn-primary,
.btn-ghost,
.btn-danger,
.seg,
.tab,
.ex-main {
  transition: transform var(--dur-fast) var(--ease-swift);
}

.fab:active,
.btn-primary:active,
.btn-ghost:active,
.btn-danger:active,
.seg:active,
.tab:active,
.ex-main:active {
  transform: scale(0.96);
}
```

- [ ] **Step 4: 回帰が無いことを確認する**

Run:

```bash
npm run test
```

Expected: 全テスト PASS。`DailyChart.test.tsx` の `toHaveStyle({ height: '110px' })` はインライン style を見るので、`transform` のアニメーションでは壊れない。

Run:

```bash
npm run build
```

Expected: エラーなし

- [ ] **Step 5: Commit**

```bash
git add src/ui/SummaryScreen.tsx src/styles.css
git commit -m "feat: バーの伸長・FAB の呼吸グロー・押下フィードバックを追加する"
```

---

## Task 7: シートの出入りアニメーション

シートが下から 280ms でせり上がり、閉じるときは 180ms で沈む。閉じアニメーションを見せるには、アンマウントを 180ms 遅らせる必要がある。`Sheet` の中に閉じ状態を持たせる。

「視差効果を減らす」設定時は待たずに即座に閉じる。**テスト環境の既定は reduced-motion(Task 4 で設定済み)なので、`✕` を押した直後に `queryByRole('dialog')` が消えることを期待する既存テストはそのまま通る。**

なお、`ExpenseSheet` の保存完了時のように `Sheet` の閉じ操作を経由せず親が直接アンマウントする経路では、閉じアニメーションは再生されない。ここは仕様として許容する(保存後は速く消えたほうが体感が良い)。

**Files:**
- Modify: `src/ui/Sheet.tsx`
- Modify: `src/styles.css`(`.sheet-backdrop` / `.sheet` のアニメーション)

**Interfaces:**
- Consumes: Task 4 の `prefersReducedMotion()`(`src/ui/motion.ts`)、Task 1 のトークン、Task 5 の reduced-motion ブロック
- Produces: CSS クラス `.sheet-backdrop.closing` / `.sheet.closing`

- [ ] **Step 1: `Sheet` に閉じ状態を持たせる**

`src/ui/Sheet.tsx` の全体を以下に置き換える。

```tsx
import { useEffect, useRef, useState, type ReactNode } from 'react';
import { useI18n } from '../i18n/LangContext';
import { prefersReducedMotion } from './motion';

/** 閉じアニメーションの長さ。styles.css の .sheet.closing と合わせる。 */
const CLOSE_MS = 180;

type Props = {
  title: string;
  onClose: () => void;
  children: ReactNode;
};

export function Sheet({ title, onClose, children }: Props) {
  const { t } = useI18n();
  const [closing, setClosing] = useState(false);
  const timer = useRef<ReturnType<typeof setTimeout> | null>(null);

  useEffect(() => {
    return () => {
      if (timer.current !== null) clearTimeout(timer.current);
    };
  }, []);

  // 閉じアニメーションを見せるためにアンマウントを 180ms 遅らせる。
  // 「視差効果を減らす」設定なら待たずに即座に閉じる。
  function requestClose() {
    if (prefersReducedMotion()) {
      onClose();
      return;
    }
    if (closing) return;
    setClosing(true);
    timer.current = setTimeout(onClose, CLOSE_MS);
  }

  return (
    <div
      className={closing ? 'sheet-backdrop closing' : 'sheet-backdrop'}
      role="presentation"
      onClick={requestClose}
    >
      <div
        className={closing ? 'sheet closing' : 'sheet'}
        role="dialog"
        aria-label={title}
        onClick={(e) => e.stopPropagation()}
      >
        <header className="sheet-header">
          <h2>{title}</h2>
          <button type="button" onClick={requestClose} aria-label={t.common.close}>
            ✕
          </button>
        </header>
        <div className="sheet-body">{children}</div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: シートのアニメーションを書く**

`src/styles.css` の末尾(reduced-motion ブロックの**手前**)に追加する。

```css
@keyframes sheet-up {
  from {
    transform: translateY(100%);
  }
  to {
    transform: translateY(0);
  }
}

.sheet-backdrop {
  animation: fade-in var(--dur-base) var(--ease-swift) both;
}

.sheet {
  animation: sheet-up var(--dur-base) var(--ease-swift) both;
}

/* 閉じは出現より速く。Sheet.tsx の CLOSE_MS = 180 と揃える。 */
.sheet-backdrop.closing {
  animation: fade-in var(--dur-fast) var(--ease-swift) reverse both;
}

.sheet.closing {
  animation: sheet-up var(--dur-fast) var(--ease-swift) reverse both;
}
```

- [ ] **Step 3: 既存のシート関連テストが通ることを確認する**

Run:

```bash
npm run test
```

Expected: 全テスト PASS。特に `App.test.tsx` / `HomeScreen.test.tsx` / `SettingsScreen.test.tsx` / `TripPickerSheet.test.tsx` / `ExpenseSheet.test.tsx` のシート開閉テストが通ること(テスト環境は reduced-motion なので `requestClose` は同期的に `onClose` を呼ぶ)。

Run:

```bash
npm run lint
```

Expected: エラーなし

- [ ] **Step 4: Commit**

```bash
git add src/ui/Sheet.tsx src/styles.css
git commit -m "feat: シートの出入りにアニメーションを付ける"
```

---

## Task 8: アニメーションの総点検(ビルドと目視)

Task 1〜7 で入れた見た目の変更を、まとめてビルドして確認する。**この Task はコード変更を伴わない。** 問題が見つかった場合のみ該当 Task の CSS を直す。

**Files:** なし(問題が見つかった場合のみ `src/styles.css`)

**Interfaces:**
- Consumes: Task 1〜7 の成果すべて
- Produces: なし

- [ ] **Step 1: 全検証を通す**

Run:

```bash
npm run test
```

Expected: 全テスト PASS

Run:

```bash
npm run build
```

Expected: エラーなし

- [ ] **Step 2: プレビューで目視確認する**

Run:

```bash
npm run preview
```

ブラウザで開き、以下を確認する。

1. 4 タブ(ホーム・集計・設定)すべてで表示崩れが無い
2. タブを切り替えると内容がフェードしながら上がってくる
3. 支出行が上から順にずれて出てくる(9 行目以降は同時)
4. 集計のセグメントを押すとインジケーターが滑らかに動き、グラフが伸び直す
5. ホーム・集計の合計金額が 0 から数え上がる
6. FAB がゆっくり明滅し、押すと少し縮む
7. FAB を押すとシートが下からせり上がり、`✕` で沈んで消える
8. 本文の文字が背景に対して読みづらくない

- [ ] **Step 3: reduced-motion で全部止まることを確認する**

ブラウザの DevTools で「Emulate CSS media feature prefers-reduced-motion: reduce」を有効にしてリロードし、上記 2〜7 のアニメーションが一切走らないこと(合計金額も即座に最終値であること)を確認する。

- [ ] **Step 4: 問題があれば直す**

崩れやくどさが見つかった場合のみ `src/styles.css` を修正し、以下でコミットする。問題が無ければこの Step はスキップする。

```bash
git add src/styles.css
git commit -m "style: 目視確認で見つかった表示の崩れを直す"
```

---

## Task 9: 修正 1 — 日別グラフの平均ラベルが上端で被る

平均ラベルは平均線の上(`top: -14px`)に出るため、平均が最大値に近いとチャート上端をはみ出し、見出しの「日別推移 / 自己負担」と重なる。**平均線がチャート上端に近くラベルの高さ 14px が収まらない場合だけ、ラベルを線の下に出す。** チャートの高さも平均線自体の位置も変えない。

**Files:**
- Modify: `src/ui/DailyChart.tsx:6`(定数追加)、`src/ui/DailyChart.tsx:36-43`
- Modify: `src/styles.css`(`.daily-avg--below span` を追加)
- Test: `src/ui/DailyChart.test.tsx`

**Interfaces:**
- Consumes: なし
- Produces: DOM の `.daily-avg` に修飾クラス `daily-avg--below` が条件付きで付く

- [ ] **Step 1: 失敗するテストを書く**

`src/ui/DailyChart.test.tsx` の `describe` の末尾に以下 2 件を追加する。

```tsx
  it('平均が最大値に近いとラベルを平均線の下に出す', () => {
    const flat: DailySeries = {
      points: [
        { date: '2026-09-12', home: 1000 },
        { date: '2026-09-13', home: 990 },
      ],
      totalHome: 1990,
      maxHome: 1000,
      peakDate: '2026-09-12',
      averageHome: 995,
    };
    const { container } = renderWithLang(<DailyChart series={flat} homeCurrency="JPY" />);

    expect(container.querySelector('.daily-avg')).toHaveClass('daily-avg--below');
  });

  it('平均が最大値より十分低ければラベルは平均線の上のまま', () => {
    const { container } = renderWithLang(<DailyChart series={threeDays} homeCurrency="JPY" />);

    expect(container.querySelector('.daily-avg')).not.toHaveClass('daily-avg--below');
  });
```

> `threeDays` は平均 1322 / 最大 2816 なので、平均線は下から 51.6px の位置(上端まで 58.4px の余裕)にあり、下側表示にはならない。`flat` は平均 995 / 最大 1000 で平均線は下から 109.45px、上端まで 0.55px しかないので下側表示になる。

- [ ] **Step 2: テストが失敗することを確認する**

Run:

```bash
npm run test -- DailyChart
```

Expected: FAIL — `Expected the element to have class: daily-avg--below`

- [ ] **Step 3: 上下判定を実装する**

`src/ui/DailyChart.tsx` の 6 行目の下に定数を足す。

```tsx
const BAR_HEIGHT = 110;
/** 平均ラベル(.daily-avg span)の高さ。styles.css の top: -14px と揃える。 */
const AVG_LABEL_HEIGHT = 14;
```

36〜43 行目を以下に置き換える。

```tsx
  const avgBottom = (averageHome / scale) * BAR_HEIGHT;
  // 平均線がチャート上端に近いと、線の上に出すラベルがはみ出して見出しと重なる。
  // 収まらないときだけ線の下に出す。線そのものの位置は変えない。
  const avgLabelBelow = BAR_HEIGHT - avgBottom < AVG_LABEL_HEIGHT;

  return (
    <div className="daily">
      <div className="daily-bars" style={{ gap: `${gap}px`, height: `${BAR_HEIGHT}px` }}>
        {averageHome > 0 && (
          <div
            className={avgLabelBelow ? 'daily-avg daily-avg--below' : 'daily-avg'}
            style={{ bottom: `${avgBottom}px` }}
          >
            <span>{t.summary.average(fmt(averageHome))}</span>
          </div>
        )}
```

> `const scale = Math.max(1, maxHome);`(34 行目)より後ろに置くこと。

- [ ] **Step 4: CSS を 1 ルール足す**

`src/styles.css` の `.daily-avg span` ルール(719 行目付近)の直後に追加する。

```css
/* 平均線が上端に近いときはラベルを線の下に回す(見出しとの重なりを避ける) */
.daily-avg--below span {
  top: 2px;
}
```

- [ ] **Step 5: テストが通ることを確認する**

Run:

```bash
npm run test -- DailyChart
```

Expected: PASS(既存 8 件 + 新規 2 件)

- [ ] **Step 6: Commit**

```bash
git add src/ui/DailyChart.tsx src/ui/DailyChart.test.tsx src/styles.css
git commit -m "fix: 日別グラフの平均ラベルが見出しと重なるのを避ける"
```

---

## Task 10: 修正 2 — エクスポート促しバナーを設定タブでは書き分ける

設定タブを開いているときにバナーが「設定からデータを書き出しましょう」と言い、しかも「設定へ」ボタンが出るのは意味がない。設定タブでは文言を「下の『バックアップを書き出す』から」に変え、「設定へ」ボタンを消す(「閉じる」は残す)。

**Files:**
- Modify: `src/i18n/ja.ts`(`app` セクション)、`src/i18n/en.ts`、`src/i18n/ko.ts`、`src/i18n/zh.ts`
- Modify: `src/app/App.tsx:59-76`
- Test: `src/app/App.test.tsx`

**Interfaces:**
- Consumes: なし
- Produces: i18n キー `t.app.exportReminderSettings: string`

- [ ] **Step 1: 失敗するテストを書く**

`src/app/App.test.tsx` の末尾、最後の `describe` の後ろに新しい `describe` を追加する。

```tsx
describe('App のエクスポート促しバナー', () => {
  async function seedEndedTrip(): Promise<void> {
    const trip = await createTrip({
      name: '上海 2026-08',
      currency: 'CNY',
      homeCurrency: 'JPY',
      startDate: '2026-08-01',
      endDate: '2026-08-05',
    });
    localStorage.setItem('trip-wallet:active-trip', trip.id);
  }

  it('ホームタブでは設定へ誘導する', async () => {
    await seedEndedTrip();
    renderWithLang(<App />);

    expect(
      await screen.findByText('旅行が終わりました。設定からデータを書き出しておきましょう。'),
    ).toBeInTheDocument();
    expect(screen.getByRole('button', { name: '設定へ' })).toBeInTheDocument();
  });

  it('設定タブでは文言を変えて「設定へ」を出さない', async () => {
    await seedEndedTrip();
    const user = userEvent.setup();
    renderWithLang(<App />);
    await screen.findByText('旅行が終わりました。設定からデータを書き出しておきましょう。');

    await user.click(screen.getByRole('button', { name: '⚙️ 設定' }));

    expect(
      screen.getByText(
        '旅行が終わりました。下の「バックアップを書き出す」からデータを書き出しておきましょう。',
      ),
    ).toBeInTheDocument();
    expect(screen.queryByRole('button', { name: '設定へ' })).not.toBeInTheDocument();
  });
});
```

> **前提の確認結果(調査済み):**
> - `createTrip`(`src/data/tripRepo.ts:17`)は `startDate?: string` / `endDate?: string | null` を受け取る。上のフィクスチャはそのまま通る。
> - アクティブ旅行の localStorage キーは `'trip-wallet:active-trip'`。既存の `App.test.tsx` の `seedTwoTrips()`(29〜33 行目)と同じ書き方。
> - バナーは `needsExportReminder`(`src/app/reminders.ts:26`)が `trip.endDate <= today` のときに出す。`today` は `todayLocal()` の実行時の日付なので、`endDate: '2026-08-05'` なら以後ずっと条件を満たす。
> - 1 件目で照合している既存文言は `src/i18n/ja.ts:24` の `exportReminder`、ボタン名は同 25 行目の `toSettings`(`'設定へ'`)。

- [ ] **Step 2: テストが失敗することを確認する**

Run:

```bash
npm run test -- App
```

Expected: FAIL — 2 件目で「下の「バックアップを書き出す」から…」が見つからない

- [ ] **Step 3: 4 言語に新しいキーを足す**

`src/i18n/ja.ts` の `app` セクション、`exportReminder` の直後に追加する。

```ts
      exportReminderSettings:
        '旅行が終わりました。下の「バックアップを書き出す」からデータを書き出しておきましょう。',
```

`src/i18n/en.ts` の同じ位置:

```ts
      exportReminderSettings:
        'Your trip has ended. Export your data with "Export a backup" below.',
```

`src/i18n/ko.ts` の同じ位置:

```ts
      exportReminderSettings:
        '여행이 끝났습니다. 아래의 「백업 내보내기」로 데이터를 내보내 두세요.',
```

`src/i18n/zh.ts` の同じ位置:

```ts
      exportReminderSettings: '行程已结束。请用下方的「导出备份」导出数据。',
```

> それぞれの文中に出てくるボタン名は、各言語の `settings.export` の文言(ja: バックアップを書き出す / en: Export a backup / ko: 백업 내보내기 / zh: 导出备份)と一致させること。

- [ ] **Step 4: バナーを書き分ける**

`src/app/App.tsx` の 59〜76 行目を以下に置き換える。

```tsx
        {showExportReminder && activeTrip !== null && (
          <div className="banner">
            {/* 設定タブを開いているときに「設定へ」と言っても意味がないので、
                文言を書き分けてボタンを出さない。 */}
            <span>{tab === 'settings' ? t.app.exportReminderSettings : t.app.exportReminder}</span>
            {tab !== 'settings' && (
              <button type="button" className="btn-primary" onClick={() => setTab('settings')}>
                {t.app.toSettings}
              </button>
            )}
            <button
              type="button"
              className="btn-ghost"
              onClick={() => {
                dismissReminder(activeTrip.id);
                setDismissed(activeTrip.id);
              }}
            >
              {t.common.close}
            </button>
          </div>
        )}
```

- [ ] **Step 5: テストと型が通ることを確認する**

Run:

```bash
npm run test
```

Expected: 全テスト PASS(新規 2 件を含む)

Run:

```bash
npm run lint
```

Expected: エラーなし。4 言語のいずれかでキーが抜けていればここで落ちる(`Dictionary = typeof ja`)。

- [ ] **Step 6: Commit**

```bash
git add src/app/App.tsx src/app/App.test.tsx src/i18n/ja.ts src/i18n/en.ts src/i18n/ko.ts src/i18n/zh.ts
git commit -m "fix: 設定タブではエクスポート促しバナーの表記を変える"
```

---

## Task 11: アイコン刷新と maskable アイコンの専用化

構図(封筒型の財布)はそのままに、グラデーションとグローで質感を上げる。加えて、Android のアダプティブアイコン用に **maskable 専用の PNG** を作る。今までは `icon-512.png` を maskable として使い回していたため、円形にくり抜かれるとモチーフの端が切れていた。

SVG ファイルを 2 枚手で管理すると片方の更新を忘れるので、maskable 用のラッパー SVG は `scripts/generate-icons.mjs` の中で組み立てる。そのために `public/icon.svg` のモチーフ部分を `<g id="motif">` で囲う(描画結果は変わらない)。

**Files:**
- Modify: `public/icon.svg`(全面差し替え)
- Modify: `scripts/generate-icons.mjs`
- Modify: `vite.config.ts`(manifest の maskable エントリ)
- Modify: `index.html`(favicon の追加)
- 生成物(commit する): `public/icon-192.png` / `public/icon-512.png` / `public/apple-touch-icon.png` / `public/icon-maskable-512.png`

**Interfaces:**
- Consumes: なし
- Produces: `public/icon-maskable-512.png`(manifest が `purpose: 'maskable'` として参照する)

- [ ] **Step 1: `public/icon.svg` を差し替える**

ファイル全体を以下に置き換える。

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <defs>
    <linearGradient id="bg" x1="0" y1="1" x2="1" y2="0">
      <stop offset="0" stop-color="#0c0a1d"/>
      <stop offset="0.55" stop-color="#1e1b4a"/>
      <stop offset="1" stop-color="#4338ca"/>
    </linearGradient>
    <linearGradient id="body" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0" stop-color="#818cf8"/>
      <stop offset="1" stop-color="#6366f1"/>
    </linearGradient>
    <linearGradient id="flap" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0" stop-color="#e0e7ff"/>
      <stop offset="1" stop-color="#a5b4fc"/>
    </linearGradient>
    <radialGradient id="glow" cx="0.7" cy="0.25" r="0.8">
      <stop offset="0" stop-color="#a855f7" stop-opacity="0.5"/>
      <stop offset="1" stop-color="#a855f7" stop-opacity="0"/>
    </radialGradient>
  </defs>
  <rect width="512" height="512" fill="url(#bg)"/>
  <rect width="512" height="512" fill="url(#glow)"/>
  <g id="motif">
    <rect x="124" y="120" width="240" height="96" rx="28" fill="url(#flap)"/>
    <rect x="104" y="168" width="304" height="216" rx="40" fill="url(#body)"/>
    <path d="M104 208 q0 -40 40 -40 h224 q40 0 40 40 l0 20 q-150 60 -304 0 z" fill="#ffffff" opacity="0.14"/>
    <circle cx="344" cy="276" r="30" fill="#12102a"/>
    <circle cx="344" cy="276" r="30" fill="none" stroke="#c7d2fe" stroke-width="4" opacity="0.55"/>
  </g>
</svg>
```

> `<g id="motif">` はスペック確定版の SVG に足した構造上のラッパー。描画結果は変わらず、Step 2 の maskable 生成でモチーフだけを取り出すために使う。

- [ ] **Step 2: 生成スクリプトに maskable を足す**

`scripts/generate-icons.mjs` の全体を以下に置き換える。

```js
// public/icon.svg から PWA 用の PNG を作る。生成物は commit する。
import { readFile, writeFile } from 'node:fs/promises';
import sharp from 'sharp';

const base = await readFile(new URL('../public/icon.svg', import.meta.url), 'utf8');

// maskable は Android がアイコンを円や角丸にくり抜くので、モチーフをセーフゾーン
// (中央 80%)の内側に収める必要がある。SVG を 2 枚手で管理すると片方の更新を
// 忘れるため、ここでラッパーを組み立てて base の defs とモチーフを流用する。
const defs = base.match(/<defs>[\s\S]*?<\/defs>/)?.[0];
const motif = base.match(/<g id="motif">[\s\S]*<\/g>/)?.[0];
if (!defs || !motif) {
  throw new Error('public/icon.svg の構造が変わっています(<defs> と <g id="motif"> が必要)');
}

// translate(64 64) scale(0.75) で 0..512 が 64..448 に移る = 中央 75%。
const maskable = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
${defs}
<rect width="512" height="512" fill="url(#bg)"/>
<rect width="512" height="512" fill="url(#glow)"/>
<g transform="translate(64 64) scale(0.75)">${motif}</g>
</svg>`;

const targets = [
  ['icon-192.png', 192, base],
  ['icon-512.png', 512, base],
  ['apple-touch-icon.png', 180, base],
  ['icon-maskable-512.png', 512, maskable],
];

for (const [name, size, source] of targets) {
  const png = await sharp(Buffer.from(source)).resize(size, size).png().toBuffer();
  await writeFile(new URL(`../public/${name}`, import.meta.url), png);
  console.log(`${name} (${size}px)`);
}
```

- [ ] **Step 3: PNG を生成する**

Run:

```bash
npm run icons
```

Expected:
```
icon-192.png (192px)
icon-512.png (512px)
apple-touch-icon.png (180px)
icon-maskable-512.png (512px)
```

生成された `public/icon-maskable-512.png` を画像ビューアで開き、モチーフが中央に小さく収まっていて背景のグラデーションが四隅まで届いていることを目で確認する。

- [ ] **Step 4: manifest と favicon を更新する**

`vite.config.ts` の manifest の `icons` 配列を以下にする。

```ts
        icons: [
          { src: 'icon-192.png', sizes: '192x192', type: 'image/png' },
          { src: 'icon-512.png', sizes: '512x512', type: 'image/png' },
          {
            src: 'icon-maskable-512.png',
            sizes: '512x512',
            type: 'image/png',
            purpose: 'maskable',
          },
        ],
```

`index.html` には `<link rel="icon">` が無く、ブラウザのタブには既定の favicon が出ている。`apple-touch-icon` の行の直前に以下を足す。

```html
    <link rel="icon" type="image/svg+xml" href="%BASE_URL%icon.svg" />
```

- [ ] **Step 5: ビルドとテストを通す**

Run:

```bash
npm run build
```

Expected: エラーなし。`dist/manifest.webmanifest` に `icon-maskable-512.png` が入り、`dist/icon-maskable-512.png` が出力されていること。

Run:

```bash
npm run test
```

Expected: 全テスト PASS

- [ ] **Step 6: Commit**

```bash
git add public/icon.svg public/icon-192.png public/icon-512.png public/apple-touch-icon.png public/icon-maskable-512.png scripts/generate-icons.mjs vite.config.ts index.html
git commit -m "feat: アイコンを刷新し maskable 専用アイコンを生成する"
```

---

## Task 12: ドキュメント更新と v1.0.7 リリース

手動確認チェックリストに今回だけの確認項目を足し、バージョンを上げ、全検証を通してから `main` へのマージ承認を取る。

**Files:**
- Modify: `README.md`(「## 手動確認チェックリスト」)
- Modify: `package.json`(version)

**Interfaces:**
- Consumes: Task 1〜11 の成果すべて
- Produces: なし(最終 Task)

- [ ] **Step 1: 手動確認チェックリストに v1.0.7 の一時項目を足す**

`README.md` の「## 手動確認チェックリスト」の恒常 12 項目の**後ろ**に、以下の見出しとチェックボックスを追加する。

```markdown
### v1.0.7 だけの確認(次のリリースで消す)

- [ ] ホーム・集計・設定の 4 タブで表示崩れが無い(すりガラスの面が背景に溶けていない)
- [ ] OS の「視差効果を減らす」を有効にすると、タブ切替・リスト入場・バー伸長・FAB の明滅・シートの出入り・金額カウントアップが全て止まる
- [ ] 本文の文字と背景のコントラストが読みづらくない(特に `--text-dim` の補足行)
- [ ] ホーム画面に追加したときのアイコンが新デザインで、Android の円形マスクでもモチーフが切れない
- [ ] 実機(低スペック Android を含む)でスクロール・タブ切替がカクつかない
```

チェックリスト冒頭の「最終確認: 2026-08-13(v1.0.6)」の日付とバージョンは、実機確認を終えた時点で `2026-08-15(v1.0.7)` に更新する。

- [ ] **Step 2: バージョンを上げる**

`package.json` の `version` を `1.0.7` にする。

```json
  "version": "1.0.7",
```

- [ ] **Step 3: 全検証を通す**

Run:

```bash
npm run lint
```

Expected: エラーなし

Run:

```bash
npm run test
```

Expected: 全テスト PASS

Run:

```bash
npm run build
```

Expected: エラーなし

Run:

```bash
npm run icons
```

Expected: 4 つの PNG が出力され、`git status` に差分が出ない(Task 11 でコミット済みのため)

- [ ] **Step 4: Commit**

```bash
git add README.md package.json
git commit -m "chore: バージョンを 1.0.7 に上げる"
```

- [ ] **Step 5: `main` へのマージ承認を取る**

AskUserQuestion で、`feature/v1.0.7-premium-gui` を `main` へマージしてよいかユーザーに確認する。承認が得られるまでマージしない。

---

## 自己レビュー

**1. スペック網羅性**

| スペックの項目 | 対応する Task |
|---|---|
| 1. デザイントークン刷新(`:root` の 6 色 + 新規トークン) | Task 1 Step 1 |
| 1. body 背景グラデーション + 固定 radial グロー | Task 1 Step 2 |
| 1. manifest / theme-color の連動 | Task 1 Step 3 |
| 2. すりガラス(合計カード・タブバー・シート) | Task 2 Step 1, 2, 4 |
| 2. オーバーレイは暗幕のみ | Task 2 Step 4 |
| 2. ヘッダーは sticky の場合のみ → 非 sticky なので適用しない | Task 2 冒頭に判断を明記 |
| 2. 量産される行はグラス不使用(半透明 surface + 縁 + 影) | Task 2 Step 2 |
| 2. バナーはアクセント縁 | Task 2 Step 5 |
| 2. `--grad-accent` の適用先(FAB / primary / 予算バー / グラフのバー) | Task 2 Step 3 |
| 2. セグメントのスライドインジケーター | Task 3 |
| 2. フォーカスリングは `--accent` | Task 2 Step 5 |
| 3. タブ切替(フェード + 8px 上昇) | Task 5 Step 1, 2 |
| 3. リスト入場スタッガー 70ms・8 行で頭打ち | Task 5 Step 2 |
| 3. モーダル / シート 出現 280ms・閉じ 180ms | Task 7 |
| 3. 予算バー / グラフのバー伸長、集計は `key={view}` で再生 | Task 6 Step 1, 2 |
| 3. 金額カウントアップ 500ms・ease-out 三乗・`useCountUp` に切り出し | Task 4 |
| 3. 日別グラフの平均線はフェードのみ | Task 6 Step 2 |
| 3. FAB 呼吸グロー 3.6s + 押下 scale(0.96) | Task 6 Step 3 |
| 3. ボタン押下 scale(0.96) | Task 6 Step 3 |
| 3. reduced-motion(CSS 一括 + JS 側 matchMedia) | Task 5 Step 3、Task 4 Step 1・4 |
| 4. `public/icon.svg` の差し替え | Task 11 Step 1 |
| 4. maskable 専用 PNG をスクリプト内ラッパーで生成 | Task 11 Step 2, 3 |
| 4. manifest の maskable エントリ差し替え | Task 11 Step 4 |
| 4. favicon の確認 | Task 11 Step 4(`<link rel="icon">` が無かったので追加) |
| 5. 修正 1 平均ラベルの被り | Task 9 |
| 5. 修正 2 バナーの設定タブ表記 | Task 10 |
| テスト: `useCountUp` ユニットテスト | Task 4 Step 2 |
| テスト: `DailyChart.test.tsx` 2 件 | Task 9 Step 1 |
| テスト: `App.test.tsx` 設定タブのバナー | Task 10 Step 1 |
| テスト: i18n 4 言語整合は型で自動検出 | Task 10 Step 5 |
| 手動確認チェックリスト 5 件 | Task 12 Step 1 |
| 検証: test / build / lint / icons 全通過 | Task 12 Step 3 |

未対応のスペック項目は無し。スコープ外(ライトテーマ・ストア用スクショ・機能追加)には Task を作っていない。

**2. プレースホルダー確認**

「TBD」「あとで」「適宜」「Task N と同様」といった記述は無い。全ての実装ステップに実際のコード片が入っている。CSS 中心で自動テストを持たない Task(1, 2, 5, 6, 7)には、代わりに Task 8 の目視確認手順と Task 12 の手動確認チェックリストを明示的に割り当てている。

**3. 型と名前の一貫性**

- `prefersReducedMotion()` — Task 4 で `src/ui/motion.ts` に定義、Task 4 と Task 7 で同名で使用。
- `useCountUp(value: number): number` — Task 4 で定義、`CountUpAmount` からのみ使用。
- `CountUpAmount({ value, currency, testId })` — Task 4 Step 7 で `src/ui/CountUpAmount.tsx` に新規作成し、Step 8 で `HomeScreen.tsx` と `SummaryScreen.tsx` が同じパスから import する。両画面とも合計金額の表示位置が早期 return より後ろにあるため、フックを直接呼ばずこのコンポーネント経由にするのが必須。
- CSS クラス名 — `.card.glass`(Task 2 で定義、Task 3 の注記で参照)、`.seg-indicator`(Task 3)、`.tab-panel`(Task 5)、`.sheet.closing` / `.sheet-backdrop.closing`(Task 7)、`.daily-avg--below`(Task 9)。重複・表記ゆれ無し。
- `@keyframes fade-in` は Task 6 Step 2 で定義し Task 7 Step 2 で再利用する。定義順が逆にならないよう、Task 7 は Task 6 の後に実行する。
- `CLOSE_MS = 180` (Sheet.tsx) と `.sheet.closing` の `var(--dur-fast)` = 180ms が一致。
- `AVG_LABEL_HEIGHT = 14` (DailyChart.tsx) と `.daily-avg span { top: -14px }` が一致。
